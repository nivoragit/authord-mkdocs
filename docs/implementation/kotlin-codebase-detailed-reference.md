# Kotlin File-by-File Technical Reference

Generated on 2026-03-20 from `206` Kotlin files across production and test sources.

This document is intended for onboarding, maintenance, debugging, and architecture navigation.

## FeatureFlagPolicy.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/flags/FeatureFlagPolicy.kt`
- **What does it do?** Contains feature logic for `FeatureFlagPolicy` in the `core-domain` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Keep business rules platform-agnostic and deterministic.
- **Key functionalities:**
  - Main declarations: `FeatureFlagPolicy`.
  - Key callable members include `allowsMvpFlow`, `disallowsFutureCycleFeatures`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.flags`
- **Module / feature area:** `core-domain` / `core/flags`
- **Architectural layer:** `domain`

### 2. Main classes / interfaces / objects inside the file
- `FeatureFlagPolicy` (data class, public (default)): Rule/policy implementation for decisions and validation.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `allowsMvpFlow` consumes no explicit parameters.
  - `disallowsFutureCycleFeatures` consumes no explicit parameters.
- **Outputs produced:**
  - `allowsMvpFlow` returns Boolean.
  - `disallowsFutureCycleFeatures` returns Boolean.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/flags/FeatureFlagPolicyTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/flags/FeatureFlagPolicyTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyService.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `FeatureFlagPolicy` (data class, public (default))
- **Key public/internal functions:**
  - `allowsMvpFlow() : Boolean` (public (default))
  - `disallowsFutureCycleFeatures() : Boolean` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- `FeatureFlagPolicy` models structured data with fields: val mvpEnabled: Boolean = true, val previewSyncEnabled: Boolean = false, val aiAssistantEnabled: Boolean = false, val vectorIntegrationEnabled: Boolean = false, val writersideParityEnabled: Boolean = false.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/flags/FeatureFlagPolicyTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyServiceTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = FeatureFlagPolicy(/* collaborators */)
val result = component.allowsMvpFlow(/* inputs */)
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/flags/FeatureFlagPolicyTest.kt` -> `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/flags/FeatureFlagPolicy.kt`.

## RouteMappingService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/navigation/RouteMappingService.kt`
- **What does it do?** Implements service-level behavior in `RouteMappingService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Keep business rules platform-agnostic and deterministic.
- **Key functionalities:**
  - Main declarations: `RouteMappingService`.
  - Key callable members include `mapToRoute`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.navigation`
- **Module / feature area:** `core-domain` / `core/navigation`
- **Architectural layer:** `domain`

### 2. Main classes / interfaces / objects inside the file
- `RouteMappingService` (class, public (default)): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `mapToRoute` consumes selectedPath: String, docsRoot: String = "docs", useDirectoryUrls: Boolean = true.
- **Outputs produced:**
  - `mapToRoute` returns String?.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/navigation/RouteMappingServiceTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/NavigationCoordinator.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/navigation/RouteMappingServiceTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/NavigationCoordinator.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `RouteMappingService` (class, public (default))
- **Key public/internal functions:**
  - `mapToRoute(selectedPath: String, docsRoot: String = "docs", useDirectoryUrls: Boolean = true) : String?` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/navigation/RouteMappingServiceTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = RouteMappingService(/* collaborators */)
val result = component.mapToRoute(/* inputs */)
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/navigation/RouteMappingServiceTest.kt` -> `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/navigation/RouteMappingService.kt`.

## RuntimeLifecycleState.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/runtime/RuntimeLifecycleState.kt`
- **What does it do?** Defines shared model/state types in `RuntimeLifecycleState`.
- **Why it matters:** Shared models must remain clear and stable because many workflows serialize or branch on them.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
  - Keep business rules platform-agnostic and deterministic.
- **Key functionalities:**
  - Main declarations: `RuntimeLifecycleState`.
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.runtime`
- **Module / feature area:** `core-domain` / `core/runtime`
- **Architectural layer:** `domain`

### 2. Main classes / interfaces / objects inside the file
- `RuntimeLifecycleState` (enum class, public (default)): Typed model representing shared state or payload data.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/runtime/RuntimeLifecycleStateTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/runtime/RuntimeLifecycleStateTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `RuntimeLifecycleState` (enum class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- File is mostly declarative (types/contracts) with limited embedded business rules.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- `RuntimeLifecycleState` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/runtime/RuntimeLifecycleStateTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
// Construct `RuntimeLifecycleState` and use exposed members
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/runtime/RuntimeLifecycleStateTest.kt` -> `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/runtime/RuntimeLifecycleState.kt`.

## ScrollSemanticService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/scroll/ScrollSemanticService.kt`
- **What does it do?** Implements service-level behavior in `ScrollSemanticService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Keep business rules platform-agnostic and deterministic.
- **Key functionalities:**
  - Main declarations: `CommentRange`, `ScrollSemanticService`.
  - Key callable members include `calculateSemanticDelta`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.scroll`
- **Module / feature area:** `core-domain` / `core/scroll`
- **Architectural layer:** `domain`

### 2. Main classes / interfaces / objects inside the file
- `CommentRange` (data class, public (default)): Primary implementation type for this file feature area.
- `ScrollSemanticService` (class, public (default)): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `calculateSemanticDelta` consumes rawDelta: Int, visibleStartOffset: Int, visibleEndOffset: Int, commentRanges: List<CommentRange>.
- **Outputs produced:**
  - `calculateSemanticDelta` returns Int.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/scroll/ScrollSemanticServiceTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/scroll/ScrollSemanticServiceTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `CommentRange` (data class, public (default))
  - `ScrollSemanticService` (class, public (default))
- **Key public/internal functions:**
  - `calculateSemanticDelta(rawDelta: Int, visibleStartOffset: Int, visibleEndOffset: Int, commentRanges: List<CommentRange>) : Int` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- `CommentRange` models structured data with fields: val startOffset: Int, val endOffset: Int.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/scroll/ScrollSemanticServiceTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = CommentRange(/* collaborators */)
val result = component.calculateSemanticDelta(/* inputs */)
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/scroll/ScrollSemanticServiceTest.kt` -> `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/scroll/ScrollSemanticService.kt`.

## PathPolicy.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/PathPolicy.kt`
- **What does it do?** Contains feature logic for `PathPolicy` in the `core-domain` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.util.Locale
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Keep business rules platform-agnostic and deterministic.
- **Key functionalities:**
  - Main declarations: `PathPolicy`.
  - Key callable members include `normalize`, `comparisonKey`, `equivalent`, `forCurrentOs`, `forOsName`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.topic`
- **Module / feature area:** `core-domain` / `core/topic`
- **Architectural layer:** `domain`

### 2. Main classes / interfaces / objects inside the file
- `PathPolicy` (data class, public (default)): Rule/policy implementation for decisions and validation.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `normalize` consumes path: String.
  - `comparisonKey` consumes path: String.
  - `equivalent` consumes left: String, right: String.
  - `forCurrentOs` consumes no explicit parameters.
  - `forOsName` consumes osName: String.
- **Outputs produced:**
  - `normalize` returns String.
  - `comparisonKey` returns String.
  - `equivalent` returns Boolean.
  - `forCurrentOs` returns PathPolicy.
  - `forOsName` returns PathPolicy.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationService.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/PathNormalizationPolicyTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationServiceTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/InstanceRegistryService.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationService.kt`, `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/PathNormalizationPolicyTest.kt`, `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationServiceTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PathPolicy` (data class, public (default))
- **Key public/internal functions:**
  - `normalize(path: String) : String` (public (default))
  - `comparisonKey(path: String) : String` (public (default))
  - `equivalent(left: String, right: String) : Boolean` (public (default))
  - `forCurrentOs() : PathPolicy` (public (default))
  - `forOsName(osName: String) : PathPolicy` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- `PathPolicy` models structured data with fields: val caseSensitiveComparison: Boolean.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddChildTransformPolicyTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddExistingFileCommandTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/ExternalLinkCommandTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/PathNormalizationPolicyTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicDomainModelTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregateMutationTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = PathPolicy(/* collaborators */)
val result = component.normalize(/* inputs */)
```
Typical caller path: `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationService.kt` -> `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/PathPolicy.kt`.

## TopicDomainModel.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicDomainModel.kt`
- **What does it do?** Defines shared model/state types in `TopicDomainModel`.
- **Why it matters:** Shared models must remain clear and stable because many workflows serialize or branch on them.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
  - Keep business rules platform-agnostic and deterministic.
- **Key functionalities:**
  - Main declarations: `TopicId`, `TopicOrder`, `TopicLink`, `TopicMetadata`.
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.topic`
- **Module / feature area:** `core-domain` / `core/topic`
- **Architectural layer:** `domain`

### 2. Main classes / interfaces / objects inside the file
- `TopicId` (value class, public (default)): Primary implementation type for this file feature area.
- `TopicOrder` (value class, public (default)): Primary implementation type for this file feature area.
- `TopicLink` (data class, public (default)): Primary implementation type for this file feature area.
- `TopicMetadata` (data class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicDomainModelTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicDomainModelTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicId` (value class, public (default))
  - `TopicOrder` (value class, public (default))
  - `TopicLink` (data class, public (default))
  - `TopicMetadata` (data class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@JvmInline`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- `TopicLink` models structured data with fields: val from: TopicId, val to: TopicId, val relation: String = "child".
- `TopicMetadata` models structured data with fields: val attributes: Map<String, String> = emptyMap(.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicDomainModelTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddChildTransformPolicyTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddExistingFileCommandTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/ExternalLinkCommandTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/PathNormalizationPolicyTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregateMutationTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
// Construct `TopicId` and use exposed members
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicDomainModelTest.kt` -> `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicDomainModel.kt`.

## TopicTreeAggregate.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregate.kt`
- **What does it do?** Contains feature logic for `TopicTreeAggregate` in the `core-domain` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.TopicTreePort, com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand, com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand, com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand, com.authord.mkdocs.ports.topic.AddTopicNodeCommand, com.authord.mkdocs.ports.topic.MoveTopicNodeCommand, com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand, com.authord.mkdocs.ports.topic.RenameTopicNodeCommand
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Keep business rules platform-agnostic and deterministic.
- **Key functionalities:**
  - Main declarations: `TopicNodeStatus`, `TopicNodeKind`, `TopicNode`, `TopicTreeAggregate`, `TopicTreeMutationService`.
  - Key callable members include `snapshot`, `bootstrapFromNav`, `visit`, `apply`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.topic`
- **Module / feature area:** `core-domain` / `core/topic`
- **Architectural layer:** `domain`

### 2. Main classes / interfaces / objects inside the file
- `TopicNodeStatus` (enum class, public (default)): Primary implementation type for this file feature area.
- `TopicNodeKind` (enum class, public (default)): Primary implementation type for this file feature area.
- `TopicNode` (data class, public (default)): Primary implementation type for this file feature area.
- `TopicTreeAggregate` (class, public (default)): Primary implementation type for this file feature area.
- `TopicTreeMutationService` (class, public (default)): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `snapshot` consumes no explicit parameters.
  - `bootstrapFromNav` consumes nav: List<TopicNavNode>.
  - `visit` consumes parentNodeId: String, children: List<TopicNavNode>.
  - `apply` consumes command: TopicTreeCommand.
- **Outputs produced:**
  - `snapshot` returns List<TopicNode>.
  - `bootstrapFromNav` returns Unit/implicit.
  - `visit` returns Unit/implicit.
  - `apply` returns TopicTreeCommandResult.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicDomainModel.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddChildTransformPolicyTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddExistingFileCommandTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/ExternalLinkCommandTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.TopicTreePort`
  - `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicDomainModel.kt`, `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddChildTransformPolicyTest.kt`, `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddExistingFileCommandTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.TopicTreePort`, `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`, `com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicNodeStatus` (enum class, public (default))
  - `TopicNodeKind` (enum class, public (default))
  - `TopicNode` (data class, public (default))
  - `TopicTreeAggregate` (class, public (default))
  - `TopicTreeMutationService` (class, public (default))
- **Key public/internal functions:**
  - `snapshot() : List<TopicNode>` (public (default))
  - `bootstrapFromNav(nav: List<TopicNavNode>) : Unit/implicit` (public (default))
  - `visit(parentNodeId: String, children: List<TopicNavNode>) : Unit/implicit` (public (default))
  - `apply(command: TopicTreeCommand) : TopicTreeCommandResult` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- `TopicNode` models structured data with fields: val nodeId: String, val parentNodeId: String?, val title: String, val orderIndex: Int, val status: TopicNodeStatus = TopicNodeStatus.ACTIVE.
- `TopicNodeStatus` constrains state to a fixed enum set used across callers.
- `TopicNodeKind` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregateMutationTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregateTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeAggregateHydrationStartupTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddChildTransformPolicyTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddExistingFileCommandTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/ExternalLinkCommandTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Large callable surface area increases regression risk; consider characterization tests before large refactors.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
```kotlin
val component = TopicNodeStatus(/* collaborators */)
val result = component.snapshot(/* inputs */)
```
Typical caller path: `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicDomainModel.kt` -> `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregate.kt`.

## TopicTreeValidationService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationService.kt`
- **What does it do?** Implements service-level behavior in `TopicTreeValidationService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicNavNode
  - External: java.net.URI
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Keep business rules platform-agnostic and deterministic.
- **Key functionalities:**
  - Main declarations: `TopicTreeValidationIssueType`, `TopicTreeValidationIssue`, `TopicTreeValidationService`.
  - Key callable members include `validate`, `visit`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.topic`
- **Module / feature area:** `core-domain` / `core/topic`
- **Architectural layer:** `domain`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeValidationIssueType` (enum class, public (default)): Primary implementation type for this file feature area.
- `TopicTreeValidationIssue` (data class, public (default)): Primary implementation type for this file feature area.
- `TopicTreeValidationService` (class, public (default)): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `validate` consumes navNodes: List<TopicNavNode>, docsRelativePaths: List<String>.
  - `visit` consumes node: TopicNavNode.
- **Outputs produced:**
  - `validate` returns List<TopicTreeValidationIssue>.
  - `visit` returns Unit/implicit.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationServiceTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeStartupLoader.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationPolicyTest.kt`
  - `tests/integration/topic-tree/PathCaseMatrixIntegrationTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicNavNode`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationServiceTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeStartupLoader.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationPolicyTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicNavNode`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeValidationIssueType` (enum class, public (default))
  - `TopicTreeValidationIssue` (data class, public (default))
  - `TopicTreeValidationService` (class, public (default))
- **Key public/internal functions:**
  - `validate(navNodes: List<TopicNavNode>, docsRelativePaths: List<String>) : List<TopicTreeValidationIssue>` (public (default))
  - `visit(node: TopicNavNode) : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- `TopicTreeValidationIssue` models structured data with fields: val type: TopicTreeValidationIssueType, val reference: String, val detail: String.
- `TopicTreeValidationIssueType` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationServiceTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddChildTransformPolicyTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddExistingFileCommandTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/ExternalLinkCommandTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/PathNormalizationPolicyTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicDomainModelTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = TopicTreeValidationIssueType(/* collaborators */)
val result = component.validate(/* inputs */)
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationServiceTest.kt` -> `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationService.kt`.

## FeatureFlagPolicyTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/flags/FeatureFlagPolicyTest.kt`
- **What does it do?** Implements unit-focused test coverage around `FeatureFlagPolicyTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `FeatureFlagPolicyTest`.
  - Key callable members include `default policy allows MVP and disables future-cycle features`, `future-cycle flags invalidate policy`.
  - Scenario-focused test cases include: `default policy allows MVP and disables future-cycle features`, `future-cycle flags invalidate policy`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.flags`
- **Module / feature area:** `core-domain` / `core/flags`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `FeatureFlagPolicyTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `default policy allows MVP and disables future-cycle features` consumes no explicit parameters.
  - `future-cycle flags invalidate policy` consumes no explicit parameters.
- **Outputs produced:**
  - `default policy allows MVP and disables future-cycle features` returns Unit/implicit.
  - `future-cycle flags invalidate policy` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `FeatureFlagPolicyTest` (class, public (default))
- **Key public/internal functions:**
  - `default policy allows MVP and disables future-cycle features() : Unit/implicit` (public (default))
  - `future-cycle flags invalidate policy() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/flags/FeatureFlagPolicyTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *FeatureFlagPolicyTest`.

## RouteMappingServiceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/navigation/RouteMappingServiceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `RouteMappingServiceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `RouteMappingServiceTest`.
  - Key callable members include `maps docs index to root`, `maps segment index to trailing slash route`, `maps segment markdown file to trailing slash route`, `maps uppercase markdown extension to route`, `maps markdown extension variant to route`, `maps nested index path`, `maps double separator index path to root`, `returns null for non-docs paths`, plus `5` additional members.
  - Scenario-focused test cases include: `maps docs index to root`, `maps segment index to trailing slash route`, `maps segment markdown file to trailing slash route`, `maps uppercase markdown extension to route`, `maps markdown extension variant to route`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.navigation`
- **Module / feature area:** `core-domain` / `core/navigation`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `RouteMappingServiceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `maps docs index to root` consumes no explicit parameters.
  - `maps segment index to trailing slash route` consumes no explicit parameters.
  - `maps segment markdown file to trailing slash route` consumes no explicit parameters.
  - `maps uppercase markdown extension to route` consumes no explicit parameters.
  - `maps markdown extension variant to route` consumes no explicit parameters.
- **Outputs produced:**
  - `maps docs index to root` returns Unit/implicit.
  - `maps segment index to trailing slash route` returns Unit/implicit.
  - `maps segment markdown file to trailing slash route` returns Unit/implicit.
  - `maps uppercase markdown extension to route` returns Unit/implicit.
  - `maps markdown extension variant to route` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `RouteMappingServiceTest` (class, public (default))
- **Key public/internal functions:**
  - `maps docs index to root() : Unit/implicit` (public (default))
  - `maps segment index to trailing slash route() : Unit/implicit` (public (default))
  - `maps segment markdown file to trailing slash route() : Unit/implicit` (public (default))
  - `maps uppercase markdown extension to route() : Unit/implicit` (public (default))
  - `maps markdown extension variant to route() : Unit/implicit` (public (default))
  - `maps nested index path() : Unit/implicit` (public (default))
  - `maps double separator index path to root() : Unit/implicit` (public (default))
  - `returns null for non-docs paths() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/navigation/RouteMappingServiceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *RouteMappingServiceTest`.

## CoverageScopePolicyTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/CoverageScopePolicyTest.kt`
- **What does it do?** Implements unit-focused test coverage around `CoverageScopePolicyTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `CoverageScopePolicyTest`.
  - Key callable members include `coverage verification requires 100 percent line coverage`.
  - Scenario-focused test cases include: `coverage verification requires 100 percent line coverage`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.quality`
- **Module / feature area:** `core-domain` / `core/quality`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `CoverageScopePolicyTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `coverage verification requires 100 percent line coverage` consumes no explicit parameters.
- **Outputs produced:**
  - `coverage verification requires 100 percent line coverage` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `CoverageScopePolicyTest` (class, public (default))
- **Key public/internal functions:**
  - `coverage verification requires 100 percent line coverage() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/CoverageScopePolicyTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *CoverageScopePolicyTest`.

## DocumentationArtifactsPolicyTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/DocumentationArtifactsPolicyTest.kt`
- **What does it do?** Implements unit-focused test coverage around `DocumentationArtifactsPolicyTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `DocumentationArtifactsPolicyTest`.
  - Key callable members include `required SDD artifacts exist for this feature`, `documentation includes function usage guidance and traceability matrix`.
  - Scenario-focused test cases include: `required SDD artifacts exist for this feature`, `documentation includes function usage guidance and traceability matrix`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.quality`
- **Module / feature area:** `core-domain` / `core/quality`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `DocumentationArtifactsPolicyTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `required SDD artifacts exist for this feature` consumes no explicit parameters.
  - `documentation includes function usage guidance and traceability matrix` consumes no explicit parameters.
- **Outputs produced:**
  - `required SDD artifacts exist for this feature` returns Unit/implicit.
  - `documentation includes function usage guidance and traceability matrix` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `DocumentationArtifactsPolicyTest` (class, public (default))
- **Key public/internal functions:**
  - `required SDD artifacts exist for this feature() : Unit/implicit` (public (default))
  - `documentation includes function usage guidance and traceability matrix() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/DocumentationArtifactsPolicyTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *DocumentationArtifactsPolicyTest`.

## KDocCoveragePolicyTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`
- **What does it do?** Implements unit-focused test coverage around `KDocCoveragePolicyTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `KDocCoveragePolicyTest`.
  - Key callable members include `phase2 public api surface declarations include KDoc`.
  - Scenario-focused test cases include: `phase2 public api surface declarations include KDoc`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.quality`
- **Module / feature area:** `core-domain` / `core/quality`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `KDocCoveragePolicyTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `phase2 public api surface declarations include KDoc` consumes no explicit parameters.
- **Outputs produced:**
  - `phase2 public api surface declarations include KDoc` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `KDocCoveragePolicyTest` (class, public (default))
- **Key public/internal functions:**
  - `phase2 public api surface declarations include KDoc() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Contains coroutine-aware execution paths and cancellation-sensitive operations.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *KDocCoveragePolicyTest`.

## PluginBuildPolicyTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/PluginBuildPolicyTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PluginBuildPolicyTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PluginBuildPolicyTest`.
  - Key callable members include `build configuration enables intellij platform plugin and runIde readiness`.
  - Scenario-focused test cases include: `build configuration enables intellij platform plugin and runIde readiness`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.quality`
- **Module / feature area:** `core-domain` / `core/quality`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PluginBuildPolicyTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `build configuration enables intellij platform plugin and runIde readiness` consumes no explicit parameters.
- **Outputs produced:**
  - `build configuration enables intellij platform plugin and runIde readiness` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PluginBuildPolicyTest` (class, public (default))
- **Key public/internal functions:**
  - `build configuration enables intellij platform plugin and runIde readiness() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/PluginBuildPolicyTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PluginBuildPolicyTest`.

## RequirementCoveragePolicyTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/RequirementCoveragePolicyTest.kt`
- **What does it do?** Implements unit-focused test coverage around `RequirementCoveragePolicyTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `RequirementCoveragePolicyTest`.
  - Key callable members include `phase2 traceability matrix lists all required requirement families`, `quality gate references jacoco coverage verification`.
  - Scenario-focused test cases include: `phase2 traceability matrix lists all required requirement families`, `quality gate references jacoco coverage verification`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.quality`
- **Module / feature area:** `core-domain` / `core/quality`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `RequirementCoveragePolicyTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `phase2 traceability matrix lists all required requirement families` consumes no explicit parameters.
  - `quality gate references jacoco coverage verification` consumes no explicit parameters.
- **Outputs produced:**
  - `phase2 traceability matrix lists all required requirement families` returns Unit/implicit.
  - `quality gate references jacoco coverage verification` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `RequirementCoveragePolicyTest` (class, public (default))
- **Key public/internal functions:**
  - `phase2 traceability matrix lists all required requirement families() : Unit/implicit` (public (default))
  - `quality gate references jacoco coverage verification() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/RequirementCoveragePolicyTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *RequirementCoveragePolicyTest`.

## RuntimeNeutralityPolicyTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/RuntimeNeutralityPolicyTest.kt`
- **What does it do?** Implements unit-focused test coverage around `RuntimeNeutralityPolicyTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `RuntimeNeutralityPolicyTest`.
  - Key callable members include `main sources avoid shell activation and hardcoded python paths`, `main sources avoid hardcoded runtime host and port defaults`.
  - Scenario-focused test cases include: `main sources avoid shell activation and hardcoded python paths`, `main sources avoid hardcoded runtime host and port defaults`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.quality`
- **Module / feature area:** `core-domain` / `core/quality`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `RuntimeNeutralityPolicyTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `main sources avoid shell activation and hardcoded python paths` consumes no explicit parameters.
  - `main sources avoid hardcoded runtime host and port defaults` consumes no explicit parameters.
- **Outputs produced:**
  - `main sources avoid shell activation and hardcoded python paths` returns Unit/implicit.
  - `main sources avoid hardcoded runtime host and port defaults` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `RuntimeNeutralityPolicyTest` (class, public (default))
- **Key public/internal functions:**
  - `main sources avoid shell activation and hardcoded python paths() : Unit/implicit` (public (default))
  - `main sources avoid hardcoded runtime host and port defaults() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/RuntimeNeutralityPolicyTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *RuntimeNeutralityPolicyTest`.

## ScopedCoveragePolicyTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/ScopedCoveragePolicyTest.kt`
- **What does it do?** Implements unit-focused test coverage around `ScopedCoveragePolicyTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `ScopedCoveragePolicyTest`.
  - Key callable members include `root build defines scoped coverage gate over required modules`, `module build files enforce 100 percent coverage minimum`, `ci workflows run fail on threshold coverage gates`.
  - Scenario-focused test cases include: `root build defines scoped coverage gate over required modules`, `module build files enforce 100 percent coverage minimum`, `ci workflows run fail on threshold coverage gates`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.quality`
- **Module / feature area:** `core-domain` / `core/quality`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `ScopedCoveragePolicyTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `root build defines scoped coverage gate over required modules` consumes no explicit parameters.
  - `module build files enforce 100 percent coverage minimum` consumes no explicit parameters.
  - `ci workflows run fail on threshold coverage gates` consumes no explicit parameters.
- **Outputs produced:**
  - `root build defines scoped coverage gate over required modules` returns Unit/implicit.
  - `module build files enforce 100 percent coverage minimum` returns Unit/implicit.
  - `ci workflows run fail on threshold coverage gates` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `ScopedCoveragePolicyTest` (class, public (default))
- **Key public/internal functions:**
  - `root build defines scoped coverage gate over required modules() : Unit/implicit` (public (default))
  - `module build files enforce 100 percent coverage minimum() : Unit/implicit` (public (default))
  - `ci workflows run fail on threshold coverage gates() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/ScopedCoveragePolicyTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *ScopedCoveragePolicyTest`.

## RuntimeLifecycleStateTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/runtime/RuntimeLifecycleStateTest.kt`
- **What does it do?** Implements unit-focused test coverage around `RuntimeLifecycleStateTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `RuntimeLifecycleStateTest`.
  - Key callable members include `runtime lifecycle enum exposes all expected states`.
  - Scenario-focused test cases include: `runtime lifecycle enum exposes all expected states`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.runtime`
- **Module / feature area:** `core-domain` / `core/runtime`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `RuntimeLifecycleStateTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `runtime lifecycle enum exposes all expected states` consumes no explicit parameters.
- **Outputs produced:**
  - `runtime lifecycle enum exposes all expected states` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `RuntimeLifecycleStateTest` (class, public (default))
- **Key public/internal functions:**
  - `runtime lifecycle enum exposes all expected states() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- Model-centric declarations: `RuntimeLifecycleStateTest`.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/runtime/RuntimeLifecycleStateTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *RuntimeLifecycleStateTest`.

## ScrollSemanticServiceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/scroll/ScrollSemanticServiceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `ScrollSemanticServiceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `ScrollSemanticServiceTest`.
  - Key callable members include `returns zero when visible region is fully comments`, `returns reduced delta when visible region partly overlaps comments`, `returns raw delta when no comment overlap exists`, `returns zero when raw delta is zero`, `applies exclusion ratio for negative deltas`.
  - Scenario-focused test cases include: `returns zero when visible region is fully comments`, `returns reduced delta when visible region partly overlaps comments`, `returns raw delta when no comment overlap exists`, `returns zero when raw delta is zero`, `applies exclusion ratio for negative deltas`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.scroll`
- **Module / feature area:** `core-domain` / `core/scroll`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `ScrollSemanticServiceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `returns zero when visible region is fully comments` consumes no explicit parameters.
  - `returns reduced delta when visible region partly overlaps comments` consumes no explicit parameters.
  - `returns raw delta when no comment overlap exists` consumes no explicit parameters.
  - `returns zero when raw delta is zero` consumes no explicit parameters.
  - `applies exclusion ratio for negative deltas` consumes no explicit parameters.
- **Outputs produced:**
  - `returns zero when visible region is fully comments` returns Unit/implicit.
  - `returns reduced delta when visible region partly overlaps comments` returns Unit/implicit.
  - `returns raw delta when no comment overlap exists` returns Unit/implicit.
  - `returns zero when raw delta is zero` returns Unit/implicit.
  - `applies exclusion ratio for negative deltas` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `ScrollSemanticServiceTest` (class, public (default))
- **Key public/internal functions:**
  - `returns zero when visible region is fully comments() : Unit/implicit` (public (default))
  - `returns reduced delta when visible region partly overlaps comments() : Unit/implicit` (public (default))
  - `returns raw delta when no comment overlap exists() : Unit/implicit` (public (default))
  - `returns zero when raw delta is zero() : Unit/implicit` (public (default))
  - `applies exclusion ratio for negative deltas() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/scroll/ScrollSemanticServiceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *ScrollSemanticServiceTest`.

## AddChildTransformPolicyTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddChildTransformPolicyTest.kt`
- **What does it do?** Implements unit-focused test coverage around `AddChildTransformPolicyTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand, com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand, com.authord.mkdocs.ports.topic.AddTopicNodeCommand, com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand, com.authord.mkdocs.ports.topic.TopicTreeCommandStatus
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `AddChildTransformPolicyTest`.
  - Key callable members include `add-child on page auto-converts parent into section and preserves original page as first child`, `add-child rejects external link target and missing target nodes`, `add-child rejects removed target duplicate child ids and invalid child input`, `add-child section branch preserves deterministic sibling ordering by order then node id`, `add-child page transform reindexes pre-existing children and handles preserved id collision`.
  - Scenario-focused test cases include: `add-child on page auto-converts parent into section and preserves original page as first child`, `add-child rejects external link target and missing target nodes`, `add-child rejects removed target duplicate child ids and invalid child input`, `add-child section branch preserves deterministic sibling ordering by order then node id`, `add-child page transform reindexes pre-existing children and handles preserved id collision`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.topic`
- **Module / feature area:** `core-domain` / `core/topic`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `AddChildTransformPolicyTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `add-child on page auto-converts parent into section and preserves original page as first child` consumes no explicit parameters.
  - `add-child rejects external link target and missing target nodes` consumes no explicit parameters.
  - `add-child rejects removed target duplicate child ids and invalid child input` consumes no explicit parameters.
  - `add-child section branch preserves deterministic sibling ordering by order then node id` consumes no explicit parameters.
  - `add-child page transform reindexes pre-existing children and handles preserved id collision` consumes no explicit parameters.
- **Outputs produced:**
  - `add-child on page auto-converts parent into section and preserves original page as first child` returns Unit/implicit.
  - `add-child rejects external link target and missing target nodes` returns Unit/implicit.
  - `add-child rejects removed target duplicate child ids and invalid child input` returns Unit/implicit.
  - `add-child section branch preserves deterministic sibling ordering by order then node id` returns Unit/implicit.
  - `add-child page transform reindexes pre-existing children and handles preserved id collision` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`, `com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand`, `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `AddChildTransformPolicyTest` (class, public (default))
- **Key public/internal functions:**
  - `add-child on page auto-converts parent into section and preserves original page as first child() : Unit/implicit` (public (default))
  - `add-child rejects external link target and missing target nodes() : Unit/implicit` (public (default))
  - `add-child rejects removed target duplicate child ids and invalid child input() : Unit/implicit` (public (default))
  - `add-child section branch preserves deterministic sibling ordering by order then node id() : Unit/implicit` (public (default))
  - `add-child page transform reindexes pre-existing children and handles preserved id collision() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`, `@Suppress`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddChildTransformPolicyTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *AddChildTransformPolicyTest`.

## AddExistingFileCommandTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddExistingFileCommandTest.kt`
- **What does it do?** Implements unit-focused test coverage around `AddExistingFileCommandTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand, com.authord.mkdocs.ports.topic.TopicTreeCommandStatus
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `AddExistingFileCommandTest`.
  - Key callable members include `add-existing-file inserts markdown-backed page node under selected parent`, `add-existing-file rejects missing parent, invalid paths, and duplicate node ids`.
  - Scenario-focused test cases include: `add-existing-file inserts markdown-backed page node under selected parent`, `add-existing-file rejects missing parent, invalid paths, and duplicate node ids`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.topic`
- **Module / feature area:** `core-domain` / `core/topic`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `AddExistingFileCommandTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `add-existing-file inserts markdown-backed page node under selected parent` consumes no explicit parameters.
  - `add-existing-file rejects missing parent, invalid paths, and duplicate node ids` consumes no explicit parameters.
- **Outputs produced:**
  - `add-existing-file inserts markdown-backed page node under selected parent` returns Unit/implicit.
  - `add-existing-file rejects missing parent, invalid paths, and duplicate node ids` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.TopicTreeCommandStatus`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand`, `com.authord.mkdocs.ports.topic.TopicTreeCommandStatus`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `AddExistingFileCommandTest` (class, public (default))
- **Key public/internal functions:**
  - `add-existing-file inserts markdown-backed page node under selected parent() : Unit/implicit` (public (default))
  - `add-existing-file rejects missing parent, invalid paths, and duplicate node ids() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddExistingFileCommandTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *AddExistingFileCommandTest`.

## ExternalLinkCommandTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/ExternalLinkCommandTest.kt`
- **What does it do?** Implements unit-focused test coverage around `ExternalLinkCommandTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand, com.authord.mkdocs.ports.topic.TopicTreeCommandStatus
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `ExternalLinkCommandTest`.
  - Key callable members include `add-external-link inserts external-link node with url`, `add-external-link rejects malformed urls and missing parent`.
  - Scenario-focused test cases include: `add-external-link inserts external-link node with url`, `add-external-link rejects malformed urls and missing parent`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.topic`
- **Module / feature area:** `core-domain` / `core/topic`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `ExternalLinkCommandTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `add-external-link inserts external-link node with url` consumes no explicit parameters.
  - `add-external-link rejects malformed urls and missing parent` consumes no explicit parameters.
- **Outputs produced:**
  - `add-external-link inserts external-link node with url` returns Unit/implicit.
  - `add-external-link rejects malformed urls and missing parent` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.TopicTreeCommandStatus`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand`, `com.authord.mkdocs.ports.topic.TopicTreeCommandStatus`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `ExternalLinkCommandTest` (class, public (default))
- **Key public/internal functions:**
  - `add-external-link inserts external-link node with url() : Unit/implicit` (public (default))
  - `add-external-link rejects malformed urls and missing parent() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/ExternalLinkCommandTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *ExternalLinkCommandTest`.

## PathNormalizationPolicyTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/PathNormalizationPolicyTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PathNormalizationPolicyTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PathNormalizationPolicyTest`.
  - Key callable members include `normalize handles empty and whitespace-only input`, `normalize handles windows drive and dot segments`, `normalize handles leading slash and relative parent traversal`, `normalize treats non-letter drive prefix as regular path`, `comparison key follows case sensitivity policy`, `equivalent uses normalized comparison key`, `for os name maps windows and mac to case-insensitive mode`, `for current os delegates to os-name mapping`.
  - Scenario-focused test cases include: `normalize handles empty and whitespace-only input`, `normalize handles windows drive and dot segments`, `normalize handles leading slash and relative parent traversal`, `normalize treats non-letter drive prefix as regular path`, `comparison key follows case sensitivity policy`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.topic`
- **Module / feature area:** `core-domain` / `core/topic`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PathNormalizationPolicyTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `normalize handles empty and whitespace-only input` consumes no explicit parameters.
  - `normalize handles windows drive and dot segments` consumes no explicit parameters.
  - `normalize handles leading slash and relative parent traversal` consumes no explicit parameters.
  - `normalize treats non-letter drive prefix as regular path` consumes no explicit parameters.
  - `comparison key follows case sensitivity policy` consumes no explicit parameters.
- **Outputs produced:**
  - `normalize handles empty and whitespace-only input` returns Unit/implicit.
  - `normalize handles windows drive and dot segments` returns Unit/implicit.
  - `normalize handles leading slash and relative parent traversal` returns Unit/implicit.
  - `normalize treats non-letter drive prefix as regular path` returns Unit/implicit.
  - `comparison key follows case sensitivity policy` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PathNormalizationPolicyTest` (class, public (default))
- **Key public/internal functions:**
  - `normalize handles empty and whitespace-only input() : Unit/implicit` (public (default))
  - `normalize handles windows drive and dot segments() : Unit/implicit` (public (default))
  - `normalize handles leading slash and relative parent traversal() : Unit/implicit` (public (default))
  - `normalize treats non-letter drive prefix as regular path() : Unit/implicit` (public (default))
  - `comparison key follows case sensitivity policy() : Unit/implicit` (public (default))
  - `equivalent uses normalized comparison key() : Unit/implicit` (public (default))
  - `for os name maps windows and mac to case-insensitive mode() : Unit/implicit` (public (default))
  - `for current os delegates to os-name mapping() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/PathNormalizationPolicyTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PathNormalizationPolicyTest`.

## TopicDomainModelTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicDomainModelTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicDomainModelTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicDomainModelTest`.
  - Key callable members include `topic id requires non-blank values`, `topic order requires non-negative values`, `topic value objects expose boxed getters`, `topic link and metadata expose fields`, `topic metadata defaults to empty attributes`, `topic tree alias points to aggregate behavior`.
  - Scenario-focused test cases include: `topic id requires non-blank values`, `topic order requires non-negative values`, `topic value objects expose boxed getters`, `topic link and metadata expose fields`, `topic metadata defaults to empty attributes`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.topic`
- **Module / feature area:** `core-domain` / `core/topic`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicDomainModelTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `topic id requires non-blank values` consumes no explicit parameters.
  - `topic order requires non-negative values` consumes no explicit parameters.
  - `topic value objects expose boxed getters` consumes no explicit parameters.
  - `topic link and metadata expose fields` consumes no explicit parameters.
  - `topic metadata defaults to empty attributes` consumes no explicit parameters.
- **Outputs produced:**
  - `topic id requires non-blank values` returns Unit/implicit.
  - `topic order requires non-negative values` returns Unit/implicit.
  - `topic value objects expose boxed getters` returns Unit/implicit.
  - `topic link and metadata expose fields` returns Unit/implicit.
  - `topic metadata defaults to empty attributes` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicDomainModelTest` (class, public (default))
- **Key public/internal functions:**
  - `topic id requires non-blank values() : Unit/implicit` (public (default))
  - `topic order requires non-negative values() : Unit/implicit` (public (default))
  - `topic value objects expose boxed getters() : Unit/implicit` (public (default))
  - `topic link and metadata expose fields() : Unit/implicit` (public (default))
  - `topic metadata defaults to empty attributes() : Unit/implicit` (public (default))
  - `topic tree alias points to aggregate behavior() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- Model-centric declarations: `TopicDomainModelTest`.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicDomainModelTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicDomainModelTest`.

## TopicTreeAggregateMutationTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregateMutationTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicTreeAggregateMutationTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.AddTopicNodeCommand, com.authord.mkdocs.ports.topic.MoveTopicNodeCommand, com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand, com.authord.mkdocs.ports.topic.RenameTopicNodeCommand, com.authord.mkdocs.ports.topic.ReorderTopicNodesCommand, com.authord.mkdocs.ports.topic.ReparentTopicNodeCommand, com.authord.mkdocs.ports.topic.TopicTreeCommandStatus
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeAggregateMutationTest`.
  - Key callable members include `add enforces unique node ids and valid parent`, `move enforces cycle safety and non-negative ordering`, `remove marks node as removed and rejects root removal`, `rename requires existing node and non-blank title`, `reparent rejects cycles and missing parents`, `reorder requires exact active sibling set`.
  - Scenario-focused test cases include: `add enforces unique node ids and valid parent`, `move enforces cycle safety and non-negative ordering`, `remove marks node as removed and rejects root removal`, `rename requires existing node and non-blank title`, `reparent rejects cycles and missing parents`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.topic`
- **Module / feature area:** `core-domain` / `core/topic`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeAggregateMutationTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `add enforces unique node ids and valid parent` consumes no explicit parameters.
  - `move enforces cycle safety and non-negative ordering` consumes no explicit parameters.
  - `remove marks node as removed and rejects root removal` consumes no explicit parameters.
  - `rename requires existing node and non-blank title` consumes no explicit parameters.
  - `reparent rejects cycles and missing parents` consumes no explicit parameters.
- **Outputs produced:**
  - `add enforces unique node ids and valid parent` returns Unit/implicit.
  - `move enforces cycle safety and non-negative ordering` returns Unit/implicit.
  - `remove marks node as removed and rejects root removal` returns Unit/implicit.
  - `rename requires existing node and non-blank title` returns Unit/implicit.
  - `reparent rejects cycles and missing parents` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.MoveTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.RenameTopicNodeCommand`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`, `com.authord.mkdocs.ports.topic.MoveTopicNodeCommand`, `com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeAggregateMutationTest` (class, public (default))
- **Key public/internal functions:**
  - `add enforces unique node ids and valid parent() : Unit/implicit` (public (default))
  - `move enforces cycle safety and non-negative ordering() : Unit/implicit` (public (default))
  - `remove marks node as removed and rejects root removal() : Unit/implicit` (public (default))
  - `rename requires existing node and non-blank title() : Unit/implicit` (public (default))
  - `reparent rejects cycles and missing parents() : Unit/implicit` (public (default))
  - `reorder requires exact active sibling set() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregateMutationTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeAggregateMutationTest`.

## TopicTreeAggregateTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregateTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicTreeAggregateTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.AddTopicNodeCommand, com.authord.mkdocs.ports.topic.MoveTopicNodeCommand, com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand, com.authord.mkdocs.ports.topic.RenameTopicNodeCommand, com.authord.mkdocs.ports.topic.ReorderTopicNodesCommand, com.authord.mkdocs.ports.topic.ReparentTopicNodeCommand, com.authord.mkdocs.ports.topic.TopicNavNode, com.authord.mkdocs.ports.topic.TopicTreeCommandStatus
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeAggregateTest`.
  - Key callable members include `add node mutation succeeds and exposes snapshot fields`, `add node rejects missing parent`, `add node rejects duplicate id`, `add node rejects invalid title or order`, `root node removal is rejected`, `move node succeeds when parent exists and no cycle`, `move node within same parent keeps expected relative order and contiguous indices`, `reparent appends to new parent and normalizes both sibling groups`, plus `22` additional members.
  - Scenario-focused test cases include: `add node mutation succeeds and exposes snapshot fields`, `add node rejects missing parent`, `add node rejects duplicate id`, `add node rejects invalid title or order`, `root node removal is rejected`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.topic`
- **Module / feature area:** `core-domain` / `core/topic`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeAggregateTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `add node mutation succeeds and exposes snapshot fields` consumes no explicit parameters.
  - `add node rejects missing parent` consumes no explicit parameters.
  - `add node rejects duplicate id` consumes no explicit parameters.
  - `add node rejects invalid title or order` consumes no explicit parameters.
  - `root node removal is rejected` consumes no explicit parameters.
- **Outputs produced:**
  - `add node mutation succeeds and exposes snapshot fields` returns Unit/implicit.
  - `add node rejects missing parent` returns Unit/implicit.
  - `add node rejects duplicate id` returns Unit/implicit.
  - `add node rejects invalid title or order` returns Unit/implicit.
  - `root node removal is rejected` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.MoveTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.RenameTopicNodeCommand`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`, `com.authord.mkdocs.ports.topic.MoveTopicNodeCommand`, `com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeAggregateTest` (class, public (default))
- **Key public/internal functions:**
  - `add node mutation succeeds and exposes snapshot fields() : Unit/implicit` (public (default))
  - `add node rejects missing parent() : Unit/implicit` (public (default))
  - `add node rejects duplicate id() : Unit/implicit` (public (default))
  - `add node rejects invalid title or order() : Unit/implicit` (public (default))
  - `root node removal is rejected() : Unit/implicit` (public (default))
  - `move node succeeds when parent exists and no cycle() : Unit/implicit` (public (default))
  - `move node within same parent keeps expected relative order and contiguous indices() : Unit/implicit` (public (default))
  - `reparent appends to new parent and normalizes both sibling groups() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`, `@Suppress`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregateTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Large callable surface area increases regression risk; consider characterization tests before large refactors.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeAggregateTest`.

## TopicTreeValidationServiceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationServiceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicTreeValidationServiceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicNavNode
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeValidationServiceTest`.
  - Key callable members include `detects broken path references in nav`, `detects duplicate nav references`, `detects malformed external links`, `validate traverses nested children and reports missing child paths`, `validate deduplicates duplicates under case-insensitive policy`, `default policy constructor and malformed uri branches are covered`.
  - Scenario-focused test cases include: `detects broken path references in nav`, `detects duplicate nav references`, `detects malformed external links`, `validate traverses nested children and reports missing child paths`, `validate deduplicates duplicates under case-insensitive policy`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.core.topic`
- **Module / feature area:** `core-domain` / `core/topic`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeValidationServiceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `detects broken path references in nav` consumes no explicit parameters.
  - `detects duplicate nav references` consumes no explicit parameters.
  - `detects malformed external links` consumes no explicit parameters.
  - `validate traverses nested children and reports missing child paths` consumes no explicit parameters.
  - `validate deduplicates duplicates under case-insensitive policy` consumes no explicit parameters.
- **Outputs produced:**
  - `detects broken path references in nav` returns Unit/implicit.
  - `detects duplicate nav references` returns Unit/implicit.
  - `detects malformed external links` returns Unit/implicit.
  - `validate traverses nested children and reports missing child paths` returns Unit/implicit.
  - `validate deduplicates duplicates under case-insensitive policy` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicNavNode`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicNavNode`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeValidationServiceTest` (class, public (default))
- **Key public/internal functions:**
  - `detects broken path references in nav() : Unit/implicit` (public (default))
  - `detects duplicate nav references() : Unit/implicit` (public (default))
  - `detects malformed external links() : Unit/implicit` (public (default))
  - `validate traverses nested children and reports missing child paths() : Unit/implicit` (public (default))
  - `validate deduplicates duplicates under case-insensitive policy() : Unit/implicit` (public (default))
  - `default policy constructor and malformed uri branches are covered() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationServiceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeValidationServiceTest`.

## TopicTreePort.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/TopicTreePort.kt`
- **What does it do?** Declares architectural contract types in `TopicTreePort` for cross-module interaction.
- **Why it matters:** Stable port contracts preserve modularity and allow implementations to vary independently.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicTreeCommand, com.authord.mkdocs.ports.topic.TopicTreeCommandResult
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
- **Key functionalities:**
  - Main declarations: `TopicTreeApiVersionRegistry`, `TopicTreePort`.
  - Key callable members include `execute`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports`
- **Module / feature area:** `extension-ports` / `ports`
- **Architectural layer:** `architecture ports/contracts`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeApiVersionRegistry` (data class, public (default)): Primary implementation type for this file feature area.
- `TopicTreePort` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `execute` consumes command: TopicTreeCommand.
- **Outputs produced:**
  - `execute` returns TopicTreeCommandResult }.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregate.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/ExtensionPortsCoverageRemediationTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeSyncOrchestratorService.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicTreeCommand`
  - `com.authord.mkdocs.ports.topic.TopicTreeCommandResult`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregate.kt`, `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/ExtensionPortsCoverageRemediationTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicTreeCommand`, `com.authord.mkdocs.ports.topic.TopicTreeCommandResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeApiVersionRegistry` (data class, public (default))
  - `TopicTreePort` (interface, public (default))
- **Key public/internal functions:**
  - `execute(command: TopicTreeCommand) : TopicTreeCommandResult }` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- `TopicTreeApiVersionRegistry` models structured data with fields: val port: String = TOPIC_TREE_PORT_API_VERSION, val applicationService: String = TOPIC_TREE_APP_SERVICE_API_VERSION, val uiService: String = TOPIC_TREE_UI_SERVICE_API_VERSION.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `tests/contract/topic-tree-command-port/TopicTreePortContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/ExtensionPortsCoverageRemediationTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/PluginCommandBusContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/TopicTreeCommandDtosCoverageTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = TopicTreeApiVersionRegistry(/* collaborators */)
val result = component.execute(/* inputs */)
```
Typical caller path: `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregate.kt` -> `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/TopicTreePort.kt`.

## DefaultPluginCommandBus.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/command/DefaultPluginCommandBus.kt`
- **What does it do?** Contains feature logic for `DefaultPluginCommandBus` in the `extension-ports` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicTreeCommand, com.authord.mkdocs.ports.topic.TopicTreeCommandResult, com.authord.mkdocs.ports.topic.TopicTreeCommandStatus, com.authord.mkdocs.ports.topic.TopicTreeViolation
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
- **Key functionalities:**
  - Main declarations: `DefaultPluginCommandBus`.
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.command`
- **Module / feature area:** `extension-ports` / `ports/command`
- **Architectural layer:** `architecture ports/contracts`

### 2. Main classes / interfaces / objects inside the file
- `DefaultPluginCommandBus` (class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/PluginCommandBusContractTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
  - `tests/contract/topic-tree-command-port/TopicTreePortContractTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicTreeCommand`
  - `com.authord.mkdocs.ports.topic.TopicTreeCommandResult`
  - `com.authord.mkdocs.ports.topic.TopicTreeCommandStatus`
  - `com.authord.mkdocs.ports.topic.TopicTreeViolation`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/PluginCommandBusContractTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`, `tests/contract/topic-tree-command-port/TopicTreePortContractTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicTreeCommand`, `com.authord.mkdocs.ports.topic.TopicTreeCommandResult`, `com.authord.mkdocs.ports.topic.TopicTreeCommandStatus`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `DefaultPluginCommandBus` (class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- File is mostly declarative (types/contracts) with limited embedded business rules.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - No obvious path match found from static name/package analysis.
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - No obvious name-matched test file found; behavior changes may need additional targeted tests.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
// Construct `DefaultPluginCommandBus` and use exposed members
```
Typical caller path: `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/PluginCommandBusContractTest.kt` -> `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/command/DefaultPluginCommandBus.kt`.

## PluginCommandBus.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/command/PluginCommandBus.kt`
- **What does it do?** Contains feature logic for `PluginCommandBus` in the `extension-ports` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicTreeCommand, com.authord.mkdocs.ports.topic.TopicTreeCommandResult
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
- **Key functionalities:**
  - Main declarations: `CommandRegistry`, `PluginCommandBus`.
  - Key callable members include `handle`, `register`, `resolve`, `dispatch`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.command`
- **Module / feature area:** `extension-ports` / `ports/command`
- **Architectural layer:** `architecture ports/contracts`

### 2. Main classes / interfaces / objects inside the file
- `CommandRegistry` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.
- `PluginCommandBus` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `handle` consumes command: TopicTreeCommand.
  - `register` consumes commandType: String, handler: TopicTreeCommandHandler.
  - `resolve` consumes commandType: String.
  - `dispatch` consumes command: TopicTreeCommand.
- **Outputs produced:**
  - `handle` returns TopicTreeCommandResult } /** * Registry contract mapping command type identifiers to handlers. * * API Version: [COMMAND_BUS_API_VERSION] * * Constraints: * - `commandType` keys are expected to align with `TopicTreeCommandType.name`. * - Missing handlers should resolve as `null` and be handled by dispatch policy. */ interface CommandRegistry.
  - `register` returns Unit/implicit.
  - `resolve` returns TopicTreeCommandHandler? } /** * Bus contract for routing topic-tree commands through a registry. * * API Version: [COMMAND_BUS_API_VERSION] */ interface PluginCommandBus.
  - `dispatch` returns TopicTreeCommandResult }.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/command/DefaultPluginCommandBus.kt`
  - `modules/infra-defaults/src/main/kotlin/com/authord/mkdocs/defaults/command/InMemoryCommandRegistry.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicTreeCommand`
  - `com.authord.mkdocs.ports.topic.TopicTreeCommandResult`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/command/DefaultPluginCommandBus.kt`, `modules/infra-defaults/src/main/kotlin/com/authord/mkdocs/defaults/command/InMemoryCommandRegistry.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicTreeCommand`, `com.authord.mkdocs.ports.topic.TopicTreeCommandResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `CommandRegistry` (interface, public (default))
  - `PluginCommandBus` (interface, public (default))
- **Key public/internal functions:**
  - `handle(command: TopicTreeCommand) : TopicTreeCommandResult } /** * Registry contract mapping command type identifiers to handlers. * * API Version: [COMMAND_BUS_API_VERSION] * * Constraints: * - `commandType` keys are expected to align with `TopicTreeCommandType.name`. * - Missing handlers should resolve as `null` and be handled by dispatch policy. */ interface CommandRegistry` (public (default))
  - `register(commandType: String, handler: TopicTreeCommandHandler) : Unit/implicit` (public (default))
  - `resolve(commandType: String) : TopicTreeCommandHandler? } /** * Bus contract for routing topic-tree commands through a registry. * * API Version: [COMMAND_BUS_API_VERSION] */ interface PluginCommandBus` (public (default))
  - `dispatch(command: TopicTreeCommand) : TopicTreeCommandResult }` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/PluginCommandBusContractTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val result = implementation.handle(/* inputs */) // implementation : CommandRegistry
```
Typical caller path: `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/command/DefaultPluginCommandBus.kt` -> `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/command/PluginCommandBus.kt`.

## PreviewSyncPort.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/preview/PreviewSyncPort.kt`
- **What does it do?** Declares architectural contract types in `PreviewSyncPort` for cross-module interaction.
- **Why it matters:** Stable port contracts preserve modularity and allow implementations to vary independently.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
- **Key functionalities:**
  - Main declarations: `PreviewSyncPort`.
  - Key callable members include `onEditorScrollSemanticDelta`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.preview`
- **Module / feature area:** `extension-ports` / `ports/preview`
- **Architectural layer:** `architecture ports/contracts`

### 2. Main classes / interfaces / objects inside the file
- `PreviewSyncPort` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `onEditorScrollSemanticDelta` consumes projectId: String, documentPath: String, delta: Int.
- **Outputs produced:**
  - `onEditorScrollSemanticDelta` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/infra-defaults/src/main/kotlin/com/authord/mkdocs/defaults/preview/NoOpPreviewSyncAdapter.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/infra-defaults/src/main/kotlin/com/authord/mkdocs/defaults/preview/NoOpPreviewSyncAdapter.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewSyncPort` (interface, public (default))
- **Key public/internal functions:**
  - `onEditorScrollSemanticDelta(projectId: String, documentPath: String, delta: Int) : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - No obvious path match found from static name/package analysis.
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - No obvious name-matched test file found; behavior changes may need additional targeted tests.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val result = implementation.onEditorScrollSemanticDelta(/* inputs */) // implementation : PreviewSyncPort
```
Typical caller path: `modules/infra-defaults/src/main/kotlin/com/authord/mkdocs/defaults/preview/NoOpPreviewSyncAdapter.kt` -> `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/preview/PreviewSyncPort.kt`.

## DocsFileGateway.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/DocsFileGateway.kt`
- **What does it do?** Implements adapter/gateway behavior in `DocsFileGateway` to connect domain flows with runtime or IO APIs.
- **Why it matters:** Adapter files are critical integration points where external behavior is normalized for the rest of the system.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Translate internal commands/models to runtime, filesystem, or integration APIs.
- **Key functionalities:**
  - Main declarations: `DocsFileGateway`.
  - Key callable members include `createMarkdownFile`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.topic`
- **Module / feature area:** `extension-ports` / `ports/topic`
- **Architectural layer:** `architecture ports/contracts`

### 2. Main classes / interfaces / objects inside the file
- `DocsFileGateway` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `createMarkdownFile` consumes instance: TopicInstanceRef, relativePath: String, initialContent: String.
- **Outputs produced:**
  - `createMarkdownFile` returns TopicGatewayResult<String> /** * Deletes or recovers a markdown file according to [mode]. * * Inputs: [instance], docs_dir-relative [relativePath], and delete [mode]. * Output: [TopicGatewayResult.Success] containing affected path. * Errors/failure modes: FILE_IO for delete/recovery failures, VALIDATION for unsupported modes. */ fun deleteMarkdownFile( instance: TopicInstanceRef, relativePath: String, mode: TopicDeleteMode, ): TopicGatewayResult<String> /** * Renames a markdown file while preserving docs scope. * * Inputs: [instance], [fromRelativePath], [toRelativePath]. * Output: [TopicGatewayResult.Success] containing final path. * Errors/failure modes: FILE_IO for rename failures, INSTANCE_SCOPE for out-of-scope paths. */ fun renameMarkdownFile( instance: TopicInstanceRef, fromRelativePath: String, toRelativePath: String, ): TopicGatewayResult<String> /** * Moves a markdown file to a different docs_dir-relative location. * * Inputs: [instance], [fromRelativePath], [toRelativePath]. * Output: [TopicGatewayResult.Success] containing destination path. * Errors/failure modes: FILE_IO for move failures, INSTANCE_SCOPE for cross-scope moves. */ fun moveMarkdownFile( instance: TopicInstanceRef, fromRelativePath: String, toRelativePath: String, ): TopicGatewayResult<String> /** * Rewrites relative markdown links in docs scope for a rename/move operation. * * Inputs: [instance], [fromRelativePath], [toRelativePath]. * Output: [TopicGatewayResult.Success] containing rewrite count. * Errors/failure modes: FILE_IO for document update failures, VALIDATION for malformed paths. */ fun rewriteRelativeMarkdownLinks( instance: TopicInstanceRef, fromRelativePath: String, toRelativePath: String, ): TopicGatewayResult<Int> /** * Inserts or replaces the first Markdown H1 heading for [relativePath]. * * Default behavior is a no-op success to preserve backward compatibility for * adapters that have not implemented heading synchronization. */ fun upsertMarkdownTitleHeading( instance: TopicInstanceRef, relativePath: String, title: String, ): TopicGatewayResult<String>.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/DocsFileGatewayContractTest.kt`
  - `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapter.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`, `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/DocsFileGatewayContractTest.kt`, `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapter.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `DocsFileGateway` (interface, public (default))
- **Key public/internal functions:**
  - `createMarkdownFile(instance: TopicInstanceRef, relativePath: String, initialContent: String) : TopicGatewayResult<String> /** * Deletes or recovers a markdown file according to [mode]. * * Inputs: [instance], docs_dir-relative [relativePath], and delete [mode]. * Output: [TopicGatewayResult.Success] containing affected path. * Errors/failure modes: FILE_IO for delete/recovery failures, VALIDATION for unsupported modes. */ fun deleteMarkdownFile( instance: TopicInstanceRef, relativePath: String, mode: TopicDeleteMode, ): TopicGatewayResult<String> /** * Renames a markdown file while preserving docs scope. * * Inputs: [instance], [fromRelativePath], [toRelativePath]. * Output: [TopicGatewayResult.Success] containing final path. * Errors/failure modes: FILE_IO for rename failures, INSTANCE_SCOPE for out-of-scope paths. */ fun renameMarkdownFile( instance: TopicInstanceRef, fromRelativePath: String, toRelativePath: String, ): TopicGatewayResult<String> /** * Moves a markdown file to a different docs_dir-relative location. * * Inputs: [instance], [fromRelativePath], [toRelativePath]. * Output: [TopicGatewayResult.Success] containing destination path. * Errors/failure modes: FILE_IO for move failures, INSTANCE_SCOPE for cross-scope moves. */ fun moveMarkdownFile( instance: TopicInstanceRef, fromRelativePath: String, toRelativePath: String, ): TopicGatewayResult<String> /** * Rewrites relative markdown links in docs scope for a rename/move operation. * * Inputs: [instance], [fromRelativePath], [toRelativePath]. * Output: [TopicGatewayResult.Success] containing rewrite count. * Errors/failure modes: FILE_IO for document update failures, VALIDATION for malformed paths. */ fun rewriteRelativeMarkdownLinks( instance: TopicInstanceRef, fromRelativePath: String, toRelativePath: String, ): TopicGatewayResult<Int> /** * Inserts or replaces the first Markdown H1 heading for [relativePath]. * * Default behavior is a no-op success to preserve backward compatibility for * adapters that have not implemented heading synchronization. */ fun upsertMarkdownTitleHeading( instance: TopicInstanceRef, relativePath: String, title: String, ): TopicGatewayResult<String>` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/DocsFileGatewayContractTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPortContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestratorContractTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val result = implementation.createMarkdownFile(/* inputs */) // implementation : DocsFileGateway
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt` -> `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/DocsFileGateway.kt`.

## InstanceRegistryPort.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPort.kt`
- **What does it do?** Declares architectural contract types in `InstanceRegistryPort` for cross-module interaction.
- **Why it matters:** Stable port contracts preserve modularity and allow implementations to vary independently.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
- **Key functionalities:**
  - Main declarations: `InstanceRegistryPort`.
  - Key callable members include `discoverDefaultInstance`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.topic`
- **Module / feature area:** `extension-ports` / `ports/topic`
- **Architectural layer:** `architecture ports/contracts`

### 2. Main classes / interfaces / objects inside the file
- `InstanceRegistryPort` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `discoverDefaultInstance` consumes projectRootPath: String.
- **Outputs produced:**
  - `discoverDefaultInstance` returns TopicGatewayResult<TopicInstanceRef?> /** * Registers a non-default instance provided by explicit config path. * * Input: explicit [instance] registration payload. * Output: [TopicGatewayResult.Success] with [Unit] when registration is accepted. * Errors/failure modes: VALIDATION for malformed config path or invalid instance data. */ fun registerInstance(instance: TopicInstanceRef): TopicGatewayResult<Unit> /** * Lists all registered instances. * * Input: none. * Output: [TopicGatewayResult.Success] with registered instances. * Errors/failure modes: INSTANCE_SCOPE if registry state cannot be resolved for project context. */ fun listInstances(): TopicGatewayResult<List<TopicInstanceRef>> /** * Selects and returns the active instance. * * Input: requested [instanceId]. * Output: [TopicGatewayResult.Success] with selected instance. * Errors/failure modes: INSTANCE_SCOPE for unknown or unavailable instances. */ fun selectActiveInstance(instanceId: String): TopicGatewayResult<TopicInstanceRef> /** * Returns currently active instance, if any. * * Input: none. * Output: [TopicGatewayResult.Success] with active instance or null. * Errors/failure modes: INSTANCE_SCOPE when active context cannot be resolved. */ fun activeInstance(): TopicGatewayResult<TopicInstanceRef?> }.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPortContractTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/InstanceRegistryService.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`, `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPortContractTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `InstanceRegistryPort` (interface, public (default))
- **Key public/internal functions:**
  - `discoverDefaultInstance(projectRootPath: String) : TopicGatewayResult<TopicInstanceRef?> /** * Registers a non-default instance provided by explicit config path. * * Input: explicit [instance] registration payload. * Output: [TopicGatewayResult.Success] with [Unit] when registration is accepted. * Errors/failure modes: VALIDATION for malformed config path or invalid instance data. */ fun registerInstance(instance: TopicInstanceRef): TopicGatewayResult<Unit> /** * Lists all registered instances. * * Input: none. * Output: [TopicGatewayResult.Success] with registered instances. * Errors/failure modes: INSTANCE_SCOPE if registry state cannot be resolved for project context. */ fun listInstances(): TopicGatewayResult<List<TopicInstanceRef>> /** * Selects and returns the active instance. * * Input: requested [instanceId]. * Output: [TopicGatewayResult.Success] with selected instance. * Errors/failure modes: INSTANCE_SCOPE for unknown or unavailable instances. */ fun selectActiveInstance(instanceId: String): TopicGatewayResult<TopicInstanceRef> /** * Returns currently active instance, if any. * * Input: none. * Output: [TopicGatewayResult.Success] with active instance or null. * Errors/failure modes: INSTANCE_SCOPE when active context cannot be resolved. */ fun activeInstance(): TopicGatewayResult<TopicInstanceRef?> }` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPortContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/DocsFileGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestratorContractTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val result = implementation.discoverDefaultInstance(/* inputs */) // implementation : InstanceRegistryPort
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt` -> `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPort.kt`.

## MkDocsConfigGateway.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGateway.kt`
- **What does it do?** Implements adapter/gateway behavior in `MkDocsConfigGateway` to connect domain flows with runtime or IO APIs.
- **Why it matters:** Adapter files are critical integration points where external behavior is normalized for the rest of the system.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Translate internal commands/models to runtime, filesystem, or integration APIs.
- **Key functionalities:**
  - Main declarations: `MkDocsConfigGateway`.
  - Key callable members include `loadConfig`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.topic`
- **Module / feature area:** `extension-ports` / `ports/topic`
- **Architectural layer:** `architecture ports/contracts`

### 2. Main classes / interfaces / objects inside the file
- `MkDocsConfigGateway` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `loadConfig` consumes instance: TopicInstanceRef.
- **Outputs produced:**
  - `loadConfig` returns TopicGatewayResult<MkDocsConfigDocument> /** * Writes config content for the active instance. * * Inputs: active [instance] and normalized [document]. * Output: [TopicGatewayResult.Success] with [Unit] when write completes. * Errors/failure modes: CONFIG_WRITE when persistence fails, VALIDATION for invalid document payload. */ fun writeConfig(instance: TopicInstanceRef, document: MkDocsConfigDocument): TopicGatewayResult<Unit> /** * Serializes the config deterministically to prevent diff churn. * * Input: normalized [document]. * Output: [TopicGatewayResult.Success] containing deterministic YAML content. * Errors/failure modes: CONFIG_WRITE when deterministic serialization fails. */ fun serializeDeterministically(document: MkDocsConfigDocument): TopicGatewayResult<String> }.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGatewayContractTest.kt`
  - `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/MkDocsYamlGateway.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`, `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGatewayContractTest.kt`, `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/MkDocsYamlGateway.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MkDocsConfigGateway` (interface, public (default))
- **Key public/internal functions:**
  - `loadConfig(instance: TopicInstanceRef) : TopicGatewayResult<MkDocsConfigDocument> /** * Writes config content for the active instance. * * Inputs: active [instance] and normalized [document]. * Output: [TopicGatewayResult.Success] with [Unit] when write completes. * Errors/failure modes: CONFIG_WRITE when persistence fails, VALIDATION for invalid document payload. */ fun writeConfig(instance: TopicInstanceRef, document: MkDocsConfigDocument): TopicGatewayResult<Unit> /** * Serializes the config deterministically to prevent diff churn. * * Input: normalized [document]. * Output: [TopicGatewayResult.Success] containing deterministic YAML content. * Errors/failure modes: CONFIG_WRITE when deterministic serialization fails. */ fun serializeDeterministically(document: MkDocsConfigDocument): TopicGatewayResult<String> }` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/DocsFileGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPortContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestratorContractTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val result = implementation.loadConfig(/* inputs */) // implementation : MkDocsConfigGateway
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt` -> `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGateway.kt`.

## TopicSyncError.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/TopicSyncError.kt`
- **What does it do?** Defines shared model/state types in `TopicSyncError`.
- **Why it matters:** Shared models must remain clear and stable because many workflows serialize or branch on them.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
- **Key functionalities:**
  - Main declarations: `TopicSyncErrorCode`, `TopicSyncError`, `DefaultTopicSyncError`, `TopicGatewayResult`, `Success`, `Failure`.
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.topic`
- **Module / feature area:** `extension-ports` / `ports/topic`
- **Architectural layer:** `architecture ports/contracts`

### 2. Main classes / interfaces / objects inside the file
- `TopicSyncErrorCode` (enum class, public (default)): Typed model representing shared state or payload data.
- `TopicSyncError` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.
- `DefaultTopicSyncError` (data class, public (default)): Typed model representing shared state or payload data.
- `TopicGatewayResult` (sealed class, public (default)): Adapter translating domain requests to external/runtime APIs.
- `Success` (data class, public (default)): Primary implementation type for this file feature area.
- `Failure` (data class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/DocsFileGateway.kt`
  - `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPort.kt`
  - `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGateway.kt`
  - `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestrator.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/DocsFileGateway.kt`, `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPort.kt`, `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGateway.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicSyncErrorCode` (enum class, public (default))
  - `TopicSyncError` (interface, public (default))
  - `DefaultTopicSyncError` (data class, public (default))
  - `TopicGatewayResult` (sealed class, public (default))
  - `Success` (data class, public (default))
  - `Failure` (data class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- `DefaultTopicSyncError` models structured data with fields: override val code: TopicSyncErrorCode, override val detail: String.
- `Failure` models structured data with fields: val error: TopicSyncError.
- `TopicSyncErrorCode` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/DocsFileGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPortContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestratorContractTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
// Construct `TopicSyncErrorCode` and use exposed members
```
Typical caller path: `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/DocsFileGateway.kt` -> `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/TopicSyncError.kt`.

## TopicSyncEvent.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/TopicSyncEvent.kt`
- **What does it do?** Defines shared model/state types in `TopicSyncEvent`.
- **Why it matters:** Shared models must remain clear and stable because many workflows serialize or branch on them.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
- **Key functionalities:**
  - Main declarations: `TopicSyncEventType`, `TopicSyncEvent`.
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.topic`
- **Module / feature area:** `extension-ports` / `ports/topic`
- **Architectural layer:** `architecture ports/contracts`

### 2. Main classes / interfaces / objects inside the file
- `TopicSyncEventType` (enum class, public (default)): Typed model representing shared state or payload data.
- `TopicSyncEvent` (data class, public (default)): Typed model representing shared state or payload data.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/ExtensionPortsCoverageRemediationTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeObservability.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/ExtensionPortsCoverageRemediationTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeObservability.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicSyncEventType` (enum class, public (default))
  - `TopicSyncEvent` (data class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- `TopicSyncEvent` models structured data with fields: val eventType: TopicSyncEventType, val projectId: String, val instanceId: String, val transactionId: String, val severity: String.
- `TopicSyncEventType` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/DocsFileGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPortContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestratorContractTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
// Construct `TopicSyncEventType` and use exposed members
```
Typical caller path: `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/ExtensionPortsCoverageRemediationTest.kt` -> `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/TopicSyncEvent.kt`.

## TopicSyncModels.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/TopicSyncModels.kt`
- **What does it do?** Defines shared model/state types in `TopicSyncModels`.
- **Why it matters:** Shared models must remain clear and stable because many workflows serialize or branch on them.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
- **Key functionalities:**
  - Main declarations: `TopicInstanceRef`, `TopicNavNode`, `MkDocsConfigDocument`, `TopicFileOperationKind`, `TopicDeleteMode`, `TopicFileOperation`, plus `2` more.
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.topic`
- **Module / feature area:** `extension-ports` / `ports/topic`
- **Architectural layer:** `architecture ports/contracts`

### 2. Main classes / interfaces / objects inside the file
- `TopicInstanceRef` (data class, public (default)): Primary implementation type for this file feature area.
- `TopicNavNode` (data class, public (default)): Primary implementation type for this file feature area.
- `MkDocsConfigDocument` (data class, public (default)): Primary implementation type for this file feature area.
- `TopicFileOperationKind` (enum class, public (default)): Primary implementation type for this file feature area.
- `TopicDeleteMode` (enum class, public (default)): Primary implementation type for this file feature area.
- `TopicFileOperation` (data class, public (default)): Primary implementation type for this file feature area.
- `TopicSyncTransaction` (data class, public (default)): IDE interaction entry point for user/editor events.
- `TopicSyncOutcome` (data class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregate.kt`
  - `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationService.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregateTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationServiceTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregate.kt`, `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeValidationService.kt`, `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregateTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicInstanceRef` (data class, public (default))
  - `TopicNavNode` (data class, public (default))
  - `MkDocsConfigDocument` (data class, public (default))
  - `TopicFileOperationKind` (enum class, public (default))
  - `TopicDeleteMode` (enum class, public (default))
  - `TopicFileOperation` (data class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- `TopicInstanceRef` models structured data with fields: val instanceId: String, val configPath: String, val docsDirPath: String.
- `TopicNavNode` models structured data with fields: val nodeId: String, val title: String, val path: String? = null, val externalUrl: String? = null, val children: List<TopicNavNode> = emptyList(.
- `MkDocsConfigDocument` models structured data with fields: val docsDir: String, val nav: List<TopicNavNode>, val notInNav: List<String> = emptyList(.
- `TopicFileOperationKind` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/DocsFileGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPortContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestratorContractTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
// Construct `TopicInstanceRef` and use exposed members
```
Typical caller path: `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregate.kt` -> `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/TopicSyncModels.kt`.

## TopicTreeAggregateBootstrapPort.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/TopicTreeAggregateBootstrapPort.kt`
- **What does it do?** Declares architectural contract types in `TopicTreeAggregateBootstrapPort` for cross-module interaction.
- **Why it matters:** Stable port contracts preserve modularity and allow implementations to vary independently.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
- **Key functionalities:**
  - Main declarations: `TopicTreeAggregateBootstrapPort`, `TopicTreeAggregateRefreshPort`.
  - Key callable members include `bootstrapTreeFromNav`, `refreshTreeFromNav`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.topic`
- **Module / feature area:** `extension-ports` / `ports/topic`
- **Architectural layer:** `architecture ports/contracts`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeAggregateBootstrapPort` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.
- `TopicTreeAggregateRefreshPort` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `bootstrapTreeFromNav` consumes treeId: String, nav: List<TopicNavNode>.
  - `refreshTreeFromNav` consumes treeId: String, nav: List<TopicNavNode>.
- **Outputs produced:**
  - `bootstrapTreeFromNav` returns Unit/implicit.
  - `refreshTreeFromNav` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregate.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeSyncOrchestratorService.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregate.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeSyncOrchestratorService.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeAggregateBootstrapPort` (interface, public (default))
  - `TopicTreeAggregateRefreshPort` (interface, public (default))
- **Key public/internal functions:**
  - `bootstrapTreeFromNav(treeId: String, nav: List<TopicNavNode>) : Unit/implicit` (public (default))
  - `refreshTreeFromNav(treeId: String, nav: List<TopicNavNode>) : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregateTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/DocsFileGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPortContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestratorContractTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val result = implementation.bootstrapTreeFromNav(/* inputs */) // implementation : TopicTreeAggregateBootstrapPort
```
Typical caller path: `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregate.kt` -> `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/TopicTreeAggregateBootstrapPort.kt`.

## TopicTreeCommandDtos.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/TopicTreeCommandDtos.kt`
- **What does it do?** Defines shared model/state types in `TopicTreeCommandDtos`.
- **Why it matters:** Shared models must remain clear and stable because many workflows serialize or branch on them.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
- **Key functionalities:**
  - Main declarations: `TopicTreeCommandType`, `TopicTreeCommand`, `AddTopicNodeCommand`, `AddChildTopicNodeCommand`, `AddExistingFileTopicNodeCommand`, `AddExternalLinkTopicNodeCommand`, plus `9` more.
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.topic`
- **Module / feature area:** `extension-ports` / `ports/topic`
- **Architectural layer:** `architecture ports/contracts`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeCommandType` (enum class, public (default)): Primary implementation type for this file feature area.
- `TopicTreeCommand` (sealed interface, public (default)): Contract/abstraction used to decouple callers from implementations.
- `AddTopicNodeCommand` (data class, public (default)): Typed model representing shared state or payload data.
- `AddChildTopicNodeCommand` (data class, public (default)): Typed model representing shared state or payload data.
- `AddExistingFileTopicNodeCommand` (data class, public (default)): Primary implementation type for this file feature area.
- `AddExternalLinkTopicNodeCommand` (data class, public (default)): Primary implementation type for this file feature area.
- `MoveTopicNodeCommand` (data class, public (default)): Primary implementation type for this file feature area.
- `RemoveTopicNodeCommand` (data class, public (default)): Primary implementation type for this file feature area.
- `RenameTopicNodeCommand` (data class, public (default)): Primary implementation type for this file feature area.
- `ReparentTopicNodeCommand` (data class, public (default)): Primary implementation type for this file feature area.
- `ReorderTopicNodesCommand` (data class, public (default)): Primary implementation type for this file feature area.
- `ValidateTopicTreeCommand` (data class, public (default)): Primary implementation type for this file feature area.
- Additional declarations: `3` more declarations not listed individually.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregate.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddChildTransformPolicyTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddExistingFileCommandTest.kt`
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/ExternalLinkCommandTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregate.kt`, `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddChildTransformPolicyTest.kt`, `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/topic/AddExistingFileCommandTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeCommandType` (enum class, public (default))
  - `TopicTreeCommand` (sealed interface, public (default))
  - `AddTopicNodeCommand` (data class, public (default))
  - `AddChildTopicNodeCommand` (data class, public (default))
  - `AddExistingFileTopicNodeCommand` (data class, public (default))
  - `AddExternalLinkTopicNodeCommand` (data class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- `AddTopicNodeCommand` models structured data with fields: override val commandId: String, override val treeId: String, val parentNodeId: String, val nodeId: String, val title: String.
- `AddChildTopicNodeCommand` models structured data with fields: override val commandId: String, override val treeId: String, val targetNodeId: String, val childNodeId: String, val childTitle: String.
- `AddExistingFileTopicNodeCommand` models structured data with fields: override val commandId: String, override val treeId: String, val parentNodeId: String, val nodeId: String, val title: String.
- `TopicTreeCommandType` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/TopicTreeCommandDtosCoverageTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/DocsFileGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPortContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestratorContractTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
// Construct `TopicTreeCommandType` and use exposed members
```
Typical caller path: `modules/core-domain/src/main/kotlin/com/authord/mkdocs/core/topic/TopicTreeAggregate.kt` -> `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/TopicTreeCommandDtos.kt`.

## TreeSyncOrchestrator.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestrator.kt`
- **What does it do?** Contains feature logic for `TreeSyncOrchestrator` in the `extension-ports` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
- **Key functionalities:**
  - Main declarations: `TreeSyncOrchestrator`.
  - Key callable members include `apply`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.topic`
- **Module / feature area:** `extension-ports` / `ports/topic`
- **Architectural layer:** `architecture ports/contracts`

### 2. Main classes / interfaces / objects inside the file
- `TreeSyncOrchestrator` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `apply` consumes transaction: TopicSyncTransaction.
- **Outputs produced:**
  - `apply` returns TopicGatewayResult<TopicSyncOutcome> /** * Rolls back an already-started transaction. * * Inputs: prior [transactionId] and human-readable [reason]. * Output: rollback [TopicSyncOutcome] status. * Errors/failure modes: ORCHESTRATION when rollback could not be executed. */ fun rollback(transactionId: String, reason: String): TopicGatewayResult<TopicSyncOutcome> /** * Executes compensating actions when full rollback is not feasible. * * Inputs: prior [transactionId] and [reason]. * Output: compensated [TopicSyncOutcome] status. * Errors/failure modes: ORCHESTRATION when compensation cannot be completed. */ fun compensate(transactionId: String, reason: String): TopicGatewayResult<TopicSyncOutcome> }.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestratorContractTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeApplicationServiceImpl.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`, `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestratorContractTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TreeSyncOrchestrator` (interface, public (default))
- **Key public/internal functions:**
  - `apply(transaction: TopicSyncTransaction) : TopicGatewayResult<TopicSyncOutcome> /** * Rolls back an already-started transaction. * * Inputs: prior [transactionId] and human-readable [reason]. * Output: rollback [TopicSyncOutcome] status. * Errors/failure modes: ORCHESTRATION when rollback could not be executed. */ fun rollback(transactionId: String, reason: String): TopicGatewayResult<TopicSyncOutcome> /** * Executes compensating actions when full rollback is not feasible. * * Inputs: prior [transactionId] and [reason]. * Output: compensated [TopicSyncOutcome] status. * Errors/failure modes: ORCHESTRATION when compensation cannot be completed. */ fun compensate(transactionId: String, reason: String): TopicGatewayResult<TopicSyncOutcome> }` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestratorContractTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeSyncOrchestratorCacheFreshnessTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeSyncOrchestratorPersistenceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TreeSyncOrchestratorAtomicityTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/DocsFileGatewayContractTest.kt`
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPortContractTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val result = implementation.apply(/* inputs */) // implementation : TreeSyncOrchestrator
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt` -> `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestrator.kt`.

## VectorStorePort.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/vector/VectorStorePort.kt`
- **What does it do?** Declares architectural contract types in `VectorStorePort` for cross-module interaction.
- **Why it matters:** Stable port contracts preserve modularity and allow implementations to vary independently.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
- **Key functionalities:**
  - Main declarations: `VectorStorePort`.
  - Key callable members include `upsert`, `search`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.vector`
- **Module / feature area:** `extension-ports` / `ports/vector`
- **Architectural layer:** `architecture ports/contracts`

### 2. Main classes / interfaces / objects inside the file
- `VectorStorePort` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `upsert` consumes documentId: String, content: String.
  - `search` consumes query: String, limit: Int.
- **Outputs produced:**
  - `upsert` returns Unit/implicit.
  - `search` returns List<String> }.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/infra-defaults/src/main/kotlin/com/authord/mkdocs/defaults/vector/NoOpVectorStoreAdapter.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/infra-defaults/src/main/kotlin/com/authord/mkdocs/defaults/vector/NoOpVectorStoreAdapter.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `VectorStorePort` (interface, public (default))
- **Key public/internal functions:**
  - `upsert(documentId: String, content: String) : Unit/implicit` (public (default))
  - `search(query: String, limit: Int) : List<String> }` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - No obvious path match found from static name/package analysis.
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - No obvious name-matched test file found; behavior changes may need additional targeted tests.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val result = implementation.upsert(/* inputs */) // implementation : VectorStorePort
```
Typical caller path: `modules/infra-defaults/src/main/kotlin/com/authord/mkdocs/defaults/vector/NoOpVectorStoreAdapter.kt` -> `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/vector/VectorStorePort.kt`.

## ExtensionPortsCoverageRemediationTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/ExtensionPortsCoverageRemediationTest.kt`
- **What does it do?** Implements unit-focused test coverage around `ExtensionPortsCoverageRemediationTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand, com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand, com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand, com.authord.mkdocs.ports.topic.AddTopicNodeCommand, com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.TopicFileOperation, com.authord.mkdocs.ports.topic.TopicFileOperationKind
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `ExtensionPortsCoverageRemediationTest`.
  - Key callable members include `covers topic sync events and errors`, `covers topic sync model defaults and explicit values`, `covers additional command dto variants`, `covers topic tree api version registry defaults`.
  - Scenario-focused test cases include: `covers topic sync events and errors`, `covers topic sync model defaults and explicit values`, `covers additional command dto variants`, `covers topic tree api version registry defaults`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports`
- **Module / feature area:** `extension-ports` / `ports`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `ExtensionPortsCoverageRemediationTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `covers topic sync events and errors` consumes no explicit parameters.
  - `covers topic sync model defaults and explicit values` consumes no explicit parameters.
  - `covers additional command dto variants` consumes no explicit parameters.
  - `covers topic tree api version registry defaults` consumes no explicit parameters.
- **Outputs produced:**
  - `covers topic sync events and errors` returns Unit/implicit.
  - `covers topic sync model defaults and explicit values` returns Unit/implicit.
  - `covers additional command dto variants` returns Unit/implicit.
  - `covers topic tree api version registry defaults` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`, `com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand`, `com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `ExtensionPortsCoverageRemediationTest` (class, public (default))
- **Key public/internal functions:**
  - `covers topic sync events and errors() : Unit/implicit` (public (default))
  - `covers topic sync model defaults and explicit values() : Unit/implicit` (public (default))
  - `covers additional command dto variants() : Unit/implicit` (public (default))
  - `covers topic tree api version registry defaults() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/ExtensionPortsCoverageRemediationTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *ExtensionPortsCoverageRemediationTest`.

## PluginCommandBusContractTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/PluginCommandBusContractTest.kt`
- **What does it do?** Defines contract assertions for `PluginCommandBusContractTest` to enforce port/interface compatibility.
- **Why it matters:** It keeps interchangeable implementations behaviorally aligned with shared contracts.
- **Dependencies:**
  - Internal: com.authord.mkdocs.defaults.command.InMemoryCommandRegistry, com.authord.mkdocs.ports.command.DefaultPluginCommandBus, com.authord.mkdocs.ports.topic.TopicTreeCommandResult, com.authord.mkdocs.ports.topic.TopicTreeCommandStatus, com.authord.mkdocs.ports.topic.ValidateTopicTreeCommand
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PluginCommandBusContractTest`.
  - Key callable members include `dispatches command to registered handler`, `returns rejected result when no handler exists`.
  - Scenario-focused test cases include: `dispatches command to registered handler`, `returns rejected result when no handler exists`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports`
- **Module / feature area:** `extension-ports` / `ports`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PluginCommandBusContractTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `dispatches command to registered handler` consumes no explicit parameters.
  - `returns rejected result when no handler exists` consumes no explicit parameters.
- **Outputs produced:**
  - `dispatches command to registered handler` returns Unit/implicit.
  - `returns rejected result when no handler exists` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.defaults.command.InMemoryCommandRegistry`
  - `com.authord.mkdocs.ports.command.DefaultPluginCommandBus`
  - `com.authord.mkdocs.ports.topic.TopicTreeCommandResult`
  - `com.authord.mkdocs.ports.topic.TopicTreeCommandStatus`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.defaults.command.InMemoryCommandRegistry`, `com.authord.mkdocs.ports.command.DefaultPluginCommandBus`, `com.authord.mkdocs.ports.topic.TopicTreeCommandResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PluginCommandBusContractTest` (class, public (default))
- **Key public/internal functions:**
  - `dispatches command to registered handler() : Unit/implicit` (public (default))
  - `returns rejected result when no handler exists() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/PluginCommandBusContractTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PluginCommandBusContractTest`.

## TopicTreeCommandDtosCoverageTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/TopicTreeCommandDtosCoverageTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicTreeCommandDtosCoverageTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.AddTopicNodeCommand, com.authord.mkdocs.ports.topic.MoveTopicNodeCommand, com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand, com.authord.mkdocs.ports.topic.RenameTopicNodeCommand, com.authord.mkdocs.ports.topic.ReorderTopicNodesCommand, com.authord.mkdocs.ports.topic.ReparentTopicNodeCommand, com.authord.mkdocs.ports.topic.TopicTreeCommandResult, com.authord.mkdocs.ports.topic.TopicTreeCommandStatus
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeCommandDtosCoverageTest`.
  - Key callable members include `command dtos expose expected fields and command types`, `result and violation dtos expose fields`.
  - Scenario-focused test cases include: `command dtos expose expected fields and command types`, `result and violation dtos expose fields`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports`
- **Module / feature area:** `extension-ports` / `ports`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeCommandDtosCoverageTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `command dtos expose expected fields and command types` consumes no explicit parameters.
  - `result and violation dtos expose fields` consumes no explicit parameters.
- **Outputs produced:**
  - `command dtos expose expected fields and command types` returns Unit/implicit.
  - `result and violation dtos expose fields` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.MoveTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.RenameTopicNodeCommand`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`, `com.authord.mkdocs.ports.topic.MoveTopicNodeCommand`, `com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeCommandDtosCoverageTest` (class, public (default))
- **Key public/internal functions:**
  - `command dtos expose expected fields and command types() : Unit/implicit` (public (default))
  - `result and violation dtos expose fields() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- Model-centric declarations: `TopicTreeCommandDtosCoverageTest`.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/TopicTreeCommandDtosCoverageTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeCommandDtosCoverageTest`.

## DocsFileGatewayContractTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/DocsFileGatewayContractTest.kt`
- **What does it do?** Defines contract assertions for `DocsFileGatewayContractTest` to enforce port/interface compatibility.
- **Why it matters:** It keeps interchangeable implementations behaviorally aligned with shared contracts.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `DocsFileGatewayContractTest`.
  - Key callable members include `defines markdown create delete rename move rewrite and heading sync operations`, `delete mode supports recoverable and nav only variants`, `default heading sync operation returns success with same relative path`.
  - Scenario-focused test cases include: `defines markdown create delete rename move rewrite and heading sync operations`, `delete mode supports recoverable and nav only variants`, `default heading sync operation returns success with same relative path`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.topic`
- **Module / feature area:** `extension-ports` / `ports/topic`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `DocsFileGatewayContractTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `defines markdown create delete rename move rewrite and heading sync operations` consumes no explicit parameters.
  - `delete mode supports recoverable and nav only variants` consumes no explicit parameters.
  - `default heading sync operation returns success with same relative path` consumes no explicit parameters.
- **Outputs produced:**
  - `defines markdown create delete rename move rewrite and heading sync operations` returns Unit/implicit.
  - `delete mode supports recoverable and nav only variants` returns Unit/implicit.
  - `default heading sync operation returns success with same relative path` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `DocsFileGatewayContractTest` (class, public (default))
- **Key public/internal functions:**
  - `defines markdown create delete rename move rewrite and heading sync operations() : Unit/implicit` (public (default))
  - `delete mode supports recoverable and nav only variants() : Unit/implicit` (public (default))
  - `default heading sync operation returns success with same relative path() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/DocsFileGatewayContractTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *DocsFileGatewayContractTest`.

## InstanceRegistryPortContractTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPortContractTest.kt`
- **What does it do?** Defines contract assertions for `InstanceRegistryPortContractTest` to enforce port/interface compatibility.
- **Why it matters:** It keeps interchangeable implementations behaviorally aligned with shared contracts.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `InstanceRegistryPortContractTest`.
  - Key callable members include `defines default discovery registration listing selection and active instance lookup`, `instance reference includes id config and docs paths`.
  - Scenario-focused test cases include: `defines default discovery registration listing selection and active instance lookup`, `instance reference includes id config and docs paths`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.topic`
- **Module / feature area:** `extension-ports` / `ports/topic`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `InstanceRegistryPortContractTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `defines default discovery registration listing selection and active instance lookup` consumes no explicit parameters.
  - `instance reference includes id config and docs paths` consumes no explicit parameters.
- **Outputs produced:**
  - `defines default discovery registration listing selection and active instance lookup` returns Unit/implicit.
  - `instance reference includes id config and docs paths` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `InstanceRegistryPortContractTest` (class, public (default))
- **Key public/internal functions:**
  - `defines default discovery registration listing selection and active instance lookup() : Unit/implicit` (public (default))
  - `instance reference includes id config and docs paths() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/InstanceRegistryPortContractTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *InstanceRegistryPortContractTest`.

## MkDocsConfigGatewayContractTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGatewayContractTest.kt`
- **What does it do?** Defines contract assertions for `MkDocsConfigGatewayContractTest` to enforce port/interface compatibility.
- **Why it matters:** It keeps interchangeable implementations behaviorally aligned with shared contracts.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `MkDocsConfigGatewayContractTest`.
  - Key callable members include `defines deterministic config load write and serialize contract`, `config document keeps nav and not in nav collections`.
  - Scenario-focused test cases include: `defines deterministic config load write and serialize contract`, `config document keeps nav and not in nav collections`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.topic`
- **Module / feature area:** `extension-ports` / `ports/topic`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `MkDocsConfigGatewayContractTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `defines deterministic config load write and serialize contract` consumes no explicit parameters.
  - `config document keeps nav and not in nav collections` consumes no explicit parameters.
- **Outputs produced:**
  - `defines deterministic config load write and serialize contract` returns Unit/implicit.
  - `config document keeps nav and not in nav collections` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MkDocsConfigGatewayContractTest` (class, public (default))
- **Key public/internal functions:**
  - `defines deterministic config load write and serialize contract() : Unit/implicit` (public (default))
  - `config document keeps nav and not in nav collections() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/MkDocsConfigGatewayContractTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *MkDocsConfigGatewayContractTest`.

## TreeSyncOrchestratorContractTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestratorContractTest.kt`
- **What does it do?** Defines contract assertions for `TreeSyncOrchestratorContractTest` to enforce port/interface compatibility.
- **Why it matters:** It keeps interchangeable implementations behaviorally aligned with shared contracts.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TreeSyncOrchestratorContractTest`.
  - Key callable members include `defines apply rollback and compensate contract`, `sync transaction requires command and instance context`.
  - Scenario-focused test cases include: `defines apply rollback and compensate contract`, `sync transaction requires command and instance context`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ports.topic`
- **Module / feature area:** `extension-ports` / `ports/topic`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TreeSyncOrchestratorContractTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `defines apply rollback and compensate contract` consumes no explicit parameters.
  - `sync transaction requires command and instance context` consumes no explicit parameters.
- **Outputs produced:**
  - `defines apply rollback and compensate contract` returns Unit/implicit.
  - `sync transaction requires command and instance context` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TreeSyncOrchestratorContractTest` (class, public (default))
- **Key public/internal functions:**
  - `defines apply rollback and compensate contract() : Unit/implicit` (public (default))
  - `sync transaction requires command and instance context() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Architecture boundary contract for decoupled modules.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/topic/TreeSyncOrchestratorContractTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TreeSyncOrchestratorContractTest`.

## InMemoryCommandRegistry.kt

### Basic file information
- **Path to the `.kt` file:** `modules/infra-defaults/src/main/kotlin/com/authord/mkdocs/defaults/command/InMemoryCommandRegistry.kt`
- **What does it do?** Contains feature logic for `InMemoryCommandRegistry` in the `infra-defaults` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.command.CommandRegistry, com.authord.mkdocs.ports.command.TopicTreeCommandHandler
  - External: java.util.concurrent.ConcurrentHashMap
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
- **Key functionalities:**
  - Main declarations: `InMemoryCommandRegistry`.
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.defaults.command`
- **Module / feature area:** `infra-defaults` / `defaults/command`
- **Architectural layer:** `infrastructure defaults`

### 2. Main classes / interfaces / objects inside the file
- `InMemoryCommandRegistry` (class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/PluginCommandBusContractTest.kt`
  - `modules/infra-defaults/src/test/kotlin/com/authord/mkdocs/defaults/command/InMemoryCommandRegistryTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
  - `tests/contract/topic-tree-command-port/TopicTreePortContractTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.command.CommandRegistry`
  - `com.authord.mkdocs.ports.command.TopicTreeCommandHandler`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/PluginCommandBusContractTest.kt`, `modules/infra-defaults/src/test/kotlin/com/authord/mkdocs/defaults/command/InMemoryCommandRegistryTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`. Downstream collaborators include `com.authord.mkdocs.ports.command.CommandRegistry`, `com.authord.mkdocs.ports.command.TopicTreeCommandHandler`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `InMemoryCommandRegistry` (class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- File is mostly declarative (types/contracts) with limited embedded business rules.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/infra-defaults/src/test/kotlin/com/authord/mkdocs/defaults/command/InMemoryCommandRegistryTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
// Construct `InMemoryCommandRegistry` and use exposed members
```
Typical caller path: `modules/extension-ports/src/test/kotlin/com/authord/mkdocs/ports/PluginCommandBusContractTest.kt` -> `modules/infra-defaults/src/main/kotlin/com/authord/mkdocs/defaults/command/InMemoryCommandRegistry.kt`.

## NoOpPreviewSyncAdapter.kt

### Basic file information
- **Path to the `.kt` file:** `modules/infra-defaults/src/main/kotlin/com/authord/mkdocs/defaults/preview/NoOpPreviewSyncAdapter.kt`
- **What does it do?** Implements adapter/gateway behavior in `NoOpPreviewSyncAdapter` to connect domain flows with runtime or IO APIs.
- **Why it matters:** Adapter files are critical integration points where external behavior is normalized for the rest of the system.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.preview.PreviewSyncPort
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Translate internal commands/models to runtime, filesystem, or integration APIs.
- **Key functionalities:**
  - Main declarations: `NoOpPreviewSyncAdapter`.
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.defaults.preview`
- **Module / feature area:** `infra-defaults` / `defaults/preview`
- **Architectural layer:** `infrastructure defaults`

### 2. Main classes / interfaces / objects inside the file
- `NoOpPreviewSyncAdapter` (class, public (default)): Adapter translating domain requests to external/runtime APIs.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/infra-defaults/src/test/kotlin/com/authord/mkdocs/defaults/ports/NoOpAdaptersTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
  - `tests/contract/topic-tree-command-port/TopicTreePortContractTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.preview.PreviewSyncPort`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/infra-defaults/src/test/kotlin/com/authord/mkdocs/defaults/ports/NoOpAdaptersTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`, `tests/contract/topic-tree-command-port/TopicTreePortContractTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.preview.PreviewSyncPort`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `NoOpPreviewSyncAdapter` (class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - No obvious path match found from static name/package analysis.
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - No obvious name-matched test file found; behavior changes may need additional targeted tests.

### 14. Known limitations / technical debt
- Implements a no-op fallback; behavior is intentionally limited and may hide missing integration wiring.

### 15. Example usage
```kotlin
// Construct `NoOpPreviewSyncAdapter` and use exposed members
```
Typical caller path: `modules/infra-defaults/src/test/kotlin/com/authord/mkdocs/defaults/ports/NoOpAdaptersTest.kt` -> `modules/infra-defaults/src/main/kotlin/com/authord/mkdocs/defaults/preview/NoOpPreviewSyncAdapter.kt`.

## NoOpVectorStoreAdapter.kt

### Basic file information
- **Path to the `.kt` file:** `modules/infra-defaults/src/main/kotlin/com/authord/mkdocs/defaults/vector/NoOpVectorStoreAdapter.kt`
- **What does it do?** Implements adapter/gateway behavior in `NoOpVectorStoreAdapter` to connect domain flows with runtime or IO APIs.
- **Why it matters:** Adapter files are critical integration points where external behavior is normalized for the rest of the system.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.vector.VectorStorePort
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Translate internal commands/models to runtime, filesystem, or integration APIs.
- **Key functionalities:**
  - Main declarations: `NoOpVectorStoreAdapter`.
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.defaults.vector`
- **Module / feature area:** `infra-defaults` / `defaults/vector`
- **Architectural layer:** `infrastructure defaults`

### 2. Main classes / interfaces / objects inside the file
- `NoOpVectorStoreAdapter` (class, public (default)): Adapter translating domain requests to external/runtime APIs.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/infra-defaults/src/test/kotlin/com/authord/mkdocs/defaults/ports/NoOpAdaptersTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
  - `tests/contract/topic-tree-command-port/TopicTreePortContractTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.vector.VectorStorePort`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/infra-defaults/src/test/kotlin/com/authord/mkdocs/defaults/ports/NoOpAdaptersTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`, `tests/contract/topic-tree-command-port/TopicTreePortContractTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.vector.VectorStorePort`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `NoOpVectorStoreAdapter` (class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- File is mostly declarative (types/contracts) with limited embedded business rules.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - No obvious path match found from static name/package analysis.
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - No obvious name-matched test file found; behavior changes may need additional targeted tests.

### 14. Known limitations / technical debt
- Implements a no-op fallback; behavior is intentionally limited and may hide missing integration wiring.

### 15. Example usage
```kotlin
// Construct `NoOpVectorStoreAdapter` and use exposed members
```
Typical caller path: `modules/infra-defaults/src/test/kotlin/com/authord/mkdocs/defaults/ports/NoOpAdaptersTest.kt` -> `modules/infra-defaults/src/main/kotlin/com/authord/mkdocs/defaults/vector/NoOpVectorStoreAdapter.kt`.

## InMemoryCommandRegistryTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/infra-defaults/src/test/kotlin/com/authord/mkdocs/defaults/command/InMemoryCommandRegistryTest.kt`
- **What does it do?** Implements unit-focused test coverage around `InMemoryCommandRegistryTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.command.TopicTreeCommandHandler, com.authord.mkdocs.ports.topic.ValidateTopicTreeCommand, com.authord.mkdocs.ports.topic.TopicTreeCommandResult, com.authord.mkdocs.ports.topic.TopicTreeCommandStatus
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `InMemoryCommandRegistryTest`.
  - Key callable members include `register and resolve handler by command type`.
  - Scenario-focused test cases include: `register and resolve handler by command type`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.defaults.command`
- **Module / feature area:** `infra-defaults` / `defaults/command`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `InMemoryCommandRegistryTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `register and resolve handler by command type` consumes no explicit parameters.
- **Outputs produced:**
  - `register and resolve handler by command type` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.command.TopicTreeCommandHandler`
  - `com.authord.mkdocs.ports.topic.ValidateTopicTreeCommand`
  - `com.authord.mkdocs.ports.topic.TopicTreeCommandResult`
  - `com.authord.mkdocs.ports.topic.TopicTreeCommandStatus`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.command.TopicTreeCommandHandler`, `com.authord.mkdocs.ports.topic.ValidateTopicTreeCommand`, `com.authord.mkdocs.ports.topic.TopicTreeCommandResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `InMemoryCommandRegistryTest` (class, public (default))
- **Key public/internal functions:**
  - `register and resolve handler by command type() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/infra-defaults/src/test/kotlin/com/authord/mkdocs/defaults/command/InMemoryCommandRegistryTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *InMemoryCommandRegistryTest`.

## NoOpAdaptersTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/infra-defaults/src/test/kotlin/com/authord/mkdocs/defaults/ports/NoOpAdaptersTest.kt`
- **What does it do?** Implements unit-focused test coverage around `NoOpAdaptersTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.defaults.preview.NoOpPreviewSyncAdapter, com.authord.mkdocs.defaults.vector.NoOpVectorStoreAdapter
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `NoOpAdaptersTest`.
  - Key callable members include `preview sync adapter captures deltas without side effects`, `vector store adapter returns empty results`.
  - Scenario-focused test cases include: `preview sync adapter captures deltas without side effects`, `vector store adapter returns empty results`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.defaults.ports`
- **Module / feature area:** `infra-defaults` / `defaults/ports`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `NoOpAdaptersTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `preview sync adapter captures deltas without side effects` consumes no explicit parameters.
  - `vector store adapter returns empty results` consumes no explicit parameters.
- **Outputs produced:**
  - `preview sync adapter captures deltas without side effects` returns Unit/implicit.
  - `vector store adapter returns empty results` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.defaults.preview.NoOpPreviewSyncAdapter`
  - `com.authord.mkdocs.defaults.vector.NoOpVectorStoreAdapter`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.defaults.preview.NoOpPreviewSyncAdapter`, `com.authord.mkdocs.defaults.vector.NoOpVectorStoreAdapter`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `NoOpAdaptersTest` (class, public (default))
- **Key public/internal functions:**
  - `preview sync adapter captures deltas without side effects() : Unit/implicit` (public (default))
  - `vector store adapter returns empty results() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/infra-defaults/src/test/kotlin/com/authord/mkdocs/defaults/ports/NoOpAdaptersTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- Implements a no-op fallback; behavior is intentionally limited and may hide missing integration wiring.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *NoOpAdaptersTest`.

## BaseUrlDetector.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/BaseUrlDetector.kt`
- **What does it do?** Contains feature logic for `BaseUrlDetector` in the `mkdocs-runtime-adapter` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Handle external runtime behavior deterministically and surface actionable diagnostics.
- **Key functionalities:**
  - Main declarations: `BaseUrlDetector`.
  - Key callable members include `normalizeDetectedUrl`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `runtime adapter/infrastructure`

### 2. Main classes / interfaces / objects inside the file
- `BaseUrlDetector` (open class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorStdoutTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorStdoutTest.kt`, `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `BaseUrlDetector` (open class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorStdoutTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DeleteSemanticsPolicyTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownHeadingSupportTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownLinkRewriteScopeTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = BaseUrlDetector(/* collaborators */)
val result = component.normalizeDetectedUrl(/* inputs */)
```
Typical caller path: `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorStdoutTest.kt` -> `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/BaseUrlDetector.kt`.

## DocsFileGatewayAdapter.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapter.kt`
- **What does it do?** Implements adapter/gateway behavior in `DocsFileGatewayAdapter` to connect domain flows with runtime or IO APIs.
- **Why it matters:** Adapter files are critical integration points where external behavior is normalized for the rest of the system.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.DocsFileGateway, com.authord.mkdocs.ports.topic.TopicDeleteMode, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncErrorCode
  - External: java.awt.Desktop, java.nio.file.Files, java.nio.file.Path, java.nio.file.Paths, java.nio.file.StandardCopyOption
- **Responsibilities:**
  - Translate internal commands/models to runtime, filesystem, or integration APIs.
  - Handle external runtime behavior deterministically and surface actionable diagnostics.
- **Key functionalities:**
  - Main declarations: `DocsFileGatewayAdapter`.
  - Key callable members include `moveInternal`, `resolveMarkdownPath`, `docsDir`, `moveToRecovery`, `normalizeRelative`, `toRelative`, `moveToSystemTrash`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `runtime adapter/infrastructure`

### 2. Main classes / interfaces / objects inside the file
- `DocsFileGatewayAdapter` (class, public (default)): Adapter translating domain requests to external/runtime APIs.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DeleteSemanticsPolicyTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownLinkRewriteScopeTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/RuntimeAdapterCoverageEdgeCasesTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.DocsFileGateway`
  - `com.authord.mkdocs.ports.topic.TopicDeleteMode`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DeleteSemanticsPolicyTest.kt`, `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`, `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownLinkRewriteScopeTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.DocsFileGateway`, `com.authord.mkdocs.ports.topic.TopicDeleteMode`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `DocsFileGatewayAdapter` (class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorStdoutTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DeleteSemanticsPolicyTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownHeadingSupportTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownLinkRewriteScopeTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = DocsFileGatewayAdapter(/* collaborators */)
val result = component.moveInternal(/* inputs */)
```
Typical caller path: `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DeleteSemanticsPolicyTest.kt` -> `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapter.kt`.

## MarkdownHeadingSupport.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/MarkdownHeadingSupport.kt`
- **What does it do?** Declares architectural contract types in `MarkdownHeadingSupport` for cross-module interaction.
- **Why it matters:** Stable port contracts preserve modularity and allow implementations to vary independently.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
  - Handle external runtime behavior deterministically and surface actionable diagnostics.
- **Key functionalities:**
  - Main declarations: `MarkdownHeadingSupport`.
  - Key callable members include `extractFirstH1`, `upsertFirstH1`, `defaultTopicContent`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `runtime adapter/infrastructure`

### 2. Main classes / interfaces / objects inside the file
- `MarkdownHeadingSupport` (object, public (default)): Singleton utility/policy holder shared across callers.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `extractFirstH1` consumes markdown: String.
  - `upsertFirstH1` consumes markdown: String, title: String.
  - `defaultTopicContent` consumes title: String.
- **Outputs produced:**
  - `extractFirstH1` returns String?.
  - `upsertFirstH1` returns String.
  - `defaultTopicContent` returns String.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapter.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownHeadingSupportTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeStartupLoader.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapter.kt`, `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`, `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownHeadingSupportTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MarkdownHeadingSupport` (object, public (default))
- **Key public/internal functions:**
  - `extractFirstH1(markdown: String) : String?` (public (default))
  - `upsertFirstH1(markdown: String, title: String) : String` (public (default))
  - `defaultTopicContent(title: String) : String` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownHeadingSupportTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorStdoutTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DeleteSemanticsPolicyTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownLinkRewriteScopeTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val result = MarkdownHeadingSupport.extractFirstH1(/* inputs */)
```
Typical caller path: `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapter.kt` -> `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/MarkdownHeadingSupport.kt`.

## MarkdownLinkRewriter.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/MarkdownLinkRewriter.kt`
- **What does it do?** Contains feature logic for `MarkdownLinkRewriter` in the `mkdocs-runtime-adapter` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncErrorCode
  - External: java.nio.file.Files, java.nio.file.Path, java.nio.file.Paths
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Handle external runtime behavior deterministically and surface actionable diagnostics.
- **Key functionalities:**
  - Main declarations: `MarkdownLinkRewriter`.
  - Key callable members include `rewrite`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `runtime adapter/infrastructure`

### 2. Main classes / interfaces / objects inside the file
- `MarkdownLinkRewriter` (class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `rewrite` consumes instance: TopicInstanceRef, fromRelativePath: String, toRelativePath: String.
- **Outputs produced:**
  - `rewrite` returns TopicGatewayResult<Int>.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapter.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/RuntimeAdapterCoverageEdgeCasesTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
  - `com.authord.mkdocs.ports.topic.TopicSyncErrorCode`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapter.kt`, `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/RuntimeAdapterCoverageEdgeCasesTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.TopicGatewayResult`, `com.authord.mkdocs.ports.topic.TopicInstanceRef`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MarkdownLinkRewriter` (class, public (default))
- **Key public/internal functions:**
  - `rewrite(instance: TopicInstanceRef, fromRelativePath: String, toRelativePath: String) : TopicGatewayResult<Int>` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorStdoutTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DeleteSemanticsPolicyTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownHeadingSupportTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownLinkRewriteScopeTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = MarkdownLinkRewriter(/* collaborators */)
val result = component.rewrite(/* inputs */)
```
Typical caller path: `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapter.kt` -> `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/MarkdownLinkRewriter.kt`.

## MkDocsYamlGateway.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/MkDocsYamlGateway.kt`
- **What does it do?** Implements adapter/gateway behavior in `MkDocsYamlGateway` to connect domain flows with runtime or IO APIs.
- **Why it matters:** Adapter files are critical integration points where external behavior is normalized for the rest of the system.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.MkDocsConfigGateway, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicNavNode, com.authord.mkdocs.ports.topic.TopicSyncErrorCode
  - External: org.yaml.snakeyaml.DumperOptions, org.yaml.snakeyaml.LoaderOptions, org.yaml.snakeyaml.Yaml, org.yaml.snakeyaml.constructor.SafeConstructor, java.nio.file.AtomicMoveNotSupportedException, java.nio.file.Files, java.nio.file.Path, java.nio.file.Paths
- **Responsibilities:**
  - Translate internal commands/models to runtime, filesystem, or integration APIs.
  - Handle external runtime behavior deterministically and surface actionable diagnostics.
- **Key functionalities:**
  - Main declarations: `MkDocsYamlGateway`.
  - Key callable members include `parseNav`, `parseNavEntry`, `parseStringList`, `serializeNav`, `deriveTitleFromPath`, `isExternalUrl`, `normalizePath`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `runtime adapter/infrastructure`

### 2. Main classes / interfaces / objects inside the file
- `MkDocsYamlGateway` (class, public (default)): Adapter translating domain requests to external/runtime APIs.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MkDocsYamlGatewayRoundTripTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/RuntimeAdapterCoverageEdgeCasesTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsProjectCreator.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
  - `com.authord.mkdocs.ports.topic.MkDocsConfigGateway`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`, `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MkDocsYamlGatewayRoundTripTest.kt`, `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/RuntimeAdapterCoverageEdgeCasesTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`, `com.authord.mkdocs.ports.topic.MkDocsConfigGateway`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MkDocsYamlGateway` (class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MkDocsYamlGatewayRoundTripTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorStdoutTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DeleteSemanticsPolicyTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownHeadingSupportTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = MkDocsYamlGateway(/* collaborators */)
val result = component.parseNav(/* inputs */)
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt` -> `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/MkDocsYamlGateway.kt`.

## MkdocsProcessManager.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/MkdocsProcessManager.kt`
- **What does it do?** Contains feature logic for `MkdocsProcessManager` in the `mkdocs-runtime-adapter` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.util.concurrent.ConcurrentHashMap, java.util.concurrent.locks.ReentrantLock
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Handle external runtime behavior deterministically and surface actionable diagnostics.
- **Key functionalities:**
  - Main declarations: `RuntimeServerConfig`, `ManagedProcessHandle`, `RuntimeStartResult`, `RuntimeProcessDiagnostics`, `RunningProcess`, `MkdocsProcessManager`.
  - Key callable members include `stop`, `isAlive`, `stdoutOutput`, `stderrOutput`, `exitCodeOrNull`, `launch`, `start`, `stop`, plus `4` additional members.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `runtime adapter/infrastructure`

### 2. Main classes / interfaces / objects inside the file
- `RuntimeServerConfig` (data class, public (default)): Primary implementation type for this file feature area.
- `ManagedProcessHandle` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.
- `RuntimeStartResult` (data class, public (default)): Primary implementation type for this file feature area.
- `RuntimeProcessDiagnostics` (data class, public (default)): Primary implementation type for this file feature area.
- `RunningProcess` (data class, private): Primary implementation type for this file feature area.
- `MkdocsProcessManager` (class, public (default)): Coordinator that sequences multi-step flows and state transitions.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `stop` consumes no explicit parameters.
  - `isAlive` consumes no explicit parameters.
  - `stdoutOutput` consumes no explicit parameters.
  - `stderrOutput` consumes no explicit parameters.
  - `exitCodeOrNull` consumes no explicit parameters.
- **Outputs produced:**
  - `stop` returns Unit/implicit.
  - `isAlive` returns Boolean /** * Returns startup output observed for this process, if available. * * Implementations that do not capture output may return an empty string. */ fun startupOutput(): String.
  - `stdoutOutput` returns String.
  - `stderrOutput` returns String.
  - `exitCodeOrNull` returns Int?.
- **Side effects:**
  - Process/runtime execution
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MkdocsProcessManagerTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/RuntimeDecouplingPolicyTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewService.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MkdocsProcessManagerTest.kt`, `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/RuntimeDecouplingPolicyTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `RuntimeServerConfig` (data class, public (default))
  - `ManagedProcessHandle` (interface, public (default))
  - `RuntimeStartResult` (data class, public (default))
  - `RuntimeProcessDiagnostics` (data class, public (default))
  - `MkdocsProcessManager` (class, public (default))
- **Key public/internal functions:**
  - `stop() : Unit/implicit` (public (default))
  - `isAlive() : Boolean /** * Returns startup output observed for this process, if available. * * Implementations that do not capture output may return an empty string. */ fun startupOutput(): String` (public (default))
  - `stdoutOutput() : String` (public (default))
  - `stderrOutput() : String` (public (default))
  - `exitCodeOrNull() : Int?` (public (default))
  - `launch(command: List<String>, workingDir: String) : ManagedProcessHandle } /** * Result returned from runtime start/restart operations. */ data class RuntimeStartResult( val started: Boolean, val processId: String, val command: List<String>, val alreadyRunning: Boolean, val startupOutput: String` (public (default))
  - `start(projectId: String, workingDir: String, config: RuntimeServerConfig = RuntimeServerConfig() : Unit/implicit` (public (default))
  - `stop(projectId: String) : Boolean` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Tracks lifecycle/disposal boundaries; resources are expected to be released deterministically.
- Contains coroutine-aware execution paths and cancellation-sensitive operations.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- `RuntimeServerConfig` models structured data with fields: val command: List<String> = listOf("mkdocs", "serve".
- `RuntimeStartResult` models structured data with fields: val started: Boolean, val processId: String, val command: List<String>, val alreadyRunning: Boolean, val startupOutput: String = "".
- `RuntimeProcessDiagnostics` models structured data with fields: val processId: String, val command: List<String>, val isAlive: Boolean, val startupOutput: String, val stdoutOutput: String.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MkdocsProcessManagerTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorStdoutTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DeleteSemanticsPolicyTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownHeadingSupportTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = RuntimeServerConfig(/* collaborators */)
val result = component.stop(/* inputs */)
```
Typical caller path: `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MkdocsProcessManagerTest.kt` -> `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/MkdocsProcessManager.kt`.

## UvBootstrapService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/UvBootstrapService.kt`
- **What does it do?** Implements service-level behavior in `UvBootstrapService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files, java.nio.file.Path, java.security.MessageDigest, java.util.concurrent.ConcurrentHashMap
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Handle external runtime behavior deterministically and surface actionable diagnostics.
- **Key functionalities:**
  - Main declarations: `CommandResult`, `BootstrapResult`, `UvBootstrapService`.
  - Key callable members include `run`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `runtime adapter/infrastructure`

### 2. Main classes / interfaces / objects inside the file
- `CommandResult` (data class, public (default)): Primary implementation type for this file feature area.
- `BootstrapResult` (data class, public (default)): Primary implementation type for this file feature area.
- `UvBootstrapService` (open class, public (default)): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `run` consumes command: List<String>, workingDir: String.
- **Outputs produced:**
  - `run` returns CommandResult } /** * Process command execution result envelope. */ data class CommandResult( val exitCode: Int, val stdout: String.
- **Side effects:**
  - Filesystem reads/writes
  - Process/runtime execution
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/RuntimeAdapterCoverageEdgeCasesTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/UvBootstrapServiceTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsProjectCreator.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/RuntimeAdapterCoverageEdgeCasesTest.kt`, `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/UvBootstrapServiceTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `CommandResult` (data class, public (default))
  - `BootstrapResult` (data class, public (default))
  - `UvBootstrapService` (open class, public (default))
- **Key public/internal functions:**
  - `run(command: List<String>, workingDir: String) : CommandResult } /** * Process command execution result envelope. */ data class CommandResult( val exitCode: Int, val stdout: String` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- `CommandResult` models structured data with fields: val exitCode: Int, val stdout: String = "", val stderr: String = "".
- `BootstrapResult` models structured data with fields: val success: Boolean, val runtimePath: String, val uvExecutablePath: String, val executedCommands: List<List<String>>, val skipped: Boolean.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- Uses hashing/encryption-adjacent APIs; verify algorithm choice and key-handling assumptions.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/UvBootstrapServiceTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorStdoutTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DeleteSemanticsPolicyTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownHeadingSupportTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = CommandResult(/* collaborators */)
val result = component.run(/* inputs */)
```
Typical caller path: `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/RuntimeAdapterCoverageEdgeCasesTest.kt` -> `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/UvBootstrapService.kt`.

## UvExecutableProvider.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/UvExecutableProvider.kt`
- **What does it do?** Contains feature logic for `UvExecutableProvider` in the `mkdocs-runtime-adapter` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.io.File, java.net.URI, java.net.http.HttpClient, java.net.http.HttpRequest, java.net.http.HttpResponse, java.nio.file.Files, java.nio.file.Path, java.nio.file.StandardCopyOption
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Handle external runtime behavior deterministically and surface actionable diagnostics.
- **Key functionalities:**
  - Main declarations: `UvExecutableResult`, `StaticUvExecutableProvider`, `UvArchiveDownloadResult`, `HttpUvArchiveDownloader`, `DefaultUvArchiveExtractor`, `ProjectManagedUvExecutableProvider`.
  - Key callable members include `resolve`, `download`, `extract`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `runtime adapter/infrastructure`

### 2. Main classes / interfaces / objects inside the file
- `UvExecutableResult` (data class, public (default)): Primary implementation type for this file feature area.
- `StaticUvExecutableProvider` (class, public (default)): Primary implementation type for this file feature area.
- `UvArchiveDownloadResult` (data class, public (default)): Primary implementation type for this file feature area.
- `HttpUvArchiveDownloader` (class, public (default)): Primary implementation type for this file feature area.
- `DefaultUvArchiveExtractor` (class, public (default)): Primary implementation type for this file feature area.
- `ProjectManagedUvExecutableProvider` (class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `resolve` consumes projectPath: String.
  - `download` consumes url: String, destination: Path.
  - `extract` consumes archivePath: Path, executablePath: Path.
- **Outputs produced:**
  - `resolve` returns UvExecutableResult } /** * Static `uv` executable provider used by unit tests and in-memory adapters. */ class StaticUvExecutableProvider( private val executable: String.
  - `download` returns UvArchiveDownloadResult } class HttpUvArchiveDownloader : UvArchiveDownloader.
  - `extract` returns UvExecutableResult } class DefaultUvArchiveExtractor : UvArchiveExtractor.
- **Side effects:**
  - Filesystem reads/writes
  - Network calls
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/UvBootstrapService.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/ProjectManagedUvExecutableProviderTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/UvBootstrapServiceTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/UvExecutableProviderCoverageTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/UvBootstrapService.kt`, `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/ProjectManagedUvExecutableProviderTest.kt`, `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/UvBootstrapServiceTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `UvExecutableResult` (data class, public (default))
  - `StaticUvExecutableProvider` (class, public (default))
  - `UvArchiveDownloadResult` (data class, public (default))
  - `HttpUvArchiveDownloader` (class, public (default))
  - `DefaultUvArchiveExtractor` (class, public (default))
  - `ProjectManagedUvExecutableProvider` (class, public (default))
- **Key public/internal functions:**
  - `resolve(projectPath: String) : UvExecutableResult } /** * Static `uv` executable provider used by unit tests and in-memory adapters. */ class StaticUvExecutableProvider( private val executable: String` (public (default))
  - `download(url: String, destination: Path) : UvArchiveDownloadResult } class HttpUvArchiveDownloader : UvArchiveDownloader` (public (default))
  - `extract(archivePath: Path, executablePath: Path) : UvExecutableResult } class DefaultUvArchiveExtractor : UvArchiveExtractor` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Throws explicit exceptions for invalid or unrecoverable scenarios.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- `UvExecutableResult` models structured data with fields: val success: Boolean, val executablePath: String = "", val errorMessage: String = "".
- `UvArchiveDownloadResult` models structured data with fields: val success: Boolean, val errorMessage: String = "".

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- Performs network operations; validate endpoint trust and request input sanitization.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/ProjectManagedUvExecutableProviderTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/UvExecutableProviderCoverageTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorStdoutTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DeleteSemanticsPolicyTest.kt`
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
```kotlin
val component = UvExecutableResult(/* collaborators */)
val result = component.resolve(/* inputs */)
```
Typical caller path: `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/UvBootstrapService.kt` -> `modules/mkdocs-runtime-adapter/src/main/kotlin/com/authord/mkdocs/runtime/UvExecutableProvider.kt`.

## BaseUrlDetectorStdoutTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorStdoutTest.kt`
- **What does it do?** Implements unit-focused test coverage around `BaseUrlDetectorStdoutTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `BaseUrlDetectorStdoutTest`.
  - Key callable members include `detects runtime base url from multiline stdout`, `trims trailing punctuation from detected url`, `returns null when stdout has no http or https url`, `ignores unrelated warning urls`.
  - Scenario-focused test cases include: `detects runtime base url from multiline stdout`, `trims trailing punctuation from detected url`, `returns null when stdout has no http or https url`, `ignores unrelated warning urls`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `BaseUrlDetectorStdoutTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `detects runtime base url from multiline stdout` consumes no explicit parameters.
  - `trims trailing punctuation from detected url` consumes no explicit parameters.
  - `returns null when stdout has no http or https url` consumes no explicit parameters.
  - `ignores unrelated warning urls` consumes no explicit parameters.
- **Outputs produced:**
  - `detects runtime base url from multiline stdout` returns Unit/implicit.
  - `trims trailing punctuation from detected url` returns Unit/implicit.
  - `returns null when stdout has no http or https url` returns Unit/implicit.
  - `ignores unrelated warning urls` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `BaseUrlDetectorStdoutTest` (class, public (default))
- **Key public/internal functions:**
  - `detects runtime base url from multiline stdout() : Unit/implicit` (public (default))
  - `trims trailing punctuation from detected url() : Unit/implicit` (public (default))
  - `returns null when stdout has no http or https url() : Unit/implicit` (public (default))
  - `ignores unrelated warning urls() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorStdoutTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *BaseUrlDetectorStdoutTest`.

## BaseUrlDetectorTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorTest.kt`
- **What does it do?** Implements unit-focused test coverage around `BaseUrlDetectorTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `BaseUrlDetectorTest`.
  - Key callable members include `detects base url from startup output`, `returns null when url is absent`.
  - Scenario-focused test cases include: `detects base url from startup output`, `returns null when url is absent`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `BaseUrlDetectorTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `detects base url from startup output` consumes no explicit parameters.
  - `returns null when url is absent` consumes no explicit parameters.
- **Outputs produced:**
  - `detects base url from startup output` returns Unit/implicit.
  - `returns null when url is absent` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `BaseUrlDetectorTest` (class, public (default))
- **Key public/internal functions:**
  - `detects base url from startup output() : Unit/implicit` (public (default))
  - `returns null when url is absent() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/BaseUrlDetectorTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *BaseUrlDetectorTest`.

## DeleteSemanticsPolicyTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DeleteSemanticsPolicyTest.kt`
- **What does it do?** Implements unit-focused test coverage around `DeleteSemanticsPolicyTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicDeleteMode, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef
  - External: java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `DeleteSemanticsPolicyTest`.
  - Key callable members include `recoverable delete with default trash strategy removes markdown safely`, `recoverable delete uses system trash path when available`, `recoverable delete falls back to recovery location when system trash unavailable`, `nav-only delete leaves underlying markdown file untouched`.
  - Scenario-focused test cases include: `recoverable delete with default trash strategy removes markdown safely`, `recoverable delete uses system trash path when available`, `recoverable delete falls back to recovery location when system trash unavailable`, `nav-only delete leaves underlying markdown file untouched`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `DeleteSemanticsPolicyTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `recoverable delete with default trash strategy removes markdown safely` consumes no explicit parameters.
  - `recoverable delete uses system trash path when available` consumes no explicit parameters.
  - `recoverable delete falls back to recovery location when system trash unavailable` consumes no explicit parameters.
  - `nav-only delete leaves underlying markdown file untouched` consumes no explicit parameters.
- **Outputs produced:**
  - `recoverable delete with default trash strategy removes markdown safely` returns Unit/implicit.
  - `recoverable delete uses system trash path when available` returns Unit/implicit.
  - `recoverable delete falls back to recovery location when system trash unavailable` returns Unit/implicit.
  - `nav-only delete leaves underlying markdown file untouched` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicDeleteMode`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicDeleteMode`, `com.authord.mkdocs.ports.topic.TopicGatewayResult`, `com.authord.mkdocs.ports.topic.TopicInstanceRef`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `DeleteSemanticsPolicyTest` (class, public (default))
- **Key public/internal functions:**
  - `recoverable delete with default trash strategy removes markdown safely() : Unit/implicit` (public (default))
  - `recoverable delete uses system trash path when available() : Unit/implicit` (public (default))
  - `recoverable delete falls back to recovery location when system trash unavailable() : Unit/implicit` (public (default))
  - `nav-only delete leaves underlying markdown file untouched() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DeleteSemanticsPolicyTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *DeleteSemanticsPolicyTest`.

## DocsFileGatewayAdapterMutationTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`
- **What does it do?** Implements unit-focused test coverage around `DocsFileGatewayAdapterMutationTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicDeleteMode, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef
  - External: java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `DocsFileGatewayAdapterMutationTest`.
  - Key callable members include `gateway synchronizes create rename move and recoverable delete operations`, `gateway upserts markdown title heading`.
  - Scenario-focused test cases include: `gateway synchronizes create rename move and recoverable delete operations`, `gateway upserts markdown title heading`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `DocsFileGatewayAdapterMutationTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `gateway synchronizes create rename move and recoverable delete operations` consumes no explicit parameters.
  - `gateway upserts markdown title heading` consumes no explicit parameters.
- **Outputs produced:**
  - `gateway synchronizes create rename move and recoverable delete operations` returns Unit/implicit.
  - `gateway upserts markdown title heading` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicDeleteMode`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicDeleteMode`, `com.authord.mkdocs.ports.topic.TopicGatewayResult`, `com.authord.mkdocs.ports.topic.TopicInstanceRef`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `DocsFileGatewayAdapterMutationTest` (class, public (default))
- **Key public/internal functions:**
  - `gateway synchronizes create rename move and recoverable delete operations() : Unit/implicit` (public (default))
  - `gateway upserts markdown title heading() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/DocsFileGatewayAdapterMutationTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *DocsFileGatewayAdapterMutationTest`.

## MarkdownHeadingSupportTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownHeadingSupportTest.kt`
- **What does it do?** Implements unit-focused test coverage around `MarkdownHeadingSupportTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `MarkdownHeadingSupportTest`.
  - Key callable members include `extract first h1 skips code fences and returns first top-level heading`, `extract first h1 handles yaml front matter`, `extract first h1 returns null when heading is missing`, `upsert first h1 replaces existing heading`, `upsert first h1 inserts heading after front matter when absent`.
  - Scenario-focused test cases include: `extract first h1 skips code fences and returns first top-level heading`, `extract first h1 handles yaml front matter`, `extract first h1 returns null when heading is missing`, `upsert first h1 replaces existing heading`, `upsert first h1 inserts heading after front matter when absent`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `MarkdownHeadingSupportTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `extract first h1 skips code fences and returns first top-level heading` consumes no explicit parameters.
  - `extract first h1 handles yaml front matter` consumes no explicit parameters.
  - `extract first h1 returns null when heading is missing` consumes no explicit parameters.
  - `upsert first h1 replaces existing heading` consumes no explicit parameters.
  - `upsert first h1 inserts heading after front matter when absent` consumes no explicit parameters.
- **Outputs produced:**
  - `extract first h1 skips code fences and returns first top-level heading` returns Unit/implicit.
  - `extract first h1 handles yaml front matter` returns Unit/implicit.
  - `extract first h1 returns null when heading is missing` returns Unit/implicit.
  - `upsert first h1 replaces existing heading` returns Unit/implicit.
  - `upsert first h1 inserts heading after front matter when absent` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MarkdownHeadingSupportTest` (class, public (default))
- **Key public/internal functions:**
  - `extract first h1 skips code fences and returns first top-level heading() : Unit/implicit` (public (default))
  - `extract first h1 handles yaml front matter() : Unit/implicit` (public (default))
  - `extract first h1 returns null when heading is missing() : Unit/implicit` (public (default))
  - `upsert first h1 replaces existing heading() : Unit/implicit` (public (default))
  - `upsert first h1 inserts heading after front matter when absent() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownHeadingSupportTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *MarkdownHeadingSupportTest`.

## MarkdownLinkRewriteScopeTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownLinkRewriteScopeTest.kt`
- **What does it do?** Implements unit-focused test coverage around `MarkdownLinkRewriteScopeTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef
  - External: java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `MarkdownLinkRewriteScopeTest`.
  - Key callable members include `rewrite updates only markdown relative links and preserves external or non-markdown targets`.
  - Scenario-focused test cases include: `rewrite updates only markdown relative links and preserves external or non-markdown targets`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `MarkdownLinkRewriteScopeTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `rewrite updates only markdown relative links and preserves external or non-markdown targets` consumes no explicit parameters.
- **Outputs produced:**
  - `rewrite updates only markdown relative links and preserves external or non-markdown targets` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicGatewayResult`, `com.authord.mkdocs.ports.topic.TopicInstanceRef`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MarkdownLinkRewriteScopeTest` (class, public (default))
- **Key public/internal functions:**
  - `rewrite updates only markdown relative links and preserves external or non-markdown targets() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MarkdownLinkRewriteScopeTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *MarkdownLinkRewriteScopeTest`.

## MkDocsYamlGatewayRoundTripTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MkDocsYamlGatewayRoundTripTest.kt`
- **What does it do?** Implements unit-focused test coverage around `MkDocsYamlGatewayRoundTripTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicNavNode
  - External: java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `MkDocsYamlGatewayRoundTripTest`.
  - Key callable members include `roundtrip parse and deterministic serialization keeps site_name docs_dir nav and not_in_nav`, `load and serialize without nav preserves no-nav mode`, `writeConfig preserves deterministic output for unchanged logical structure`, `writeConfig preserves unknown keys and custom docs_dir when nav is present`, `serializeDeterministically uses document docs_dir when no raw yaml is present`.
  - Scenario-focused test cases include: `roundtrip parse and deterministic serialization keeps site_name docs_dir nav and not_in_nav`, `load and serialize without nav preserves no-nav mode`, `writeConfig preserves deterministic output for unchanged logical structure`, `writeConfig preserves unknown keys and custom docs_dir when nav is present`, `serializeDeterministically uses document docs_dir when no raw yaml is present`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `MkDocsYamlGatewayRoundTripTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `roundtrip parse and deterministic serialization keeps site_name docs_dir nav and not_in_nav` consumes no explicit parameters.
  - `load and serialize without nav preserves no-nav mode` consumes no explicit parameters.
  - `writeConfig preserves deterministic output for unchanged logical structure` consumes no explicit parameters.
  - `writeConfig preserves unknown keys and custom docs_dir when nav is present` consumes no explicit parameters.
  - `serializeDeterministically uses document docs_dir when no raw yaml is present` consumes no explicit parameters.
- **Outputs produced:**
  - `roundtrip parse and deterministic serialization keeps site_name docs_dir nav and not_in_nav` returns Unit/implicit.
  - `load and serialize without nav preserves no-nav mode` returns Unit/implicit.
  - `writeConfig preserves deterministic output for unchanged logical structure` returns Unit/implicit.
  - `writeConfig preserves unknown keys and custom docs_dir when nav is present` returns Unit/implicit.
  - `serializeDeterministically uses document docs_dir when no raw yaml is present` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
  - `com.authord.mkdocs.ports.topic.TopicNavNode`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`, `com.authord.mkdocs.ports.topic.TopicGatewayResult`, `com.authord.mkdocs.ports.topic.TopicInstanceRef`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MkDocsYamlGatewayRoundTripTest` (class, public (default))
- **Key public/internal functions:**
  - `roundtrip parse and deterministic serialization keeps site_name docs_dir nav and not_in_nav() : Unit/implicit` (public (default))
  - `load and serialize without nav preserves no-nav mode() : Unit/implicit` (public (default))
  - `writeConfig preserves deterministic output for unchanged logical structure() : Unit/implicit` (public (default))
  - `writeConfig preserves unknown keys and custom docs_dir when nav is present() : Unit/implicit` (public (default))
  - `serializeDeterministically uses document docs_dir when no raw yaml is present() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MkDocsYamlGatewayRoundTripTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *MkDocsYamlGatewayRoundTripTest`.

## MkdocsProcessManagerTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MkdocsProcessManagerTest.kt`
- **What does it do?** Implements unit-focused test coverage around `MkdocsProcessManagerTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `FakeHandle`, `MkdocsProcessManagerTest`.
  - Key callable members include `managed process handle default startup output is empty`, `runtime start result default startup output is empty`, `ensures single server instance per project`, `restart stops previous process and launches a new one`, `stop returns false when no process exists`, `stop and dispose return true for running process`, `restart without existing process returns not started result`, `start appends caller supplied extra args`, plus `1` additional members.
  - Scenario-focused test cases include: `managed process handle default startup output is empty`, `runtime start result default startup output is empty`, `ensures single server instance per project`, `restart stops previous process and launches a new one`, `stop returns false when no process exists`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `FakeHandle` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `MkdocsProcessManagerTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `managed process handle default startup output is empty` consumes no explicit parameters.
  - `runtime start result default startup output is empty` consumes no explicit parameters.
  - `ensures single server instance per project` consumes no explicit parameters.
  - `restart stops previous process and launches a new one` consumes no explicit parameters.
  - `stop returns false when no process exists` consumes no explicit parameters.
- **Outputs produced:**
  - `managed process handle default startup output is empty` returns Unit/implicit.
  - `runtime start result default startup output is empty` returns Unit/implicit.
  - `ensures single server instance per project` returns Unit/implicit.
  - `restart stops previous process and launches a new one` returns Unit/implicit.
  - `stop returns false when no process exists` returns Unit/implicit.
- **Side effects:**
  - Process/runtime execution


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MkdocsProcessManagerTest` (class, public (default))
- **Key public/internal functions:**
  - `managed process handle default startup output is empty() : Unit/implicit` (public (default))
  - `runtime start result default startup output is empty() : Unit/implicit` (public (default))
  - `ensures single server instance per project() : Unit/implicit` (public (default))
  - `restart stops previous process and launches a new one() : Unit/implicit` (public (default))
  - `stop returns false when no process exists() : Unit/implicit` (public (default))
  - `stop and dispose return true for running process() : Unit/implicit` (public (default))
  - `restart without existing process returns not started result() : Unit/implicit` (public (default))
  - `start appends caller supplied extra args() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Throws explicit exceptions for invalid or unrecoverable scenarios.

### 8. Lifecycle / state behavior
- Tracks lifecycle/disposal boundaries; resources are expected to be released deterministically.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/MkdocsProcessManagerTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *MkdocsProcessManagerTest`.

## ProjectManagedUvExecutableProviderTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/ProjectManagedUvExecutableProviderTest.kt`
- **What does it do?** Implements unit-focused test coverage around `ProjectManagedUvExecutableProviderTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files, java.nio.file.Path, java.nio.file.attribute.PosixFilePermission
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `ProjectManagedUvExecutableProviderTest`.
  - Key callable members include `reuses existing project managed uv executable`, `uses explicit configured uv path directly without copying`, `downloads uv into project managed tools path when discovery fails`, `returns actionable failure when uv cannot be discovered or downloaded`.
  - Scenario-focused test cases include: `reuses existing project managed uv executable`, `uses explicit configured uv path directly without copying`, `downloads uv into project managed tools path when discovery fails`, `returns actionable failure when uv cannot be discovered or downloaded`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `ProjectManagedUvExecutableProviderTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `reuses existing project managed uv executable` consumes no explicit parameters.
  - `uses explicit configured uv path directly without copying` consumes no explicit parameters.
  - `downloads uv into project managed tools path when discovery fails` consumes no explicit parameters.
  - `returns actionable failure when uv cannot be discovered or downloaded` consumes no explicit parameters.
- **Outputs produced:**
  - `reuses existing project managed uv executable` returns Unit/implicit.
  - `uses explicit configured uv path directly without copying` returns Unit/implicit.
  - `downloads uv into project managed tools path when discovery fails` returns Unit/implicit.
  - `returns actionable failure when uv cannot be discovered or downloaded` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `ProjectManagedUvExecutableProviderTest` (class, public (default))
- **Key public/internal functions:**
  - `reuses existing project managed uv executable() : Unit/implicit` (public (default))
  - `uses explicit configured uv path directly without copying() : Unit/implicit` (public (default))
  - `downloads uv into project managed tools path when discovery fails() : Unit/implicit` (public (default))
  - `returns actionable failure when uv cannot be discovered or downloaded() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/ProjectManagedUvExecutableProviderTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *ProjectManagedUvExecutableProviderTest`.

## RuntimeAdapterCoverageEdgeCasesTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/RuntimeAdapterCoverageEdgeCasesTest.kt`
- **What does it do?** Implements unit-focused test coverage around `RuntimeAdapterCoverageEdgeCasesTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.TopicDeleteMode, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicNavNode
  - External: java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `RuntimeAdapterCoverageEdgeCasesTest`.
  - Key callable members include `mkdocs yaml gateway covers parse and write failure branches`, `docs file gateway adapter covers validation and io failure branches`, `markdown link rewriter covers instance scope io and non-target markdown branches`, `uv bootstrap service covers two-phase install with get-deps`, `uv bootstrap detects mkdocs yaml file variant`, `uv bootstrap uses windows python path when unix path does not exist`, `uv bootstrap deduplicates discovered dependencies`, `uv bootstrap handles unreadable config file gracefully`, plus `1` additional members.
  - Scenario-focused test cases include: `mkdocs yaml gateway covers parse and write failure branches`, `docs file gateway adapter covers validation and io failure branches`, `markdown link rewriter covers instance scope io and non-target markdown branches`, `uv bootstrap service covers two-phase install with get-deps`, `uv bootstrap detects mkdocs yaml file variant`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `RuntimeAdapterCoverageEdgeCasesTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `mkdocs yaml gateway covers parse and write failure branches` consumes no explicit parameters.
  - `docs file gateway adapter covers validation and io failure branches` consumes no explicit parameters.
  - `markdown link rewriter covers instance scope io and non-target markdown branches` consumes no explicit parameters.
  - `uv bootstrap service covers two-phase install with get-deps` consumes no explicit parameters.
  - `uv bootstrap detects mkdocs yaml file variant` consumes no explicit parameters.
- **Outputs produced:**
  - `mkdocs yaml gateway covers parse and write failure branches` returns Unit/implicit.
  - `docs file gateway adapter covers validation and io failure branches` returns Unit/implicit.
  - `markdown link rewriter covers instance scope io and non-target markdown branches` returns Unit/implicit.
  - `uv bootstrap service covers two-phase install with get-deps` returns Unit/implicit.
  - `uv bootstrap detects mkdocs yaml file variant` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
  - `com.authord.mkdocs.ports.topic.TopicDeleteMode`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`, `com.authord.mkdocs.ports.topic.TopicDeleteMode`, `com.authord.mkdocs.ports.topic.TopicGatewayResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `RuntimeAdapterCoverageEdgeCasesTest` (class, public (default))
- **Key public/internal functions:**
  - `mkdocs yaml gateway covers parse and write failure branches() : Unit/implicit` (public (default))
  - `docs file gateway adapter covers validation and io failure branches() : Unit/implicit` (public (default))
  - `markdown link rewriter covers instance scope io and non-target markdown branches() : Unit/implicit` (public (default))
  - `uv bootstrap service covers two-phase install with get-deps() : Unit/implicit` (public (default))
  - `uv bootstrap detects mkdocs yaml file variant() : Unit/implicit` (public (default))
  - `uv bootstrap uses windows python path when unix path does not exist() : Unit/implicit` (public (default))
  - `uv bootstrap deduplicates discovered dependencies() : Unit/implicit` (public (default))
  - `uv bootstrap handles unreadable config file gracefully() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/RuntimeAdapterCoverageEdgeCasesTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *RuntimeAdapterCoverageEdgeCasesTest`.

## RuntimeDecouplingPolicyTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/RuntimeDecouplingPolicyTest.kt`
- **What does it do?** Implements unit-focused test coverage around `RuntimeDecouplingPolicyTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `RuntimeDecouplingPolicyTest`.
  - Key callable members include `default runtime command does not hardcode host or port`, `manager preserves caller-defined command without injecting runtime endpoint`.
  - Scenario-focused test cases include: `default runtime command does not hardcode host or port`, `manager preserves caller-defined command without injecting runtime endpoint`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `RuntimeDecouplingPolicyTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `default runtime command does not hardcode host or port` consumes no explicit parameters.
  - `manager preserves caller-defined command without injecting runtime endpoint` consumes no explicit parameters.
- **Outputs produced:**
  - `default runtime command does not hardcode host or port` returns Unit/implicit.
  - `manager preserves caller-defined command without injecting runtime endpoint` returns Unit/implicit.
- **Side effects:**
  - Process/runtime execution


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `RuntimeDecouplingPolicyTest` (class, public (default))
- **Key public/internal functions:**
  - `default runtime command does not hardcode host or port() : Unit/implicit` (public (default))
  - `manager preserves caller-defined command without injecting runtime endpoint() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/RuntimeDecouplingPolicyTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *RuntimeDecouplingPolicyTest`.

## UvBootstrapServiceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/UvBootstrapServiceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `UvBootstrapServiceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Path
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `UvBootstrapServiceTest`.
  - Key callable members include `command result exposes stdout and stderr properties`, `runs bootstrap and install commands on first activation`, `uses resolved uv executable path for all commands`, `fails bootstrap when uv executable cannot be resolved`, `reuses existing runtime directory and skips uv venv command`, `continues when setup reports existing virtual environment and installs mkdocs`, `continues when setup reports existing virtual environment in stdout`, `installs dependencies discovered by mkdocs get-deps`, plus `9` additional members.
  - Scenario-focused test cases include: `command result exposes stdout and stderr properties`, `runs bootstrap and install commands on first activation`, `uses resolved uv executable path for all commands`, `fails bootstrap when uv executable cannot be resolved`, `reuses existing runtime directory and skips uv venv command`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `UvBootstrapServiceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `command result exposes stdout and stderr properties` consumes no explicit parameters.
  - `runs bootstrap and install commands on first activation` consumes no explicit parameters.
  - `uses resolved uv executable path for all commands` consumes no explicit parameters.
  - `fails bootstrap when uv executable cannot be resolved` consumes no explicit parameters.
  - `reuses existing runtime directory and skips uv venv command` consumes no explicit parameters.
- **Outputs produced:**
  - `command result exposes stdout and stderr properties` returns Unit/implicit.
  - `runs bootstrap and install commands on first activation` returns Unit/implicit.
  - `uses resolved uv executable path for all commands` returns Unit/implicit.
  - `fails bootstrap when uv executable cannot be resolved` returns Unit/implicit.
  - `reuses existing runtime directory and skips uv venv command` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `UvBootstrapServiceTest` (class, public (default))
- **Key public/internal functions:**
  - `command result exposes stdout and stderr properties() : Unit/implicit` (public (default))
  - `runs bootstrap and install commands on first activation() : Unit/implicit` (public (default))
  - `uses resolved uv executable path for all commands() : Unit/implicit` (public (default))
  - `fails bootstrap when uv executable cannot be resolved() : Unit/implicit` (public (default))
  - `reuses existing runtime directory and skips uv venv command() : Unit/implicit` (public (default))
  - `continues when setup reports existing virtual environment and installs mkdocs() : Unit/implicit` (public (default))
  - `continues when setup reports existing virtual environment in stdout() : Unit/implicit` (public (default))
  - `installs dependencies discovered by mkdocs get-deps() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/UvBootstrapServiceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *UvBootstrapServiceTest`.

## UvExecutableProviderCoverageTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/UvExecutableProviderCoverageTest.kt`
- **What does it do?** Implements unit-focused test coverage around `UvExecutableProviderCoverageTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.sun.net.httpserver.HttpServer, java.io.File, java.net.InetSocketAddress, java.net.ServerSocket, java.nio.file.Files, java.nio.file.Path, java.nio.file.attribute.PosixFilePermission, java.util.zip.ZipEntry
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `UvExecutableProviderCoverageTest`.
  - Key callable members include `static provider returns configured executable`, `http archive downloader covers success http error and connection failure`, `default archive extractor covers zip and tar branches plus failures`, `provider covers path candidate and common fallback discovery`, `provider covers managed windows executable and download failure branches`, `provider resolves windows common candidate from USERPROFILE`, `provider covers unsupported os and architecture targets`.
  - Scenario-focused test cases include: `static provider returns configured executable`, `http archive downloader covers success http error and connection failure`, `default archive extractor covers zip and tar branches plus failures`, `provider covers path candidate and common fallback discovery`, `provider covers managed windows executable and download failure branches`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.runtime`
- **Module / feature area:** `mkdocs-runtime-adapter` / `runtime`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `UvExecutableProviderCoverageTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `static provider returns configured executable` consumes no explicit parameters.
  - `http archive downloader covers success http error and connection failure` consumes no explicit parameters.
  - `default archive extractor covers zip and tar branches plus failures` consumes no explicit parameters.
  - `provider covers path candidate and common fallback discovery` consumes no explicit parameters.
  - `provider covers managed windows executable and download failure branches` consumes no explicit parameters.
- **Outputs produced:**
  - `static provider returns configured executable` returns Unit/implicit.
  - `http archive downloader covers success http error and connection failure` returns Unit/implicit.
  - `default archive extractor covers zip and tar branches plus failures` returns Unit/implicit.
  - `provider covers path candidate and common fallback discovery` returns Unit/implicit.
  - `provider covers managed windows executable and download failure branches` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - Process/runtime execution


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `UvExecutableProviderCoverageTest` (class, public (default))
- **Key public/internal functions:**
  - `static provider returns configured executable() : Unit/implicit` (public (default))
  - `http archive downloader covers success http error and connection failure() : Unit/implicit` (public (default))
  - `default archive extractor covers zip and tar branches plus failures() : Unit/implicit` (public (default))
  - `provider covers path candidate and common fallback discovery() : Unit/implicit` (public (default))
  - `provider covers managed windows executable and download failure branches() : Unit/implicit` (public (default))
  - `provider resolves windows common candidate from USERPROFILE() : Unit/implicit` (public (default))
  - `provider covers unsupported os and architecture targets() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Runtime/process/filesystem adapter for MkDocs + uv tooling.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/mkdocs-runtime-adapter/src/test/kotlin/com/authord/mkdocs/runtime/UvExecutableProviderCoverageTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *UvExecutableProviderCoverageTest`.

## ActivationErrorPresenter.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenter.kt`
- **What does it do?** Defines shared model/state types in `ActivationErrorPresenter`.
- **Why it matters:** Shared models must remain clear and stable because many workflows serialize or branch on them.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
- **Key functionalities:**
  - Main declarations: `ActivationFailureReason`, `ActivationErrorPresenter`.
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `UI/application coordination`

### 2. Main classes / interfaces / objects inside the file
- `ActivationFailureReason` (enum class, public (default)): Primary implementation type for this file feature area.
- `ActivationErrorPresenter` (open class, public (default)): Typed model representing shared state or payload data.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenterTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginActivationServiceErrorTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenterTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `ActivationFailureReason` (enum class, public (default))
  - `ActivationErrorPresenter` (open class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- `ActivationFailureReason` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenterTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsExplorerServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisherTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/OutOfScopeGuardrailsTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
// Construct `ActivationFailureReason` and use exposed members
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenter.kt`.

## DocsExplorerService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/DocsExplorerService.kt`
- **What does it do?** Implements service-level behavior in `DocsExplorerService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
- **Key functionalities:**
  - Main declarations: `DocsExplorerService`.
  - Key callable members include `discoverMarkdownFiles`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `UI/application coordination`

### 2. Main classes / interfaces / objects inside the file
- `DocsExplorerService` (class, public (default)): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `discoverMarkdownFiles` consumes projectPath: String, docsRoot: String = "docs".
- **Outputs produced:**
  - `discoverMarkdownFiles` returns List<String>.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsExplorerServiceTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsExplorerServiceTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `DocsExplorerService` (class, public (default))
- **Key public/internal functions:**
  - `discoverMarkdownFiles(projectPath: String, docsRoot: String = "docs") : List<String>` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsExplorerServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenterTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisherTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/OutOfScopeGuardrailsTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = DocsExplorerService(/* collaborators */)
val result = component.discoverMarkdownFiles(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsExplorerServiceTest.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/DocsExplorerService.kt`.

## DocsFileSelectionPublisher.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisher.kt`
- **What does it do?** Contains feature logic for `DocsFileSelectionPublisher` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
- **Key functionalities:**
  - Main declarations: `DocsFileSelectedEvent`, `DocsFileSelectionPublisher`.
  - Key callable members include `subscribe`, `publish`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `UI/application coordination`

### 2. Main classes / interfaces / objects inside the file
- `DocsFileSelectedEvent` (data class, public (default)): Typed model representing shared state or payload data.
- `DocsFileSelectionPublisher` (class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `subscribe` consumes listener: (DocsFileSelectedEvent.
  - `publish` consumes event: DocsFileSelectedEvent.
- **Outputs produced:**
  - `subscribe` returns Unit/implicit.
  - `publish` returns Unit/implicit.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisherTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisherTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `DocsFileSelectedEvent` (data class, public (default))
  - `DocsFileSelectionPublisher` (class, public (default))
- **Key public/internal functions:**
  - `subscribe(listener: (DocsFileSelectedEvent) : Unit/implicit` (public (default))
  - `publish(event: DocsFileSelectedEvent) : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- `DocsFileSelectedEvent` models structured data with fields: val projectId: String, val selectedPath: String.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisherTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenterTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsExplorerServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/OutOfScopeGuardrailsTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = DocsFileSelectedEvent(/* collaborators */)
val result = component.subscribe(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisherTest.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisher.kt`.

## FeatureFlagPolicyService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyService.kt`
- **What does it do?** Implements service-level behavior in `FeatureFlagPolicyService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.flags.FeatureFlagPolicy
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
- **Key functionalities:**
  - Main declarations: `FeatureFlagPolicyService`.
  - Key callable members include `current`, `update`, `allowsMvpOnlyExecution`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `UI/application coordination`

### 2. Main classes / interfaces / objects inside the file
- `FeatureFlagPolicyService` (class, public (default)): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `current` consumes no explicit parameters.
  - `update` consumes policy: FeatureFlagPolicy.
  - `allowsMvpOnlyExecution` consumes no explicit parameters.
- **Outputs produced:**
  - `current` returns FeatureFlagPolicy.
  - `update` returns Unit/implicit.
  - `allowsMvpOnlyExecution` returns Boolean.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/OutOfScopeGuardrailsTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationServiceTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.core.flags.FeatureFlagPolicy`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyServiceTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/OutOfScopeGuardrailsTest.kt`. Downstream collaborators include `com.authord.mkdocs.core.flags.FeatureFlagPolicy`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `FeatureFlagPolicyService` (class, public (default))
- **Key public/internal functions:**
  - `current() : FeatureFlagPolicy` (public (default))
  - `update(policy: FeatureFlagPolicy) : Unit/implicit` (public (default))
  - `allowsMvpOnlyExecution() : Boolean` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/flags/FeatureFlagPolicyTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenterTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsExplorerServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisherTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = FeatureFlagPolicyService(/* collaborators */)
val result = component.current(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyService.kt`.

## NavigationCoordinator.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/NavigationCoordinator.kt`
- **What does it do?** Contains feature logic for `NavigationCoordinator` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.navigation.RouteMappingService
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
- **Key functionalities:**
  - Main declarations: `NavigationResult`, `NavigationCoordinator`.
  - Key callable members include `onFileSelected`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `UI/application coordination`

### 2. Main classes / interfaces / objects inside the file
- `NavigationResult` (data class, public (default)): Primary implementation type for this file feature area.
- `NavigationCoordinator` (class, public (default)): Coordinator that sequences multi-step flows and state transitions.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `onFileSelected` consumes projectId: String, selectedPath: String, useDirectoryUrls: Boolean = true.
- **Outputs produced:**
  - `onFileSelected` returns NavigationResult.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationServiceTest.kt`
  - `tests/integration/explorer-preview-navigation/ExplorerSelectionToPreviewIT.kt`
- **What this file calls:**
  - `com.authord.mkdocs.core.navigation.RouteMappingService`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationServiceTest.kt`. Downstream collaborators include `com.authord.mkdocs.core.navigation.RouteMappingService`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `NavigationResult` (data class, public (default))
  - `NavigationCoordinator` (class, public (default))
- **Key public/internal functions:**
  - `onFileSelected(projectId: String, selectedPath: String, useDirectoryUrls: Boolean = true) : NavigationResult` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- `NavigationResult` models structured data with fields: val applied: Boolean, val route: String = "", val message: String = "".

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenterTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsExplorerServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisherTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/OutOfScopeGuardrailsTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = NavigationResult(/* collaborators */)
val result = component.onFileSelected(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/NavigationCoordinator.kt`.

## PluginActivationService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`
- **What does it do?** Implements service-level behavior in `PluginActivationService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.flags.FeatureFlagPolicy, com.authord.mkdocs.runtime.BaseUrlDetector, com.authord.mkdocs.runtime.MkdocsProcessManager, com.authord.mkdocs.runtime.RuntimeProcessDiagnostics, com.authord.mkdocs.runtime.RuntimeServerConfig, com.authord.mkdocs.runtime.UvBootstrapService, com.authord.mkdocs.ui.intellij.AuthordUiBundle, com.authord.mkdocs.ui.intellij.SiteContext
  - External: com.intellij.openapi.application.PathManager, com.intellij.openapi.diagnostic.Logger, java.net.HttpURLConnection, java.net.InetAddress, java.net.URI, java.net.ServerSocket, java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
- **Key functionalities:**
  - Main declarations: `HttpURLConnectionReadinessProbe`, `ActivationResult`, `PluginActivationService`, `StartupAttemptFailure`, `StartupReadiness`, `Ready`, plus `2` more.
  - Key callable members include `isReady`, `activate`, `disposeProjectResources`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `UI/application coordination`

### 2. Main classes / interfaces / objects inside the file
- `HttpURLConnectionReadinessProbe` (open class, public (default)): Primary implementation type for this file feature area.
- `ActivationResult` (data class, public (default)): Primary implementation type for this file feature area.
- `PluginActivationService` (class, public (default)): Service component that centralizes business/workflow logic.
- `StartupAttemptFailure` (data class, private): Primary implementation type for this file feature area.
- `StartupReadiness` (sealed interface, private): Contract/abstraction used to decouple callers from implementations.
- `Ready` (data class, public (default)): Primary implementation type for this file feature area.
- `ProcessExited` (data class, public (default)): Primary implementation type for this file feature area.
- `TimedOut` (data class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `isReady` consumes baseUrl: String.
  - `activate` consumes projectId: String, projectPath: String, @Suppress("UNUSED_PARAMETER".
  - `disposeProjectResources` consumes projectPath: String.
- **Outputs produced:**
  - `isReady` returns Boolean } /** * Lightweight HTTP readiness probe using [HttpURLConnection]. */ open class HttpURLConnectionReadinessProbe( private val connectTimeoutMillis: Int.
  - `activate` returns Unit/implicit.
  - `disposeProjectResources` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - Network calls


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewResultReporter.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouteLoadCoordinator.kt`
- **What this file calls:**
  - `com.authord.mkdocs.core.flags.FeatureFlagPolicy`
  - `com.authord.mkdocs.runtime.BaseUrlDetector`
  - `com.authord.mkdocs.runtime.MkdocsProcessManager`
  - `com.authord.mkdocs.runtime.RuntimeProcessDiagnostics`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewResultReporter.kt`. Downstream collaborators include `com.authord.mkdocs.core.flags.FeatureFlagPolicy`, `com.authord.mkdocs.runtime.BaseUrlDetector`, `com.authord.mkdocs.runtime.MkdocsProcessManager`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `HttpURLConnectionReadinessProbe` (open class, public (default))
  - `ActivationResult` (data class, public (default))
  - `PluginActivationService` (class, public (default))
  - `Ready` (data class, public (default))
  - `ProcessExited` (data class, public (default))
  - `TimedOut` (data class, public (default))
- **Key public/internal functions:**
  - `isReady(baseUrl: String) : Boolean } /** * Lightweight HTTP readiness probe using [HttpURLConnection]. */ open class HttpURLConnectionReadinessProbe( private val connectTimeoutMillis: Int` (public (default))
  - `activate(projectId: String, projectPath: String, @Suppress("UNUSED_PARAMETER") : Unit/implicit` (public (default))
  - `disposeProjectResources(projectPath: String) : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Suppress`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `ActivationResult` models structured data with fields: val success: Boolean, val reason: ActivationFailureReason? = null, val message: String = "", val previewUrl: String = "".
- `StartupAttemptFailure` models structured data with fields: val baseUrl: String, val failureSummary: String, val diagnostics: RuntimeProcessDiagnostics?, val startupOutput: String.
- `Ready` models structured data with fields: val diagnostics: RuntimeProcessDiagnostics?.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- Performs network operations; validate endpoint trust and request input sanitization.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginActivationServiceErrorTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginActivationServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenterTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsExplorerServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisherTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyServiceTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Large callable surface area increases regression risk; consider characterization tests before large refactors.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
```kotlin
val component = HttpURLConnectionReadinessProbe(/* collaborators */)
val result = component.isReady(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`.

## PluginCompositionRoot.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
- **What does it do?** Contains feature logic for `PluginCompositionRoot` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.defaults.command.InMemoryCommandRegistry, com.authord.mkdocs.defaults.preview.NoOpPreviewSyncAdapter, com.authord.mkdocs.defaults.vector.NoOpVectorStoreAdapter, com.authord.mkdocs.ports.TopicTreePort, com.authord.mkdocs.ports.command.DefaultPluginCommandBus, com.authord.mkdocs.ports.command.PluginCommandBus, com.authord.mkdocs.ports.preview.PreviewSyncPort, com.authord.mkdocs.ports.topic.DefaultTopicSyncError
  - External: com.intellij.openapi.components.service, com.intellij.openapi.project.Project
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
- **Key functionalities:**
  - Main declarations: `PluginCompositionRoot`, `Wiring`, `NoOpMkDocsConfigGateway`, `NoOpDocsFileGateway`, `NoOpTreeSyncOrchestrator`, `DefaultTopicTreeApplicationService`, plus `1` more.
  - Key callable members include `create`, `runtimeIntegration`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `UI/application coordination`

### 2. Main classes / interfaces / objects inside the file
- `PluginCompositionRoot` (class, public (default)): Primary implementation type for this file feature area.
- `Wiring` (data class, public (default)): Primary implementation type for this file feature area.
- `NoOpMkDocsConfigGateway` (class, private): Adapter translating domain requests to external/runtime APIs.
- `NoOpDocsFileGateway` (class, private): Adapter translating domain requests to external/runtime APIs.
- `NoOpTreeSyncOrchestrator` (class, private): Coordinator that sequences multi-step flows and state transitions.
- `DefaultTopicTreeApplicationService` (class, private): Service component that centralizes business/workflow logic.
- `DefaultTopicTreeUiService` (class, private): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `create` consumes topicTreePort: TopicTreePort.
  - `runtimeIntegration` consumes project: Project.
- **Outputs produced:**
  - `create` returns Wiring.
  - `runtimeIntegration` returns PluginRuntimeIntegrationService.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsAction.kt`
- **What this file calls:**
  - `com.authord.mkdocs.defaults.command.InMemoryCommandRegistry`
  - `com.authord.mkdocs.defaults.preview.NoOpPreviewSyncAdapter`
  - `com.authord.mkdocs.defaults.vector.NoOpVectorStoreAdapter`
  - `com.authord.mkdocs.ports.TopicTreePort`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`. Downstream collaborators include `com.authord.mkdocs.defaults.command.InMemoryCommandRegistry`, `com.authord.mkdocs.defaults.preview.NoOpPreviewSyncAdapter`, `com.authord.mkdocs.defaults.vector.NoOpVectorStoreAdapter`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PluginCompositionRoot` (class, public (default))
  - `Wiring` (data class, public (default))
- **Key public/internal functions:**
  - `create(topicTreePort: TopicTreePort) : Wiring` (public (default))
  - `runtimeIntegration(project: Project) : PluginRuntimeIntegrationService` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `Wiring` models structured data with fields: val commandBus: PluginCommandBus, val previewSyncPort: PreviewSyncPort, val vectorStorePort: VectorStorePort, val commandRegistry: InMemoryCommandRegistry, val mkDocsConfigGateway: MkDocsConfigGateway.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginCompositionRootCoverageTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginCompositionRootTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenterTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsExplorerServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisherTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyServiceTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = PluginCompositionRoot(/* collaborators */)
val result = component.create(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`.

## PreviewNavigationFailureHandler.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PreviewNavigationFailureHandler.kt`
- **What does it do?** Contains feature logic for `PreviewNavigationFailureHandler` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
- **Key functionalities:**
  - Main declarations: `PreviewNavigationFailureHandler`.
  - Key callable members include `messageFor`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `UI/application coordination`

### 2. Main classes / interfaces / objects inside the file
- `PreviewNavigationFailureHandler` (class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `messageFor` consumes selectedPath: String.
- **Outputs produced:**
  - `messageFor` returns String.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/NavigationCoordinator.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationServiceTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/NavigationCoordinator.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewNavigationFailureHandler` (class, public (default))
- **Key public/internal functions:**
  - `messageFor(selectedPath: String) : String` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenterTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsExplorerServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisherTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/OutOfScopeGuardrailsTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = PreviewNavigationFailureHandler(/* collaborators */)
val result = component.messageFor(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/NavigationCoordinator.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PreviewNavigationFailureHandler.kt`.

## PreviewPaneCoordinator.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PreviewPaneCoordinator.kt`
- **What does it do?** Contains feature logic for `PreviewPaneCoordinator` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.util.concurrent.ConcurrentHashMap
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
- **Key functionalities:**
  - Main declarations: `PreviewPaneState`, `PreviewPaneCoordinator`.
  - Key callable members include `navigate`, `currentUrl`, `currentState`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `UI/application coordination`

### 2. Main classes / interfaces / objects inside the file
- `PreviewPaneState` (data class, public (default)): Typed model representing shared state or payload data.
- `PreviewPaneCoordinator` (open class, public (default)): Coordinator that sequences multi-step flows and state transitions.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `navigate` consumes projectId: String, route: String.
  - `currentUrl` consumes projectId: String.
  - `currentState` consumes projectId: String.
- **Outputs produced:**
  - `navigate` returns PreviewPaneState?.
  - `currentUrl` returns String?.
  - `currentState` returns PreviewPaneState?.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/NavigationCoordinator.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/NavigationCoordinator.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewPaneState` (data class, public (default))
  - `PreviewPaneCoordinator` (open class, public (default))
- **Key public/internal functions:**
  - `navigate(projectId: String, route: String) : PreviewPaneState?` (public (default))
  - `currentUrl(projectId: String) : String?` (public (default))
  - `currentState(projectId: String) : PreviewPaneState?` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- `PreviewPaneState` models structured data with fields: val projectId: String, val baseUrl: String, val currentRoute: String, val isOpen: Boolean.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PreviewPaneCoordinatorTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenterTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsExplorerServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisherTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = PreviewPaneState(/* collaborators */)
val result = component.navigate(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/NavigationCoordinator.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PreviewPaneCoordinator.kt`.

## AuthordBackgroundTaskRunner.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordBackgroundTaskRunner.kt`
- **What does it do?** Contains feature logic for `AuthordBackgroundTaskRunner` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.progress.ProgressIndicator, com.intellij.openapi.progress.ProgressManager, com.intellij.openapi.progress.Task, com.intellij.openapi.project.Project
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- No top-level class/interface/object declarations detected; file is function/typealias/constant oriented.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - No clear exposed type; file may hold private/internal helpers only.
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- File is mostly declarative (types/contracts) with limited embedded business rules.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Suppress`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
// Construct `AuthordBackgroundTaskRunner` and use exposed members
```
Caller relationship is indirect or framework-driven; no strong static caller signal detected.

## AuthordMarkdownOpenPreviewListener.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt`
- **What does it do?** Handles UI/IDE interaction concerns in `AuthordMarkdownOpenPreviewListener`.
- **Why it matters:** This file directly shapes user-triggered behavior and IDE integration quality.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ui.PluginCompositionRoot
  - External: com.intellij.openapi.fileEditor.FileEditorManager, com.intellij.openapi.fileEditor.FileEditorManagerListener, com.intellij.openapi.project.DumbAware, com.intellij.openapi.project.Project, com.intellij.openapi.util.SystemInfoRt, com.intellij.openapi.vfs.VirtualFile
- **Responsibilities:**
  - Bind IDE/UI events to application services and maintain interaction consistency.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `AuthordMarkdownOpenPreviewListener`.
  - Key callable members include `onMarkdownOpened`, `isFileSelectedForPreview`, `comparablePath`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `AuthordMarkdownOpenPreviewListener` (class, internal): IDE interaction entry point for user/editor events.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginDescriptorRegistrationTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ui.PluginCompositionRoot`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginDescriptorRegistrationTest.kt`. Downstream collaborators include `com.authord.mkdocs.ui.PluginCompositionRoot`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `AuthordMarkdownOpenPreviewListener` (class, internal)
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = AuthordMarkdownOpenPreviewListener(/* collaborators */)
val result = component.onMarkdownOpened(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt`.

## AuthordMarkdownSplitEditorProvider.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`
- **What does it do?** Contains feature logic for `AuthordMarkdownSplitEditorProvider` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ui.PluginCompositionRoot
  - External: com.intellij.icons.AllIcons, com.intellij.openapi.application.ApplicationManager, com.intellij.openapi.application.ModalityState, com.intellij.openapi.actionSystem.ActionGroup, com.intellij.openapi.actionSystem.AnActionEvent, com.intellij.openapi.actionSystem.DefaultActionGroup, com.intellij.openapi.editor.Document, com.intellij.openapi.editor.Editor
- **Responsibilities:**
  - Bind IDE/UI events to application services and maintain interaction consistency.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `AuthordMarkdownSplitEditorProvider`, `AuthordMarkdownPreviewFileEditorProvider`, `AuthordMarkdownEditorWithPreview`, `AuthordMarkdownPreviewFileEditor`.
  - Key callable members include `isAuthordPreviewEligible`, `refreshPreviewAfterRuntimeStart`, `preferredAuthordSplitLayout`, `storeAuthordSplitLayout`, `parseAuthordSplitLayoutName`, `bindSourceEditor`, `restartPreviewAndRefresh`, `refreshForSelectedFile`, plus `1` additional members.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `AuthordMarkdownSplitEditorProvider` (class, public (default)): Primary implementation type for this file feature area.
- `AuthordMarkdownPreviewFileEditorProvider` (class, private): Primary implementation type for this file feature area.
- `AuthordMarkdownEditorWithPreview` (class, private): Primary implementation type for this file feature area.
- `AuthordMarkdownPreviewFileEditor` (class, internal): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `isAuthordPreviewEligible` consumes project: Project, filePath: String, router: PreviewRouterService = PreviewRouterService(.
  - `refreshPreviewAfterRuntimeStart` consumes trigger: PreviewStartTrigger.
  - `preferredAuthordSplitLayout` consumes project: Project?.
  - `storeAuthordSplitLayout` consumes project: Project?, layout: TextEditorWithPreview.Layout.
  - `parseAuthordSplitLayoutName` consumes stored: String?.
- **Outputs produced:**
  - `isAuthordPreviewEligible` returns Unit/implicit.
  - `refreshPreviewAfterRuntimeStart` returns Boolean.
  - `preferredAuthordSplitLayout` returns TextEditorWithPreview.Layout?.
  - `storeAuthordSplitLayout` returns Unit/implicit.
  - `parseAuthordSplitLayoutName` returns TextEditorWithPreview.Layout?.
- **Side effects:**
  - Filesystem reads/writes
  - IntelliJ UI state updates
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ui.PluginCompositionRoot`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`. Downstream collaborators include `com.authord.mkdocs.ui.PluginCompositionRoot`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `AuthordMarkdownSplitEditorProvider` (class, public (default))
  - `AuthordMarkdownPreviewFileEditor` (class, internal)
- **Key public/internal functions:**
  - `isAuthordPreviewEligible(project: Project, filePath: String, router: PreviewRouterService = PreviewRouterService() : Unit/implicit` (internal)
  - `refreshPreviewAfterRuntimeStart(trigger: PreviewStartTrigger) : Boolean` (internal)
  - `preferredAuthordSplitLayout(project: Project?) : TextEditorWithPreview.Layout?` (internal)
  - `storeAuthordSplitLayout(project: Project?, layout: TextEditorWithPreview.Layout) : Unit/implicit` (internal)
  - `parseAuthordSplitLayoutName(stored: String?) : TextEditorWithPreview.Layout?` (internal)
  - `bindSourceEditor(editor: Editor) : Unit/implicit` (internal)
  - `restartPreviewAndRefresh() : Unit/implicit` (internal)
  - `refreshForSelectedFile(autoStart: Boolean, trigger: PreviewStartTrigger) : Boolean` (internal)

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Tracks lifecycle/disposal boundaries; resources are expected to be released deterministically.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Volatile`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Large callable surface area increases regression risk; consider characterization tests before large refactors.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
```kotlin
val component = AuthordMarkdownSplitEditorProvider(/* collaborators */)
val result = component.isAuthordPreviewEligible(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`.

## AuthordPreviewSettingsService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsService.kt`
- **What does it do?** Implements service-level behavior in `AuthordPreviewSettingsService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.components.PersistentStateComponent, com.intellij.openapi.components.Service, com.intellij.openapi.components.State, com.intellij.openapi.components.Storage, com.intellij.openapi.components.StoragePathMacros
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `AuthordPreviewSettingsState`, `AuthordPreviewSettingsService`.
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `AuthordPreviewSettingsState` (data class, public (default)): Typed model representing shared state or payload data.
- `AuthordPreviewSettingsService` (class, public (default)): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `AuthordPreviewSettingsState` (data class, public (default))
  - `AuthordPreviewSettingsService` (class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- File is mostly declarative (types/contracts) with limited embedded business rules.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Service`, `@State`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `AuthordPreviewSettingsState` models structured data with fields: var autoOpenPreviewOnMarkdownOpen: Boolean = true.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
// Construct `AuthordPreviewSettingsState` and use exposed members
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsService.kt`.

## AuthordSplitEditorLayoutStateService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateService.kt`
- **What does it do?** Implements service-level behavior in `AuthordSplitEditorLayoutStateService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.components.PersistentStateComponent, com.intellij.openapi.components.Service, com.intellij.openapi.components.State, com.intellij.openapi.components.Storage, com.intellij.openapi.components.StoragePathMacros, com.intellij.openapi.fileEditor.TextEditorWithPreview
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Define stable contracts/models consumed across module boundaries.
  - Bind IDE/UI events to application services and maintain interaction consistency.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `AuthordSplitEditorLayoutState`, `AuthordSplitEditorLayoutStateService`.
  - Key callable members include `preferredLayout`, `setPreferredLayout`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `AuthordSplitEditorLayoutState` (data class, public (default)): Typed model representing shared state or payload data.
- `AuthordSplitEditorLayoutStateService` (class, public (default)): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `preferredLayout` consumes no explicit parameters.
  - `setPreferredLayout` consumes layout: TextEditorWithPreview.Layout.
- **Outputs produced:**
  - `preferredLayout` returns TextEditorWithPreview.Layout.
  - `setPreferredLayout` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginDescriptorRegistrationTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `AuthordSplitEditorLayoutState` (data class, public (default))
  - `AuthordSplitEditorLayoutStateService` (class, public (default))
- **Key public/internal functions:**
  - `preferredLayout() : TextEditorWithPreview.Layout` (public (default))
  - `setPreferredLayout(layout: TextEditorWithPreview.Layout) : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Service`, `@State`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `AuthordSplitEditorLayoutState` models structured data with fields: var preferredLayoutName: String = TextEditorWithPreview.Layout.SHOW_EDITOR_AND_PREVIEW.name.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = AuthordSplitEditorLayoutState(/* collaborators */)
val result = component.preferredLayout(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateService.kt`.

## AuthordUiBundle.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordUiBundle.kt`
- **What does it do?** Contains feature logic for `AuthordUiBundle` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.DynamicBundle, org.jetbrains.annotations.PropertyKey
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `AuthordUiBundle`.
  - Key callable members include `message`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `AuthordUiBundle` (object, public (default)): Singleton utility/policy holder shared across callers.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `message` consumes @PropertyKey(resourceBundle = BUNDLE_NAME.
- **Outputs produced:**
  - `message` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsProjectCreator.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsProjectCreator.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `AuthordUiBundle` (object, public (default))
- **Key public/internal functions:**
  - `message(@PropertyKey(resourceBundle = BUNDLE_NAME) : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val result = AuthordUiBundle.message(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordUiBundle.kt`.

## CompatibilityReleaseGateService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/CompatibilityReleaseGateService.kt`
- **What does it do?** Implements service-level behavior in `CompatibilityReleaseGateService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `CompatibilityReleaseGateDecision`, `CompatibilityReleaseGateService`.
  - Key callable members include `evaluate`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `CompatibilityReleaseGateDecision` (data class, public (default)): Primary implementation type for this file feature area.
- `CompatibilityReleaseGateService` (class, public (default)): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `evaluate` consumes regressions: List<CompatibilityRegression>, projectId: String, instanceId: String.
- **Outputs produced:**
  - `evaluate` returns CompatibilityReleaseGateDecision.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsAction.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewCompatibilityCriteriaTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsActionInvocationTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsAction.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewCompatibilityCriteriaTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsActionInvocationTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `CompatibilityReleaseGateDecision` (data class, public (default))
  - `CompatibilityReleaseGateService` (class, public (default))
- **Key public/internal functions:**
  - `evaluate(regressions: List<CompatibilityRegression>, projectId: String, instanceId: String) : CompatibilityReleaseGateDecision` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `CompatibilityReleaseGateDecision` models structured data with fields: val blocked: Boolean, val unresolvedHighSeverity: List<CompatibilityRegression>, val summary: String.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = CompatibilityReleaseGateDecision(/* collaborators */)
val result = component.evaluate(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsAction.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/CompatibilityReleaseGateService.kt`.

## CompatibilitySeverityClassifier.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/CompatibilitySeverityClassifier.kt`
- **What does it do?** Contains feature logic for `CompatibilitySeverityClassifier` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `CompatibilityRegressionKind`, `CompatibilitySeverity`, `CompatibilityRegression`, `CompatibilitySeverityClassifier`.
  - Key callable members include `classify`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `CompatibilityRegressionKind` (enum class, public (default)): Primary implementation type for this file feature area.
- `CompatibilitySeverity` (enum class, public (default)): Primary implementation type for this file feature area.
- `CompatibilityRegression` (data class, public (default)): Primary implementation type for this file feature area.
- `CompatibilitySeverityClassifier` (class, public (default)): Rule/policy implementation for decisions and validation.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `classify` consumes regression: CompatibilityRegression.
- **Outputs produced:**
  - `classify` returns CompatibilitySeverity.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/CompatibilityReleaseGateService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsAction.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewCompatibilityCriteriaTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsActionInvocationTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/CompatibilityReleaseGateService.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsAction.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewCompatibilityCriteriaTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `CompatibilityRegressionKind` (enum class, public (default))
  - `CompatibilitySeverity` (enum class, public (default))
  - `CompatibilityRegression` (data class, public (default))
  - `CompatibilitySeverityClassifier` (class, public (default))
- **Key public/internal functions:**
  - `classify(regression: CompatibilityRegression) : CompatibilitySeverity` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `CompatibilityRegression` models structured data with fields: val id: String, val kind: CompatibilityRegressionKind, val description: String, val resolved: Boolean.
- `CompatibilityRegressionKind` constrains state to a fixed enum set used across callers.
- `CompatibilitySeverity` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = CompatibilityRegressionKind(/* collaborators */)
val result = component.classify(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/CompatibilityReleaseGateService.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/CompatibilitySeverityClassifier.kt`.

## DocsScopeResolver.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolver.kt`
- **What does it do?** Contains feature logic for `DocsScopeResolver` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.project.Project, java.nio.file.Files, java.nio.file.Path, java.util.Locale
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `DocsScopeStatus`, `DocsScopeResult`, `DocsScopeResolver`, `ConfigScope`.
  - Key callable members include `resolve`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `DocsScopeStatus` (enum class, internal): Primary implementation type for this file feature area.
- `DocsScopeResult` (data class, internal): Primary implementation type for this file feature area.
- `DocsScopeResolver` (class, internal): Primary implementation type for this file feature area.
- `ConfigScope` (data class, private): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `resolve` consumes project: Project, filePath: String.
- **Outputs produced:**
  - `resolve` returns DocsScopeResult.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouterService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandler.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouterService.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandler.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `DocsScopeStatus` (enum class, internal)
  - `DocsScopeResult` (data class, internal)
  - `DocsScopeResolver` (class, internal)
- **Key public/internal functions:**
  - `resolve(project: Project, filePath: String) : DocsScopeResult` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `DocsScopeResult` models structured data with fields: val status: DocsScopeStatus, val configPath: String? = null, val docsDirPath: String? = null, val detail: String? = null.
- `ConfigScope` models structured data with fields: val configPath: Path, val docsDirPath: Path?, val invalidReason: String?.
- `DocsScopeStatus` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = DocsScopeStatus(/* collaborators */)
val result = component.resolve(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouterService.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolver.kt`.

## FailureBypassStore.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/FailureBypassStore.kt`
- **What does it do?** Contains feature logic for `FailureBypassStore` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.components.Service, com.intellij.openapi.util.SystemInfoRt, java.util.Locale, java.util.concurrent.ConcurrentHashMap
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `FailureBypassStore`.
  - Key callable members include `markBypass`, `isBypassed`, `clearBypass`, `clearAll`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `FailureBypassStore` (class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `markBypass` consumes filePath: String, configPath: String?.
  - `isBypassed` consumes filePath: String, configPath: String?.
  - `clearBypass` consumes filePath: String, configPath: String?.
  - `clearAll` consumes no explicit parameters.
- **Outputs produced:**
  - `markBypass` returns Unit/implicit.
  - `isBypassed` returns Boolean.
  - `clearBypass` returns Unit/implicit.
  - `clearAll` returns Unit/implicit.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouterService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandler.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouterService.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandler.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `FailureBypassStore` (class, public (default))
- **Key public/internal functions:**
  - `markBypass(filePath: String, configPath: String?) : Unit/implicit` (public (default))
  - `isBypassed(filePath: String, configPath: String?) : Boolean` (public (default))
  - `clearBypass(filePath: String, configPath: String?) : Unit/implicit` (public (default))
  - `clearAll() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Service`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = FailureBypassStore(/* collaborators */)
val result = component.markBypass(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/FailureBypassStore.kt`.

## InstanceRegistryService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/InstanceRegistryService.kt`
- **What does it do?** Implements service-level behavior in `InstanceRegistryService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.topic.PathPolicy, com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.InstanceRegistryPort, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncErrorCode
  - External: java.nio.file.Files, java.nio.file.Path, java.util.concurrent.ConcurrentHashMap
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `PersistedInstanceRegistryState`, `InstanceRegistryService`, `InstanceRegistryStateStore`, `InMemoryInstanceRegistryStateStore`.
  - Key callable members include `load`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `PersistedInstanceRegistryState` (data class, public (default)): Typed model representing shared state or payload data.
- `InstanceRegistryService` (class, public (default)): Service component that centralizes business/workflow logic.
- `InstanceRegistryStateStore` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.
- `InMemoryInstanceRegistryStateStore` (object, private): Singleton utility/policy holder shared across callers.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `load` consumes projectKey: String.
- **Outputs produced:**
  - `load` returns PersistedInstanceRegistryState? /** * Saves persisted state for a project key. */ fun save(projectKey: String, state: PersistedInstanceRegistryState) } private object InMemoryInstanceRegistryStateStore : InstanceRegistryStateStore.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginCompositionRootCoverageTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/InstanceRegistryDiscoveryTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.core.topic.PathPolicy`
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.InstanceRegistryPort`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginCompositionRootCoverageTest.kt`. Downstream collaborators include `com.authord.mkdocs.core.topic.PathPolicy`, `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.InstanceRegistryPort`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PersistedInstanceRegistryState` (data class, public (default))
  - `InstanceRegistryService` (class, public (default))
  - `InstanceRegistryStateStore` (interface, public (default))
- **Key public/internal functions:**
  - `load(projectKey: String) : PersistedInstanceRegistryState? /** * Saves persisted state for a project key. */ fun save(projectKey: String, state: PersistedInstanceRegistryState) } private object InMemoryInstanceRegistryStateStore : InstanceRegistryStateStore` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `PersistedInstanceRegistryState` models structured data with fields: val instances: List<TopicInstanceRef>, val activeInstanceId: String?.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = PersistedInstanceRegistryState(/* collaborators */)
val result = component.load(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/InstanceRegistryService.kt`.

## InstanceSwitchCoordinator.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/InstanceSwitchCoordinator.kt`
- **What does it do?** Contains feature logic for `InstanceSwitchCoordinator` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncErrorCode, com.authord.mkdocs.ports.topic.TopicSyncOutcome
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `InstanceSwitchOutcome`, `InstanceSwitchCoordinator`.
  - Key callable members include `switchActiveInstance`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `InstanceSwitchOutcome` (data class, public (default)): Primary implementation type for this file feature area.
- `InstanceSwitchCoordinator` (class, public (default)): Coordinator that sequences multi-step flows and state transitions.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `switchActiveInstance` consumes instanceId: String.
- **Outputs produced:**
  - `switchActiveInstance` returns TopicGatewayResult<InstanceSwitchOutcome>.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanelUiContractTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
  - `com.authord.mkdocs.ports.topic.TopicSyncErrorCode`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanelUiContractTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.TopicGatewayResult`, `com.authord.mkdocs.ports.topic.TopicInstanceRef`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `InstanceSwitchOutcome` (data class, public (default))
  - `InstanceSwitchCoordinator` (class, public (default))
- **Key public/internal functions:**
  - `switchActiveInstance(instanceId: String) : TopicGatewayResult<InstanceSwitchOutcome>` (public (default))

### 6. Business rules or important logic
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `InstanceSwitchOutcome` models structured data with fields: val selectedInstance: TopicInstanceRef, val refreshOutcome: TopicSyncOutcome?.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = InstanceSwitchOutcome(/* collaborators */)
val result = component.switchActiveInstance(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/InstanceSwitchCoordinator.kt`.

## MkDocsPreviewBrowserService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserService.kt`
- **What does it do?** Implements service-level behavior in `MkDocsPreviewBrowserService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.Disposable, com.intellij.openapi.components.Service, com.intellij.openapi.diagnostic.Logger, com.intellij.openapi.project.Project, java.awt.BorderLayout, javax.swing.JComponent, javax.swing.JPanel
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `PreviewOwnerKind`, `MkDocsPreviewBrowserService`, `OwnerRegistryEntry`, `NoOpPreviewContent`.
  - Key callable members include `ensurePreviewContent`, `previewContentOrNull`, `hasInitializedPreviewContent`, `markMarkdownActivated`, `markdownPreviewActivated`, `loadUrl`, `resetLastLoadedUrl`, `previewContentForOwner`, plus `4` additional members.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `PreviewOwnerKind` (enum class, public (default)): Primary implementation type for this file feature area.
- `MkDocsPreviewBrowserService` (class, public (default)): Service component that centralizes business/workflow logic.
- `OwnerRegistryEntry` (data class, private): Primary implementation type for this file feature area.
- `NoOpPreviewContent` (class, private): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `ensurePreviewContent` consumes no explicit parameters.
  - `previewContentOrNull` consumes no explicit parameters.
  - `hasInitializedPreviewContent` consumes no explicit parameters.
  - `markMarkdownActivated` consumes no explicit parameters.
  - `markdownPreviewActivated` consumes no explicit parameters.
- **Outputs produced:**
  - `ensurePreviewContent` returns PreviewContent.
  - `previewContentOrNull` returns PreviewContent?.
  - `hasInitializedPreviewContent` returns Boolean.
  - `markMarkdownActivated` returns PreviewContent.
  - `markdownPreviewActivated` returns Boolean.
- **Side effects:**
  - IntelliJ UI state updates


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewOwnerKind` (enum class, public (default))
  - `MkDocsPreviewBrowserService` (class, public (default))
- **Key public/internal functions:**
  - `ensurePreviewContent() : PreviewContent` (public (default))
  - `previewContentOrNull() : PreviewContent?` (public (default))
  - `hasInitializedPreviewContent() : Boolean` (public (default))
  - `markMarkdownActivated() : PreviewContent` (public (default))
  - `markdownPreviewActivated() : Boolean` (public (default))
  - `loadUrl(url: String, forceReload: Boolean = false) : Unit/implicit` (public (default))
  - `resetLastLoadedUrl() : Unit/implicit` (public (default))
  - `previewContentForOwner(ownerKind: PreviewOwnerKind, ownerId: String) : PreviewContent` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Tracks lifecycle/disposal boundaries; resources are expected to be released deterministically.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Service`, `@Suppress`, `@Volatile`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `OwnerRegistryEntry` models structured data with fields: val ownerKey: String, val ownerKind: PreviewOwnerKind, val content: OwnedPreviewContent, var requestedActive: Boolean = false, var activationOrder: Long = 0L.
- `PreviewOwnerKind` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = PreviewOwnerKind(/* collaborators */)
val result = component.ensurePreviewContent(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserService.kt`.

## MkDocsPreviewService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewService.kt`
- **What does it do?** Implements service-level behavior in `MkDocsPreviewService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: com.authord.mkdocs.runtime.MkdocsProcessManager
  - External: com.intellij.openapi.Disposable, com.intellij.openapi.diagnostic.Logger
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `MkDocsPreviewService`.
  - Key callable members include `stopServer`, `isServerRunning`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `MkDocsPreviewService` (class, public (default)): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `stopServer` consumes no explicit parameters.
  - `isServerRunning` consumes no explicit parameters.
- **Outputs produced:**
  - `stopServer` returns Boolean.
  - `isServerRunning` returns Boolean.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
- **What this file calls:**
  - `com.authord.mkdocs.runtime.MkdocsProcessManager`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`. Downstream collaborators include `com.authord.mkdocs.runtime.MkdocsProcessManager`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MkDocsPreviewService` (class, public (default))
- **Key public/internal functions:**
  - `stopServer() : Boolean` (public (default))
  - `isServerRunning() : Boolean` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Tracks lifecycle/disposal boundaries; resources are expected to be released deterministically.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = MkDocsPreviewService(/* collaborators */)
val result = component.stopServer(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewService.kt`.

## MkDocsProjectCreator.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsProjectCreator.kt`
- **What does it do?** Contains feature logic for `MkDocsProjectCreator` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.runtime.CommandResult, com.authord.mkdocs.runtime.CommandRunner, com.authord.mkdocs.runtime.MkDocsYamlGateway, com.authord.mkdocs.runtime.ProjectManagedUvExecutableProvider, com.authord.mkdocs.runtime.UvExecutableProvider
  - External: java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `MkDocsProjectCreationResult`, `MkDocsProjectCreator`.
  - Key callable members include `createProject`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `MkDocsProjectCreationResult` (data class, public (default)): Primary implementation type for this file feature area.
- `MkDocsProjectCreator` (class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `createProject` consumes projectRootPath: String, requestedProjectName: String.
- **Outputs produced:**
  - `createProject` returns MkDocsProjectCreationResult.
- **Side effects:**
  - Filesystem reads/writes
  - Process/runtime execution


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkDocsProjectCreatorTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactoryTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
  - `com.authord.mkdocs.runtime.CommandResult`
  - `com.authord.mkdocs.runtime.CommandRunner`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkDocsProjectCreatorTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactoryTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicGatewayResult`, `com.authord.mkdocs.ports.topic.TopicInstanceRef`, `com.authord.mkdocs.runtime.CommandResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MkDocsProjectCreationResult` (data class, public (default))
  - `MkDocsProjectCreator` (class, public (default))
- **Key public/internal functions:**
  - `createProject(projectRootPath: String, requestedProjectName: String) : MkDocsProjectCreationResult` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `MkDocsProjectCreationResult` models structured data with fields: val success: Boolean, val message: String = "".

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkDocsProjectCreatorTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = MkDocsProjectCreationResult(/* collaborators */)
val result = component.createProject(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsProjectCreator.kt`.

## MkdocsConfigLocator.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsConfigLocator.kt`
- **What does it do?** Contains feature logic for `MkdocsConfigLocator` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.FileVisitResult, java.nio.file.Files, java.nio.file.Path, java.nio.file.SimpleFileVisitor, java.nio.file.attribute.BasicFileAttributes, java.util.Locale, java.util.Optional, java.util.concurrent.ConcurrentHashMap
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `MkdocsConfigLocator`.
  - Key callable members include `findMkdocsConfig`, `findAllMkdocsConfigs`, `isMkdocsConfigPath`, `mkdocsConfigCandidateFileNames`, `isMarkdownPath`, `hasMkdocsConfig`, `hasConfigFile`, `isUnderProject`, plus `4` additional members.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `MkdocsConfigLocator` (object, private): Singleton utility/policy holder shared across callers.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `findMkdocsConfig` consumes projectRoot: Path.
  - `findAllMkdocsConfigs` consumes projectRoot: Path.
  - `isMkdocsConfigPath` consumes path: String.
  - `mkdocsConfigCandidateFileNames` consumes no explicit parameters.
  - `isMarkdownPath` consumes path: String.
- **Outputs produced:**
  - `findMkdocsConfig` returns Path?.
  - `findAllMkdocsConfigs` returns List<Path>.
  - `isMkdocsConfigPath` returns Boolean.
  - `mkdocsConfigCandidateFileNames` returns List<String>.
  - `isMarkdownPath` returns Boolean.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - No clear exposed type; file may hold private/internal helpers only.
- **Key public/internal functions:**
  - `findMkdocsConfig(projectRoot: Path) : Path?` (internal)
  - `findAllMkdocsConfigs(projectRoot: Path) : List<Path>` (internal)
  - `isMkdocsConfigPath(path: String) : Boolean` (internal)
  - `mkdocsConfigCandidateFileNames() : List<String>` (internal)
  - `isMarkdownPath(path: String) : Boolean` (internal)
  - `hasMkdocsConfig(projectBasePath: String) : Boolean` (internal)
  - `hasConfigFile(projectBasePath: String) : Boolean` (internal)
  - `isUnderProject(projectBasePath: String, selectedPath: String) : Boolean` (internal)

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsConfigLocatorTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val result = MkdocsConfigLocator.findMkdocsConfig(/* inputs */)
```
Caller relationship is indirect or framework-driven; no strong static caller signal detected.

## MkdocsScrollSyncEngine.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsScrollSyncEngine.kt`
- **What does it do?** Contains feature logic for `MkdocsScrollSyncEngine` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.editor.Document, com.intellij.openapi.editor.event.DocumentEvent, java.text.Normalizer, java.util.concurrent.atomic.AtomicBoolean, java.util.concurrent.atomic.AtomicLong
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `AnchorType`, `SourceAnchor`, `DomAnchor`, `Knot`, `LineRange`, `HardAnchorMatch`, plus `11` more.
  - Key callable members include `focusRatio`, `resolveSourceState`, `currentState`, `invalidateAnchors`, `resetSyncState`, `recordDocumentChange`, `requestMapRebuild`, `onEditorScroll`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `AnchorType` (enum class, public (default)): Primary implementation type for this file feature area.
- `SourceAnchor` (data class, internal): Primary implementation type for this file feature area.
- `DomAnchor` (data class, internal): Primary implementation type for this file feature area.
- `Knot` (data class, internal): Primary implementation type for this file feature area.
- `LineRange` (data class, internal): Primary implementation type for this file feature area.
- `HardAnchorMatch` (data class, internal): Primary implementation type for this file feature area.
- `MapState` (data class, internal): Typed model representing shared state or payload data.
- `PreviewScrollCommand` (data class, internal): Primary implementation type for this file feature area.
- `SourceDocumentState` (data class, internal): Typed model representing shared state or payload data.
- `MutableAnchorDraft` (data class, private): Primary implementation type for this file feature area.
- `SourceAnchorSegment` (data class, private): Primary implementation type for this file feature area.
- `AnchorMatch` (data class, private): Primary implementation type for this file feature area.
- Additional declarations: `5` more declarations not listed individually.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `focusRatio` consumes no explicit parameters.
  - `resolveSourceState` consumes document: Document.
  - `currentState` consumes no explicit parameters.
  - `invalidateAnchors` consumes no explicit parameters.
  - `resetSyncState` consumes no explicit parameters.
- **Outputs produced:**
  - `focusRatio` returns Double.
  - `resolveSourceState` returns SourceDocumentState.
  - `currentState` returns MapState?.
  - `invalidateAnchors` returns Unit/implicit.
  - `resetSyncState` returns Unit/implicit.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsScrollSyncEngineTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactoryTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsScrollSyncEngineTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `AnchorType` (enum class, public (default))
  - `SourceAnchor` (data class, internal)
  - `DomAnchor` (data class, internal)
  - `Knot` (data class, internal)
  - `LineRange` (data class, internal)
  - `HardAnchorMatch` (data class, internal)
- **Key public/internal functions:**
  - `focusRatio() : Double` (public (default))
  - `resolveSourceState(document: Document) : SourceDocumentState` (public (default))
  - `currentState() : MapState?` (public (default))
  - `invalidateAnchors() : Unit/implicit` (public (default))
  - `resetSyncState() : Unit/implicit` (public (default))
  - `recordDocumentChange(document: Document, event: DocumentEvent) : Unit/implicit` (public (default))
  - `requestMapRebuild(document: Document, lineHeightPx: Int, loadPreviewSnapshot: ((PreviewDomSnapshot?) : Unit/implicit` (public (default))
  - `onEditorScroll(document: Document, lineHeightPx: Int, editorScrollTopPx: Double, editorViewportHeightPx: Double, loadPreviewSnapshot: (((PreviewDomSnapshot?) : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Volatile`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `SourceAnchor` models structured data with fields: val id: String, val type: AnchorType, val level: Int?, val startLine: Int, val endLine: Int.
- `DomAnchor` models structured data with fields: val id: String, val type: AnchorType, val level: Int?, val top: Double, val bottom: Double.
- `Knot` models structured data with fields: val editorY: Double, val previewY: Double, val hard: Boolean, val confidence: Double.
- `AnchorType` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- Contains auth/credential-sensitive terms; review storage, transport, and logging paths carefully.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsScrollSyncEngineTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Large callable surface area increases regression risk; consider characterization tests before large refactors.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
```kotlin
val component = AnchorType(/* collaborators */)
val result = component.focusRatio(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsScrollSyncEngine.kt`.

## MkdocsToolWindowFactory.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
- **What does it do?** Handles UI/IDE interaction concerns in `MkdocsToolWindowFactory`.
- **Why it matters:** This file directly shapes user-triggered behavior and IDE integration quality.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.topic.TopicTreeMutationService, com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.InstanceRegistryPort, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ui.ActivationResult, com.authord.mkdocs.ui.PluginCompositionRoot, com.authord.mkdocs.runtime.DocsFileGatewayAdapter
  - External: com.intellij.icons.AllIcons, com.intellij.ide.BrowserUtil, com.intellij.openapi.actionSystem.ActionManager, com.intellij.openapi.actionSystem.AnAction, com.intellij.openapi.actionSystem.AnActionEvent, com.intellij.openapi.actionSystem.DefaultActionGroup, com.intellij.openapi.editor.Document, com.intellij.openapi.editor.EditorFactory
- **Responsibilities:**
  - Bind IDE/UI events to application services and maintain interaction consistency.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `ShellLayoutMode`, `MkdocsToolWindowFactory`, `TopicTreeControllers`, `PreviewScrollMetrics`, `PreviewDomAnchor`, `PreviewDomSnapshot`, plus `7` more.
  - Key callable members include `createShellContentPanel`, `createShellToolbar`, `createRestartPluginAction`, `restartPlugin`, `createSetShellLayoutModeAction`, `setShellLayoutMode`, `shouldReconcileForVfsEvents`, `applyPreviewRoute`, plus `22` additional members.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `ShellLayoutMode` (enum class, internal): Primary implementation type for this file feature area.
- `MkdocsToolWindowFactory` (class, public (default)): Primary implementation type for this file feature area.
- `TopicTreeControllers` (data class, public (default)): Primary implementation type for this file feature area.
- `PreviewScrollMetrics` (data class, public (default)): Primary implementation type for this file feature area.
- `PreviewDomAnchor` (data class, public (default)): Primary implementation type for this file feature area.
- `PreviewDomSnapshot` (data class, public (default)): Primary implementation type for this file feature area.
- `ShellContent` (data class, private): Primary implementation type for this file feature area.
- `PreviewContent` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.
- `DeferredPreviewContent` (class, private): Primary implementation type for this file feature area.
- `JcefPreviewContent` (class, private): Primary implementation type for this file feature area.
- `PreviewNavigationDecision` (data class, internal): Primary implementation type for this file feature area.
- `PreviewNavigationPolicy` (object, internal): Singleton utility/policy holder shared across callers.
- Additional declarations: `1` more declarations not listed individually.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `createShellContentPanel` consumes project: Project, runtimeService: PluginRuntimeIntegrationService = runtimeServiceResolver(project.
  - `createShellToolbar` consumes target: JComponent, project: Project, runtimeService: PluginRuntimeIntegrationService, previewContent: PreviewContent, splitter: OnePixelSplitter?.
  - `createRestartPluginAction` consumes project: Project, runtimeService: PluginRuntimeIntegrationService, previewContent: PreviewContent.
  - `restartPlugin` consumes project: Project, runtimeService: PluginRuntimeIntegrationService, previewContent: PreviewContent.
  - `createSetShellLayoutModeAction` consumes project: Project, splitter: OnePixelSplitter?, mode: ShellLayoutMode.
- **Outputs produced:**
  - `createShellContentPanel` returns Unit/implicit.
  - `createShellToolbar` returns JComponent.
  - `createRestartPluginAction` returns AnAction.
  - `restartPlugin` returns ActivationResult.
  - `createSetShellLayoutModeAction` returns AnAction.
- **Side effects:**
  - Filesystem reads/writes
  - IntelliJ UI state updates
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsScrollSyncEngine.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouteLoadCoordinator.kt`
- **What this file calls:**
  - `com.authord.mkdocs.core.topic.TopicTreeMutationService`
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
  - `com.authord.mkdocs.ports.topic.InstanceRegistryPort`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserService.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsScrollSyncEngine.kt`. Downstream collaborators include `com.authord.mkdocs.core.topic.TopicTreeMutationService`, `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`, `com.authord.mkdocs.ports.topic.InstanceRegistryPort`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `ShellLayoutMode` (enum class, internal)
  - `MkdocsToolWindowFactory` (class, public (default))
  - `TopicTreeControllers` (data class, public (default))
  - `PreviewScrollMetrics` (data class, public (default))
  - `PreviewDomAnchor` (data class, public (default))
  - `PreviewDomSnapshot` (data class, public (default))
- **Key public/internal functions:**
  - `createShellContentPanel(project: Project, runtimeService: PluginRuntimeIntegrationService = runtimeServiceResolver(project) : Unit/implicit` (internal)
  - `createShellToolbar(target: JComponent, project: Project, runtimeService: PluginRuntimeIntegrationService, previewContent: PreviewContent, splitter: OnePixelSplitter?) : JComponent` (internal)
  - `createRestartPluginAction(project: Project, runtimeService: PluginRuntimeIntegrationService, previewContent: PreviewContent) : AnAction` (internal)
  - `restartPlugin(project: Project, runtimeService: PluginRuntimeIntegrationService, previewContent: PreviewContent) : ActivationResult` (internal)
  - `createSetShellLayoutModeAction(project: Project, splitter: OnePixelSplitter?, mode: ShellLayoutMode) : AnAction` (internal)
  - `setShellLayoutMode(projectKey: String, splitter: OnePixelSplitter, mode: ShellLayoutMode) : Unit/implicit` (internal)
  - `shouldReconcileForVfsEvents(project: Project, events: List<VFileEvent>) : Boolean` (internal)
  - `applyPreviewRoute(project: Project, runtimeService: PluginRuntimeIntegrationService, previewContent: PreviewContent, selectedPath: String) : Boolean` (internal)

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Tracks lifecycle/disposal boundaries; resources are expected to be released deterministically.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Volatile`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `TopicTreeControllers` models structured data with fields: val actionController: TopicTreeActionController, val dragDropController: TopicTreeDragDropController, val failureRecoveryPresenter: TopicTreeFailureRecoveryPresenter, val instanceSwitchCoordinator: InstanceSwitchCoordinator, val instanceRegistryPort: InstanceRegistryPort?.
- `PreviewScrollMetrics` models structured data with fields: val scrollY: Double, val maxScrollY: Double.
- `PreviewDomAnchor` models structured data with fields: val id: String?, val type: AnchorType, val level: Int?, val top: Double, val bottom: Double.
- `ShellLayoutMode` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- Contains auth/credential-sensitive terms; review storage, transport, and logging paths carefully.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactoryTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Large callable surface area increases regression risk; consider characterization tests before large refactors.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
```kotlin
val component = ShellLayoutMode(/* collaborators */)
val result = component.createShellContentPanel(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`.

## MkdocsVenvDirectoryExcludePolicy.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsVenvDirectoryExcludePolicy.kt`
- **What does it do?** Contains feature logic for `MkdocsVenvDirectoryExcludePolicy` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.project.ProjectManager, com.intellij.openapi.roots.impl.DirectoryIndexExcludePolicy, com.intellij.openapi.vfs.VfsUtilCore, java.nio.file.Path
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `MkdocsVenvDirectoryExcludePolicy`.
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `MkdocsVenvDirectoryExcludePolicy` (class, public (default)): Rule/policy implementation for decisions and validation.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginDescriptorRegistrationTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginDescriptorRegistrationTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MkdocsVenvDirectoryExcludePolicy` (class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
// Construct `MkdocsVenvDirectoryExcludePolicy` and use exposed members
```
Typical caller path: `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginDescriptorRegistrationTest.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsVenvDirectoryExcludePolicy.kt`.

## MultiInstanceReconciliationCoordinator.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MultiInstanceReconciliationCoordinator.kt`
- **What does it do?** Contains feature logic for `MultiInstanceReconciliationCoordinator` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.TopicInstanceRef
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `InstanceReconciliationInput`, `MultiInstanceReconciliationResult`, `MultiInstanceReconciliationCoordinator`.
  - Key callable members include `reconcile`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `InstanceReconciliationInput` (data class, public (default)): Primary implementation type for this file feature area.
- `MultiInstanceReconciliationResult` (data class, public (default)): Primary implementation type for this file feature area.
- `MultiInstanceReconciliationCoordinator` (class, public (default)): Coordinator that sequences multi-step flows and state transitions.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `reconcile` consumes projectId: String, inputs: List<InstanceReconciliationInput>.
- **Outputs produced:**
  - `reconcile` returns MultiInstanceReconciliationResult.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MultiInstanceReconciliationTest.kt`
  - `tests/integration/topic-tree/InstanceScaleProfileIntegrationTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MultiInstanceReconciliationTest.kt`, `tests/integration/topic-tree/InstanceScaleProfileIntegrationTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`, `com.authord.mkdocs.ports.topic.TopicInstanceRef`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `InstanceReconciliationInput` (data class, public (default))
  - `MultiInstanceReconciliationResult` (data class, public (default))
  - `MultiInstanceReconciliationCoordinator` (class, public (default))
- **Key public/internal functions:**
  - `reconcile(projectId: String, inputs: List<InstanceReconciliationInput>) : MultiInstanceReconciliationResult` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `InstanceReconciliationInput` models structured data with fields: val instance: TopicInstanceRef, val config: MkDocsConfigDocument, val docsMarkdownPaths: List<String>.
- `MultiInstanceReconciliationResult` models structured data with fields: val statesByInstanceId: Map<String, StartupTreeState>.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MultiInstanceReconciliationTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = InstanceReconciliationInput(/* collaborators */)
val result = component.reconcile(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MultiInstanceReconciliationTest.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MultiInstanceReconciliationCoordinator.kt`.

## PluginRuntimeIntegrationService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
- **What does it do?** Implements service-level behavior in `PluginRuntimeIntegrationService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.flags.FeatureFlagPolicy, com.authord.mkdocs.runtime.CommandResult, com.authord.mkdocs.runtime.CommandRunner, com.authord.mkdocs.runtime.ManagedProcessHandle, com.authord.mkdocs.runtime.MkdocsProcessManager, com.authord.mkdocs.runtime.ProcessLauncher, com.authord.mkdocs.runtime.ProjectManagedUvExecutableProvider, com.authord.mkdocs.runtime.StaticUvExecutableProvider
  - External: com.intellij.ui.JBColor, com.intellij.openapi.application.ApplicationManager, com.intellij.openapi.application.ModalityState, com.intellij.openapi.Disposable, com.intellij.openapi.components.Service, com.intellij.openapi.project.Project, com.intellij.openapi.util.Disposer, com.intellij.openapi.util.Key
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `PreviewStartTrigger`, `PreviewRouteIntentSource`, `PreviewRouteIntent`, `RuntimeIntegrationDependencies`, `PluginRuntimeIntegrationService`, `ConfigFingerprintSnapshot`, plus `7` more.
  - Key callable members include `startupOutput`, `createDefault`, `overrideDependenciesForTesting`, `canStartPreview`, `startPreview`, `startPreviewAsync`, `startPreviewWithProgress`, `stopPreview`, plus `18` additional members.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `PreviewStartTrigger` (enum class, public (default)): Primary implementation type for this file feature area.
- `PreviewRouteIntentSource` (enum class, public (default)): Primary implementation type for this file feature area.
- `PreviewRouteIntent` (data class, public (default)): Primary implementation type for this file feature area.
- `RuntimeIntegrationDependencies` (data class, public (default)): Primary implementation type for this file feature area.
- `PluginRuntimeIntegrationService` (class, public (default)): Service component that centralizes business/workflow logic.
- `ConfigFingerprintSnapshot` (data class, private): Primary implementation type for this file feature area.
- `VerifiedPreviewRouteEntry` (data class, private): Primary implementation type for this file feature area.
- `VerifiedPreviewRouteCacheState` (data class, private): Typed model representing shared state or payload data.
- `RuntimeScope` (data class, private): Primary implementation type for this file feature area.
- `ProjectUserDataStartupOutputProvider` (class, public (default)): Primary implementation type for this file feature area.
- `InMemoryCommandRunner` (class, public (default)): Primary implementation type for this file feature area.
- `InMemoryManagedProcessHandle` (class, private): Primary implementation type for this file feature area.
- Additional declarations: `1` more declarations not listed individually.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `startupOutput` consumes project: Project, trigger: PreviewStartTrigger.
  - `createDefault` consumes no explicit parameters.
  - `overrideDependenciesForTesting` consumes dependencies: RuntimeIntegrationDependencies.
  - `canStartPreview` consumes no explicit parameters.
  - `startPreview` consumes trigger: PreviewStartTrigger = PreviewStartTrigger.ACTION.
- **Outputs produced:**
  - `startupOutput` returns String } /** * Shared runtime integration dependencies used by IntelliJ shell entry points. */ data class RuntimeIntegrationDependencies( val activationService: PluginActivationService, val processManager: MkdocsProcessManager, val previewPaneCoordinator: PreviewPaneCoordinator, val navigationCoordinator: NavigationCoordinator, val featureFlagPolicyService: FeatureFlagPolicyService, val startupOutputProvider: StartupOutputProvider, ).
  - `createDefault` returns RuntimeIntegrationDependencies.
  - `overrideDependenciesForTesting` returns Unit/implicit.
  - `canStartPreview` returns Boolean.
  - `startPreview` returns ActivationResult.
- **Side effects:**
  - Filesystem reads/writes
  - Process/runtime execution
  - Network calls
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolver.kt`
- **What this file calls:**
  - `com.authord.mkdocs.core.flags.FeatureFlagPolicy`
  - `com.authord.mkdocs.runtime.CommandResult`
  - `com.authord.mkdocs.runtime.CommandRunner`
  - `com.authord.mkdocs.runtime.ManagedProcessHandle`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`. Downstream collaborators include `com.authord.mkdocs.core.flags.FeatureFlagPolicy`, `com.authord.mkdocs.runtime.CommandResult`, `com.authord.mkdocs.runtime.CommandRunner`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewStartTrigger` (enum class, public (default))
  - `PreviewRouteIntentSource` (enum class, public (default))
  - `PreviewRouteIntent` (data class, public (default))
  - `RuntimeIntegrationDependencies` (data class, public (default))
  - `PluginRuntimeIntegrationService` (class, public (default))
  - `ProjectUserDataStartupOutputProvider` (class, public (default))
- **Key public/internal functions:**
  - `startupOutput(project: Project, trigger: PreviewStartTrigger) : String } /** * Shared runtime integration dependencies used by IntelliJ shell entry points. */ data class RuntimeIntegrationDependencies( val activationService: PluginActivationService, val processManager: MkdocsProcessManager, val previewPaneCoordinator: PreviewPaneCoordinator, val navigationCoordinator: NavigationCoordinator, val featureFlagPolicyService: FeatureFlagPolicyService, val startupOutputProvider: StartupOutputProvider, )` (public (default))
  - `createDefault() : RuntimeIntegrationDependencies` (public (default))
  - `overrideDependenciesForTesting(dependencies: RuntimeIntegrationDependencies) : Unit/implicit` (internal)
  - `canStartPreview() : Boolean` (public (default))
  - `startPreview(trigger: PreviewStartTrigger = PreviewStartTrigger.ACTION) : ActivationResult` (public (default))
  - `startPreviewAsync(trigger: PreviewStartTrigger = PreviewStartTrigger.ACTION, onComplete: (ActivationResult) : Unit/implicit` (public (default))
  - `startPreviewWithProgress(trigger: PreviewStartTrigger = PreviewStartTrigger.ACTION, onComplete: (ActivationResult) : Unit/implicit` (public (default))
  - `stopPreview() : Boolean` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Tracks lifecycle/disposal boundaries; resources are expected to be released deterministically.
- Contains coroutine-aware execution paths and cancellation-sensitive operations.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Service`, `@Volatile`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `PreviewRouteIntent` models structured data with fields: val selectedPath: String, val route: String, val targetUrl: String, val docsDirPath: String?, val configPath: String?.
- `RuntimeIntegrationDependencies` models structured data with fields: val activationService: PluginActivationService, val processManager: MkdocsProcessManager, val previewPaneCoordinator: PreviewPaneCoordinator, val navigationCoordinator: NavigationCoordinator, val featureFlagPolicyService: FeatureFlagPolicyService.
- `ConfigFingerprintSnapshot` models structured data with fields: val hasKnownFingerprint: Boolean, val hasChanged: Boolean, val currentFingerprint: String?.
- `PreviewStartTrigger` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- Uses hashing/encryption-adjacent APIs; verify algorithm choice and key-handling assumptions.
- Performs network operations; validate endpoint trust and request input sanitization.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Large callable surface area increases regression risk; consider characterization tests before large refactors.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
```kotlin
val component = PreviewStartTrigger(/* collaborators */)
val result = component.startupOutput(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`.

## PreviewErrorExcerptFormatter.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewErrorExcerptFormatter.kt`
- **What does it do?** Defines shared model/state types in `PreviewErrorExcerptFormatter`.
- **Why it matters:** Shared models must remain clear and stable because many workflows serialize or branch on them.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.util.Locale
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Key callable members include `extractExactErrorExcerpt`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- No top-level class/interface/object declarations detected; file is function/typealias/constant oriented.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `extractExactErrorExcerpt` consumes rawMessage: String, maxLines: Int = EXACT_ERROR_MAX_LINES, maxChars: Int = EXACT_ERROR_MAX_CHARS.
- **Outputs produced:**
  - `extractExactErrorExcerpt` returns String.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - No clear exposed type; file may hold private/internal helpers only.
- **Key public/internal functions:**
  - `extractExactErrorExcerpt(rawMessage: String, maxLines: Int = EXACT_ERROR_MAX_LINES, maxChars: Int = EXACT_ERROR_MAX_CHARS) : String` (internal)

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewErrorExcerptFormatterTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = PreviewErrorExcerptFormatter(/* collaborators */)
val result = component.extractExactErrorExcerpt(/* inputs */)
```
Caller relationship is indirect or framework-driven; no strong static caller signal detected.

## PreviewFailureNotifier.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewFailureNotifier.kt`
- **What does it do?** Contains feature logic for `PreviewFailureNotifier` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.notification.NotificationAction, com.intellij.notification.NotificationGroupManager, com.intellij.notification.NotificationType, com.intellij.openapi.application.PathManager, com.intellij.openapi.fileEditor.FileEditorManager, com.intellij.openapi.project.Project, com.intellij.openapi.vfs.LocalFileSystem, java.nio.file.Path
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `PreviewFailureNotifier`, `PreviewFailureNotificationPayload`.
  - Key callable members include `notifyStartupFailure`, `clearDedupeStateForTests`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `PreviewFailureNotifier` (class, internal): Primary implementation type for this file feature area.
- `PreviewFailureNotificationPayload` (data class, internal): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `notifyStartupFailure` consumes project: Project, failure: PreviewStartupFailure, configPath: String?, exactErrorExcerpt: String? = null, onRetry: ((.
  - `clearDedupeStateForTests` consumes no explicit parameters.
- **Outputs produced:**
  - `notifyStartupFailure` returns Unit/implicit.
  - `clearDedupeStateForTests` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - IntelliJ UI state updates
  - In-memory mutable state
  - Asynchronous stream emission/collection


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandler.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewFailureNotifierTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandlerTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandler.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewFailureNotifierTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewFailureNotifier` (class, internal)
  - `PreviewFailureNotificationPayload` (data class, internal)
- **Key public/internal functions:**
  - `notifyStartupFailure(project: Project, failure: PreviewStartupFailure, configPath: String?, exactErrorExcerpt: String? = null, onRetry: (() : Unit/implicit` (public (default))
  - `clearDedupeStateForTests() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `PreviewFailureNotificationPayload` models structured data with fields: val failure: PreviewStartupFailure, val configPath: String?, val exactErrorExcerpt: String?, val onRetry: ((.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewFailureNotifierTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = PreviewFailureNotifier(/* collaborators */)
val result = component.notifyStartupFailure(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandler.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewFailureNotifier.kt`.

## PreviewResultReporter.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewResultReporter.kt`
- **What does it do?** Declares architectural contract types in `PreviewResultReporter` for cross-module interaction.
- **Why it matters:** Stable port contracts preserve modularity and allow implementations to vary independently.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ui.ActivationResult
  - External: com.intellij.notification.NotificationGroupManager, com.intellij.notification.NotificationType, com.intellij.openapi.project.Project
- **Responsibilities:**
  - Define stable contracts/models consumed across module boundaries.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Key callable members include `presentPreviewResult`, `presentAuthordNotification`, `formatPreviewResultMessage`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- No top-level class/interface/object declarations detected; file is function/typealias/constant oriented.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `presentPreviewResult` consumes project: Project, message: String, success: Boolean.
  - `presentAuthordNotification` consumes project: Project?, message: String, type: NotificationType.
  - `formatPreviewResultMessage` consumes result: ActivationResult.
- **Outputs produced:**
  - `presentPreviewResult` returns Unit/implicit.
  - `presentAuthordNotification` returns Unit/implicit.
  - `formatPreviewResultMessage` returns String.
- **Side effects:**
  - IntelliJ UI state updates


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ui.ActivationResult`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ui.ActivationResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - No clear exposed type; file may hold private/internal helpers only.
- **Key public/internal functions:**
  - `presentPreviewResult(project: Project, message: String, success: Boolean) : Unit/implicit` (internal)
  - `presentAuthordNotification(project: Project?, message: String, type: NotificationType) : Unit/implicit` (internal)
  - `formatPreviewResultMessage(result: ActivationResult) : String` (internal)

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewResultReporterTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = PreviewResultReporter(/* collaborators */)
val result = component.presentPreviewResult(/* inputs */)
```
Caller relationship is indirect or framework-driven; no strong static caller signal detected.

## PreviewRouteLoadCoordinator.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouteLoadCoordinator.kt`
- **What does it do?** Contains feature logic for `PreviewRouteLoadCoordinator` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.notification.NotificationType, com.intellij.openapi.application.ApplicationManager, com.intellij.openapi.application.ModalityState, com.intellij.openapi.project.Project, java.net.HttpURLConnection, java.net.URI
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `PreviewRouteFlowState`, `RouteReadinessOutcome`, `Cancelled`, `Unavailable`, `Ready`.
  - Key callable members include `loadPreviewRouteWithReadinessGuard`, `isPreviewRouteReady`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `PreviewRouteFlowState` (enum class, public (default)): Typed model representing shared state or payload data.
- `RouteReadinessOutcome` (sealed interface, private): Contract/abstraction used to decouple callers from implementations.
- `Cancelled` (object, public (default)): Singleton utility/policy holder shared across callers.
- `Unavailable` (object, public (default)): Singleton utility/policy holder shared across callers.
- `Ready` (data class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `loadPreviewRouteWithReadinessGuard` consumes project: Project, targetUrl: String, isRequestCurrent: (.
  - `isPreviewRouteReady` consumes routeUrl: String, connectTimeoutMillis: Int = 300, readTimeoutMillis: Int = 300.
- **Outputs produced:**
  - `loadPreviewRouteWithReadinessGuard` returns Unit/implicit.
  - `isPreviewRouteReady` returns Boolean.
- **Side effects:**
  - Filesystem reads/writes
  - Network calls


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouteLoadCoordinatorTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouteLoadCoordinatorTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewRouteFlowState` (enum class, public (default))
  - `Cancelled` (object, public (default))
  - `Unavailable` (object, public (default))
  - `Ready` (data class, public (default))
- **Key public/internal functions:**
  - `loadPreviewRouteWithReadinessGuard(project: Project, targetUrl: String, isRequestCurrent: () : Unit/implicit` (internal)
  - `isPreviewRouteReady(routeUrl: String, connectTimeoutMillis: Int = 300, readTimeoutMillis: Int = 300) : Boolean` (internal)

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `Ready` models structured data with fields: val url: String, val forceReload: Boolean.
- `PreviewRouteFlowState` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- Performs network operations; validate endpoint trust and request input sanitization.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouteLoadCoordinatorTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
```kotlin
val component = PreviewRouteFlowState(/* collaborators */)
val result = component.loadPreviewRouteWithReadinessGuard(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouteLoadCoordinator.kt`.

## PreviewRouterService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouterService.kt`
- **What does it do?** Implements service-level behavior in `PreviewRouterService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.project.Project
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `PreviewRouteTarget`, `PreviewRouteReason`, `PreviewDecision`, `PreviewRouterService`.
  - Key callable members include `decide`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `PreviewRouteTarget` (enum class, internal): Primary implementation type for this file feature area.
- `PreviewRouteReason` (enum class, internal): Primary implementation type for this file feature area.
- `PreviewDecision` (data class, internal): Primary implementation type for this file feature area.
- `PreviewRouterService` (class, internal): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `decide` consumes project: Project, filePath: String.
- **Outputs produced:**
  - `decide` returns PreviewDecision.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandler.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandler.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewRouteTarget` (enum class, internal)
  - `PreviewRouteReason` (enum class, internal)
  - `PreviewDecision` (data class, internal)
  - `PreviewRouterService` (class, internal)
- **Key public/internal functions:**
  - `decide(project: Project, filePath: String) : PreviewDecision` (public (default))

### 6. Business rules or important logic
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `PreviewDecision` models structured data with fields: val target: PreviewRouteTarget, val scopeStatus: DocsScopeStatus, val reason: PreviewRouteReason, val configPath: String? = null, val detail: String? = null.
- `PreviewRouteTarget` constrains state to a fixed enum set used across callers.
- `PreviewRouteReason` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouterServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = PreviewRouteTarget(/* collaborators */)
val result = component.decide(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouterService.kt`.

## PreviewStartupFailureClassifier.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureClassifier.kt`
- **What does it do?** Contains feature logic for `PreviewStartupFailureClassifier` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `PreviewStartupFailureCategory`, `PreviewStartupFailure`, `PreviewStartupFailureClassifier`.
  - Key callable members include `classify`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `PreviewStartupFailureCategory` (enum class, internal): Primary implementation type for this file feature area.
- `PreviewStartupFailure` (data class, internal): Primary implementation type for this file feature area.
- `PreviewStartupFailureClassifier` (object, internal): Singleton utility/policy holder shared across callers.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `classify` consumes rawMessage: String.
- **Outputs produced:**
  - `classify` returns PreviewStartupFailure.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewFailureNotifier.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewResultReporter.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandler.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewFailureNotifierTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewFailureNotifier.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewResultReporter.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandler.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewStartupFailureCategory` (enum class, internal)
  - `PreviewStartupFailure` (data class, internal)
  - `PreviewStartupFailureClassifier` (object, internal)
- **Key public/internal functions:**
  - `classify(rawMessage: String) : PreviewStartupFailure` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `PreviewStartupFailure` models structured data with fields: val category: PreviewStartupFailureCategory, val reason: String, val nextStep: String, val installPackage: String? = null, val primaryLine: String = "".
- `PreviewStartupFailureCategory` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureClassifierTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = PreviewStartupFailureCategory(/* collaborators */)
val result = component.classify(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewFailureNotifier.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureClassifier.kt`.

## PreviewStartupFailureHandler.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandler.kt`
- **What does it do?** Contains feature logic for `PreviewStartupFailureHandler` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ui.ActivationResult
  - External: com.intellij.openapi.application.ApplicationManager, com.intellij.openapi.application.ModalityState, com.intellij.openapi.fileEditor.FileEditorManager, com.intellij.openapi.project.Project, com.intellij.openapi.vfs.LocalFileSystem
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `PreviewStartupFailureHandler`.
  - Key callable members include `handleFailure`, `reopenFileForRoutingRefresh`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `PreviewStartupFailureHandler` (class, internal): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `handleFailure` consumes project: Project, result: ActivationResult, selectedPath: String?, onRetry: ((.
  - `reopenFileForRoutingRefresh` consumes project: Project, selectedPath: String.
- **Outputs produced:**
  - `handleFailure` returns Unit/implicit.
  - `reopenFileForRoutingRefresh` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandlerTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ui.ActivationResult`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProvider.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`. Downstream collaborators include `com.authord.mkdocs.ui.ActivationResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewStartupFailureHandler` (class, internal)
- **Key public/internal functions:**
  - `handleFailure(project: Project, result: ActivationResult, selectedPath: String?, onRetry: (() : Unit/implicit` (public (default))
  - `reopenFileForRoutingRefresh(project: Project, selectedPath: String) : Unit/implicit` (internal)

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandlerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = PreviewStartupFailureHandler(/* collaborators */)
val result = component.handleFailure(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListener.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandler.kt`.

## ReloadCoalescingGate.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/ReloadCoalescingGate.kt`
- **What does it do?** Contains feature logic for `ReloadCoalescingGate` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.util.concurrent.atomic.AtomicLong
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `ReloadCoalescingGate`.
  - Key callable members include `shouldSuppressReload`, `recordReload`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `ReloadCoalescingGate` (class, internal): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `shouldSuppressReload` consumes no explicit parameters.
  - `recordReload` consumes no explicit parameters.
- **Outputs produced:**
  - `shouldSuppressReload` returns Boolean.
  - `recordReload` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserService.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ReloadCoalescingGateTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserService.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserServiceTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ReloadCoalescingGateTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `ReloadCoalescingGate` (class, internal)
- **Key public/internal functions:**
  - `shouldSuppressReload() : Boolean` (public (default))
  - `recordReload() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ReloadCoalescingGateTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = ReloadCoalescingGate(/* collaborators */)
val result = component.shouldSuppressReload(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserService.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/ReloadCoalescingGate.kt`.

## SetupPageRenderer.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/SetupPageRenderer.kt`
- **What does it do?** Contains feature logic for `SetupPageRenderer` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `SetupPageRenderer`.
  - Key callable members include `render`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `SetupPageRenderer` (class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `render` consumes createProjectBridgeScript: String, suggestedProjectName: String = AuthordUiBundle.message("activation.default.siteName".
- **Outputs produced:**
  - `render` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/SetupPageRendererTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/SetupPageRendererTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `SetupPageRenderer` (class, public (default))
- **Key public/internal functions:**
  - `render(createProjectBridgeScript: String, suggestedProjectName: String = AuthordUiBundle.message("activation.default.siteName") : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@media`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/SetupPageRendererTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = SetupPageRenderer(/* collaborators */)
val result = component.render(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/SetupPageRenderer.kt`.

## SetupPanel.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/SetupPanel.kt`
- **What does it do?** Handles UI/IDE interaction concerns in `SetupPanel`.
- **Why it matters:** This file directly shapes user-triggered behavior and IDE integration quality.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.ide.BrowserUtil, com.intellij.openapi.application.ApplicationManager, com.intellij.openapi.ui.Messages, com.intellij.ui.JBColor, com.intellij.ui.components.JBLabel, com.intellij.ui.components.JBPanel, com.intellij.util.ui.JBFont, com.intellij.util.ui.JBUI
- **Responsibilities:**
  - Bind IDE/UI events to application services and maintain interaction consistency.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `SetupPanel`, `ChevronDownIcon`, `QuestionCircleIcon`.
  - Key callable members include `setLoading`, `requestProjectNameFromUser`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `SetupPanel` (class, internal): Primary implementation type for this file feature area.
- `ChevronDownIcon` (class, private): Primary implementation type for this file feature area.
- `QuestionCircleIcon` (class, private): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `setLoading` consumes loading: Boolean.
  - `requestProjectNameFromUser` consumes initial: String.
- **Outputs produced:**
  - `setLoading` returns Unit/implicit.
  - `requestProjectNameFromUser` returns String?.
- **Side effects:**
  - Filesystem reads/writes
  - IntelliJ UI state updates


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactoryTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactoryTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `SetupPanel` (class, internal)
- **Key public/internal functions:**
  - `setLoading(loading: Boolean) : Unit/implicit` (public (default))
  - `requestProjectNameFromUser(initial: String) : String?` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Tracks lifecycle/disposal boundaries; resources are expected to be released deterministically.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = SetupPanel(/* collaborators */)
val result = component.setLoading(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/SetupPanel.kt`.

## SiteContextResolver.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/SiteContextResolver.kt`
- **What does it do?** Contains feature logic for `SiteContextResolver` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files, java.nio.file.Path, java.util.Locale
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `SiteContext`, `SiteContextResolver`.
  - Key callable members include `resolveProjectDefault`, `resolveFromRuntimeCommand`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `SiteContext` (data class, internal): Primary implementation type for this file feature area.
- `SiteContextResolver` (class, internal): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `resolveProjectDefault` consumes projectRoot: Path.
  - `resolveFromRuntimeCommand` consumes projectRoot: Path?, command: List<String>.
- **Outputs produced:**
  - `resolveProjectDefault` returns SiteContext?.
  - `resolveFromRuntimeCommand` returns SiteContext?.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `SiteContext` (data class, internal)
  - `SiteContextResolver` (class, internal)
- **Key public/internal functions:**
  - `resolveProjectDefault(projectRoot: Path) : SiteContext?` (public (default))
  - `resolveFromRuntimeCommand(projectRoot: Path?, command: List<String>) : SiteContext?` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `SiteContext` models structured data with fields: val projectRoot: Path, val configPath: Path, val docsDirPath: Path, val useDirectoryUrls: Boolean.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = SiteContext(/* collaborators */)
val result = component.resolveProjectDefault(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginActivationService.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/SiteContextResolver.kt`.

## StartMkdocsAction.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsAction.kt`
- **What does it do?** Handles UI/IDE interaction concerns in `StartMkdocsAction`.
- **Why it matters:** This file directly shapes user-triggered behavior and IDE integration quality.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ui.PluginCompositionRoot
  - External: com.intellij.openapi.actionSystem.ActionUpdateThread, com.intellij.openapi.actionSystem.AnAction, com.intellij.openapi.actionSystem.AnActionEvent, com.intellij.openapi.application.ApplicationManager, com.intellij.openapi.project.Project, com.intellij.openapi.project.DumbAware
- **Responsibilities:**
  - Bind IDE/UI events to application services and maintain interaction consistency.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `StartMkdocsAction`, `ActionPresentationState`.
  - Key callable members include `resolvePresentationState`, `invokeForProject`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `StartMkdocsAction` (class, public (default)): IDE interaction entry point for user/editor events.
- `ActionPresentationState` (data class, internal): IDE interaction entry point for user/editor events.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `resolvePresentationState` consumes project: Project?.
  - `invokeForProject` consumes project: Project?, onSuccessfulStart: ((.
- **Outputs produced:**
  - `resolvePresentationState` returns ActionPresentationState.
  - `invokeForProject` returns Unit/implicit.
- **Side effects:**
  - IntelliJ UI state updates


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsMarkdownToolbarAction.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginDescriptorRegistrationTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsActionInvocationTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsActionPresentationTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ui.PluginCompositionRoot`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsMarkdownToolbarAction.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginDescriptorRegistrationTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsActionInvocationTest.kt`. Downstream collaborators include `com.authord.mkdocs.ui.PluginCompositionRoot`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `StartMkdocsAction` (class, public (default))
  - `ActionPresentationState` (data class, internal)
- **Key public/internal functions:**
  - `resolvePresentationState(project: Project?) : ActionPresentationState` (internal)
  - `invokeForProject(project: Project?, onSuccessfulStart: (() : Unit/implicit` (internal)

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `ActionPresentationState` models structured data with fields: val visible: Boolean, val enabled: Boolean.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsActionInvocationTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsActionPresentationTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = StartMkdocsAction(/* collaborators */)
val result = component.resolvePresentationState(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsMarkdownToolbarAction.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsAction.kt`.

## StartMkdocsMarkdownToolbarAction.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsMarkdownToolbarAction.kt`
- **What does it do?** Handles UI/IDE interaction concerns in `StartMkdocsMarkdownToolbarAction`.
- **Why it matters:** This file directly shapes user-triggered behavior and IDE integration quality.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.actionSystem.ActionUpdateThread, com.intellij.openapi.actionSystem.AnAction, com.intellij.openapi.actionSystem.AnActionEvent, com.intellij.openapi.actionSystem.CommonDataKeys, com.intellij.openapi.project.DumbAware
- **Responsibilities:**
  - Bind IDE/UI events to application services and maintain interaction consistency.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `StartMkdocsMarkdownToolbarAction`.
  - Key callable members include `isMarkdownEditorContext`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `StartMkdocsMarkdownToolbarAction` (class, public (default)): IDE interaction entry point for user/editor events.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `isMarkdownEditorContext` consumes event: AnActionEvent.
- **Outputs produced:**
  - `isMarkdownEditorContext` returns Boolean.
- **Side effects:**
  - IntelliJ UI state updates


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginDescriptorRegistrationTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsMarkdownToolbarActionTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginDescriptorRegistrationTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsMarkdownToolbarActionTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `StartMkdocsMarkdownToolbarAction` (class, public (default))
- **Key public/internal functions:**
  - `isMarkdownEditorContext(event: AnActionEvent) : Boolean` (internal)

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsMarkdownToolbarActionTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = StartMkdocsMarkdownToolbarAction(/* collaborators */)
val result = component.isMarkdownEditorContext(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginDescriptorRegistrationTest.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsMarkdownToolbarAction.kt`.

## StartupReconciliationCoordinator.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationCoordinator.kt`
- **What does it do?** Contains feature logic for `StartupReconciliationCoordinator` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.MkDocsConfigDocument
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `StartupReconciliationCoordinator`.
  - Key callable members include `reconcile`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `StartupReconciliationCoordinator` (class, public (default)): Coordinator that sequences multi-step flows and state transitions.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `reconcile` consumes config: MkDocsConfigDocument, docsMarkdownPaths: List<String>, projectId: String = "unknown-project", instanceId: String = "default".
- **Outputs produced:**
  - `reconcile` returns StartupTreeState.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MultiInstanceReconciliationCoordinator.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationPerformanceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationPolicyTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MultiInstanceReconciliationCoordinator.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationPerformanceTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `StartupReconciliationCoordinator` (class, public (default))
- **Key public/internal functions:**
  - `reconcile(config: MkDocsConfigDocument, docsMarkdownPaths: List<String>, projectId: String = "unknown-project", instanceId: String = "default") : StartupTreeState` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = StartupReconciliationCoordinator(/* collaborators */)
val result = component.reconcile(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationCoordinator.kt`.

## SystemRuntimeAdapters.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/SystemRuntimeAdapters.kt`
- **What does it do?** Implements adapter/gateway behavior in `SystemRuntimeAdapters` to connect domain flows with runtime or IO APIs.
- **Why it matters:** Adapter files are critical integration points where external behavior is normalized for the rest of the system.
- **Dependencies:**
  - Internal: com.authord.mkdocs.runtime.CommandResult, com.authord.mkdocs.runtime.CommandRunner, com.authord.mkdocs.runtime.ManagedProcessHandle, com.authord.mkdocs.runtime.ProcessLauncher
  - External: java.io.File, java.io.OutputStream, java.util.ArrayDeque, java.util.concurrent.TimeUnit, java.util.concurrent.atomic.AtomicInteger, java.util.concurrent.atomic.AtomicBoolean
- **Responsibilities:**
  - Translate internal commands/models to runtime, filesystem, or integration APIs.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `ShutdownHookRegistrar`, `RuntimeShutdownHookRegistrar`, `ProcessBuilderSystemProcessFactory`, `ParentPipeGuard`, `ShellParentPipeGuardLauncher`, `ProcessBuilderCommandRunner`, plus `3` more.
  - Key callable members include `register`, `start`, `stop`, `launch`, `appendLine`, `snapshot`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `ShutdownHookRegistrar` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.
- `RuntimeShutdownHookRegistrar` (object, public (default)): Singleton utility/policy holder shared across callers.
- `ProcessBuilderSystemProcessFactory` (class, public (default)): Primary implementation type for this file feature area.
- `ParentPipeGuard` (data class, private): Rule/policy implementation for decisions and validation.
- `ShellParentPipeGuardLauncher` (object, private): Singleton utility/policy holder shared across callers.
- `ProcessBuilderCommandRunner` (class, public (default)): Primary implementation type for this file feature area.
- `ProcessBackedManagedProcessHandle` (class, private): Primary implementation type for this file feature area.
- `ProcessBuilderProcessLauncher` (class, public (default)): Primary implementation type for this file feature area.
- `CappedOutputBuffer` (class, private): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `register` consumes hook: Thread.
  - `start` consumes command: List<String>, workingDir: String, mergeErrorStream: Boolean.
  - `stop` consumes no explicit parameters.
  - `launch` consumes targetPid: Long, workingDir: String.
  - `appendLine` consumes line: String.
- **Outputs produced:**
  - `register` returns Boolean /** * Unregisters one shutdown hook thread. */ fun unregister(hook: Thread): Boolean } /** * Default shutdown-hook registrar backed by [Runtime]. */ object RuntimeShutdownHookRegistrar : ShutdownHookRegistrar.
  - `start` returns Process } /** * Default JVM process factory using [ProcessBuilder]. */ class ProcessBuilderSystemProcessFactory : SystemProcessFactory.
  - `stop` returns Unit/implicit.
  - `launch` returns ParentPipeGuard? } private object ShellParentPipeGuardLauncher : ParentPipeGuardLauncher.
  - `appendLine` returns Unit/implicit.
- **Side effects:**
  - Process/runtime execution
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsProjectCreator.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/SystemRuntimeAdaptersTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.runtime.CommandResult`
  - `com.authord.mkdocs.runtime.CommandRunner`
  - `com.authord.mkdocs.runtime.ManagedProcessHandle`
  - `com.authord.mkdocs.runtime.ProcessLauncher`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsProjectCreator.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationService.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/SystemRuntimeAdaptersTest.kt`. Downstream collaborators include `com.authord.mkdocs.runtime.CommandResult`, `com.authord.mkdocs.runtime.CommandRunner`, `com.authord.mkdocs.runtime.ManagedProcessHandle`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `ShutdownHookRegistrar` (interface, public (default))
  - `RuntimeShutdownHookRegistrar` (object, public (default))
  - `ProcessBuilderSystemProcessFactory` (class, public (default))
  - `ProcessBuilderCommandRunner` (class, public (default))
  - `ProcessBuilderProcessLauncher` (class, public (default))
- **Key public/internal functions:**
  - `register(hook: Thread) : Boolean /** * Unregisters one shutdown hook thread. */ fun unregister(hook: Thread): Boolean } /** * Default shutdown-hook registrar backed by [Runtime]. */ object RuntimeShutdownHookRegistrar : ShutdownHookRegistrar` (public (default))
  - `start(command: List<String>, workingDir: String, mergeErrorStream: Boolean) : Process } /** * Default JVM process factory using [ProcessBuilder]. */ class ProcessBuilderSystemProcessFactory : SystemProcessFactory` (public (default))
  - `stop() : Unit/implicit` (public (default))
  - `launch(targetPid: Long, workingDir: String) : ParentPipeGuard? } private object ShellParentPipeGuardLauncher : ParentPipeGuardLauncher` (public (default))
  - `appendLine(line: String) : Unit/implicit` (public (default))
  - `snapshot() : String` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Contains coroutine-aware execution paths and cancellation-sensitive operations.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `ParentPipeGuard` models structured data with fields: private val process: Process, private val sentinelOutputStream: OutputStream.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/SystemRuntimeAdaptersTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
```kotlin
val result = implementation.register(/* inputs */) // implementation : ShutdownHookRegistrar
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkDocsProjectCreator.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/SystemRuntimeAdapters.kt`.

## ToolWindowVisibility.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/ToolWindowVisibility.kt`
- **What does it do?** Handles UI/IDE interaction concerns in `ToolWindowVisibility`.
- **Why it matters:** This file directly shapes user-triggered behavior and IDE integration quality.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.project.Project, com.intellij.openapi.wm.ToolWindowManager
- **Responsibilities:**
  - Bind IDE/UI events to application services and maintain interaction consistency.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Key callable members include `showAuthordToolWindow`, `toggleAuthordToolWindow`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- No top-level class/interface/object declarations detected; file is function/typealias/constant oriented.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `showAuthordToolWindow` consumes project: Project.
  - `toggleAuthordToolWindow` consumes project: Project.
- **Outputs produced:**
  - `showAuthordToolWindow` returns Unit/implicit.
  - `toggleAuthordToolWindow` returns Unit/implicit.
- **Side effects:**
  - IntelliJ UI state updates


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - No clear exposed type; file may hold private/internal helpers only.
- **Key public/internal functions:**
  - `showAuthordToolWindow(project: Project) : Unit/implicit` (internal)
  - `toggleAuthordToolWindow(project: Project) : Unit/implicit` (internal)

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = ToolWindowVisibility(/* collaborators */)
val result = component.showAuthordToolWindow(/* inputs */)
```
Caller relationship is indirect or framework-driven; no strong static caller signal detected.

## TopicTreeActionController.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeActionController.kt`
- **What does it do?** Handles UI/IDE interaction concerns in `TopicTreeActionController`.
- **Why it matters:** This file directly shapes user-triggered behavior and IDE integration quality.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.topic.PathPolicy, com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand, com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand, com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand, com.authord.mkdocs.ports.topic.AddTopicNodeCommand, com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand, com.authord.mkdocs.ports.topic.RenameTopicNodeCommand, com.authord.mkdocs.ports.topic.TopicGatewayResult
  - External: java.util.UUID
- **Responsibilities:**
  - Bind IDE/UI events to application services and maintain interaction consistency.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `TopicTreeActionController`.
  - Key callable members include `createTopic`, `addChildTopic`, `addExistingFile`, `addExternalLink`, `renameTopic`, `removeTopic`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeActionController` (class, public (default)): IDE interaction entry point for user/editor events.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `createTopic` consumes treeId: String, parentNodeId: String, title: String, orderIndex: Int, sourcePath: String? = null, nodeId: String = nodeIdFactory(.
  - `addChildTopic` consumes treeId: String, targetNodeId: String, title: String, orderIndex: Int, sourcePath: String? = null, childNodeId: String = nodeIdFactory(.
  - `addExistingFile` consumes treeId: String, parentNodeId: String, title: String, relativePath: String, orderIndex: Int, nodeId: String = nodeIdFactory(.
  - `addExternalLink` consumes treeId: String, parentNodeId: String, title: String, externalUrl: String, orderIndex: Int, nodeId: String = nodeIdFactory(.
  - `renameTopic` consumes treeId: String, nodeId: String, newTitle: String.
- **Outputs produced:**
  - `createTopic` returns Unit/implicit.
  - `addChildTopic` returns Unit/implicit.
  - `addExistingFile` returns Unit/implicit.
  - `addExternalLink` returns Unit/implicit.
  - `renameTopic` returns TopicMutationDispatchResult.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeActionControllerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanelUiContractTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.core.topic.PathPolicy`
  - `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeActionControllerTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanelUiContractTest.kt`. Downstream collaborators include `com.authord.mkdocs.core.topic.PathPolicy`, `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`, `com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeActionController` (class, public (default))
- **Key public/internal functions:**
  - `createTopic(treeId: String, parentNodeId: String, title: String, orderIndex: Int, sourcePath: String? = null, nodeId: String = nodeIdFactory() : Unit/implicit` (public (default))
  - `addChildTopic(treeId: String, targetNodeId: String, title: String, orderIndex: Int, sourcePath: String? = null, childNodeId: String = nodeIdFactory() : Unit/implicit` (public (default))
  - `addExistingFile(treeId: String, parentNodeId: String, title: String, relativePath: String, orderIndex: Int, nodeId: String = nodeIdFactory() : Unit/implicit` (public (default))
  - `addExternalLink(treeId: String, parentNodeId: String, title: String, externalUrl: String, orderIndex: Int, nodeId: String = nodeIdFactory() : Unit/implicit` (public (default))
  - `renameTopic(treeId: String, nodeId: String, newTitle: String) : TopicMutationDispatchResult` (public (default))
  - `removeTopic(treeId: String, nodeId: String) : TopicMutationDispatchResult` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeActionControllerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = TopicTreeActionController(/* collaborators */)
val result = component.createTopic(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeActionController.kt`.

## TopicTreeApplicationService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeApplicationService.kt`
- **What does it do?** Implements service-level behavior in `TopicTreeApplicationService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.TopicSyncOutcome, com.authord.mkdocs.ports.topic.TopicSyncTransaction
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `TopicTreeApplicationService`.
  - Key callable members include `execute`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeApplicationService` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `execute` consumes transaction: TopicSyncTransaction.
- **Outputs produced:**
  - `execute` returns TopicGatewayResult<TopicSyncOutcome> /** * Requests rollback/compensation path for an in-flight transaction. * * Inputs: [transactionId] and rollback [reason]. * Output: [TopicGatewayResult.Success] with rollback outcome metadata. * Errors/failure modes: typed orchestration failures when rollback cannot be applied. */ fun rollback(transactionId: String, reason: String): TopicGatewayResult<TopicSyncOutcome> /** * Returns current active instance context. * * Input: none. * Output: [TopicGatewayResult.Success] with active instance or null. * Errors/failure modes: instance-scope resolution failures from registry ports. */ fun activeInstance(): TopicGatewayResult<TopicInstanceRef?> /** * Hydrates in-memory aggregate state from persisted config when startup reconciliation runs. */ fun hydrateTreeFromConfig( treeId: String, instanceId: String, config: MkDocsConfigDocument, ): TopicGatewayResult<Unit>.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`
  - `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/TopicTreePort.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeApplicationServiceImpl.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
  - `com.authord.mkdocs.ports.topic.TopicSyncOutcome`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`, `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/TopicTreePort.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicGatewayResult`, `com.authord.mkdocs.ports.topic.TopicInstanceRef`, `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeApplicationService` (interface, public (default))
- **Key public/internal functions:**
  - `execute(transaction: TopicSyncTransaction) : TopicGatewayResult<TopicSyncOutcome> /** * Requests rollback/compensation path for an in-flight transaction. * * Inputs: [transactionId] and rollback [reason]. * Output: [TopicGatewayResult.Success] with rollback outcome metadata. * Errors/failure modes: typed orchestration failures when rollback cannot be applied. */ fun rollback(transactionId: String, reason: String): TopicGatewayResult<TopicSyncOutcome> /** * Returns current active instance context. * * Input: none. * Output: [TopicGatewayResult.Success] with active instance or null. * Errors/failure modes: instance-scope resolution failures from registry ports. */ fun activeInstance(): TopicGatewayResult<TopicInstanceRef?> /** * Hydrates in-memory aggregate state from persisted config when startup reconciliation runs. */ fun hydrateTreeFromConfig( treeId: String, instanceId: String, config: MkDocsConfigDocument, ): TopicGatewayResult<Unit>` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeApplicationServiceContractTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val result = implementation.execute(/* inputs */) // implementation : TopicTreeApplicationService
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeApplicationService.kt`.

## TopicTreeApplicationServiceImpl.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeApplicationServiceImpl.kt`
- **What does it do?** Implements service-level behavior in `TopicTreeApplicationServiceImpl` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.InstanceRegistryPort, com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncOutcome, com.authord.mkdocs.ports.topic.TopicSyncTransaction, com.authord.mkdocs.ports.topic.TreeSyncOrchestrator
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `TopicTreeApplicationServiceImpl`.
  - Primarily declarative file with limited executable methods.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeApplicationServiceImpl` (class, public (default)): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - No significant public callable input surface detected (type declarations or private implementation details dominate).
- **Outputs produced:**
  - Outputs are primarily implicit side effects or type exposure rather than return values.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MutationFlowIndependenceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeAggregateHydrationStartupTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.InstanceRegistryPort`
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MutationFlowIndependenceTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeAggregateHydrationStartupTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.InstanceRegistryPort`, `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`, `com.authord.mkdocs.ports.topic.TopicGatewayResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeApplicationServiceImpl` (class, public (default))
- **Key public/internal functions:**
  - No public/internal function entry points detected.

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
// Construct `TopicTreeApplicationServiceImpl` and use exposed members
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeApplicationServiceImpl.kt`.

## TopicTreeDragDropController.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeDragDropController.kt`
- **What does it do?** Contains feature logic for `TopicTreeDragDropController` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.MoveTopicNodeCommand, com.authord.mkdocs.ports.topic.ReorderTopicNodesCommand, com.authord.mkdocs.ports.topic.ReparentTopicNodeCommand, com.authord.mkdocs.ports.topic.TopicGatewayResult
  - External: java.util.UUID
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `TopicTreeDragDropController`.
  - Key callable members include `moveTopic`, `reparentTopic`, `reorderTopics`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeDragDropController` (class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `moveTopic` consumes treeId: String, nodeId: String, newParentNodeId: String, newOrderIndex: Int.
  - `reparentTopic` consumes treeId: String, nodeId: String, newParentNodeId: String.
  - `reorderTopics` consumes treeId: String, parentNodeId: String, orderedNodeIds: List<String>.
- **Outputs produced:**
  - `moveTopic` returns TopicMutationDispatchResult.
  - `reparentTopic` returns TopicMutationDispatchResult.
  - `reorderTopics` returns TopicMutationDispatchResult.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeDragDropControllerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanelUiContractTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.MoveTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.ReorderTopicNodesCommand`
  - `com.authord.mkdocs.ports.topic.ReparentTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeDragDropControllerTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanelUiContractTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.MoveTopicNodeCommand`, `com.authord.mkdocs.ports.topic.ReorderTopicNodesCommand`, `com.authord.mkdocs.ports.topic.ReparentTopicNodeCommand`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeDragDropController` (class, public (default))
- **Key public/internal functions:**
  - `moveTopic(treeId: String, nodeId: String, newParentNodeId: String, newOrderIndex: Int) : TopicMutationDispatchResult` (public (default))
  - `reparentTopic(treeId: String, nodeId: String, newParentNodeId: String) : TopicMutationDispatchResult` (public (default))
  - `reorderTopics(treeId: String, parentNodeId: String, orderedNodeIds: List<String>) : TopicMutationDispatchResult` (public (default))

### 6. Business rules or important logic
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeDragDropControllerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = TopicTreeDragDropController(/* collaborators */)
val result = component.moveTopic(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeDragDropController.kt`.

## TopicTreeFailureRecoveryPresenter.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeFailureRecoveryPresenter.kt`
- **What does it do?** Contains feature logic for `TopicTreeFailureRecoveryPresenter` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicSyncError, com.authord.mkdocs.ports.topic.TopicSyncErrorCode, com.authord.mkdocs.ports.topic.TopicSyncOutcome
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `TopicTreeRecoveryMessage`, `TopicMutationDispatchResult`, `TopicTreeFailureRecoveryPresenter`.
  - Key callable members include `present`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeRecoveryMessage` (data class, public (default)): Primary implementation type for this file feature area.
- `TopicMutationDispatchResult` (data class, public (default)): Primary implementation type for this file feature area.
- `TopicTreeFailureRecoveryPresenter` (class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `present` consumes operationLabel: String, error: TopicSyncError.
- **Outputs produced:**
  - `present` returns TopicTreeRecoveryMessage.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeActionController.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeDragDropController.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanel.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicSyncError`
  - `com.authord.mkdocs.ports.topic.TopicSyncErrorCode`
  - `com.authord.mkdocs.ports.topic.TopicSyncOutcome`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeActionController.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeDragDropController.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicGatewayResult`, `com.authord.mkdocs.ports.topic.TopicSyncError`, `com.authord.mkdocs.ports.topic.TopicSyncErrorCode`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeRecoveryMessage` (data class, public (default))
  - `TopicMutationDispatchResult` (data class, public (default))
  - `TopicTreeFailureRecoveryPresenter` (class, public (default))
- **Key public/internal functions:**
  - `present(operationLabel: String, error: TopicSyncError) : TopicTreeRecoveryMessage` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `TopicTreeRecoveryMessage` models structured data with fields: val summary: String, val guidance: String, val nonDestructive: Boolean = true.
- `TopicMutationDispatchResult` models structured data with fields: val result: TopicGatewayResult<TopicSyncOutcome>, val recovery: TopicTreeRecoveryMessage? = null.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeFailureRecoveryPresenterTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = TopicTreeRecoveryMessage(/* collaborators */)
val result = component.present(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeFailureRecoveryPresenter.kt`.

## TopicTreeFallbackBuilder.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeFallbackBuilder.kt`
- **What does it do?** Contains feature logic for `TopicTreeFallbackBuilder` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.topic.PathPolicy, com.authord.mkdocs.ports.topic.TopicNavNode
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `TopicTreeFallbackBuilder`, `MutableItem`, `MutablePage`, `MutableSection`.
  - Key callable members include `build`, `toNavNode`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeFallbackBuilder` (class, public (default)): Primary implementation type for this file feature area.
- `MutableItem` (sealed interface, private): Contract/abstraction used to decouple callers from implementations.
- `MutablePage` (data class, private): Primary implementation type for this file feature area.
- `MutableSection` (data class, private): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `build` consumes docsDir: String, docsMarkdownPaths: List<String>.
  - `toNavNode` consumes pathPolicy: PathPolicy.
- **Outputs produced:**
  - `build` returns List<TopicNavNode>.
  - `toNavNode` returns TopicNavNode } private data class MutablePage( override val path: String, private val title: String, ) : MutableItem.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeStartupLoader.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeSyncOrchestratorService.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeFallbackBuilderTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.core.topic.PathPolicy`
  - `com.authord.mkdocs.ports.topic.TopicNavNode`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeStartupLoader.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeSyncOrchestratorService.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeFallbackBuilderTest.kt`. Downstream collaborators include `com.authord.mkdocs.core.topic.PathPolicy`, `com.authord.mkdocs.ports.topic.TopicNavNode`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeFallbackBuilder` (class, public (default))
- **Key public/internal functions:**
  - `build(docsDir: String, docsMarkdownPaths: List<String>) : List<TopicNavNode>` (public (default))
  - `toNavNode(pathPolicy: PathPolicy) : TopicNavNode } private data class MutablePage( override val path: String, private val title: String, ) : MutableItem` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `MutablePage` models structured data with fields: override val path: String, private val title: String.
- `MutableSection` models structured data with fields: override val path: String, private val title: String, val children: MutableList<MutableItem> = mutableListOf(.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeFallbackBuilderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = TopicTreeFallbackBuilder(/* collaborators */)
val result = component.build(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeStartupLoader.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeFallbackBuilder.kt`.

## TopicTreeObservability.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeObservability.kt`
- **What does it do?** Contains feature logic for `TopicTreeObservability` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicSyncEvent, com.authord.mkdocs.ports.topic.TopicSyncEventType
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `TopicTreeObservability`.
  - Key callable members include `emitStartupReconciliation`, `emitWatcherTrigger`, `emitValidationReport`, `emitCompatibilityGate`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeObservability` (class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `emitStartupReconciliation` consumes projectId: String, instanceId: String, message: String, severity: String.
  - `emitWatcherTrigger` consumes projectId: String, instanceId: String, reason: String, followUp: WatcherFollowUp.
  - `emitValidationReport` consumes projectId: String, instanceId: String, issueCount: Int.
  - `emitCompatibilityGate` consumes projectId: String, instanceId: String, blocked: Boolean, unresolvedHighCount: Int.
- **Outputs produced:**
  - `emitStartupReconciliation` returns Unit/implicit.
  - `emitWatcherTrigger` returns Unit/implicit.
  - `emitValidationReport` returns Unit/implicit.
  - `emitCompatibilityGate` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/CompatibilityReleaseGateService.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationCoordinator.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicSyncEvent`
  - `com.authord.mkdocs.ports.topic.TopicSyncEventType`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/CompatibilityReleaseGateService.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationCoordinator.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicSyncEvent`, `com.authord.mkdocs.ports.topic.TopicSyncEventType`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeObservability` (class, public (default))
- **Key public/internal functions:**
  - `emitStartupReconciliation(projectId: String, instanceId: String, message: String, severity: String) : Unit/implicit` (public (default))
  - `emitWatcherTrigger(projectId: String, instanceId: String, reason: String, followUp: WatcherFollowUp) : Unit/implicit` (public (default))
  - `emitValidationReport(projectId: String, instanceId: String, issueCount: Int) : Unit/implicit` (public (default))
  - `emitCompatibilityGate(projectId: String, instanceId: String, blocked: Boolean, unresolvedHighCount: Int) : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = TopicTreeObservability(/* collaborators */)
val result = component.emitStartupReconciliation(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/CompatibilityReleaseGateService.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeObservability.kt`.

## TopicTreeScopeGuard.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeScopeGuard.kt`
- **What does it do?** Contains feature logic for `TopicTreeScopeGuard` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.InstanceRegistryPort, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncErrorCode, com.authord.mkdocs.ports.topic.TopicTreeCommand
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `TopicTreeScopeGuard`.
  - Key callable members include `ensureCommandScope`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeScopeGuard` (class, public (default)): Rule/policy implementation for decisions and validation.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `ensureCommandScope` consumes command: TopicTreeCommand.
- **Outputs produced:**
  - `ensureCommandScope` returns TopicGatewayResult<TopicInstanceRef>.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeUiServiceImpl.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/InstanceScopeIsolationTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.InstanceRegistryPort`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeUiServiceImpl.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/InstanceScopeIsolationTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.InstanceRegistryPort`, `com.authord.mkdocs.ports.topic.TopicGatewayResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeScopeGuard` (class, public (default))
- **Key public/internal functions:**
  - `ensureCommandScope(command: TopicTreeCommand) : TopicGatewayResult<TopicInstanceRef>` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = TopicTreeScopeGuard(/* collaborators */)
val result = component.ensureCommandScope(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeUiServiceImpl.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeScopeGuard.kt`.

## TopicTreeStartupLoader.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeStartupLoader.kt`
- **What does it do?** Contains feature logic for `TopicTreeStartupLoader` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.topic.PathPolicy, com.authord.mkdocs.core.topic.TopicTreeValidationIssue, com.authord.mkdocs.core.topic.TopicTreeValidationService, com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.TopicNavNode, com.authord.mkdocs.runtime.MarkdownHeadingSupport
  - External: java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Provide focused implementation logic for this module feature area.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `StartupTreeSource`, `StartupTreeState`, `TopicTreeStartupLoader`.
  - Key callable members include `load`, `visit`, `resolveNode`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `StartupTreeSource` (enum class, public (default)): Primary implementation type for this file feature area.
- `StartupTreeState` (data class, public (default)): Typed model representing shared state or payload data.
- `TopicTreeStartupLoader` (class, public (default)): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `load` consumes config: MkDocsConfigDocument, docsMarkdownPaths: List<String>.
  - `visit` consumes node: TopicNavNode.
  - `resolveNode` consumes node: TopicNavNode.
- **Outputs produced:**
  - `load` returns StartupTreeState.
  - `visit` returns Unit/implicit.
  - `resolveNode` returns TopicNavNode.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MultiInstanceReconciliationCoordinator.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationCoordinator.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanel.kt`
- **What this file calls:**
  - `com.authord.mkdocs.core.topic.PathPolicy`
  - `com.authord.mkdocs.core.topic.TopicTreeValidationIssue`
  - `com.authord.mkdocs.core.topic.TopicTreeValidationService`
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MultiInstanceReconciliationCoordinator.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationCoordinator.kt`. Downstream collaborators include `com.authord.mkdocs.core.topic.PathPolicy`, `com.authord.mkdocs.core.topic.TopicTreeValidationIssue`, `com.authord.mkdocs.core.topic.TopicTreeValidationService`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `StartupTreeSource` (enum class, public (default))
  - `StartupTreeState` (data class, public (default))
  - `TopicTreeStartupLoader` (class, public (default))
- **Key public/internal functions:**
  - `load(config: MkDocsConfigDocument, docsMarkdownPaths: List<String>) : StartupTreeState` (public (default))
  - `visit(node: TopicNavNode) : Unit/implicit` (public (default))
  - `resolveNode(node: TopicNavNode) : TopicNavNode` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `StartupTreeState` models structured data with fields: val source: StartupTreeSource, val nodes: List<TopicNavNode>, val navOrderedPaths: List<String>, val unlinkedPaths: List<String>, val validationIssues: List<TopicTreeValidationIssue>.
- `StartupTreeSource` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeStartupLoaderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = StartupTreeSource(/* collaborators */)
val result = component.load(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeStartupLoader.kt`.

## TopicTreeSyncOrchestratorService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeSyncOrchestratorService.kt`
- **What does it do?** Implements service-level behavior in `TopicTreeSyncOrchestratorService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.TopicTreePort, com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand, com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand, com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand, com.authord.mkdocs.ports.topic.AddTopicNodeCommand, com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.DocsFileGateway, com.authord.mkdocs.ports.topic.MkDocsConfigGateway
  - External: java.nio.file.Files, java.nio.file.Path, java.util.ArrayDeque, java.util.concurrent.ConcurrentHashMap
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `TopicTreeSyncOrchestratorService`, `ConfigMutationResult`, `NavNodeContext`.
  - Key callable members include `hydrateAggregateFromConfig`, `collect`, `rebind`, `visit`, `visit`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeSyncOrchestratorService` (class, public (default)): Service component that centralizes business/workflow logic.
- `ConfigMutationResult` (data class, private): Primary implementation type for this file feature area.
- `NavNodeContext` (data class, private): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `hydrateAggregateFromConfig` consumes treeId: String, instanceId: String, config: MkDocsConfigDocument.
  - `collect` consumes nodes: List<TopicNavNode>.
  - `rebind` consumes nodes: List<TopicNavNode>.
  - `visit` consumes node: TopicNavNode.
  - `visit` consumes current: TopicNavNode.
- **Outputs produced:**
  - `hydrateAggregateFromConfig` returns Unit/implicit.
  - `collect` returns Unit/implicit.
  - `rebind` returns List<TopicNavNode>.
  - `visit` returns Unit/implicit.
  - `visit` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state
  - Asynchronous stream emission/collection


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeApplicationServiceImpl.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MutationFeedbackPerformanceTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.TopicTreePort`
  - `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.AddExternalLinkTopicNodeCommand`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeApplicationServiceImpl.kt`. Downstream collaborators include `com.authord.mkdocs.ports.TopicTreePort`, `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`, `com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeSyncOrchestratorService` (class, public (default))
- **Key public/internal functions:**
  - `hydrateAggregateFromConfig(treeId: String, instanceId: String, config: MkDocsConfigDocument) : Unit/implicit` (internal)
  - `collect(nodes: List<TopicNavNode>) : Unit/implicit` (public (default))
  - `rebind(nodes: List<TopicNavNode>) : List<TopicNavNode>` (public (default))
  - `visit(node: TopicNavNode) : Unit/implicit` (public (default))
  - `visit(current: TopicNavNode) : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `ConfigMutationResult` models structured data with fields: val document: MkDocsConfigDocument, val fileOperations: List<TopicFileOperation>.
- `NavNodeContext` models structured data with fields: val node: TopicNavNode, val parentNodeId: String?.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Large callable surface area increases regression risk; consider characterization tests before large refactors.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
```kotlin
val component = TopicTreeSyncOrchestratorService(/* collaborators */)
val result = component.hydrateAggregateFromConfig(/* inputs */)
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeSyncOrchestratorService.kt`.

## TopicTreeUiService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeUiService.kt`
- **What does it do?** Implements service-level behavior in `TopicTreeUiService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncOutcome, com.authord.mkdocs.ports.topic.TopicTreeCommand
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `TopicTreeUiService`.
  - Key callable members include `dispatch`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeUiService` (interface, public (default)): Contract/abstraction used to decouple callers from implementations.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `dispatch` consumes command: TopicTreeCommand.
- **Outputs produced:**
  - `dispatch` returns TopicGatewayResult<TopicSyncOutcome> /** * Forces refresh of active tree model in UI state. * * Input: none. * Output: [TopicGatewayResult.Success] with refresh reconciliation outcome. * Errors/failure modes: typed reconciliation/config failures from application services. */ fun refreshActiveTree(): TopicGatewayResult<TopicSyncOutcome> /** * Selects an instance for subsequent UI-scoped operations. * * Input: target [instanceId] chosen by the user. * Output: [TopicGatewayResult.Success] with the active instance context. * Errors/failure modes: INSTANCE_SCOPE errors for unknown/unavailable instances. */ fun selectInstance(instanceId: String): TopicGatewayResult<TopicInstanceRef> }.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`
  - `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/TopicTreePort.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/InstanceSwitchCoordinator.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
  - `com.authord.mkdocs.ports.topic.TopicSyncOutcome`
  - `com.authord.mkdocs.ports.topic.TopicTreeCommand`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`, `modules/extension-ports/src/main/kotlin/com/authord/mkdocs/ports/TopicTreePort.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/PluginCompositionRoot.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicGatewayResult`, `com.authord.mkdocs.ports.topic.TopicInstanceRef`, `com.authord.mkdocs.ports.topic.TopicSyncOutcome`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeUiService` (interface, public (default))
- **Key public/internal functions:**
  - `dispatch(command: TopicTreeCommand) : TopicGatewayResult<TopicSyncOutcome> /** * Forces refresh of active tree model in UI state. * * Input: none. * Output: [TopicGatewayResult.Success] with refresh reconciliation outcome. * Errors/failure modes: typed reconciliation/config failures from application services. */ fun refreshActiveTree(): TopicGatewayResult<TopicSyncOutcome> /** * Selects an instance for subsequent UI-scoped operations. * * Input: target [instanceId] chosen by the user. * Output: [TopicGatewayResult.Success] with the active instance context. * Errors/failure modes: INSTANCE_SCOPE errors for unknown/unavailable instances. */ fun selectInstance(instanceId: String): TopicGatewayResult<TopicInstanceRef> }` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeUiServiceContractTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val result = implementation.dispatch(/* inputs */) // implementation : TopicTreeUiService
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeUiService.kt`.

## TopicTreeUiServiceImpl.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeUiServiceImpl.kt`
- **What does it do?** Implements service-level behavior in `TopicTreeUiServiceImpl` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.InstanceRegistryPort, com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncErrorCode, com.authord.mkdocs.ports.topic.TopicSyncOutcome, com.authord.mkdocs.ports.topic.TopicSyncTransaction
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `TopicTreeUiServiceImpl`.
  - Key callable members include `hydrateTreeFromConfig`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeUiServiceImpl` (class, public (default)): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `hydrateTreeFromConfig` consumes treeId: String, instanceId: String, config: MkDocsConfigDocument.
- **Outputs produced:**
  - `hydrateTreeFromConfig` returns TopicGatewayResult<Unit>.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MutationFlowIndependenceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeAggregateHydrationStartupTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.InstanceRegistryPort`
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MutationFlowIndependenceTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeAggregateHydrationStartupTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.InstanceRegistryPort`, `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeUiServiceImpl` (class, public (default))
- **Key public/internal functions:**
  - `hydrateTreeFromConfig(treeId: String, instanceId: String, config: MkDocsConfigDocument) : TopicGatewayResult<Unit>` (internal)

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = TopicTreeUiServiceImpl(/* collaborators */)
val result = component.hydrateTreeFromConfig(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeUiServiceImpl.kt`.

## TopicTreeWatcherCoordinator.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWatcherCoordinator.kt`
- **What does it do?** Contains feature logic for `TopicTreeWatcherCoordinator` in the `ui-plugin` module.
- **Why it matters:** The file contributes to core plugin behavior and influences downstream workflows in its feature area.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.topic.PathPolicy
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `WatcherEventKind`, `TopicTreeFileChange`, `WatcherFollowUp`, `WatcherDecision`, `TopicTreeWatcherCoordinator`.
  - Key callable members include `evaluate`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `WatcherEventKind` (enum class, public (default)): Typed model representing shared state or payload data.
- `TopicTreeFileChange` (data class, public (default)): Primary implementation type for this file feature area.
- `WatcherFollowUp` (enum class, public (default)): Primary implementation type for this file feature area.
- `WatcherDecision` (data class, public (default)): Primary implementation type for this file feature area.
- `TopicTreeWatcherCoordinator` (class, public (default)): Coordinator that sequences multi-step flows and state transitions.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `evaluate` consumes event: TopicTreeFileChange.
- **Outputs produced:**
  - `evaluate` returns WatcherDecision.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeObservability.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.core.topic.PathPolicy`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeObservability.kt`. Downstream collaborators include `com.authord.mkdocs.core.topic.PathPolicy`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `WatcherEventKind` (enum class, public (default))
  - `TopicTreeFileChange` (data class, public (default))
  - `WatcherFollowUp` (enum class, public (default))
  - `WatcherDecision` (data class, public (default))
  - `TopicTreeWatcherCoordinator` (class, public (default))
- **Key public/internal functions:**
  - `evaluate(event: TopicTreeFileChange) : WatcherDecision` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `TopicTreeFileChange` models structured data with fields: val kind: WatcherEventKind, val path: String, val newPath: String? = null.
- `WatcherDecision` models structured data with fields: val triggerReconciliation: Boolean, val followUp: WatcherFollowUp, val destructiveAction: Boolean, val reason: String.
- `WatcherEventKind` constrains state to a fixed enum set used across callers.
- `WatcherFollowUp` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = WatcherEventKind(/* collaborators */)
val result = component.evaluate(/* inputs */)
```
Typical caller path: `modules/core-domain/src/test/kotlin/com/authord/mkdocs/core/quality/KDocCoveragePolicyTest.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWatcherCoordinator.kt`.

## TopicTreeWorkspacePanel.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanel.kt`
- **What does it do?** Handles UI/IDE interaction concerns in `TopicTreeWorkspacePanel`.
- **Why it matters:** This file directly shapes user-triggered behavior and IDE integration quality.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicNavNode, com.authord.mkdocs.ports.topic.TopicSyncErrorCode, com.authord.mkdocs.ui.PluginCompositionRoot
  - External: com.intellij.icons.AllIcons, com.intellij.notification.NotificationType, com.intellij.openapi.actionSystem.ActionManager, com.intellij.openapi.actionSystem.AnActionEvent, com.intellij.openapi.actionSystem.DefaultActionGroup, com.intellij.openapi.application.ApplicationManager, com.intellij.openapi.fileEditor.FileEditorManager, com.intellij.openapi.vfs.LocalFileSystem
- **Responsibilities:**
  - Bind IDE/UI events to application services and maintain interaction consistency.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `TopicTreeNodeKind`, `TopicTreeNodeView`, `TopicTreeNodeTransferable`, `TopicTreeWorkspacePanel`, `TocHeaderAction`, `TocTreeFileNameToolTip`, plus `1` more.
  - Key callable members include `root`, `render`, `reconcileFromDisk`, `visit`, `visit`, `resolveFilePath`, `visit`, `visit`, plus `13` additional members.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeNodeKind` (enum class, internal): Primary implementation type for this file feature area.
- `TopicTreeNodeView` (data class, internal): Primary implementation type for this file feature area.
- `TopicTreeNodeTransferable` (class, private): Primary implementation type for this file feature area.
- `TopicTreeWorkspacePanel` (class, internal): Primary implementation type for this file feature area.
- `TocHeaderAction` (data class, private): IDE interaction entry point for user/editor events.
- `TocTreeFileNameToolTip` (class, private): Primary implementation type for this file feature area.
- `TopicTreeTransferHandler` (class, private): Primary implementation type for this file feature area.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `root` consumes no explicit parameters.
  - `render` consumes state: StartupTreeState.
  - `reconcileFromDisk` consumes no explicit parameters.
  - `visit` consumes node: DefaultMutableTreeNode.
  - `visit` consumes node: DefaultMutableTreeNode.
- **Outputs produced:**
  - `root` returns TopicTreeNodeView.
  - `render` returns Unit/implicit.
  - `reconcileFromDisk` returns Unit/implicit.
  - `visit` returns Unit/implicit.
  - `visit` returns DefaultMutableTreeNode?.
- **Side effects:**
  - Filesystem reads/writes
  - IntelliJ UI state updates
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanelUiContractTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicNavNode`
  - `com.authord.mkdocs.ports.topic.TopicSyncErrorCode`
  - `com.authord.mkdocs.ui.PluginCompositionRoot`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanelUiContractTest.kt`. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicGatewayResult`, `com.authord.mkdocs.ports.topic.TopicNavNode`, `com.authord.mkdocs.ports.topic.TopicSyncErrorCode`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeNodeKind` (enum class, internal)
  - `TopicTreeNodeView` (data class, internal)
  - `TopicTreeWorkspacePanel` (class, internal)
- **Key public/internal functions:**
  - `root() : TopicTreeNodeView` (public (default))
  - `render(state: StartupTreeState) : Unit/implicit` (public (default))
  - `reconcileFromDisk() : Unit/implicit` (public (default))
  - `visit(node: DefaultMutableTreeNode) : Unit/implicit` (public (default))
  - `visit(node: DefaultMutableTreeNode) : DefaultMutableTreeNode?` (public (default))
  - `resolveFilePath(node: TopicTreeNodeView) : String?` (internal)
  - `visit(current: DefaultMutableTreeNode) : String?` (public (default))
  - `visit(nodes: List<TopicNavNode>) : TopicNavNode?` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Tracks lifecycle/disposal boundaries; resources are expected to be released deterministically.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `TopicTreeNodeView` models structured data with fields: val nodeId: String, val title: String, val parentNodeId: String?, val path: String? = null, val externalUrl: String? = null.
- `TocHeaderAction` models structured data with fields: val tooltip: String, val icon: Icon, val perform: (.
- `TopicTreeNodeKind` constrains state to a fixed enum set used across callers.

### 11. Concurrency / async behavior
- Contains explicit cancellation checks/paths for long-running work.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanelUiContractTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Large callable surface area increases regression risk; consider characterization tests before large refactors.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
```kotlin
val component = TopicTreeNodeKind(/* collaborators */)
val result = component.root(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactory.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanel.kt`.

## UnlinkedFilesBucketService.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/UnlinkedFilesBucketService.kt`
- **What does it do?** Implements service-level behavior in `UnlinkedFilesBucketService` for reusable feature logic.
- **Why it matters:** Service files centralize decisions so multiple callers can depend on consistent behavior.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.topic.PathPolicy, com.authord.mkdocs.ports.topic.TopicNavNode
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Coordinate multi-step workflows and state transitions for its feature domain.
  - Respect IntelliJ lifecycle/disposal conventions to avoid stale UI resources.
- **Key functionalities:**
  - Main declarations: `UnlinkedFilesBucketService`.
  - Key callable members include `derive`, `visit`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `UI + IDE integration`

### 2. Main classes / interfaces / objects inside the file
- `UnlinkedFilesBucketService` (class, public (default)): Service component that centralizes business/workflow logic.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `derive` consumes docsDir: String, docsMarkdownPaths: List<String>, navNodes: List<TopicNavNode>.
  - `visit` consumes node: TopicNavNode.
- **Outputs produced:**
  - `derive` returns List<String>.
  - `visit` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeStartupLoader.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/UnlinkedFilesBucketServiceTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.core.topic.PathPolicy`
  - `com.authord.mkdocs.ports.topic.TopicNavNode`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeStartupLoader.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/UnlinkedFilesBucketServiceTest.kt`. Downstream collaborators include `com.authord.mkdocs.core.topic.PathPolicy`, `com.authord.mkdocs.ports.topic.TopicNavNode`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `UnlinkedFilesBucketService` (class, public (default))
- **Key public/internal functions:**
  - `derive(docsDir: String, docsMarkdownPaths: List<String>, navNodes: List<TopicNavNode>) : List<String>` (public (default))
  - `visit(node: TopicNavNode) : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- No explicit lifecycle framework hooks found; state appears local or caller-managed.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** No framework annotations detected in this file.
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** Yes
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/UnlinkedFilesBucketServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Collaborators are injected/composed directly; mocking behavior is defined in corresponding test files.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
```kotlin
val component = UnlinkedFilesBucketService(/* collaborators */)
val result = component.derive(/* inputs */)
```
Typical caller path: `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeStartupLoader.kt` -> `modules/ui-plugin/src/main/kotlin/com/authord/mkdocs/ui/intellij/UnlinkedFilesBucketService.kt`.

## ActivationErrorPresenterTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenterTest.kt`
- **What does it do?** Implements unit-focused test coverage around `ActivationErrorPresenterTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `ActivationErrorPresenterTest`.
  - Key callable members include `renders message per failure reason`, `includes details when provided`.
  - Scenario-focused test cases include: `renders message per failure reason`, `includes details when provided`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `ActivationErrorPresenterTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `renders message per failure reason` consumes no explicit parameters.
  - `includes details when provided` consumes no explicit parameters.
- **Outputs produced:**
  - `renders message per failure reason` returns Unit/implicit.
  - `includes details when provided` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `ActivationErrorPresenterTest` (class, public (default))
- **Key public/internal functions:**
  - `renders message per failure reason() : Unit/implicit` (public (default))
  - `includes details when provided() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- Model-centric declarations: `ActivationErrorPresenterTest`.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/ActivationErrorPresenterTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *ActivationErrorPresenterTest`.

## DocsExplorerServiceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsExplorerServiceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `DocsExplorerServiceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `DocsExplorerServiceTest`.
  - Key callable members include `discovers markdown files under docs root only`, `returns empty list when docs directory does not exist`.
  - Scenario-focused test cases include: `discovers markdown files under docs root only`, `returns empty list when docs directory does not exist`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `DocsExplorerServiceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `discovers markdown files under docs root only` consumes no explicit parameters.
  - `returns empty list when docs directory does not exist` consumes no explicit parameters.
- **Outputs produced:**
  - `discovers markdown files under docs root only` returns Unit/implicit.
  - `returns empty list when docs directory does not exist` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `DocsExplorerServiceTest` (class, public (default))
- **Key public/internal functions:**
  - `discovers markdown files under docs root only() : Unit/implicit` (public (default))
  - `returns empty list when docs directory does not exist() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsExplorerServiceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *DocsExplorerServiceTest`.

## DocsFileSelectionPublisherTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisherTest.kt`
- **What does it do?** Implements unit-focused test coverage around `DocsFileSelectionPublisherTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `DocsFileSelectionPublisherTest`.
  - Key callable members include `publishes selected file events to subscribers`.
  - Scenario-focused test cases include: `publishes selected file events to subscribers`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `DocsFileSelectionPublisherTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `publishes selected file events to subscribers` consumes no explicit parameters.
- **Outputs produced:**
  - `publishes selected file events to subscribers` returns Unit/implicit.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `DocsFileSelectionPublisherTest` (class, public (default))
- **Key public/internal functions:**
  - `publishes selected file events to subscribers() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/DocsFileSelectionPublisherTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *DocsFileSelectionPublisherTest`.

## FeatureFlagPolicyServiceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyServiceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `FeatureFlagPolicyServiceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.flags.FeatureFlagPolicy
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `FeatureFlagPolicyServiceTest`.
  - Key callable members include `current and update expose active policy`.
  - Scenario-focused test cases include: `current and update expose active policy`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `FeatureFlagPolicyServiceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `current and update expose active policy` consumes no explicit parameters.
- **Outputs produced:**
  - `current and update expose active policy` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.flags.FeatureFlagPolicy`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.flags.FeatureFlagPolicy`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `FeatureFlagPolicyServiceTest` (class, public (default))
- **Key public/internal functions:**
  - `current and update expose active policy() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/FeatureFlagPolicyServiceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *FeatureFlagPolicyServiceTest`.

## NavigationCoordinatorTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`
- **What does it do?** Implements unit-focused test coverage around `NavigationCoordinatorTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.navigation.RouteMappingService
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `NavigationCoordinatorTest`.
  - Key callable members include `applies mapped route to preview`, `returns failure when mapping cannot be resolved`.
  - Scenario-focused test cases include: `applies mapped route to preview`, `returns failure when mapping cannot be resolved`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `NavigationCoordinatorTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `applies mapped route to preview` consumes no explicit parameters.
  - `returns failure when mapping cannot be resolved` consumes no explicit parameters.
- **Outputs produced:**
  - `applies mapped route to preview` returns Unit/implicit.
  - `returns failure when mapping cannot be resolved` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.navigation.RouteMappingService`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.navigation.RouteMappingService`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `NavigationCoordinatorTest` (class, public (default))
- **Key public/internal functions:**
  - `applies mapped route to preview() : Unit/implicit` (public (default))
  - `returns failure when mapping cannot be resolved() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/NavigationCoordinatorTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *NavigationCoordinatorTest`.

## OutOfScopeGuardrailsTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/OutOfScopeGuardrailsTest.kt`
- **What does it do?** Implements unit-focused test coverage around `OutOfScopeGuardrailsTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.flags.FeatureFlagPolicy
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `OutOfScopeGuardrailsTest`.
  - Key callable members include `default policy enforces mvp-only behavior`, `future-cycle flags disable mvp-only guardrail`.
  - Scenario-focused test cases include: `default policy enforces mvp-only behavior`, `future-cycle flags disable mvp-only guardrail`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `OutOfScopeGuardrailsTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `default policy enforces mvp-only behavior` consumes no explicit parameters.
  - `future-cycle flags disable mvp-only guardrail` consumes no explicit parameters.
- **Outputs produced:**
  - `default policy enforces mvp-only behavior` returns Unit/implicit.
  - `future-cycle flags disable mvp-only guardrail` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.flags.FeatureFlagPolicy`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.flags.FeatureFlagPolicy`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `OutOfScopeGuardrailsTest` (class, public (default))
- **Key public/internal functions:**
  - `default policy enforces mvp-only behavior() : Unit/implicit` (public (default))
  - `future-cycle flags disable mvp-only guardrail() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/OutOfScopeGuardrailsTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *OutOfScopeGuardrailsTest`.

## PluginActivationServiceErrorTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginActivationServiceErrorTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PluginActivationServiceErrorTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.flags.FeatureFlagPolicy, com.authord.mkdocs.runtime.BaseUrlDetector, com.authord.mkdocs.runtime.ManagedProcessHandle, com.authord.mkdocs.runtime.MkdocsProcessManager, com.authord.mkdocs.runtime.UvBootstrapService, com.authord.mkdocs.runtime.BootstrapResult, com.authord.mkdocs.ui.intellij.AuthordUiBundle
  - External: org.junit.jupiter.api.Assertions.assertEquals, org.junit.jupiter.api.Test, java.nio.file.Path
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PluginActivationServiceErrorTest`, `StubUvBootstrapService`, `StubBaseUrlDetector`, `StubPreviewPaneCoordinator`, `StubErrorPresenter`.
  - Key callable members include `test startFailureDetails returns git error message`, `test startFailureDetails returns git command error message`, `test startFailureDetails returns module missing message`.
  - Scenario-focused test cases include: `test startFailureDetails returns git error message`, `test startFailureDetails returns git command error message`, `test startFailureDetails returns module missing message`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PluginActivationServiceErrorTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `StubUvBootstrapService` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `StubBaseUrlDetector` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `StubPreviewPaneCoordinator` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `StubErrorPresenter` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `test startFailureDetails returns git error message` consumes no explicit parameters.
  - `test startFailureDetails returns git command error message` consumes no explicit parameters.
  - `test startFailureDetails returns module missing message` consumes no explicit parameters.
- **Outputs produced:**
  - `test startFailureDetails returns git error message` returns Unit/implicit.
  - `test startFailureDetails returns git command error message` returns Unit/implicit.
  - `test startFailureDetails returns module missing message` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - Process/runtime execution


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.flags.FeatureFlagPolicy`
  - `com.authord.mkdocs.runtime.BaseUrlDetector`
  - `com.authord.mkdocs.runtime.ManagedProcessHandle`
  - `com.authord.mkdocs.runtime.MkdocsProcessManager`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.flags.FeatureFlagPolicy`, `com.authord.mkdocs.runtime.BaseUrlDetector`, `com.authord.mkdocs.runtime.ManagedProcessHandle`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PluginActivationServiceErrorTest` (class, public (default))
  - `StubUvBootstrapService` (class, public (default))
  - `StubBaseUrlDetector` (class, public (default))
  - `StubPreviewPaneCoordinator` (class, public (default))
  - `StubErrorPresenter` (class, public (default))
- **Key public/internal functions:**
  - `test startFailureDetails returns git error message() : Unit/implicit` (public (default))
  - `test startFailureDetails returns git command error message() : Unit/implicit` (public (default))
  - `test startFailureDetails returns module missing message() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- Model-centric declarations: `PluginActivationServiceErrorTest`, `StubErrorPresenter`.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginActivationServiceErrorTest.kt`
- **Mocked collaborators:**
  - Uses mocking framework directly in this file.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PluginActivationServiceErrorTest`.

## PluginActivationServiceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginActivationServiceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PluginActivationServiceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.flags.FeatureFlagPolicy, com.authord.mkdocs.runtime.BaseUrlDetector, com.authord.mkdocs.runtime.CommandResult, com.authord.mkdocs.runtime.ManagedProcessHandle, com.authord.mkdocs.runtime.MkdocsProcessManager, com.authord.mkdocs.runtime.ProcessLauncher, com.authord.mkdocs.runtime.UvBootstrapService, com.authord.mkdocs.runtime.StaticUvExecutableProvider
  - External: java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `ActivationHandle`, `PluginActivationServiceTest`, `RecordingStartLauncher`.
  - Key callable members include `fails when mvp flow is disabled`, `fails when bootstrap fails`, `fails when process start returns not started`, `includes startup output details when process start fails`, `fails fast with mkdocs config hint when config is missing`, `fails when readiness probe does not succeed and retries stop the process`, `succeeds when bootstrap start and readiness probe succeeds`, `startup output is not required when readiness probe succeeds`, plus `8` additional members.
  - Scenario-focused test cases include: `fails when mvp flow is disabled`, `fails when bootstrap fails`, `fails when process start returns not started`, `includes startup output details when process start fails`, `fails fast with mkdocs config hint when config is missing`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `ActivationHandle` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `PluginActivationServiceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingStartLauncher` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `fails when mvp flow is disabled` consumes no explicit parameters.
  - `fails when bootstrap fails` consumes no explicit parameters.
  - `fails when process start returns not started` consumes no explicit parameters.
  - `includes startup output details when process start fails` consumes no explicit parameters.
  - `fails fast with mkdocs config hint when config is missing` consumes no explicit parameters.
- **Outputs produced:**
  - `fails when mvp flow is disabled` returns Unit/implicit.
  - `fails when bootstrap fails` returns Unit/implicit.
  - `fails when process start returns not started` returns Unit/implicit.
  - `includes startup output details when process start fails` returns Unit/implicit.
  - `fails fast with mkdocs config hint when config is missing` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - Process/runtime execution


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.flags.FeatureFlagPolicy`
  - `com.authord.mkdocs.runtime.BaseUrlDetector`
  - `com.authord.mkdocs.runtime.CommandResult`
  - `com.authord.mkdocs.runtime.ManagedProcessHandle`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.flags.FeatureFlagPolicy`, `com.authord.mkdocs.runtime.BaseUrlDetector`, `com.authord.mkdocs.runtime.CommandResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PluginActivationServiceTest` (class, public (default))
- **Key public/internal functions:**
  - `fails when mvp flow is disabled() : Unit/implicit` (public (default))
  - `fails when bootstrap fails() : Unit/implicit` (public (default))
  - `fails when process start returns not started() : Unit/implicit` (public (default))
  - `includes startup output details when process start fails() : Unit/implicit` (public (default))
  - `fails fast with mkdocs config hint when config is missing() : Unit/implicit` (public (default))
  - `fails when readiness probe does not succeed and retries stop the process() : Unit/implicit` (public (default))
  - `succeeds when bootstrap start and readiness probe succeeds() : Unit/implicit` (public (default))
  - `startup output is not required when readiness probe succeeds() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Contains coroutine-aware execution paths and cancellation-sensitive operations.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginActivationServiceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PluginActivationServiceTest`.

## PluginCompositionRootCoverageTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginCompositionRootCoverageTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PluginCompositionRootCoverageTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.topic.TopicTreeMutationService, com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.InstanceRegistryPort, com.authord.mkdocs.ports.topic.TopicDeleteMode, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncErrorCode, com.authord.mkdocs.ports.topic.TopicSyncOutcome
  - External: java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PluginCompositionRootCoverageTest`, `RecordingApplicationService`, `StubInstanceRegistry`.
  - Key callable members include `default no-op gateways and orchestrator return unsupported failures`, `default application service delegates execute rollback and active-instance lookup`, `instance registry service covers discovery registration and selection branches`, `default ui service dispatch refresh and instance-selection branches are covered`.
  - Scenario-focused test cases include: `default no-op gateways and orchestrator return unsupported failures`, `default application service delegates execute rollback and active-instance lookup`, `instance registry service covers discovery registration and selection branches`, `default ui service dispatch refresh and instance-selection branches are covered`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PluginCompositionRootCoverageTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingApplicationService` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `StubInstanceRegistry` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `default no-op gateways and orchestrator return unsupported failures` consumes no explicit parameters.
  - `default application service delegates execute rollback and active-instance lookup` consumes no explicit parameters.
  - `instance registry service covers discovery registration and selection branches` consumes no explicit parameters.
  - `default ui service dispatch refresh and instance-selection branches are covered` consumes no explicit parameters.
- **Outputs produced:**
  - `default no-op gateways and orchestrator return unsupported failures` returns Unit/implicit.
  - `default application service delegates execute rollback and active-instance lookup` returns Unit/implicit.
  - `instance registry service covers discovery registration and selection branches` returns Unit/implicit.
  - `default ui service dispatch refresh and instance-selection branches are covered` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.topic.TopicTreeMutationService`
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.InstanceRegistryPort`
  - `com.authord.mkdocs.ports.topic.TopicDeleteMode`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.topic.TopicTreeMutationService`, `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.InstanceRegistryPort`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PluginCompositionRootCoverageTest` (class, public (default))
- **Key public/internal functions:**
  - `default no-op gateways and orchestrator return unsupported failures() : Unit/implicit` (public (default))
  - `default application service delegates execute rollback and active-instance lookup() : Unit/implicit` (public (default))
  - `instance registry service covers discovery registration and selection branches() : Unit/implicit` (public (default))
  - `default ui service dispatch refresh and instance-selection branches are covered() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`, `@Suppress`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginCompositionRootCoverageTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PluginCompositionRootCoverageTest`.

## PluginCompositionRootTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginCompositionRootTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PluginCompositionRootTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.topic.TopicTreeMutationService, com.authord.mkdocs.ports.topic.AddTopicNodeCommand, com.authord.mkdocs.ui.intellij.IntellijTestFixtures, com.authord.mkdocs.ui.intellij.PluginRuntimeIntegrationService
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PluginCompositionRootTest`.
  - Key callable members include `wires command bus and default adapters`, `resolves project runtime integration service through composition root`.
  - Scenario-focused test cases include: `wires command bus and default adapters`, `resolves project runtime integration service through composition root`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PluginCompositionRootTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `wires command bus and default adapters` consumes no explicit parameters.
  - `resolves project runtime integration service through composition root` consumes no explicit parameters.
- **Outputs produced:**
  - `wires command bus and default adapters` returns Unit/implicit.
  - `resolves project runtime integration service through composition root` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.topic.TopicTreeMutationService`
  - `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`
  - `com.authord.mkdocs.ui.intellij.IntellijTestFixtures`
  - `com.authord.mkdocs.ui.intellij.PluginRuntimeIntegrationService`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.topic.TopicTreeMutationService`, `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`, `com.authord.mkdocs.ui.intellij.IntellijTestFixtures`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PluginCompositionRootTest` (class, public (default))
- **Key public/internal functions:**
  - `wires command bus and default adapters() : Unit/implicit` (public (default))
  - `resolves project runtime integration service through composition root() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginCompositionRootTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PluginCompositionRootTest`.

## PreviewPaneCoordinatorTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PreviewPaneCoordinatorTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PreviewPaneCoordinatorTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PreviewPaneCoordinatorTest`.
  - Key callable members include `opens side-by-side preview and tracks active url`, `returns null for missing session and keeps root url stable`, `normalizes route without leading or trailing slash`, `preserves html route without appending trailing slash`, `handles edge cases in url normalization`.
  - Scenario-focused test cases include: `opens side-by-side preview and tracks active url`, `returns null for missing session and keeps root url stable`, `normalizes route without leading or trailing slash`, `preserves html route without appending trailing slash`, `handles edge cases in url normalization`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui`
- **Module / feature area:** `ui-plugin` / `ui`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PreviewPaneCoordinatorTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `opens side-by-side preview and tracks active url` consumes no explicit parameters.
  - `returns null for missing session and keeps root url stable` consumes no explicit parameters.
  - `normalizes route without leading or trailing slash` consumes no explicit parameters.
  - `preserves html route without appending trailing slash` consumes no explicit parameters.
  - `handles edge cases in url normalization` consumes no explicit parameters.
- **Outputs produced:**
  - `opens side-by-side preview and tracks active url` returns Unit/implicit.
  - `returns null for missing session and keeps root url stable` returns Unit/implicit.
  - `normalizes route without leading or trailing slash` returns Unit/implicit.
  - `preserves html route without appending trailing slash` returns Unit/implicit.
  - `handles edge cases in url normalization` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewPaneCoordinatorTest` (class, public (default))
- **Key public/internal functions:**
  - `opens side-by-side preview and tracks active url() : Unit/implicit` (public (default))
  - `returns null for missing session and keeps root url stable() : Unit/implicit` (public (default))
  - `normalizes route without leading or trailing slash() : Unit/implicit` (public (default))
  - `preserves html route without appending trailing slash() : Unit/implicit` (public (default))
  - `handles edge cases in url normalization() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PreviewPaneCoordinatorTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PreviewPaneCoordinatorTest`.

## AuthordMarkdownOpenPreviewListenerTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
- **What does it do?** Implements unit-focused test coverage around `AuthordMarkdownOpenPreviewListenerTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.flags.FeatureFlagPolicy
  - External: com.intellij.openapi.project.Project, java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `AuthordMarkdownOpenPreviewListenerTest`.
  - Key callable members include `docs markdown open activates browser service and starts runtime`, `non-docs markdown open is ignored`, `listener respects auto open preview setting`, `docs startup failure applies bypass and notifies`.
  - Scenario-focused test cases include: `docs markdown open activates browser service and starts runtime`, `non-docs markdown open is ignored`, `listener respects auto open preview setting`, `docs startup failure applies bypass and notifies`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `AuthordMarkdownOpenPreviewListenerTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `docs markdown open activates browser service and starts runtime` consumes no explicit parameters.
  - `non-docs markdown open is ignored` consumes no explicit parameters.
  - `listener respects auto open preview setting` consumes no explicit parameters.
  - `docs startup failure applies bypass and notifies` consumes no explicit parameters.
- **Outputs produced:**
  - `docs markdown open activates browser service and starts runtime` returns Unit/implicit.
  - `non-docs markdown open is ignored` returns Unit/implicit.
  - `listener respects auto open preview setting` returns Unit/implicit.
  - `docs startup failure applies bypass and notifies` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.flags.FeatureFlagPolicy`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.flags.FeatureFlagPolicy`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `AuthordMarkdownOpenPreviewListenerTest` (class, public (default))
- **Key public/internal functions:**
  - `docs markdown open activates browser service and starts runtime() : Unit/implicit` (public (default))
  - `non-docs markdown open is ignored() : Unit/implicit` (public (default))
  - `listener respects auto open preview setting() : Unit/implicit` (public (default))
  - `docs startup failure applies bypass and notifies() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *AuthordMarkdownOpenPreviewListenerTest`.

## AuthordMarkdownSplitEditorProviderTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
- **What does it do?** Implements unit-focused test coverage around `AuthordMarkdownSplitEditorProviderTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.fileEditor.TextEditorWithPreview, com.intellij.testFramework.LightVirtualFile, java.nio.file.Files, javax.swing.JPanel
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `AuthordMarkdownSplitEditorProviderTest`, `RecordingPreviewContent`.
  - Key callable members include `split preview eligibility requires mkdocs config`, `split preview eligibility rejects nested config that does not scope selected markdown`, `split preview eligibility rejects markdown outside the project root`, `split preview eligibility accepts docs scoped markdown only`, `refresh suppresses missing config notification when auto start is disabled`, `refresh reports missing config when auto start is enabled`, `preferred layout defaults to editor and preview when project setting was never stored`, `store layout persists shared preference for project`, plus `3` additional members.
  - Scenario-focused test cases include: `split preview eligibility requires mkdocs config`, `split preview eligibility rejects nested config that does not scope selected markdown`, `split preview eligibility rejects markdown outside the project root`, `split preview eligibility accepts docs scoped markdown only`, `refresh suppresses missing config notification when auto start is disabled`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `AuthordMarkdownSplitEditorProviderTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingPreviewContent` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `split preview eligibility requires mkdocs config` consumes no explicit parameters.
  - `split preview eligibility rejects nested config that does not scope selected markdown` consumes no explicit parameters.
  - `split preview eligibility rejects markdown outside the project root` consumes no explicit parameters.
  - `split preview eligibility accepts docs scoped markdown only` consumes no explicit parameters.
  - `refresh suppresses missing config notification when auto start is disabled` consumes no explicit parameters.
- **Outputs produced:**
  - `split preview eligibility requires mkdocs config` returns Unit/implicit.
  - `split preview eligibility rejects nested config that does not scope selected markdown` returns Unit/implicit.
  - `split preview eligibility rejects markdown outside the project root` returns Unit/implicit.
  - `split preview eligibility accepts docs scoped markdown only` returns Unit/implicit.
  - `refresh suppresses missing config notification when auto start is disabled` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - IntelliJ UI state updates
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserServiceTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactoryTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserServiceTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactoryTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `AuthordMarkdownSplitEditorProviderTest` (class, public (default))
- **Key public/internal functions:**
  - `split preview eligibility requires mkdocs config() : Unit/implicit` (public (default))
  - `split preview eligibility rejects nested config that does not scope selected markdown() : Unit/implicit` (public (default))
  - `split preview eligibility rejects markdown outside the project root() : Unit/implicit` (public (default))
  - `split preview eligibility accepts docs scoped markdown only() : Unit/implicit` (public (default))
  - `refresh suppresses missing config notification when auto start is disabled() : Unit/implicit` (public (default))
  - `refresh reports missing config when auto start is enabled() : Unit/implicit` (public (default))
  - `preferred layout defaults to editor and preview when project setting was never stored() : Unit/implicit` (public (default))
  - `store layout persists shared preference for project() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *AuthordMarkdownSplitEditorProviderTest`.

## AuthordPreviewSettingsServiceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `AuthordPreviewSettingsServiceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `AuthordPreviewSettingsServiceTest`.
  - Key callable members include `auto-open setting defaults to true`, `auto-open setting can be disabled through state load`.
  - Scenario-focused test cases include: `auto-open setting defaults to true`, `auto-open setting can be disabled through state load`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `AuthordPreviewSettingsServiceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `auto-open setting defaults to true` consumes no explicit parameters.
  - `auto-open setting can be disabled through state load` consumes no explicit parameters.
- **Outputs produced:**
  - `auto-open setting defaults to true` returns Unit/implicit.
  - `auto-open setting can be disabled through state load` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `AuthordPreviewSettingsServiceTest` (class, public (default))
- **Key public/internal functions:**
  - `auto-open setting defaults to true() : Unit/implicit` (public (default))
  - `auto-open setting can be disabled through state load() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordPreviewSettingsServiceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *AuthordPreviewSettingsServiceTest`.

## AuthordSplitEditorLayoutStateServiceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `AuthordSplitEditorLayoutStateServiceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.fileEditor.TextEditorWithPreview
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `AuthordSplitEditorLayoutStateServiceTest`.
  - Key callable members include `service defaults to editor and preview layout`, `service stores and resolves preferred layout`, `service falls back to default for invalid persisted layout`.
  - Scenario-focused test cases include: `service defaults to editor and preview layout`, `service stores and resolves preferred layout`, `service falls back to default for invalid persisted layout`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `AuthordSplitEditorLayoutStateServiceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `service defaults to editor and preview layout` consumes no explicit parameters.
  - `service stores and resolves preferred layout` consumes no explicit parameters.
  - `service falls back to default for invalid persisted layout` consumes no explicit parameters.
- **Outputs produced:**
  - `service defaults to editor and preview layout` returns Unit/implicit.
  - `service stores and resolves preferred layout` returns Unit/implicit.
  - `service falls back to default for invalid persisted layout` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `AuthordSplitEditorLayoutStateServiceTest` (class, public (default))
- **Key public/internal functions:**
  - `service defaults to editor and preview layout() : Unit/implicit` (public (default))
  - `service stores and resolves preferred layout() : Unit/implicit` (public (default))
  - `service falls back to default for invalid persisted layout() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- Model-centric declarations: `AuthordSplitEditorLayoutStateServiceTest`.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordSplitEditorLayoutStateServiceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *AuthordSplitEditorLayoutStateServiceTest`.

## DocsScopeResolverTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **What does it do?** Implements unit-focused test coverage around `DocsScopeResolverTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `DocsScopeResolverTest`.
  - Key callable members include `docs scope resolver marks docs markdown as docs scoped`, `docs scope resolver marks non docs markdown as non docs`, `docs scope resolver marks tied docs roots as ambiguous`, `docs scope resolver marks invalid docs dir as misconfigured`.
  - Scenario-focused test cases include: `docs scope resolver marks docs markdown as docs scoped`, `docs scope resolver marks non docs markdown as non docs`, `docs scope resolver marks tied docs roots as ambiguous`, `docs scope resolver marks invalid docs dir as misconfigured`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `DocsScopeResolverTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `docs scope resolver marks docs markdown as docs scoped` consumes no explicit parameters.
  - `docs scope resolver marks non docs markdown as non docs` consumes no explicit parameters.
  - `docs scope resolver marks tied docs roots as ambiguous` consumes no explicit parameters.
  - `docs scope resolver marks invalid docs dir as misconfigured` consumes no explicit parameters.
- **Outputs produced:**
  - `docs scope resolver marks docs markdown as docs scoped` returns Unit/implicit.
  - `docs scope resolver marks non docs markdown as non docs` returns Unit/implicit.
  - `docs scope resolver marks tied docs roots as ambiguous` returns Unit/implicit.
  - `docs scope resolver marks invalid docs dir as misconfigured` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `DocsScopeResolverTest` (class, public (default))
- **Key public/internal functions:**
  - `docs scope resolver marks docs markdown as docs scoped() : Unit/implicit` (public (default))
  - `docs scope resolver marks non docs markdown as non docs() : Unit/implicit` (public (default))
  - `docs scope resolver marks tied docs roots as ambiguous() : Unit/implicit` (public (default))
  - `docs scope resolver marks invalid docs dir as misconfigured() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *DocsScopeResolverTest`.

## ExternalMoveHandlingTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **What does it do?** Implements unit-focused test coverage around `ExternalMoveHandlingTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `ExternalMoveHandlingTest`.
  - Key callable members include `inside docs_dir moved outside triggers broken reference validation without destructive action`, `outside docs_dir moved inside creates unlinked file follow-up`, `rename or move wholly outside project root is ignored`.
  - Scenario-focused test cases include: `inside docs_dir moved outside triggers broken reference validation without destructive action`, `outside docs_dir moved inside creates unlinked file follow-up`, `rename or move wholly outside project root is ignored`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `ExternalMoveHandlingTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `inside docs_dir moved outside triggers broken reference validation without destructive action` consumes no explicit parameters.
  - `outside docs_dir moved inside creates unlinked file follow-up` consumes no explicit parameters.
  - `rename or move wholly outside project root is ignored` consumes no explicit parameters.
- **Outputs produced:**
  - `inside docs_dir moved outside triggers broken reference validation without destructive action` returns Unit/implicit.
  - `outside docs_dir moved inside creates unlinked file follow-up` returns Unit/implicit.
  - `rename or move wholly outside project root is ignored` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `ExternalMoveHandlingTest` (class, public (default))
- **Key public/internal functions:**
  - `inside docs_dir moved outside triggers broken reference validation without destructive action() : Unit/implicit` (public (default))
  - `outside docs_dir moved inside creates unlinked file follow-up() : Unit/implicit` (public (default))
  - `rename or move wholly outside project root is ignored() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ExternalMoveHandlingTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *ExternalMoveHandlingTest`.

## InstanceRegistryDiscoveryTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/InstanceRegistryDiscoveryTest.kt`
- **What does it do?** Implements unit-focused test coverage around `InstanceRegistryDiscoveryTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicGatewayResult
  - External: java.nio.file.Path
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `InstanceRegistryDiscoveryTest`.
  - Key callable members include `discovers root mkdocs yml as default instance`, `registers additional instances only through explicit add instance flow`.
  - Scenario-focused test cases include: `discovers root mkdocs yml as default instance`, `registers additional instances only through explicit add instance flow`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `InstanceRegistryDiscoveryTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `discovers root mkdocs yml as default instance` consumes no explicit parameters.
  - `registers additional instances only through explicit add instance flow` consumes no explicit parameters.
- **Outputs produced:**
  - `discovers root mkdocs yml as default instance` returns Unit/implicit.
  - `registers additional instances only through explicit add instance flow` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicGatewayResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `InstanceRegistryDiscoveryTest` (class, public (default))
- **Key public/internal functions:**
  - `discovers root mkdocs yml as default instance() : Unit/implicit` (public (default))
  - `registers additional instances only through explicit add instance flow() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/InstanceRegistryDiscoveryTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *InstanceRegistryDiscoveryTest`.

## InstanceScopeIsolationTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/InstanceScopeIsolationTest.kt`
- **What does it do?** Implements unit-focused test coverage around `InstanceScopeIsolationTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.AddTopicNodeCommand, com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.InstanceRegistryPort, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncErrorCode
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `InstanceScopeIsolationTest`, `FixedActiveInstanceRegistry`.
  - Key callable members include `scope guard allows mutation only for active instance`.
  - Scenario-focused test cases include: `scope guard allows mutation only for active instance`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `InstanceScopeIsolationTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `FixedActiveInstanceRegistry` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `scope guard allows mutation only for active instance` consumes no explicit parameters.
- **Outputs produced:**
  - `scope guard allows mutation only for active instance` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.InstanceRegistryPort`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`, `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.InstanceRegistryPort`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `InstanceScopeIsolationTest` (class, public (default))
- **Key public/internal functions:**
  - `scope guard allows mutation only for active instance() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/InstanceScopeIsolationTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *InstanceScopeIsolationTest`.

## InstanceSelectionPersistenceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/InstanceSelectionPersistenceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `InstanceSelectionPersistenceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `InstanceSelectionPersistenceTest`.
  - Key callable members include `last selected instance is restored when reopening same project`.
  - Scenario-focused test cases include: `last selected instance is restored when reopening same project`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `InstanceSelectionPersistenceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `last selected instance is restored when reopening same project` consumes no explicit parameters.
- **Outputs produced:**
  - `last selected instance is restored when reopening same project` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicGatewayResult`, `com.authord.mkdocs.ports.topic.TopicInstanceRef`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `InstanceSelectionPersistenceTest` (class, public (default))
- **Key public/internal functions:**
  - `last selected instance is restored when reopening same project() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/InstanceSelectionPersistenceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *InstanceSelectionPersistenceTest`.

## IntellijTestFixtures.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/IntellijTestFixtures.kt`
- **What does it do?** Implements unit-focused test coverage around `IntellijTestFixtures` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.actionSystem.ActionPlaces, com.intellij.openapi.actionSystem.ActionManager, com.intellij.openapi.actionSystem.ActionPopupMenu, com.intellij.openapi.actionSystem.ActionToolbar, com.intellij.openapi.actionSystem.AnAction, com.intellij.openapi.actionSystem.AnActionEvent, com.intellij.openapi.actionSystem.ActionGroup, com.intellij.openapi.actionSystem.CommonDataKeys
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `IntellijTestFixtures`, `ToolWindowFixture`.
  - Key callable members include `actionEvent`, `project`, `toolWindowFixture`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `IntellijTestFixtures` (object, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `ToolWindowFixture` (data class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `actionEvent` consumes action: AnAction, project: Project?, virtualFilePath: String? = null.
  - `project` consumes basePath: String? = "/tmp/project", locationHash: String = "project-hash", services: Map<Class<*>, Any> = emptyMap(.
  - `toolWindowFixture` consumes toolWindowId: String? = null.
- **Outputs produced:**
  - `actionEvent` returns AnActionEvent.
  - `project` returns Unit/implicit.
  - `toolWindowFixture` returns ToolWindowFixture.
- **Side effects:**
  - Filesystem reads/writes
  - IntelliJ UI state updates
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginCompositionRootTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/DocsScopeResolverTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/PluginCompositionRootTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownOpenPreviewListenerTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `IntellijTestFixtures` (object, public (default))
  - `ToolWindowFixture` (data class, public (default))
- **Key public/internal functions:**
  - `actionEvent(action: AnAction, project: Project?, virtualFilePath: String? = null) : AnActionEvent` (public (default))
  - `project(basePath: String? = "/tmp/project", locationHash: String = "project-hash", services: Map<Class<*>, Any> = emptyMap() : Unit/implicit` (public (default))
  - `toolWindowFixture(toolWindowId: String? = null) : ToolWindowFixture` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Tracks lifecycle/disposal boundaries; resources are expected to be released deterministically.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Suppress`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- `ToolWindowFixture` models structured data with fields: val toolWindow: ToolWindow, val removedAllCalls: MutableList<Boolean>, val addedComponents: MutableList<JComponent>.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/IntellijTestFixtures.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *IntellijTestFixtures`.

## MkDocsPreviewBrowserServiceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserServiceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `MkDocsPreviewBrowserServiceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: javax.swing.JPanel
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `MkDocsPreviewBrowserServiceTest`, `RecordingPreviewContent`.
  - Key callable members include `ensurePreviewContent returns a single instance per project service`, `markMarkdownActivated toggles activation and initializes preview surface`, `split owner takes preview component ownership and tool window regains it after split deactivation`, `shared browser deduplicates same route across owners but still navigates changed routes`, `force reload bypasses shared last url dedupe`, `resetLastLoadedUrl clears dedupe cache`, `reload gate suppresses duplicate non forced navigation after recent browser reload`, `force reload bypasses coalescing gate`.
  - Scenario-focused test cases include: `ensurePreviewContent returns a single instance per project service`, `markMarkdownActivated toggles activation and initializes preview surface`, `split owner takes preview component ownership and tool window regains it after split deactivation`, `shared browser deduplicates same route across owners but still navigates changed routes`, `force reload bypasses shared last url dedupe`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `MkDocsPreviewBrowserServiceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingPreviewContent` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `ensurePreviewContent returns a single instance per project service` consumes no explicit parameters.
  - `markMarkdownActivated toggles activation and initializes preview surface` consumes no explicit parameters.
  - `split owner takes preview component ownership and tool window regains it after split deactivation` consumes no explicit parameters.
  - `shared browser deduplicates same route across owners but still navigates changed routes` consumes no explicit parameters.
  - `force reload bypasses shared last url dedupe` consumes no explicit parameters.
- **Outputs produced:**
  - `ensurePreviewContent returns a single instance per project service` returns Unit/implicit.
  - `markMarkdownActivated toggles activation and initializes preview surface` returns Unit/implicit.
  - `split owner takes preview component ownership and tool window regains it after split deactivation` returns Unit/implicit.
  - `shared browser deduplicates same route across owners but still navigates changed routes` returns Unit/implicit.
  - `force reload bypasses shared last url dedupe` returns Unit/implicit.
- **Side effects:**
  - IntelliJ UI state updates
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactoryTest.kt`
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactoryTest.kt`. Downstream collaborators include local logic + stdlib.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MkDocsPreviewBrowserServiceTest` (class, public (default))
- **Key public/internal functions:**
  - `ensurePreviewContent returns a single instance per project service() : Unit/implicit` (public (default))
  - `markMarkdownActivated toggles activation and initializes preview surface() : Unit/implicit` (public (default))
  - `split owner takes preview component ownership and tool window regains it after split deactivation() : Unit/implicit` (public (default))
  - `shared browser deduplicates same route across owners but still navigates changed routes() : Unit/implicit` (public (default))
  - `force reload bypasses shared last url dedupe() : Unit/implicit` (public (default))
  - `resetLastLoadedUrl clears dedupe cache() : Unit/implicit` (public (default))
  - `reload gate suppresses duplicate non forced navigation after recent browser reload() : Unit/implicit` (public (default))
  - `force reload bypasses coalescing gate() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserServiceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *MkDocsPreviewBrowserServiceTest`.

## MkDocsProjectCreatorTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkDocsProjectCreatorTest.kt`
- **What does it do?** Implements unit-focused test coverage around `MkDocsProjectCreatorTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.runtime.CommandResult, com.authord.mkdocs.runtime.CommandRunner, com.authord.mkdocs.runtime.MkDocsYamlGateway, com.authord.mkdocs.runtime.UvExecutableProvider, com.authord.mkdocs.runtime.UvExecutableResult
  - External: java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `RecordingCommandRunner`, `StaticUvProvider`, `MkDocsProjectCreatorTest`.
  - Key callable members include `createProject resolves uv writes authord scaffold and nav in project root`, `createProject fails when project name is blank`, `createProject fails when uv resolution fails`.
  - Scenario-focused test cases include: `createProject resolves uv writes authord scaffold and nav in project root`, `createProject fails when project name is blank`, `createProject fails when uv resolution fails`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `RecordingCommandRunner` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `StaticUvProvider` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `MkDocsProjectCreatorTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `createProject resolves uv writes authord scaffold and nav in project root` consumes no explicit parameters.
  - `createProject fails when project name is blank` consumes no explicit parameters.
  - `createProject fails when uv resolution fails` consumes no explicit parameters.
- **Outputs produced:**
  - `createProject resolves uv writes authord scaffold and nav in project root` returns Unit/implicit.
  - `createProject fails when project name is blank` returns Unit/implicit.
  - `createProject fails when uv resolution fails` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - Process/runtime execution
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
  - `com.authord.mkdocs.runtime.CommandResult`
  - `com.authord.mkdocs.runtime.CommandRunner`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicGatewayResult`, `com.authord.mkdocs.ports.topic.TopicInstanceRef`, `com.authord.mkdocs.runtime.CommandResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MkDocsProjectCreatorTest` (class, public (default))
- **Key public/internal functions:**
  - `createProject resolves uv writes authord scaffold and nav in project root() : Unit/implicit` (public (default))
  - `createProject fails when project name is blank() : Unit/implicit` (public (default))
  - `createProject fails when uv resolution fails() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkDocsProjectCreatorTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *MkDocsProjectCreatorTest`.

## MkdocsConfigLocatorTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsConfigLocatorTest.kt`
- **What does it do?** Implements unit-focused test coverage around `MkdocsConfigLocatorTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `MkdocsConfigLocatorTest`.
  - Key callable members include `clearCacheBeforeEach`, `clearCacheAfterEach`, `findMkdocsConfig resolves project root config and prefers mkdocs yml over yaml`, `findMkdocsConfig discovers nested config when root config is absent`, `findMkdocsConfig supports underscore variants and prefers shallower config`, `findMkdocsConfig uses cache and requires invalidation to observe config creation`, `eligibility helpers validate markdown extension config presence and project boundary`.
  - Scenario-focused test cases include: `findMkdocsConfig resolves project root config and prefers mkdocs yml over yaml`, `findMkdocsConfig discovers nested config when root config is absent`, `findMkdocsConfig supports underscore variants and prefers shallower config`, `findMkdocsConfig uses cache and requires invalidation to observe config creation`, `eligibility helpers validate markdown extension config presence and project boundary`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `MkdocsConfigLocatorTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `clearCacheBeforeEach` consumes no explicit parameters.
  - `clearCacheAfterEach` consumes no explicit parameters.
  - `findMkdocsConfig resolves project root config and prefers mkdocs yml over yaml` consumes no explicit parameters.
  - `findMkdocsConfig discovers nested config when root config is absent` consumes no explicit parameters.
  - `findMkdocsConfig supports underscore variants and prefers shallower config` consumes no explicit parameters.
- **Outputs produced:**
  - `clearCacheBeforeEach` returns Unit/implicit.
  - `clearCacheAfterEach` returns Unit/implicit.
  - `findMkdocsConfig resolves project root config and prefers mkdocs yml over yaml` returns Unit/implicit.
  - `findMkdocsConfig discovers nested config when root config is absent` returns Unit/implicit.
  - `findMkdocsConfig supports underscore variants and prefers shallower config` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MkdocsConfigLocatorTest` (class, public (default))
- **Key public/internal functions:**
  - `clearCacheBeforeEach() : Unit/implicit` (public (default))
  - `clearCacheAfterEach() : Unit/implicit` (public (default))
  - `findMkdocsConfig resolves project root config and prefers mkdocs yml over yaml() : Unit/implicit` (public (default))
  - `findMkdocsConfig discovers nested config when root config is absent() : Unit/implicit` (public (default))
  - `findMkdocsConfig supports underscore variants and prefers shallower config() : Unit/implicit` (public (default))
  - `findMkdocsConfig uses cache and requires invalidation to observe config creation() : Unit/implicit` (public (default))
  - `eligibility helpers validate markdown extension config presence and project boundary() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@BeforeTest`, `@AfterTest`, `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsConfigLocatorTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *MkdocsConfigLocatorTest`.

## MkdocsScrollSyncEngineTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsScrollSyncEngineTest.kt`
- **What does it do?** Implements unit-focused test coverage around `MkdocsScrollSyncEngineTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.editor.event.DocumentEvent, com.intellij.openapi.editor.impl.DocumentImpl
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `MkdocsScrollSyncEngineTest`.
  - Key callable members include `source extraction omits markdown comments while preserving fenced code context`, `hard heading matching prefers stable heading ids with occurrences`, `global non crossing hard matching avoids A B A vs B A top to bottom jump`, `post dp acceptance guard drops ambiguous neighborhood matches`, `onEditorScroll requests dom snapshot once and reuses cached map for following scrolls`, `onEditorScroll applies pixel hysteresis to avoid jitter`, `source extraction captures code table image and heading anchors`, `recordDocumentChange marks dirty range and rebuild clears dirty flags`, plus `2` additional members.
  - Scenario-focused test cases include: `source extraction omits markdown comments while preserving fenced code context`, `hard heading matching prefers stable heading ids with occurrences`, `global non crossing hard matching avoids A B A vs B A top to bottom jump`, `post dp acceptance guard drops ambiguous neighborhood matches`, `onEditorScroll requests dom snapshot once and reuses cached map for following scrolls`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `MkdocsScrollSyncEngineTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `source extraction omits markdown comments while preserving fenced code context` consumes no explicit parameters.
  - `hard heading matching prefers stable heading ids with occurrences` consumes no explicit parameters.
  - `global non crossing hard matching avoids A B A vs B A top to bottom jump` consumes no explicit parameters.
  - `post dp acceptance guard drops ambiguous neighborhood matches` consumes no explicit parameters.
  - `onEditorScroll requests dom snapshot once and reuses cached map for following scrolls` consumes no explicit parameters.
- **Outputs produced:**
  - `source extraction omits markdown comments while preserving fenced code context` returns Unit/implicit.
  - `hard heading matching prefers stable heading ids with occurrences` returns Unit/implicit.
  - `global non crossing hard matching avoids A B A vs B A top to bottom jump` returns Unit/implicit.
  - `post dp acceptance guard drops ambiguous neighborhood matches` returns Unit/implicit.
  - `onEditorScroll requests dom snapshot once and reuses cached map for following scrolls` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MkdocsScrollSyncEngineTest` (class, public (default))
- **Key public/internal functions:**
  - `source extraction omits markdown comments while preserving fenced code context() : Unit/implicit` (public (default))
  - `hard heading matching prefers stable heading ids with occurrences() : Unit/implicit` (public (default))
  - `global non crossing hard matching avoids A B A vs B A top to bottom jump() : Unit/implicit` (public (default))
  - `post dp acceptance guard drops ambiguous neighborhood matches() : Unit/implicit` (public (default))
  - `onEditorScroll requests dom snapshot once and reuses cached map for following scrolls() : Unit/implicit` (public (default))
  - `onEditorScroll applies pixel hysteresis to avoid jitter() : Unit/implicit` (public (default))
  - `source extraction captures code table image and heading anchors() : Unit/implicit` (public (default))
  - `recordDocumentChange marks dirty range and rebuild clears dirty flags() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsScrollSyncEngineTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *MkdocsScrollSyncEngineTest`.

## MkdocsToolWindowFactoryTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactoryTest.kt`
- **What does it do?** Implements unit-focused test coverage around `MkdocsToolWindowFactoryTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.runtime.CommandResult, com.authord.mkdocs.runtime.UvExecutableResult
  - External: com.intellij.openapi.Disposable, com.intellij.ui.OnePixelSplitter, com.intellij.openapi.editor.impl.DocumentImpl, com.intellij.openapi.vfs.VirtualFile, com.intellij.openapi.vfs.VirtualFileSystem, com.intellij.openapi.vfs.newvfs.BulkFileListener, com.intellij.openapi.vfs.newvfs.events.VFileEvent, com.intellij.testFramework.LightVirtualFile
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `RecordingPreviewContent`, `NoOpTopicTreeUiService`, `StubVFileEvent`, `MkdocsToolWindowFactoryTest`.
  - Key callable members include `loadedUrls`, `scrolledYValues`, `scrollSyncTokens`, `setupPageLoadCount`, `triggerSetupProjectCreate`, `visit`, `create tool window content wires topic tree action and drag drop controllers`, `tool window applies instance selection through switch coordinator`, plus `37` additional members.
  - Scenario-focused test cases include: `create tool window content wires topic tree action and drag drop controllers`, `tool window applies instance selection through switch coordinator`, `create tool window content registers minimal shell panel`, `create tool window content includes topic tree component`, `create tool window content enters setup mode when mkdocs config is missing`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `RecordingPreviewContent` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `NoOpTopicTreeUiService` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `StubVFileEvent` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `MkdocsToolWindowFactoryTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `loadedUrls` consumes no explicit parameters.
  - `scrolledYValues` consumes no explicit parameters.
  - `scrollSyncTokens` consumes no explicit parameters.
  - `setupPageLoadCount` consumes no explicit parameters.
  - `triggerSetupProjectCreate` consumes projectName: String.
- **Outputs produced:**
  - `loadedUrls` returns List<String>.
  - `scrolledYValues` returns List<Double>.
  - `scrollSyncTokens` returns List<Long?>.
  - `setupPageLoadCount` returns Int.
  - `triggerSetupProjectCreate` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - IntelliJ UI state updates
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserServiceTest.kt`
- **What this file calls:**
  - `com.authord.mkdocs.runtime.CommandResult`
  - `com.authord.mkdocs.runtime.UvExecutableResult`
- **Upstream / downstream relationships:**
  - Upstream callers include `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/AuthordMarkdownSplitEditorProviderTest.kt`, `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkDocsPreviewBrowserServiceTest.kt`. Downstream collaborators include `com.authord.mkdocs.runtime.CommandResult`, `com.authord.mkdocs.runtime.UvExecutableResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MkdocsToolWindowFactoryTest` (class, public (default))
- **Key public/internal functions:**
  - `loadedUrls() : List<String>` (public (default))
  - `scrolledYValues() : List<Double>` (public (default))
  - `scrollSyncTokens() : List<Long?>` (public (default))
  - `setupPageLoadCount() : Int` (public (default))
  - `triggerSetupProjectCreate(projectName: String) : Unit/implicit` (public (default))
  - `visit(component: java.awt.Component) : Unit/implicit` (public (default))
  - `create tool window content wires topic tree action and drag drop controllers() : Unit/implicit` (public (default))
  - `tool window applies instance selection through switch coordinator() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Tracks lifecycle/disposal boundaries; resources are expected to be released deterministically.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- Model-centric declarations: `StubVFileEvent`.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- Contains auth/credential-sensitive terms; review storage, transport, and logging paths carefully.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MkdocsToolWindowFactoryTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Large callable surface area increases regression risk; consider characterization tests before large refactors.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *MkdocsToolWindowFactoryTest`.

## MultiInstanceReconciliationTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MultiInstanceReconciliationTest.kt`
- **What does it do?** Implements unit-focused test coverage around `MultiInstanceReconciliationTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicNavNode
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `MultiInstanceReconciliationTest`.
  - Key callable members include `reconciliation runs per instance with nav-first isolation`, `missing nav file remains validation issue without destructive changes`.
  - Scenario-focused test cases include: `reconciliation runs per instance with nav-first isolation`, `missing nav file remains validation issue without destructive changes`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `MultiInstanceReconciliationTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `reconciliation runs per instance with nav-first isolation` consumes no explicit parameters.
  - `missing nav file remains validation issue without destructive changes` consumes no explicit parameters.
- **Outputs produced:**
  - `reconciliation runs per instance with nav-first isolation` returns Unit/implicit.
  - `missing nav file remains validation issue without destructive changes` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
  - `com.authord.mkdocs.ports.topic.TopicNavNode`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`, `com.authord.mkdocs.ports.topic.TopicInstanceRef`, `com.authord.mkdocs.ports.topic.TopicNavNode`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MultiInstanceReconciliationTest` (class, public (default))
- **Key public/internal functions:**
  - `reconciliation runs per instance with nav-first isolation() : Unit/implicit` (public (default))
  - `missing nav file remains validation issue without destructive changes() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MultiInstanceReconciliationTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *MultiInstanceReconciliationTest`.

## MutationFeedbackPerformanceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MutationFeedbackPerformanceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `MutationFeedbackPerformanceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.TopicTreePort, com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.DocsFileGateway, com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.MkDocsConfigGateway, com.authord.mkdocs.ports.topic.TopicDeleteMode, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `MutationFeedbackPerformanceTest`, `FastTopicTreePort`, `FastConfigGateway`, `FastDocsFileGateway`.
  - Key callable members include `mutation feedback stays within 300ms p95 on local orchestration`.
  - Scenario-focused test cases include: `mutation feedback stays within 300ms p95 on local orchestration`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `MutationFeedbackPerformanceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `FastTopicTreePort` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `FastConfigGateway` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `FastDocsFileGateway` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `mutation feedback stays within 300ms p95 on local orchestration` consumes no explicit parameters.
- **Outputs produced:**
  - `mutation feedback stays within 300ms p95 on local orchestration` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.TopicTreePort`
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.DocsFileGateway`
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.TopicTreePort`, `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.DocsFileGateway`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MutationFeedbackPerformanceTest` (class, public (default))
- **Key public/internal functions:**
  - `mutation feedback stays within 300ms p95 on local orchestration() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MutationFeedbackPerformanceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *MutationFeedbackPerformanceTest`.

## MutationFlowIndependenceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MutationFlowIndependenceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `MutationFlowIndependenceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.InstanceRegistryPort, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncErrorCode, com.authord.mkdocs.ports.topic.TopicSyncOutcome, com.authord.mkdocs.ports.topic.TopicSyncTransaction, com.authord.mkdocs.ports.topic.TopicTreeCommand
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `MutationFlowIndependenceTest`, `RecordingOrchestrator`, `InMemoryRegistry`, `EmptyRegistry`.
  - Key callable members include `ui mutation dispatch works with preloaded tree context without startup watcher scaffolding`, `dispatch rejects when no active instance is available`.
  - Scenario-focused test cases include: `ui mutation dispatch works with preloaded tree context without startup watcher scaffolding`, `dispatch rejects when no active instance is available`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `MutationFlowIndependenceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingOrchestrator` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `InMemoryRegistry` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `EmptyRegistry` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `ui mutation dispatch works with preloaded tree context without startup watcher scaffolding` consumes no explicit parameters.
  - `dispatch rejects when no active instance is available` consumes no explicit parameters.
- **Outputs produced:**
  - `ui mutation dispatch works with preloaded tree context without startup watcher scaffolding` returns Unit/implicit.
  - `dispatch rejects when no active instance is available` returns Unit/implicit.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.InstanceRegistryPort`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.InstanceRegistryPort`, `com.authord.mkdocs.ports.topic.TopicGatewayResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `MutationFlowIndependenceTest` (class, public (default))
- **Key public/internal functions:**
  - `ui mutation dispatch works with preloaded tree context without startup watcher scaffolding() : Unit/implicit` (public (default))
  - `dispatch rejects when no active instance is available() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/MutationFlowIndependenceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *MutationFlowIndependenceTest`.

## OrchestratorLogicTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/OrchestratorLogicTest.kt`
- **What does it do?** Implements unit-focused test coverage around `OrchestratorLogicTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.TopicTreePort, com.authord.mkdocs.ports.topic.*
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `OrchestratorLogicTest`, `StubTopicTreePort`, `StubConfigGateway`, `StubDocsFileGateway`.
  - Key callable members include `apply generates file creation ops for AddChildTopicNodeCommand`, `apply generates file creation ops for AddTopicNodeCommand with null sourcePath (Derivation)`.
  - Scenario-focused test cases include: `apply generates file creation ops for AddChildTopicNodeCommand`, `apply generates file creation ops for AddTopicNodeCommand with null sourcePath (Derivation)`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `OrchestratorLogicTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `StubTopicTreePort` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `StubConfigGateway` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `StubDocsFileGateway` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `apply generates file creation ops for AddChildTopicNodeCommand` consumes no explicit parameters.
  - `apply generates file creation ops for AddTopicNodeCommand with null sourcePath (Derivation)` consumes no explicit parameters.
- **Outputs produced:**
  - `apply generates file creation ops for AddChildTopicNodeCommand` returns Unit/implicit.
  - `apply generates file creation ops for AddTopicNodeCommand with null sourcePath (Derivation)` returns Unit/implicit.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.TopicTreePort`
  - `com.authord.mkdocs.ports.topic.*`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.TopicTreePort`, `com.authord.mkdocs.ports.topic.*`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `OrchestratorLogicTest` (class, public (default))
  - `StubTopicTreePort` (class, public (default))
  - `StubConfigGateway` (class, public (default))
  - `StubDocsFileGateway` (class, public (default))
- **Key public/internal functions:**
  - `apply generates file creation ops for AddChildTopicNodeCommand() : Unit/implicit` (public (default))
  - `apply generates file creation ops for AddTopicNodeCommand with null sourcePath (Derivation)() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/OrchestratorLogicTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *OrchestratorLogicTest`.

## PluginDescriptorRegistrationTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginDescriptorRegistrationTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PluginDescriptorRegistrationTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PluginDescriptorRegistrationTest`.
  - Key callable members include `plugin descriptor registers platform dependency tool window and start action`, `markdown toolbar descriptor registers authord preview action in markdown toolbar`.
  - Scenario-focused test cases include: `plugin descriptor registers platform dependency tool window and start action`, `markdown toolbar descriptor registers authord preview action in markdown toolbar`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PluginDescriptorRegistrationTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `plugin descriptor registers platform dependency tool window and start action` consumes no explicit parameters.
  - `markdown toolbar descriptor registers authord preview action in markdown toolbar` consumes no explicit parameters.
- **Outputs produced:**
  - `plugin descriptor registers platform dependency tool window and start action` returns Unit/implicit.
  - `markdown toolbar descriptor registers authord preview action in markdown toolbar` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - IntelliJ UI state updates


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PluginDescriptorRegistrationTest` (class, public (default))
- **Key public/internal functions:**
  - `plugin descriptor registers platform dependency tool window and start action() : Unit/implicit` (public (default))
  - `markdown toolbar descriptor registers authord preview action in markdown toolbar() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginDescriptorRegistrationTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PluginDescriptorRegistrationTest`.

## PluginRuntimeIntegrationServiceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationServiceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PluginRuntimeIntegrationServiceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.runtime.BaseUrlDetector, com.authord.mkdocs.runtime.CommandResult, com.authord.mkdocs.runtime.CommandRunner, com.authord.mkdocs.runtime.ManagedProcessHandle, com.authord.mkdocs.runtime.MkdocsProcessManager, com.authord.mkdocs.runtime.ProcessLauncher, com.authord.mkdocs.runtime.UvBootstrapService, com.authord.mkdocs.ui.ActivationErrorPresenter
  - External: java.nio.file.Files, java.nio.file.attribute.FileTime, java.util.concurrent.TimeUnit, java.util.concurrent.atomic.AtomicInteger
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `LifecycleHandle`, `CountingProcessLauncher`, `SuccessCommandRunner`, `MutationReloadRecordingPreviewContent`, `PluginRuntimeIntegrationServiceTest`.
  - Key callable members include `startPreview fails when project path is unavailable`, `runtime integration preserves single-instance lifecycle through process manager`, `startPreview does not restart running runtime before first config fingerprint is known`, `startPreview reloads mkdocs config changes by restarting runtime`, `restartPreview restarts runtime process and re-runs activation flow`, `restartPreview preserves current route after runtime restart`, `dispose stops running runtime process for project`, `project startup output provider reads value from project user data`, plus `20` additional members.
  - Scenario-focused test cases include: `startPreview fails when project path is unavailable`, `runtime integration preserves single-instance lifecycle through process manager`, `startPreview does not restart running runtime before first config fingerprint is known`, `startPreview reloads mkdocs config changes by restarting runtime`, `restartPreview restarts runtime process and re-runs activation flow`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `LifecycleHandle` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `CountingProcessLauncher` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `SuccessCommandRunner` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `MutationReloadRecordingPreviewContent` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `PluginRuntimeIntegrationServiceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `startPreview fails when project path is unavailable` consumes no explicit parameters.
  - `runtime integration preserves single-instance lifecycle through process manager` consumes no explicit parameters.
  - `startPreview does not restart running runtime before first config fingerprint is known` consumes no explicit parameters.
  - `startPreview reloads mkdocs config changes by restarting runtime` consumes no explicit parameters.
  - `restartPreview restarts runtime process and re-runs activation flow` consumes no explicit parameters.
- **Outputs produced:**
  - `startPreview fails when project path is unavailable` returns Unit/implicit.
  - `runtime integration preserves single-instance lifecycle through process manager` returns Unit/implicit.
  - `startPreview does not restart running runtime before first config fingerprint is known` returns Unit/implicit.
  - `startPreview reloads mkdocs config changes by restarting runtime` returns Unit/implicit.
  - `restartPreview restarts runtime process and re-runs activation flow` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - Process/runtime execution
  - IntelliJ UI state updates
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.runtime.BaseUrlDetector`
  - `com.authord.mkdocs.runtime.CommandResult`
  - `com.authord.mkdocs.runtime.CommandRunner`
  - `com.authord.mkdocs.runtime.ManagedProcessHandle`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.runtime.BaseUrlDetector`, `com.authord.mkdocs.runtime.CommandResult`, `com.authord.mkdocs.runtime.CommandRunner`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PluginRuntimeIntegrationServiceTest` (class, public (default))
- **Key public/internal functions:**
  - `startPreview fails when project path is unavailable() : Unit/implicit` (public (default))
  - `runtime integration preserves single-instance lifecycle through process manager() : Unit/implicit` (public (default))
  - `startPreview does not restart running runtime before first config fingerprint is known() : Unit/implicit` (public (default))
  - `startPreview reloads mkdocs config changes by restarting runtime() : Unit/implicit` (public (default))
  - `restartPreview restarts runtime process and re-runs activation flow() : Unit/implicit` (public (default))
  - `restartPreview preserves current route after runtime restart() : Unit/implicit` (public (default))
  - `dispose stops running runtime process for project() : Unit/implicit` (public (default))
  - `project startup output provider reads value from project user data() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Tracks lifecycle/disposal boundaries; resources are expected to be released deterministically.
- Contains coroutine-aware execution paths and cancellation-sensitive operations.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PluginRuntimeIntegrationServiceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Large callable surface area increases regression risk; consider characterization tests before large refactors.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PluginRuntimeIntegrationServiceTest`.

## PreviewCompatibilityCriteriaTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewCompatibilityCriteriaTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PreviewCompatibilityCriteriaTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PreviewCompatibilityCriteriaTest`.
  - Key callable members include `classifier maps high-severity rubric categories to high`, `release gate blocks unresolved high-severity compatibility regressions`, `release gate passes when high-severity findings are resolved`.
  - Scenario-focused test cases include: `classifier maps high-severity rubric categories to high`, `release gate blocks unresolved high-severity compatibility regressions`, `release gate passes when high-severity findings are resolved`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PreviewCompatibilityCriteriaTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `classifier maps high-severity rubric categories to high` consumes no explicit parameters.
  - `release gate blocks unresolved high-severity compatibility regressions` consumes no explicit parameters.
  - `release gate passes when high-severity findings are resolved` consumes no explicit parameters.
- **Outputs produced:**
  - `classifier maps high-severity rubric categories to high` returns Unit/implicit.
  - `release gate blocks unresolved high-severity compatibility regressions` returns Unit/implicit.
  - `release gate passes when high-severity findings are resolved` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewCompatibilityCriteriaTest` (class, public (default))
- **Key public/internal functions:**
  - `classifier maps high-severity rubric categories to high() : Unit/implicit` (public (default))
  - `release gate blocks unresolved high-severity compatibility regressions() : Unit/implicit` (public (default))
  - `release gate passes when high-severity findings are resolved() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewCompatibilityCriteriaTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PreviewCompatibilityCriteriaTest`.

## PreviewErrorExcerptFormatterTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewErrorExcerptFormatterTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PreviewErrorExcerptFormatterTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PreviewErrorExcerptFormatterTest`.
  - Key callable members include `extracts stderr tail when available`, `falls back to details section when tails are absent`, `truncates long excerpts`.
  - Scenario-focused test cases include: `extracts stderr tail when available`, `falls back to details section when tails are absent`, `truncates long excerpts`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PreviewErrorExcerptFormatterTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `extracts stderr tail when available` consumes no explicit parameters.
  - `falls back to details section when tails are absent` consumes no explicit parameters.
  - `truncates long excerpts` consumes no explicit parameters.
- **Outputs produced:**
  - `extracts stderr tail when available` returns Unit/implicit.
  - `falls back to details section when tails are absent` returns Unit/implicit.
  - `truncates long excerpts` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewErrorExcerptFormatterTest` (class, public (default))
- **Key public/internal functions:**
  - `extracts stderr tail when available() : Unit/implicit` (public (default))
  - `falls back to details section when tails are absent() : Unit/implicit` (public (default))
  - `truncates long excerpts() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- Model-centric declarations: `PreviewErrorExcerptFormatterTest`.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewErrorExcerptFormatterTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PreviewErrorExcerptFormatterTest`.

## PreviewFailureNotifierTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewFailureNotifierTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PreviewFailureNotifierTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PreviewFailureNotifierTest`.
  - Key callable members include `clearDedupeState`, `notification dedupe suppresses repeated identical failures`.
  - Scenario-focused test cases include: `notification dedupe suppresses repeated identical failures`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PreviewFailureNotifierTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `clearDedupeState` consumes no explicit parameters.
  - `notification dedupe suppresses repeated identical failures` consumes no explicit parameters.
- **Outputs produced:**
  - `clearDedupeState` returns Unit/implicit.
  - `notification dedupe suppresses repeated identical failures` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewFailureNotifierTest` (class, public (default))
- **Key public/internal functions:**
  - `clearDedupeState() : Unit/implicit` (public (default))
  - `notification dedupe suppresses repeated identical failures() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@BeforeTest`, `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewFailureNotifierTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PreviewFailureNotifierTest`.

## PreviewNavigationPolicyTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewNavigationPolicyTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PreviewNavigationPolicyTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PreviewNavigationPolicyTest`.
  - Key callable members include `allows local preview urls in main frame`, `blocks non-local main-frame navigation and opens system browser`, `blocks non-local navigation without user gesture and keeps browser in-pane`, `allows non-main-frame navigation to avoid iframe breakage`, `allows browser internal schemes`.
  - Scenario-focused test cases include: `allows local preview urls in main frame`, `blocks non-local main-frame navigation and opens system browser`, `blocks non-local navigation without user gesture and keeps browser in-pane`, `allows non-main-frame navigation to avoid iframe breakage`, `allows browser internal schemes`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PreviewNavigationPolicyTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `allows local preview urls in main frame` consumes no explicit parameters.
  - `blocks non-local main-frame navigation and opens system browser` consumes no explicit parameters.
  - `blocks non-local navigation without user gesture and keeps browser in-pane` consumes no explicit parameters.
  - `allows non-main-frame navigation to avoid iframe breakage` consumes no explicit parameters.
  - `allows browser internal schemes` consumes no explicit parameters.
- **Outputs produced:**
  - `allows local preview urls in main frame` returns Unit/implicit.
  - `blocks non-local main-frame navigation and opens system browser` returns Unit/implicit.
  - `blocks non-local navigation without user gesture and keeps browser in-pane` returns Unit/implicit.
  - `allows non-main-frame navigation to avoid iframe breakage` returns Unit/implicit.
  - `allows browser internal schemes` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewNavigationPolicyTest` (class, public (default))
- **Key public/internal functions:**
  - `allows local preview urls in main frame() : Unit/implicit` (public (default))
  - `blocks non-local main-frame navigation and opens system browser() : Unit/implicit` (public (default))
  - `blocks non-local navigation without user gesture and keeps browser in-pane() : Unit/implicit` (public (default))
  - `allows non-main-frame navigation to avoid iframe breakage() : Unit/implicit` (public (default))
  - `allows browser internal schemes() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewNavigationPolicyTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PreviewNavigationPolicyTest`.

## PreviewResultReporterTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewResultReporterTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PreviewResultReporterTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ui.ActivationFailureReason, com.authord.mkdocs.ui.ActivationResult
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PreviewResultReporterTest`.
  - Key callable members include `formats started message for successful activation`, `formats already-running message for successful reuse`, `formats failure message from activation details`, `formats failure message with truncated exact error excerpt`, `formats fallback failure message when activation message is blank`, `formats success message with unknown url when preview url is blank`, `failure reporting path remains invokable for project notifications`, `success reporting path remains invokable without notification dispatch`.
  - Scenario-focused test cases include: `formats started message for successful activation`, `formats already-running message for successful reuse`, `formats failure message from activation details`, `formats failure message with truncated exact error excerpt`, `formats fallback failure message when activation message is blank`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PreviewResultReporterTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `formats started message for successful activation` consumes no explicit parameters.
  - `formats already-running message for successful reuse` consumes no explicit parameters.
  - `formats failure message from activation details` consumes no explicit parameters.
  - `formats failure message with truncated exact error excerpt` consumes no explicit parameters.
  - `formats fallback failure message when activation message is blank` consumes no explicit parameters.
- **Outputs produced:**
  - `formats started message for successful activation` returns Unit/implicit.
  - `formats already-running message for successful reuse` returns Unit/implicit.
  - `formats failure message from activation details` returns Unit/implicit.
  - `formats failure message with truncated exact error excerpt` returns Unit/implicit.
  - `formats fallback failure message when activation message is blank` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ui.ActivationFailureReason`
  - `com.authord.mkdocs.ui.ActivationResult`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ui.ActivationFailureReason`, `com.authord.mkdocs.ui.ActivationResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewResultReporterTest` (class, public (default))
- **Key public/internal functions:**
  - `formats started message for successful activation() : Unit/implicit` (public (default))
  - `formats already-running message for successful reuse() : Unit/implicit` (public (default))
  - `formats failure message from activation details() : Unit/implicit` (public (default))
  - `formats failure message with truncated exact error excerpt() : Unit/implicit` (public (default))
  - `formats fallback failure message when activation message is blank() : Unit/implicit` (public (default))
  - `formats success message with unknown url when preview url is blank() : Unit/implicit` (public (default))
  - `failure reporting path remains invokable for project notifications() : Unit/implicit` (public (default))
  - `success reporting path remains invokable without notification dispatch() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewResultReporterTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PreviewResultReporterTest`.

## PreviewRouteLoadCoordinatorTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouteLoadCoordinatorTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PreviewRouteLoadCoordinatorTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PreviewRouteLoadCoordinatorTest`.
  - Key callable members include `loads once when route is immediately ready`, `emits ordered flow states when route resolves successfully`, `loads once after transient 404 window when route eventually becomes ready`, `warns once when route never becomes ready`, `loads html fallback route when directory style route stays unavailable`, `dirty livereload mode waits with extended window and resolves delayed route`, `dirty livereload mode replays pending route after initial unavailability`, `dirty livereload mode nudges config once per probe cycle to avoid rebuild loops`.
  - Scenario-focused test cases include: `loads once when route is immediately ready`, `emits ordered flow states when route resolves successfully`, `loads once after transient 404 window when route eventually becomes ready`, `warns once when route never becomes ready`, `loads html fallback route when directory style route stays unavailable`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PreviewRouteLoadCoordinatorTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `loads once when route is immediately ready` consumes no explicit parameters.
  - `emits ordered flow states when route resolves successfully` consumes no explicit parameters.
  - `loads once after transient 404 window when route eventually becomes ready` consumes no explicit parameters.
  - `warns once when route never becomes ready` consumes no explicit parameters.
  - `loads html fallback route when directory style route stays unavailable` consumes no explicit parameters.
- **Outputs produced:**
  - `loads once when route is immediately ready` returns Unit/implicit.
  - `emits ordered flow states when route resolves successfully` returns Unit/implicit.
  - `loads once after transient 404 window when route eventually becomes ready` returns Unit/implicit.
  - `warns once when route never becomes ready` returns Unit/implicit.
  - `loads html fallback route when directory style route stays unavailable` returns Unit/implicit.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewRouteLoadCoordinatorTest` (class, public (default))
- **Key public/internal functions:**
  - `loads once when route is immediately ready() : Unit/implicit` (public (default))
  - `emits ordered flow states when route resolves successfully() : Unit/implicit` (public (default))
  - `loads once after transient 404 window when route eventually becomes ready() : Unit/implicit` (public (default))
  - `warns once when route never becomes ready() : Unit/implicit` (public (default))
  - `loads html fallback route when directory style route stays unavailable() : Unit/implicit` (public (default))
  - `dirty livereload mode waits with extended window and resolves delayed route() : Unit/implicit` (public (default))
  - `dirty livereload mode replays pending route after initial unavailability() : Unit/implicit` (public (default))
  - `dirty livereload mode nudges config once per probe cycle to avoid rebuild loops() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouteLoadCoordinatorTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PreviewRouteLoadCoordinatorTest`.

## PreviewRouterServiceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouterServiceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PreviewRouterServiceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PreviewRouterServiceTest`.
  - Key callable members include `router routes docs scoped markdown to authord`, `router routes non docs markdown to intellij`, `router routes ambiguous scope markdown to intellij`, `router routes misconfigured scope markdown to intellij`, `router honors failure bypass for docs scoped markdown`.
  - Scenario-focused test cases include: `router routes docs scoped markdown to authord`, `router routes non docs markdown to intellij`, `router routes ambiguous scope markdown to intellij`, `router routes misconfigured scope markdown to intellij`, `router honors failure bypass for docs scoped markdown`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PreviewRouterServiceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `router routes docs scoped markdown to authord` consumes no explicit parameters.
  - `router routes non docs markdown to intellij` consumes no explicit parameters.
  - `router routes ambiguous scope markdown to intellij` consumes no explicit parameters.
  - `router routes misconfigured scope markdown to intellij` consumes no explicit parameters.
  - `router honors failure bypass for docs scoped markdown` consumes no explicit parameters.
- **Outputs produced:**
  - `router routes docs scoped markdown to authord` returns Unit/implicit.
  - `router routes non docs markdown to intellij` returns Unit/implicit.
  - `router routes ambiguous scope markdown to intellij` returns Unit/implicit.
  - `router routes misconfigured scope markdown to intellij` returns Unit/implicit.
  - `router honors failure bypass for docs scoped markdown` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewRouterServiceTest` (class, public (default))
- **Key public/internal functions:**
  - `router routes docs scoped markdown to authord() : Unit/implicit` (public (default))
  - `router routes non docs markdown to intellij() : Unit/implicit` (public (default))
  - `router routes ambiguous scope markdown to intellij() : Unit/implicit` (public (default))
  - `router routes misconfigured scope markdown to intellij() : Unit/implicit` (public (default))
  - `router honors failure bypass for docs scoped markdown() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewRouterServiceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PreviewRouterServiceTest`.

## PreviewStartupFailureClassifierTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureClassifierTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PreviewStartupFailureClassifierTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PreviewStartupFailureClassifierTest`.
  - Key callable members include `classifies missing material dependency`, `classifies mkdocs config parse errors`, `classifies early process exit`, `classifies readiness timeout`, `classifies port bind failures`, `classifies unknown startup failure as unknown`.
  - Scenario-focused test cases include: `classifies missing material dependency`, `classifies mkdocs config parse errors`, `classifies early process exit`, `classifies readiness timeout`, `classifies port bind failures`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PreviewStartupFailureClassifierTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `classifies missing material dependency` consumes no explicit parameters.
  - `classifies mkdocs config parse errors` consumes no explicit parameters.
  - `classifies early process exit` consumes no explicit parameters.
  - `classifies readiness timeout` consumes no explicit parameters.
  - `classifies port bind failures` consumes no explicit parameters.
- **Outputs produced:**
  - `classifies missing material dependency` returns Unit/implicit.
  - `classifies mkdocs config parse errors` returns Unit/implicit.
  - `classifies early process exit` returns Unit/implicit.
  - `classifies readiness timeout` returns Unit/implicit.
  - `classifies port bind failures` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewStartupFailureClassifierTest` (class, public (default))
- **Key public/internal functions:**
  - `classifies missing material dependency() : Unit/implicit` (public (default))
  - `classifies mkdocs config parse errors() : Unit/implicit` (public (default))
  - `classifies early process exit() : Unit/implicit` (public (default))
  - `classifies readiness timeout() : Unit/implicit` (public (default))
  - `classifies port bind failures() : Unit/implicit` (public (default))
  - `classifies unknown startup failure as unknown() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureClassifierTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PreviewStartupFailureClassifierTest`.

## PreviewStartupFailureHandlerTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandlerTest.kt`
- **What does it do?** Implements unit-focused test coverage around `PreviewStartupFailureHandlerTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ui.ActivationFailureReason, com.authord.mkdocs.ui.ActivationResult
  - External: java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PreviewStartupFailureHandlerTest`.
  - Key callable members include `startup failure applies bypass and triggers fallback for docs scoped markdown`, `retry action clears bypass before retrying`.
  - Scenario-focused test cases include: `startup failure applies bypass and triggers fallback for docs scoped markdown`, `retry action clears bypass before retrying`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `PreviewStartupFailureHandlerTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `startup failure applies bypass and triggers fallback for docs scoped markdown` consumes no explicit parameters.
  - `retry action clears bypass before retrying` consumes no explicit parameters.
- **Outputs produced:**
  - `startup failure applies bypass and triggers fallback for docs scoped markdown` returns Unit/implicit.
  - `retry action clears bypass before retrying` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ui.ActivationFailureReason`
  - `com.authord.mkdocs.ui.ActivationResult`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ui.ActivationFailureReason`, `com.authord.mkdocs.ui.ActivationResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PreviewStartupFailureHandlerTest` (class, public (default))
- **Key public/internal functions:**
  - `startup failure applies bypass and triggers fallback for docs scoped markdown() : Unit/implicit` (public (default))
  - `retry action clears bypass before retrying() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/PreviewStartupFailureHandlerTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PreviewStartupFailureHandlerTest`.

## ReloadCoalescingGateTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ReloadCoalescingGateTest.kt`
- **What does it do?** Implements unit-focused test coverage around `ReloadCoalescingGateTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `ReloadCoalescingGateTest`.
  - Key callable members include `first reload is never suppressed and subsequent reload inside window is suppressed`, `reload is allowed once coalescing window elapses`, `non-positive coalescing window disables suppression`.
  - Scenario-focused test cases include: `first reload is never suppressed and subsequent reload inside window is suppressed`, `reload is allowed once coalescing window elapses`, `non-positive coalescing window disables suppression`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `ReloadCoalescingGateTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `first reload is never suppressed and subsequent reload inside window is suppressed` consumes no explicit parameters.
  - `reload is allowed once coalescing window elapses` consumes no explicit parameters.
  - `non-positive coalescing window disables suppression` consumes no explicit parameters.
- **Outputs produced:**
  - `first reload is never suppressed and subsequent reload inside window is suppressed` returns Unit/implicit.
  - `reload is allowed once coalescing window elapses` returns Unit/implicit.
  - `non-positive coalescing window disables suppression` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `ReloadCoalescingGateTest` (class, public (default))
- **Key public/internal functions:**
  - `first reload is never suppressed and subsequent reload inside window is suppressed() : Unit/implicit` (public (default))
  - `reload is allowed once coalescing window elapses() : Unit/implicit` (public (default))
  - `non-positive coalescing window disables suppression() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/ReloadCoalescingGateTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *ReloadCoalescingGateTest`.

## SetupPageRendererTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/SetupPageRendererTest.kt`
- **What does it do?** Implements unit-focused test coverage around `SetupPageRendererTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `SetupPageRendererTest`.
  - Key callable members include `render contains setup empty state actions and bridge injection`.
  - Scenario-focused test cases include: `render contains setup empty state actions and bridge injection`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `SetupPageRendererTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `render contains setup empty state actions and bridge injection` consumes no explicit parameters.
- **Outputs produced:**
  - `render contains setup empty state actions and bridge injection` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `SetupPageRendererTest` (class, public (default))
- **Key public/internal functions:**
  - `render contains setup empty state actions and bridge injection() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/SetupPageRendererTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *SetupPageRendererTest`.

## StartMkdocsActionInvocationTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsActionInvocationTest.kt`
- **What does it do?** Implements unit-focused test coverage around `StartMkdocsActionInvocationTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.flags.FeatureFlagPolicy
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `StartMkdocsActionInvocationTest`.
  - Key callable members include `actionPerformed is no-op when project context is missing`, `actionPerformed delegates start request to runtime integration service`, `actionPerformed remains invokable while runtime is already running`, `actionPerformed does not start runtime when action is not startable`, `actionPerformed blocks start when compatibility release gate is blocked`, `actionPerformed entrypoint handles null project event`, `actionPerformed async branch invokes success callback`.
  - Scenario-focused test cases include: `actionPerformed is no-op when project context is missing`, `actionPerformed delegates start request to runtime integration service`, `actionPerformed remains invokable while runtime is already running`, `actionPerformed does not start runtime when action is not startable`, `actionPerformed blocks start when compatibility release gate is blocked`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `StartMkdocsActionInvocationTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `actionPerformed is no-op when project context is missing` consumes no explicit parameters.
  - `actionPerformed delegates start request to runtime integration service` consumes no explicit parameters.
  - `actionPerformed remains invokable while runtime is already running` consumes no explicit parameters.
  - `actionPerformed does not start runtime when action is not startable` consumes no explicit parameters.
  - `actionPerformed blocks start when compatibility release gate is blocked` consumes no explicit parameters.
- **Outputs produced:**
  - `actionPerformed is no-op when project context is missing` returns Unit/implicit.
  - `actionPerformed delegates start request to runtime integration service` returns Unit/implicit.
  - `actionPerformed remains invokable while runtime is already running` returns Unit/implicit.
  - `actionPerformed does not start runtime when action is not startable` returns Unit/implicit.
  - `actionPerformed blocks start when compatibility release gate is blocked` returns Unit/implicit.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.flags.FeatureFlagPolicy`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.flags.FeatureFlagPolicy`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `StartMkdocsActionInvocationTest` (class, public (default))
- **Key public/internal functions:**
  - `actionPerformed is no-op when project context is missing() : Unit/implicit` (public (default))
  - `actionPerformed delegates start request to runtime integration service() : Unit/implicit` (public (default))
  - `actionPerformed remains invokable while runtime is already running() : Unit/implicit` (public (default))
  - `actionPerformed does not start runtime when action is not startable() : Unit/implicit` (public (default))
  - `actionPerformed blocks start when compatibility release gate is blocked() : Unit/implicit` (public (default))
  - `actionPerformed entrypoint handles null project event() : Unit/implicit` (public (default))
  - `actionPerformed async branch invokes success callback() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsActionInvocationTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *StartMkdocsActionInvocationTest`.

## StartMkdocsActionPresentationTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsActionPresentationTest.kt`
- **What does it do?** Implements unit-focused test coverage around `StartMkdocsActionPresentationTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.flags.FeatureFlagPolicy
  - External: com.intellij.openapi.actionSystem.ActionUpdateThread
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `StartMkdocsActionPresentationTest`.
  - Key callable members include `action update thread is background thread`, `update hides action when no project is available`, `update keeps action visible and enabled when runtime can start`, `update keeps action enabled while preview runtime is already running`, `update disables action when feature policy blocks mvp execution`, `update applies presentation flags through action event`, `default resolver path reads runtime service from project container`.
  - Scenario-focused test cases include: `action update thread is background thread`, `update hides action when no project is available`, `update keeps action visible and enabled when runtime can start`, `update keeps action enabled while preview runtime is already running`, `update disables action when feature policy blocks mvp execution`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `StartMkdocsActionPresentationTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `action update thread is background thread` consumes no explicit parameters.
  - `update hides action when no project is available` consumes no explicit parameters.
  - `update keeps action visible and enabled when runtime can start` consumes no explicit parameters.
  - `update keeps action enabled while preview runtime is already running` consumes no explicit parameters.
  - `update disables action when feature policy blocks mvp execution` consumes no explicit parameters.
- **Outputs produced:**
  - `action update thread is background thread` returns Unit/implicit.
  - `update hides action when no project is available` returns Unit/implicit.
  - `update keeps action visible and enabled when runtime can start` returns Unit/implicit.
  - `update keeps action enabled while preview runtime is already running` returns Unit/implicit.
  - `update disables action when feature policy blocks mvp execution` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.flags.FeatureFlagPolicy`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.flags.FeatureFlagPolicy`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `StartMkdocsActionPresentationTest` (class, public (default))
- **Key public/internal functions:**
  - `action update thread is background thread() : Unit/implicit` (public (default))
  - `update hides action when no project is available() : Unit/implicit` (public (default))
  - `update keeps action visible and enabled when runtime can start() : Unit/implicit` (public (default))
  - `update keeps action enabled while preview runtime is already running() : Unit/implicit` (public (default))
  - `update disables action when feature policy blocks mvp execution() : Unit/implicit` (public (default))
  - `update applies presentation flags through action event() : Unit/implicit` (public (default))
  - `default resolver path reads runtime service from project container() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsActionPresentationTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *StartMkdocsActionPresentationTest`.

## StartMkdocsMarkdownToolbarActionTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsMarkdownToolbarActionTest.kt`
- **What does it do?** Implements unit-focused test coverage around `StartMkdocsMarkdownToolbarActionTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: com.intellij.openapi.actionSystem.ActionUpdateThread, java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `StartMkdocsMarkdownToolbarActionTest`.
  - Key callable members include `toolbar action update thread is background thread`, `update is visible and enabled in markdown context when preview can start`, `update is hidden and disabled in non-markdown context`, `actionPerformed delegates to existing start pipeline for markdown context`, `actionPerformed is no-op outside markdown context`, `actionPerformed opens authord setup mode but does not start runtime when mkdocs config is missing`.
  - Scenario-focused test cases include: `toolbar action update thread is background thread`, `update is visible and enabled in markdown context when preview can start`, `update is hidden and disabled in non-markdown context`, `actionPerformed delegates to existing start pipeline for markdown context`, `actionPerformed is no-op outside markdown context`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `StartMkdocsMarkdownToolbarActionTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `toolbar action update thread is background thread` consumes no explicit parameters.
  - `update is visible and enabled in markdown context when preview can start` consumes no explicit parameters.
  - `update is hidden and disabled in non-markdown context` consumes no explicit parameters.
  - `actionPerformed delegates to existing start pipeline for markdown context` consumes no explicit parameters.
  - `actionPerformed is no-op outside markdown context` consumes no explicit parameters.
- **Outputs produced:**
  - `toolbar action update thread is background thread` returns Unit/implicit.
  - `update is visible and enabled in markdown context when preview can start` returns Unit/implicit.
  - `update is hidden and disabled in non-markdown context` returns Unit/implicit.
  - `actionPerformed delegates to existing start pipeline for markdown context` returns Unit/implicit.
  - `actionPerformed is no-op outside markdown context` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `StartMkdocsMarkdownToolbarActionTest` (class, public (default))
- **Key public/internal functions:**
  - `toolbar action update thread is background thread() : Unit/implicit` (public (default))
  - `update is visible and enabled in markdown context when preview can start() : Unit/implicit` (public (default))
  - `update is hidden and disabled in non-markdown context() : Unit/implicit` (public (default))
  - `actionPerformed delegates to existing start pipeline for markdown context() : Unit/implicit` (public (default))
  - `actionPerformed is no-op outside markdown context() : Unit/implicit` (public (default))
  - `actionPerformed opens authord setup mode but does not start runtime when mkdocs config is missing() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartMkdocsMarkdownToolbarActionTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *StartMkdocsMarkdownToolbarActionTest`.

## StartupReconciliationPerformanceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationPerformanceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `StartupReconciliationPerformanceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.TopicNavNode
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `StartupReconciliationPerformanceTest`.
  - Key callable members include `startup reconciliation meets p95 threshold for reference dataset`.
  - Scenario-focused test cases include: `startup reconciliation meets p95 threshold for reference dataset`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `StartupReconciliationPerformanceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `startup reconciliation meets p95 threshold for reference dataset` consumes no explicit parameters.
- **Outputs produced:**
  - `startup reconciliation meets p95 threshold for reference dataset` returns Unit/implicit.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
  - `com.authord.mkdocs.ports.topic.TopicNavNode`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`, `com.authord.mkdocs.ports.topic.TopicNavNode`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `StartupReconciliationPerformanceTest` (class, public (default))
- **Key public/internal functions:**
  - `startup reconciliation meets p95 threshold for reference dataset() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationPerformanceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *StartupReconciliationPerformanceTest`.

## StartupReconciliationPolicyTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationPolicyTest.kt`
- **What does it do?** Implements unit-focused test coverage around `StartupReconciliationPolicyTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.topic.TopicTreeValidationIssueType, com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.TopicNavNode
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `StartupReconciliationPolicyTest`.
  - Key callable members include `applies nav-first reconciliation with non-destructive policy`, `keeps fallback mode deterministic when nav is missing`.
  - Scenario-focused test cases include: `applies nav-first reconciliation with non-destructive policy`, `keeps fallback mode deterministic when nav is missing`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `StartupReconciliationPolicyTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `applies nav-first reconciliation with non-destructive policy` consumes no explicit parameters.
  - `keeps fallback mode deterministic when nav is missing` consumes no explicit parameters.
- **Outputs produced:**
  - `applies nav-first reconciliation with non-destructive policy` returns Unit/implicit.
  - `keeps fallback mode deterministic when nav is missing` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.topic.TopicTreeValidationIssueType`
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
  - `com.authord.mkdocs.ports.topic.TopicNavNode`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.topic.TopicTreeValidationIssueType`, `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`, `com.authord.mkdocs.ports.topic.TopicNavNode`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `StartupReconciliationPolicyTest` (class, public (default))
- **Key public/internal functions:**
  - `applies nav-first reconciliation with non-destructive policy() : Unit/implicit` (public (default))
  - `keeps fallback mode deterministic when nav is missing() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/StartupReconciliationPolicyTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *StartupReconciliationPolicyTest`.

## SystemRuntimeAdaptersTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/SystemRuntimeAdaptersTest.kt`
- **What does it do?** Implements unit-focused test coverage around `SystemRuntimeAdaptersTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: java.io.ByteArrayInputStream, java.io.ByteArrayOutputStream, java.io.InputStream, java.io.OutputStream, java.util.concurrent.TimeUnit
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `FakeProcess`, `RecordingProcessFactory`, `RecordingShutdownHookRegistrar`, `SystemRuntimeAdaptersTest`.
  - Key callable members include `command runner executes command and captures merged output`, `command runner returns failure envelope when process start throws`, `command runner times out long-running command and terminates process`, `process launcher captures startup output from process stream`, `process launcher returns empty startup output when none is emitted`, `process launcher registers and unregisters shutdown hook around handle lifecycle`, `registered shutdown hook forcibly stops process when invoked`, `stop forcibly destroys process when graceful shutdown times out`.
  - Scenario-focused test cases include: `command runner executes command and captures merged output`, `command runner returns failure envelope when process start throws`, `command runner times out long-running command and terminates process`, `process launcher captures startup output from process stream`, `process launcher returns empty startup output when none is emitted`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `FakeProcess` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingProcessFactory` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingShutdownHookRegistrar` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `SystemRuntimeAdaptersTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `command runner executes command and captures merged output` consumes no explicit parameters.
  - `command runner returns failure envelope when process start throws` consumes no explicit parameters.
  - `command runner times out long-running command and terminates process` consumes no explicit parameters.
  - `process launcher captures startup output from process stream` consumes no explicit parameters.
  - `process launcher returns empty startup output when none is emitted` consumes no explicit parameters.
- **Outputs produced:**
  - `command runner executes command and captures merged output` returns Unit/implicit.
  - `command runner returns failure envelope when process start throws` returns Unit/implicit.
  - `command runner times out long-running command and terminates process` returns Unit/implicit.
  - `process launcher captures startup output from process stream` returns Unit/implicit.
  - `process launcher returns empty startup output when none is emitted` returns Unit/implicit.
- **Side effects:**
  - Process/runtime execution
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `SystemRuntimeAdaptersTest` (class, public (default))
- **Key public/internal functions:**
  - `command runner executes command and captures merged output() : Unit/implicit` (public (default))
  - `command runner returns failure envelope when process start throws() : Unit/implicit` (public (default))
  - `command runner times out long-running command and terminates process() : Unit/implicit` (public (default))
  - `process launcher captures startup output from process stream() : Unit/implicit` (public (default))
  - `process launcher returns empty startup output when none is emitted() : Unit/implicit` (public (default))
  - `process launcher registers and unregisters shutdown hook around handle lifecycle() : Unit/implicit` (public (default))
  - `registered shutdown hook forcibly stops process when invoked() : Unit/implicit` (public (default))
  - `stop forcibly destroys process when graceful shutdown times out() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- Throws explicit exceptions for invalid or unrecoverable scenarios.

### 8. Lifecycle / state behavior
- Contains coroutine-aware execution paths and cancellation-sensitive operations.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/SystemRuntimeAdaptersTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *SystemRuntimeAdaptersTest`.

## TopicTreeActionControllerTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeActionControllerTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicTreeActionControllerTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand, com.authord.mkdocs.ports.topic.AddTopicNodeCommand, com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncErrorCode, com.authord.mkdocs.ports.topic.TopicSyncOutcome, com.authord.mkdocs.ports.topic.TopicTreeCommand
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeActionControllerTest`, `RecordingTopicTreeUiService`.
  - Key callable members include `create topic dispatches add command with normalized path`, `add existing file normalizes relative markdown path`, `failed remove returns non-destructive recovery guidance`.
  - Scenario-focused test cases include: `create topic dispatches add command with normalized path`, `add existing file normalizes relative markdown path`, `failed remove returns non-destructive recovery guidance`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeActionControllerTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingTopicTreeUiService` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `create topic dispatches add command with normalized path` consumes no explicit parameters.
  - `add existing file normalizes relative markdown path` consumes no explicit parameters.
  - `failed remove returns non-destructive recovery guidance` consumes no explicit parameters.
- **Outputs produced:**
  - `create topic dispatches add command with normalized path` returns Unit/implicit.
  - `add existing file normalizes relative markdown path` returns Unit/implicit.
  - `failed remove returns non-destructive recovery guidance` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.AddExistingFileTopicNodeCommand`, `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`, `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeActionControllerTest` (class, public (default))
- **Key public/internal functions:**
  - `create topic dispatches add command with normalized path() : Unit/implicit` (public (default))
  - `add existing file normalizes relative markdown path() : Unit/implicit` (public (default))
  - `failed remove returns non-destructive recovery guidance() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeActionControllerTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeActionControllerTest`.

## TopicTreeAggregateHydrationStartupTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeAggregateHydrationStartupTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicTreeAggregateHydrationStartupTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.topic.TopicTreeAggregate, com.authord.mkdocs.core.topic.TopicTreeMutationService, com.authord.mkdocs.ports.TopicTreePort, com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.DocsFileGateway, com.authord.mkdocs.ports.topic.InstanceRegistryPort, com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.MkDocsConfigGateway
  - External: java.nio.file.Files, javax.swing.JComponent, javax.swing.JPanel
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeAggregateHydrationStartupTest`, `HydrationPreviewContent`, `HydrationConfigGateway`, `HydrationRecordingDocsGateway`, `HydrationNoOpDocsGateway`, `HydrationSingleInstanceRegistry`, plus `1` more.
  - Key callable members include `startup reconciliation hydrates aggregate from loaded config nav`, `startup reconciliation hydrates aggregate from fallback nodes when nav is absent`, `orchestrator hydrates aggregate before first rename command`, `orchestrator synthesizes fallback nav from docs when config nav is missing for rename`, `orchestrator synthesizes fallback nav from docs when config nav is missing for add child`, `no-nav mode refreshes aggregate from docs snapshot between transactions`, `reload hydration refreshes cached config state before follow-up mutations`.
  - Scenario-focused test cases include: `startup reconciliation hydrates aggregate from loaded config nav`, `startup reconciliation hydrates aggregate from fallback nodes when nav is absent`, `orchestrator hydrates aggregate before first rename command`, `orchestrator synthesizes fallback nav from docs when config nav is missing for rename`, `orchestrator synthesizes fallback nav from docs when config nav is missing for add child`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeAggregateHydrationStartupTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `HydrationPreviewContent` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `HydrationConfigGateway` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `HydrationRecordingDocsGateway` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `HydrationNoOpDocsGateway` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `HydrationSingleInstanceRegistry` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `HydrationAlwaysSuccessTopicTreePort` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `startup reconciliation hydrates aggregate from loaded config nav` consumes no explicit parameters.
  - `startup reconciliation hydrates aggregate from fallback nodes when nav is absent` consumes no explicit parameters.
  - `orchestrator hydrates aggregate before first rename command` consumes no explicit parameters.
  - `orchestrator synthesizes fallback nav from docs when config nav is missing for rename` consumes no explicit parameters.
  - `orchestrator synthesizes fallback nav from docs when config nav is missing for add child` consumes no explicit parameters.
- **Outputs produced:**
  - `startup reconciliation hydrates aggregate from loaded config nav` returns Unit/implicit.
  - `startup reconciliation hydrates aggregate from fallback nodes when nav is absent` returns Unit/implicit.
  - `orchestrator hydrates aggregate before first rename command` returns Unit/implicit.
  - `orchestrator synthesizes fallback nav from docs when config nav is missing for rename` returns Unit/implicit.
  - `orchestrator synthesizes fallback nav from docs when config nav is missing for add child` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - IntelliJ UI state updates
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.topic.TopicTreeAggregate`
  - `com.authord.mkdocs.core.topic.TopicTreeMutationService`
  - `com.authord.mkdocs.ports.TopicTreePort`
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.topic.TopicTreeAggregate`, `com.authord.mkdocs.core.topic.TopicTreeMutationService`, `com.authord.mkdocs.ports.TopicTreePort`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeAggregateHydrationStartupTest` (class, public (default))
- **Key public/internal functions:**
  - `startup reconciliation hydrates aggregate from loaded config nav() : Unit/implicit` (public (default))
  - `startup reconciliation hydrates aggregate from fallback nodes when nav is absent() : Unit/implicit` (public (default))
  - `orchestrator hydrates aggregate before first rename command() : Unit/implicit` (public (default))
  - `orchestrator synthesizes fallback nav from docs when config nav is missing for rename() : Unit/implicit` (public (default))
  - `orchestrator synthesizes fallback nav from docs when config nav is missing for add child() : Unit/implicit` (public (default))
  - `no-nav mode refreshes aggregate from docs snapshot between transactions() : Unit/implicit` (public (default))
  - `reload hydration refreshes cached config state before follow-up mutations() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`, `@Suppress`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeAggregateHydrationStartupTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeAggregateHydrationStartupTest`.

## TopicTreeApplicationServiceContractTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeApplicationServiceContractTest.kt`
- **What does it do?** Defines contract assertions for `TopicTreeApplicationServiceContractTest` to enforce port/interface compatibility.
- **Why it matters:** It keeps interchangeable implementations behaviorally aligned with shared contracts.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.TOPIC_TREE_APP_SERVICE_API_VERSION
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeApplicationServiceContractTest`.
  - Key callable members include `application service exposes execute rollback and active instance methods`, `application service api version follows semantic format`.
  - Scenario-focused test cases include: `application service exposes execute rollback and active instance methods`, `application service api version follows semantic format`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeApplicationServiceContractTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `application service exposes execute rollback and active instance methods` consumes no explicit parameters.
  - `application service api version follows semantic format` consumes no explicit parameters.
- **Outputs produced:**
  - `application service exposes execute rollback and active instance methods` returns Unit/implicit.
  - `application service api version follows semantic format` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.TOPIC_TREE_APP_SERVICE_API_VERSION`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.TOPIC_TREE_APP_SERVICE_API_VERSION`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeApplicationServiceContractTest` (class, public (default))
- **Key public/internal functions:**
  - `application service exposes execute rollback and active instance methods() : Unit/implicit` (public (default))
  - `application service api version follows semantic format() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeApplicationServiceContractTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeApplicationServiceContractTest`.

## TopicTreeDragDropControllerTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeDragDropControllerTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicTreeDragDropControllerTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.MoveTopicNodeCommand, com.authord.mkdocs.ports.topic.ReorderTopicNodesCommand, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncErrorCode, com.authord.mkdocs.ports.topic.TopicSyncOutcome, com.authord.mkdocs.ports.topic.TopicTreeCommand
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeDragDropControllerTest`, `RecordingDragDropUiService`.
  - Key callable members include `move topic dispatches move command`, `reorder failure returns recovery guidance`.
  - Scenario-focused test cases include: `move topic dispatches move command`, `reorder failure returns recovery guidance`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeDragDropControllerTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingDragDropUiService` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `move topic dispatches move command` consumes no explicit parameters.
  - `reorder failure returns recovery guidance` consumes no explicit parameters.
- **Outputs produced:**
  - `move topic dispatches move command` returns Unit/implicit.
  - `reorder failure returns recovery guidance` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.MoveTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.ReorderTopicNodesCommand`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.MoveTopicNodeCommand`, `com.authord.mkdocs.ports.topic.ReorderTopicNodesCommand`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeDragDropControllerTest` (class, public (default))
- **Key public/internal functions:**
  - `move topic dispatches move command() : Unit/implicit` (public (default))
  - `reorder failure returns recovery guidance() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeDragDropControllerTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeDragDropControllerTest`.

## TopicTreeFailureRecoveryPresenterTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeFailureRecoveryPresenterTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicTreeFailureRecoveryPresenterTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.TopicSyncErrorCode
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeFailureRecoveryPresenterTest`.
  - Key callable members include `validation failure guidance states no changes applied`, `orchestration failure guidance states rollback compensation`.
  - Scenario-focused test cases include: `validation failure guidance states no changes applied`, `orchestration failure guidance states rollback compensation`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeFailureRecoveryPresenterTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `validation failure guidance states no changes applied` consumes no explicit parameters.
  - `orchestration failure guidance states rollback compensation` consumes no explicit parameters.
- **Outputs produced:**
  - `validation failure guidance states no changes applied` returns Unit/implicit.
  - `orchestration failure guidance states rollback compensation` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.TopicSyncErrorCode`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.TopicSyncErrorCode`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeFailureRecoveryPresenterTest` (class, public (default))
- **Key public/internal functions:**
  - `validation failure guidance states no changes applied() : Unit/implicit` (public (default))
  - `orchestration failure guidance states rollback compensation() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeFailureRecoveryPresenterTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeFailureRecoveryPresenterTest`.

## TopicTreeFallbackBuilderTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeFallbackBuilderTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicTreeFallbackBuilderTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeFallbackBuilderTest`.
  - Key callable members include `builds deterministic fallback tree from docs dir markdown paths`, `creates section hierarchy when fallback paths contain directories`, `supports absolute docs dir with absolute markdown paths`.
  - Scenario-focused test cases include: `builds deterministic fallback tree from docs dir markdown paths`, `creates section hierarchy when fallback paths contain directories`, `supports absolute docs dir with absolute markdown paths`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeFallbackBuilderTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `builds deterministic fallback tree from docs dir markdown paths` consumes no explicit parameters.
  - `creates section hierarchy when fallback paths contain directories` consumes no explicit parameters.
  - `supports absolute docs dir with absolute markdown paths` consumes no explicit parameters.
- **Outputs produced:**
  - `builds deterministic fallback tree from docs dir markdown paths` returns Unit/implicit.
  - `creates section hierarchy when fallback paths contain directories` returns Unit/implicit.
  - `supports absolute docs dir with absolute markdown paths` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeFallbackBuilderTest` (class, public (default))
- **Key public/internal functions:**
  - `builds deterministic fallback tree from docs dir markdown paths() : Unit/implicit` (public (default))
  - `creates section hierarchy when fallback paths contain directories() : Unit/implicit` (public (default))
  - `supports absolute docs dir with absolute markdown paths() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeFallbackBuilderTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeFallbackBuilderTest`.

## TopicTreeFileMutationDerivationTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeFileMutationDerivationTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicTreeFileMutationDerivationTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.TopicTreePort, com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand, com.authord.mkdocs.ports.topic.AddTopicNodeCommand, com.authord.mkdocs.ports.topic.DocsFileGateway, com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.MkDocsConfigGateway, com.authord.mkdocs.ports.topic.MoveTopicNodeCommand, com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeFileMutationDerivationTest`, `SuccessfulTopicTreePortForDerivation`, `MutableConfigGatewayForDerivation`, `RecordingDocsGatewayForDerivation`.
  - Key callable members include `add topic without source path derives markdown path and creates file`, `add child derives create path when parent is section without direct path`, `no-nav child then child-of-child creates folder hierarchy without config writes`, `no-nav child-of-child after fallback-style rehydrate preserves folder hierarchy without config writes`, `no-nav rename section alias rewrites folder markdown paths without config writes`, `rename section in nav mode updates section index heading without renaming file`, `no-nav move section alias rewrites folder markdown paths without config writes`, `same-parent move applies pre-removal index semantics in config nav`, plus `7` additional members.
  - Scenario-focused test cases include: `add topic without source path derives markdown path and creates file`, `add child derives create path when parent is section without direct path`, `no-nav child then child-of-child creates folder hierarchy without config writes`, `no-nav child-of-child after fallback-style rehydrate preserves folder hierarchy without config writes`, `no-nav rename section alias rewrites folder markdown paths without config writes`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeFileMutationDerivationTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `SuccessfulTopicTreePortForDerivation` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `MutableConfigGatewayForDerivation` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingDocsGatewayForDerivation` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `add topic without source path derives markdown path and creates file` consumes no explicit parameters.
  - `add child derives create path when parent is section without direct path` consumes no explicit parameters.
  - `no-nav child then child-of-child creates folder hierarchy without config writes` consumes no explicit parameters.
  - `no-nav child-of-child after fallback-style rehydrate preserves folder hierarchy without config writes` consumes no explicit parameters.
  - `no-nav rename section alias rewrites folder markdown paths without config writes` consumes no explicit parameters.
- **Outputs produced:**
  - `add topic without source path derives markdown path and creates file` returns Unit/implicit.
  - `add child derives create path when parent is section without direct path` returns Unit/implicit.
  - `no-nav child then child-of-child creates folder hierarchy without config writes` returns Unit/implicit.
  - `no-nav child-of-child after fallback-style rehydrate preserves folder hierarchy without config writes` returns Unit/implicit.
  - `no-nav rename section alias rewrites folder markdown paths without config writes` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.TopicTreePort`
  - `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.DocsFileGateway`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.TopicTreePort`, `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`, `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeFileMutationDerivationTest` (class, public (default))
- **Key public/internal functions:**
  - `add topic without source path derives markdown path and creates file() : Unit/implicit` (public (default))
  - `add child derives create path when parent is section without direct path() : Unit/implicit` (public (default))
  - `no-nav child then child-of-child creates folder hierarchy without config writes() : Unit/implicit` (public (default))
  - `no-nav child-of-child after fallback-style rehydrate preserves folder hierarchy without config writes() : Unit/implicit` (public (default))
  - `no-nav rename section alias rewrites folder markdown paths without config writes() : Unit/implicit` (public (default))
  - `rename section in nav mode updates section index heading without renaming file() : Unit/implicit` (public (default))
  - `no-nav move section alias rewrites folder markdown paths without config writes() : Unit/implicit` (public (default))
  - `same-parent move applies pre-removal index semantics in config nav() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeFileMutationDerivationTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeFileMutationDerivationTest`.

## TopicTreeStartupLoaderTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeStartupLoaderTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicTreeStartupLoaderTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.TopicNavNode
  - External: java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeStartupLoaderTest`.
  - Key callable members include `loads startup tree from canonical nav when nav exists`, `preserves nav ordering for startup tree`, `loads fallback tree when docs dir is absolute path`, `keeps canonical nav mode when nav key is present but empty`, `resolves nav display title from markdown h1 when present`, `reloading startup state reflects updated markdown h1 title`.
  - Scenario-focused test cases include: `loads startup tree from canonical nav when nav exists`, `preserves nav ordering for startup tree`, `loads fallback tree when docs dir is absolute path`, `keeps canonical nav mode when nav key is present but empty`, `resolves nav display title from markdown h1 when present`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeStartupLoaderTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `loads startup tree from canonical nav when nav exists` consumes no explicit parameters.
  - `preserves nav ordering for startup tree` consumes no explicit parameters.
  - `loads fallback tree when docs dir is absolute path` consumes no explicit parameters.
  - `keeps canonical nav mode when nav key is present but empty` consumes no explicit parameters.
  - `resolves nav display title from markdown h1 when present` consumes no explicit parameters.
- **Outputs produced:**
  - `loads startup tree from canonical nav when nav exists` returns Unit/implicit.
  - `preserves nav ordering for startup tree` returns Unit/implicit.
  - `loads fallback tree when docs dir is absolute path` returns Unit/implicit.
  - `keeps canonical nav mode when nav key is present but empty` returns Unit/implicit.
  - `resolves nav display title from markdown h1 when present` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
  - `com.authord.mkdocs.ports.topic.TopicNavNode`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`, `com.authord.mkdocs.ports.topic.TopicNavNode`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeStartupLoaderTest` (class, public (default))
- **Key public/internal functions:**
  - `loads startup tree from canonical nav when nav exists() : Unit/implicit` (public (default))
  - `preserves nav ordering for startup tree() : Unit/implicit` (public (default))
  - `loads fallback tree when docs dir is absolute path() : Unit/implicit` (public (default))
  - `keeps canonical nav mode when nav key is present but empty() : Unit/implicit` (public (default))
  - `resolves nav display title from markdown h1 when present() : Unit/implicit` (public (default))
  - `reloading startup state reflects updated markdown h1 title() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeStartupLoaderTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeStartupLoaderTest`.

## TopicTreeSyncOrchestratorCacheFreshnessTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeSyncOrchestratorCacheFreshnessTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicTreeSyncOrchestratorCacheFreshnessTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.TopicTreePort, com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand, com.authord.mkdocs.ports.topic.DocsFileGateway, com.authord.mkdocs.ports.topic.TopicDeleteMode, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncOutcome, com.authord.mkdocs.ports.topic.TopicSyncTransaction
  - External: java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeSyncOrchestratorCacheFreshnessTest`, `AlwaysSuccessTopicTreePort`, `NoopDocsGateway`.
  - Key callable members include `nav-present cache reloads after external config file change`.
  - Scenario-focused test cases include: `nav-present cache reloads after external config file change`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeSyncOrchestratorCacheFreshnessTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `AlwaysSuccessTopicTreePort` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `NoopDocsGateway` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `nav-present cache reloads after external config file change` consumes no explicit parameters.
- **Outputs produced:**
  - `nav-present cache reloads after external config file change` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.TopicTreePort`
  - `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.DocsFileGateway`
  - `com.authord.mkdocs.ports.topic.TopicDeleteMode`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.TopicTreePort`, `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`, `com.authord.mkdocs.ports.topic.DocsFileGateway`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeSyncOrchestratorCacheFreshnessTest` (class, public (default))
- **Key public/internal functions:**
  - `nav-present cache reloads after external config file change() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `try/catch` to capture operational failures and map them to controlled outcomes.
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeSyncOrchestratorCacheFreshnessTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeSyncOrchestratorCacheFreshnessTest`.

## TopicTreeSyncOrchestratorPersistenceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeSyncOrchestratorPersistenceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicTreeSyncOrchestratorPersistenceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.TopicTreePort, com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand, com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.DocsFileGateway, com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.MkDocsConfigGateway, com.authord.mkdocs.ports.topic.TopicDeleteMode, com.authord.mkdocs.ports.topic.TopicGatewayResult
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeSyncOrchestratorPersistenceTest`, `SuccessfulPort`, `RecordingConfigGateway`, `RecordingDocsGateway`.
  - Key callable members include `add child mutation persists nav and creates markdown file`.
  - Scenario-focused test cases include: `add child mutation persists nav and creates markdown file`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeSyncOrchestratorPersistenceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `SuccessfulPort` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingConfigGateway` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingDocsGateway` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `add child mutation persists nav and creates markdown file` consumes no explicit parameters.
- **Outputs produced:**
  - `add child mutation persists nav and creates markdown file` returns Unit/implicit.
- **Side effects:**
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.TopicTreePort`
  - `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.DocsFileGateway`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.TopicTreePort`, `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`, `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeSyncOrchestratorPersistenceTest` (class, public (default))
- **Key public/internal functions:**
  - `add child mutation persists nav and creates markdown file() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Uses `when` branching to map states/inputs into behavior-specific outcomes.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeSyncOrchestratorPersistenceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeSyncOrchestratorPersistenceTest`.

## TopicTreeUiServiceContractTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeUiServiceContractTest.kt`
- **What does it do?** Defines contract assertions for `TopicTreeUiServiceContractTest` to enforce port/interface compatibility.
- **Why it matters:** It keeps interchangeable implementations behaviorally aligned with shared contracts.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.TOPIC_TREE_UI_SERVICE_API_VERSION
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeUiServiceContractTest`.
  - Key callable members include `ui service exposes dispatch refresh and instance selection methods`, `ui service api version follows semantic format`.
  - Scenario-focused test cases include: `ui service exposes dispatch refresh and instance selection methods`, `ui service api version follows semantic format`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeUiServiceContractTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `ui service exposes dispatch refresh and instance selection methods` consumes no explicit parameters.
  - `ui service api version follows semantic format` consumes no explicit parameters.
- **Outputs produced:**
  - `ui service exposes dispatch refresh and instance selection methods` returns Unit/implicit.
  - `ui service api version follows semantic format` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.TOPIC_TREE_UI_SERVICE_API_VERSION`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.TOPIC_TREE_UI_SERVICE_API_VERSION`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeUiServiceContractTest` (class, public (default))
- **Key public/internal functions:**
  - `ui service exposes dispatch refresh and instance selection methods() : Unit/implicit` (public (default))
  - `ui service api version follows semantic format() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeUiServiceContractTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeUiServiceContractTest`.

## TopicTreeWatcherTriggerMatrixTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWatcherTriggerMatrixTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TopicTreeWatcherTriggerMatrixTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: Local module/package types only.
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeWatcherTriggerMatrixTest`.
  - Key callable members include `triggers reconciliation on mkdocs config file lifecycle changes`, `triggers reconciliation on markdown changes within docs_dir only`.
  - Scenario-focused test cases include: `triggers reconciliation on mkdocs config file lifecycle changes`, `triggers reconciliation on markdown changes within docs_dir only`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeWatcherTriggerMatrixTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `triggers reconciliation on mkdocs config file lifecycle changes` consumes no explicit parameters.
  - `triggers reconciliation on markdown changes within docs_dir only` consumes no explicit parameters.
- **Outputs produced:**
  - `triggers reconciliation on mkdocs config file lifecycle changes` returns Unit/implicit.
  - `triggers reconciliation on markdown changes within docs_dir only` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - Mostly local logic / standard library usage; no internal import-heavy downstream calls.
- **Upstream / downstream relationships:**
  - Appears isolated or indirectly wired; verify runtime registration points for concrete call paths.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeWatcherTriggerMatrixTest` (class, public (default))
- **Key public/internal functions:**
  - `triggers reconciliation on mkdocs config file lifecycle changes() : Unit/implicit` (public (default))
  - `triggers reconciliation on markdown changes within docs_dir only() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWatcherTriggerMatrixTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeWatcherTriggerMatrixTest`.

## TopicTreeWorkspacePanelUiContractTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanelUiContractTest.kt`
- **What does it do?** Defines contract assertions for `TopicTreeWorkspacePanelUiContractTest` to enforce port/interface compatibility.
- **Why it matters:** It keeps interchangeable implementations behaviorally aligned with shared contracts.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.InstanceRegistryPort, com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand, com.authord.mkdocs.ports.topic.AddTopicNodeCommand, com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand, com.authord.mkdocs.ports.topic.RenameTopicNodeCommand, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreeWorkspacePanelUiContractTest`, `RecordingUiService`, `RecordingInstanceRegistryPort`.
  - Key callable members include `tooltips use exact contract strings`, `header actions follow required toc order`, `menu structure matches contract order`, `enablement follows capability and selection rules`, `expand all header action publishes expanded status`, `collapse all header action collapses first visible toc node`, `sync toc and editor header action reports unavailable without project`, `toc remove action dispatches through controller pipeline`, plus `16` additional members.
  - Scenario-focused test cases include: `tooltips use exact contract strings`, `header actions follow required toc order`, `menu structure matches contract order`, `enablement follows capability and selection rules`, `expand all header action publishes expanded status`, ...

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreeWorkspacePanelUiContractTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingUiService` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingInstanceRegistryPort` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `tooltips use exact contract strings` consumes no explicit parameters.
  - `header actions follow required toc order` consumes no explicit parameters.
  - `menu structure matches contract order` consumes no explicit parameters.
  - `enablement follows capability and selection rules` consumes no explicit parameters.
  - `expand all header action publishes expanded status` consumes no explicit parameters.
- **Outputs produced:**
  - `tooltips use exact contract strings` returns Unit/implicit.
  - `header actions follow required toc order` returns Unit/implicit.
  - `menu structure matches contract order` returns Unit/implicit.
  - `enablement follows capability and selection rules` returns Unit/implicit.
  - `expand all header action publishes expanded status` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.InstanceRegistryPort`
  - `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`
  - `com.authord.mkdocs.ports.topic.AddTopicNodeCommand`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.InstanceRegistryPort`, `com.authord.mkdocs.ports.topic.AddChildTopicNodeCommand`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreeWorkspacePanelUiContractTest` (class, public (default))
- **Key public/internal functions:**
  - `tooltips use exact contract strings() : Unit/implicit` (public (default))
  - `header actions follow required toc order() : Unit/implicit` (public (default))
  - `menu structure matches contract order() : Unit/implicit` (public (default))
  - `enablement follows capability and selection rules() : Unit/implicit` (public (default))
  - `expand all header action publishes expanded status() : Unit/implicit` (public (default))
  - `collapse all header action collapses first visible toc node() : Unit/implicit` (public (default))
  - `sync toc and editor header action reports unavailable without project() : Unit/implicit` (public (default))
  - `toc remove action dispatches through controller pipeline() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.
- Relies on Kotlin null-safety operators (`?.`, `?:`) for nullable branches.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TopicTreeWorkspacePanelUiContractTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Large callable surface area increases regression risk; consider characterization tests before large refactors.

### 14. Known limitations / technical debt
- File is relatively large (>350 LOC), which may complicate safe modifications without extraction/refactor.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreeWorkspacePanelUiContractTest`.

## TreeSyncOrchestratorAtomicityTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TreeSyncOrchestratorAtomicityTest.kt`
- **What does it do?** Implements unit-focused test coverage around `TreeSyncOrchestratorAtomicityTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.TopicTreePort, com.authord.mkdocs.ports.topic.DefaultTopicSyncError, com.authord.mkdocs.ports.topic.DocsFileGateway, com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.MkDocsConfigGateway, com.authord.mkdocs.ports.topic.RemoveTopicNodeCommand, com.authord.mkdocs.ports.topic.TopicDeleteMode, com.authord.mkdocs.ports.topic.TopicFileOperation
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TreeSyncOrchestratorAtomicityTest`, `SuccessfulTopicTreePort`, `SuccessfulConfigGateway`, `MutableStateConfigGateway`, `RecordingDocsFileGateway`.
  - Key callable members include `apply succeeds when command config and file operations all succeed`, `apply rolls back with compensation when a later file operation fails`, `failed remove mutation keeps cached nav state consistent for retry`.
  - Scenario-focused test cases include: `apply succeeds when command config and file operations all succeed`, `apply rolls back with compensation when a later file operation fails`, `failed remove mutation keeps cached nav state consistent for retry`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `TreeSyncOrchestratorAtomicityTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `SuccessfulTopicTreePort` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `SuccessfulConfigGateway` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `MutableStateConfigGateway` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `RecordingDocsFileGateway` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `apply succeeds when command config and file operations all succeed` consumes no explicit parameters.
  - `apply rolls back with compensation when a later file operation fails` consumes no explicit parameters.
  - `failed remove mutation keeps cached nav state consistent for retry` consumes no explicit parameters.
- **Outputs produced:**
  - `apply succeeds when command config and file operations all succeed` returns Unit/implicit.
  - `apply rolls back with compensation when a later file operation fails` returns Unit/implicit.
  - `failed remove mutation keeps cached nav state consistent for retry` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes
  - In-memory mutable state


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.TopicTreePort`
  - `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`
  - `com.authord.mkdocs.ports.topic.DocsFileGateway`
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.TopicTreePort`, `com.authord.mkdocs.ports.topic.DefaultTopicSyncError`, `com.authord.mkdocs.ports.topic.DocsFileGateway`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TreeSyncOrchestratorAtomicityTest` (class, public (default))
- **Key public/internal functions:**
  - `apply succeeds when command config and file operations all succeed() : Unit/implicit` (public (default))
  - `apply rolls back with compensation when a later file operation fails() : Unit/implicit` (public (default))
  - `failed remove mutation keeps cached nav state consistent for retry() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- Model-centric declarations: `MutableStateConfigGateway`.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/TreeSyncOrchestratorAtomicityTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TreeSyncOrchestratorAtomicityTest`.

## UnlinkedFilesBucketServiceTest.kt

### Basic file information
- **Path to the `.kt` file:** `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/UnlinkedFilesBucketServiceTest.kt`
- **What does it do?** Implements unit-focused test coverage around `UnlinkedFilesBucketServiceTest` logic and edge cases.
- **Why it matters:** It provides fast feedback for refactoring and guards against local behavior regressions.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.TopicNavNode
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Cover happy-path and edge-case behavior for fast local regression detection.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `UnlinkedFilesBucketServiceTest`.
  - Key callable members include `derives docs_dir markdown files not present in nav`, `normalizes docs paths when nav paths use relative separators`.
  - Scenario-focused test cases include: `derives docs_dir markdown files not present in nav`, `normalizes docs paths when nav paths use relative separators`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `ui-plugin` / `ui/intellij`
- **Architectural layer:** `unit testing`

### 2. Main classes / interfaces / objects inside the file
- `UnlinkedFilesBucketServiceTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `derives docs_dir markdown files not present in nav` consumes no explicit parameters.
  - `normalizes docs paths when nav paths use relative separators` consumes no explicit parameters.
- **Outputs produced:**
  - `derives docs_dir markdown files not present in nav` returns Unit/implicit.
  - `normalizes docs paths when nav paths use relative separators` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.TopicNavNode`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.TopicNavNode`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `UnlinkedFilesBucketServiceTest` (class, public (default))
- **Key public/internal functions:**
  - `derives docs_dir markdown files not present in nav() : Unit/implicit` (public (default))
  - `normalizes docs paths when nav paths use relative separators() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `modules/ui-plugin/src/test/kotlin/com/authord/mkdocs/ui/intellij/UnlinkedFilesBucketServiceTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *UnlinkedFilesBucketServiceTest`.

## TopicTreePortContractTest.kt

### Basic file information
- **Path to the `.kt` file:** `tests/contract/topic-tree-command-port/TopicTreePortContractTest.kt`
- **What does it do?** Defines contract assertions for `TopicTreePortContractTest` to enforce port/interface compatibility.
- **Why it matters:** It keeps interchangeable implementations behaviorally aligned with shared contracts.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.topic.TopicTreeMutationService, com.authord.mkdocs.defaults.command.InMemoryCommandRegistry, com.authord.mkdocs.defaults.preview.NoOpPreviewSyncAdapter, com.authord.mkdocs.defaults.vector.NoOpVectorStoreAdapter, com.authord.mkdocs.ports.command.DefaultPluginCommandBus, com.authord.mkdocs.ports.topic.AddTopicNodeCommand, com.authord.mkdocs.ports.topic.RenameTopicNodeCommand, com.authord.mkdocs.ports.topic.TopicTreeCommandStatus
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Enforce shared port behavior so implementations remain substitutable.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `TopicTreePortContractTest`.
  - Key callable members include `command bus and topic-tree port work with default adapters`.
  - Scenario-focused test cases include: `command bus and topic-tree port work with default adapters`.

### 1. Package / module context
- **Package name:** `contract.topic`
- **Module / feature area:** `contract-tests` / `topic-tree command-port contract`
- **Architectural layer:** `contract testing`

### 2. Main classes / interfaces / objects inside the file
- `TopicTreePortContractTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `command bus and topic-tree port work with default adapters` consumes no explicit parameters.
- **Outputs produced:**
  - `command bus and topic-tree port work with default adapters` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.topic.TopicTreeMutationService`
  - `com.authord.mkdocs.defaults.command.InMemoryCommandRegistry`
  - `com.authord.mkdocs.defaults.preview.NoOpPreviewSyncAdapter`
  - `com.authord.mkdocs.defaults.vector.NoOpVectorStoreAdapter`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.topic.TopicTreeMutationService`, `com.authord.mkdocs.defaults.command.InMemoryCommandRegistry`, `com.authord.mkdocs.defaults.preview.NoOpPreviewSyncAdapter`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `TopicTreePortContractTest` (class, public (default))
- **Key public/internal functions:**
  - `command bus and topic-tree port work with default adapters() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `tests/contract/topic-tree-command-port/TopicTreePortContractTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *TopicTreePortContractTest`.

## ExplorerSelectionToPreviewIT.kt

### Basic file information
- **Path to the `.kt` file:** `tests/integration/explorer-preview-navigation/ExplorerSelectionToPreviewIT.kt`
- **What does it do?** Provides integration-level verification for `ExplorerSelectionToPreviewIT` scenarios across module boundaries.
- **Why it matters:** It validates real coordination paths and protects against regressions that unit tests miss.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.navigation.RouteMappingService, com.authord.mkdocs.ui.NavigationCoordinator, com.authord.mkdocs.ui.PreviewNavigationFailureHandler, com.authord.mkdocs.ui.PreviewPaneCoordinator
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Verify cross-module orchestration under realistic runtime/project conditions.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `ExplorerSelectionToPreviewIT`.
  - Key callable members include `file selection updates preview route`.
  - Scenario-focused test cases include: `file selection updates preview route`.

### 1. Package / module context
- **Package name:** `integration.navigation`
- **Module / feature area:** `integration-tests` / `general`
- **Architectural layer:** `integration testing`

### 2. Main classes / interfaces / objects inside the file
- `ExplorerSelectionToPreviewIT` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `file selection updates preview route` consumes no explicit parameters.
- **Outputs produced:**
  - `file selection updates preview route` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.navigation.RouteMappingService`
  - `com.authord.mkdocs.ui.NavigationCoordinator`
  - `com.authord.mkdocs.ui.PreviewNavigationFailureHandler`
  - `com.authord.mkdocs.ui.PreviewPaneCoordinator`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.navigation.RouteMappingService`, `com.authord.mkdocs.ui.NavigationCoordinator`, `com.authord.mkdocs.ui.PreviewNavigationFailureHandler`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `ExplorerSelectionToPreviewIT` (class, public (default))
- **Key public/internal functions:**
  - `file selection updates preview route() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `tests/integration/explorer-preview-navigation/ExplorerSelectionToPreviewIT.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *ExplorerSelectionToPreviewIT`.

## ActivationToPreviewIT.kt

### Basic file information
- **Path to the `.kt` file:** `tests/integration/runtime-lifecycle/ActivationToPreviewIT.kt`
- **What does it do?** Provides integration-level verification for `ActivationToPreviewIT` scenarios across module boundaries.
- **Why it matters:** It validates real coordination paths and protects against regressions that unit tests miss.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.flags.FeatureFlagPolicy, com.authord.mkdocs.runtime.BaseUrlDetector, com.authord.mkdocs.runtime.CommandResult, com.authord.mkdocs.runtime.ManagedProcessHandle, com.authord.mkdocs.runtime.MkdocsProcessManager, com.authord.mkdocs.runtime.ProcessLauncher, com.authord.mkdocs.runtime.UvBootstrapService, com.authord.mkdocs.ui.ActivationErrorPresenter
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Verify cross-module orchestration under realistic runtime/project conditions.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `IntegrationHandle`, `ActivationToPreviewIT`.
  - Key callable members include `activation starts runtime and opens preview`.
  - Scenario-focused test cases include: `activation starts runtime and opens preview`.

### 1. Package / module context
- **Package name:** `integration.runtime`
- **Module / feature area:** `integration-tests` / `runtime-lifecycle integration`
- **Architectural layer:** `integration testing`

### 2. Main classes / interfaces / objects inside the file
- `IntegrationHandle` (class, private): Test declaration that codifies expected behavior and regression scenarios.
- `ActivationToPreviewIT` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `activation starts runtime and opens preview` consumes no explicit parameters.
- **Outputs produced:**
  - `activation starts runtime and opens preview` returns Unit/implicit.
- **Side effects:**
  - Process/runtime execution


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.flags.FeatureFlagPolicy`
  - `com.authord.mkdocs.runtime.BaseUrlDetector`
  - `com.authord.mkdocs.runtime.CommandResult`
  - `com.authord.mkdocs.runtime.ManagedProcessHandle`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.flags.FeatureFlagPolicy`, `com.authord.mkdocs.runtime.BaseUrlDetector`, `com.authord.mkdocs.runtime.CommandResult`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `ActivationToPreviewIT` (class, public (default))
- **Key public/internal functions:**
  - `activation starts runtime and opens preview() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Core behavior is concentrated in callable members declared in this file.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `tests/integration/runtime-lifecycle/ActivationToPreviewIT.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *ActivationToPreviewIT`.

## InstanceScaleProfileIntegrationTest.kt

### Basic file information
- **Path to the `.kt` file:** `tests/integration/topic-tree/InstanceScaleProfileIntegrationTest.kt`
- **What does it do?** Provides integration-level verification for `InstanceScaleProfileIntegrationTest` scenarios across module boundaries.
- **Why it matters:** It validates real coordination paths and protects against regressions that unit tests miss.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.topic.MkDocsConfigDocument, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicNavNode
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Verify cross-module orchestration under realistic runtime/project conditions.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `InstanceScaleProfileIntegrationTest`.
  - Key callable members include `reconciles five instances and ten thousand docs without cross-instance leakage`.
  - Scenario-focused test cases include: `reconciles five instances and ten thousand docs without cross-instance leakage`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `integration-tests` / `topic-tree integration`
- **Architectural layer:** `integration testing`

### 2. Main classes / interfaces / objects inside the file
- `InstanceScaleProfileIntegrationTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `reconciles five instances and ten thousand docs without cross-instance leakage` consumes no explicit parameters.
- **Outputs produced:**
  - `reconciles five instances and ten thousand docs without cross-instance leakage` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`
  - `com.authord.mkdocs.ports.topic.TopicInstanceRef`
  - `com.authord.mkdocs.ports.topic.TopicNavNode`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.topic.MkDocsConfigDocument`, `com.authord.mkdocs.ports.topic.TopicInstanceRef`, `com.authord.mkdocs.ports.topic.TopicNavNode`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `InstanceScaleProfileIntegrationTest` (class, public (default))
- **Key public/internal functions:**
  - `reconciles five instances and ten thousand docs without cross-instance leakage() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `tests/integration/topic-tree/InstanceScaleProfileIntegrationTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *InstanceScaleProfileIntegrationTest`.

## PartialSyncZeroStateIntegrationTest.kt

### Basic file information
- **Path to the `.kt` file:** `tests/integration/topic-tree/PartialSyncZeroStateIntegrationTest.kt`
- **What does it do?** Provides integration-level verification for `PartialSyncZeroStateIntegrationTest` scenarios across module boundaries.
- **Why it matters:** It validates real coordination paths and protects against regressions that unit tests miss.
- **Dependencies:**
  - Internal: com.authord.mkdocs.ports.TopicTreePort, com.authord.mkdocs.ports.topic.TopicFileOperation, com.authord.mkdocs.ports.topic.TopicFileOperationKind, com.authord.mkdocs.ports.topic.TopicGatewayResult, com.authord.mkdocs.ports.topic.TopicInstanceRef, com.authord.mkdocs.ports.topic.TopicSyncTransaction, com.authord.mkdocs.ports.topic.TopicTreeCommand, com.authord.mkdocs.ports.topic.TopicTreeCommandResult
  - External: java.nio.file.Files
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Verify cross-module orchestration under realistic runtime/project conditions.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PartialSyncZeroStateIntegrationTest`, `SuccessfulTopicTreePortForIntegration`.
  - Key callable members include `failed file operation triggers compensation and leaves no unresolved partial state`.
  - Scenario-focused test cases include: `failed file operation triggers compensation and leaves no unresolved partial state`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `integration-tests` / `topic-tree integration`
- **Architectural layer:** `integration testing`

### 2. Main classes / interfaces / objects inside the file
- `PartialSyncZeroStateIntegrationTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.
- `SuccessfulTopicTreePortForIntegration` (class, private): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `failed file operation triggers compensation and leaves no unresolved partial state` consumes no explicit parameters.
- **Outputs produced:**
  - `failed file operation triggers compensation and leaves no unresolved partial state` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.ports.TopicTreePort`
  - `com.authord.mkdocs.ports.topic.TopicFileOperation`
  - `com.authord.mkdocs.ports.topic.TopicFileOperationKind`
  - `com.authord.mkdocs.ports.topic.TopicGatewayResult`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.ports.TopicTreePort`, `com.authord.mkdocs.ports.topic.TopicFileOperation`, `com.authord.mkdocs.ports.topic.TopicFileOperationKind`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PartialSyncZeroStateIntegrationTest` (class, public (default))
- **Key public/internal functions:**
  - `failed file operation triggers compensation and leaves no unresolved partial state() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Uses `when` branching to map states/inputs into behavior-specific outcomes.
- Implements compensation/recovery paths for partial failures.

### 7. Error handling / failure behavior
- Uses `Result`/`runCatching`-style error channels in parts of the flow.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- Model-centric declarations: `PartialSyncZeroStateIntegrationTest`.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `tests/integration/topic-tree/PartialSyncZeroStateIntegrationTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PartialSyncZeroStateIntegrationTest`.

## PathCaseMatrixIntegrationTest.kt

### Basic file information
- **Path to the `.kt` file:** `tests/integration/topic-tree/PathCaseMatrixIntegrationTest.kt`
- **What does it do?** Provides integration-level verification for `PathCaseMatrixIntegrationTest` scenarios across module boundaries.
- **Why it matters:** It validates real coordination paths and protects against regressions that unit tests miss.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.topic.PathPolicy, com.authord.mkdocs.core.topic.TopicTreeValidationIssueType, com.authord.mkdocs.core.topic.TopicTreeValidationService, com.authord.mkdocs.ports.topic.TopicNavNode
  - External: Kotlin/Java standard library only.
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Verify cross-module orchestration under realistic runtime/project conditions.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `PathCaseMatrixIntegrationTest`.
  - Key callable members include `normalization uses canonical separators and resolves dot segments`, `comparison follows OS matrix case rules`, `case-only path differences follow validation behavior per OS`.
  - Scenario-focused test cases include: `normalization uses canonical separators and resolves dot segments`, `comparison follows OS matrix case rules`, `case-only path differences follow validation behavior per OS`.

### 1. Package / module context
- **Package name:** `com.authord.mkdocs.ui.intellij`
- **Module / feature area:** `integration-tests` / `topic-tree integration`
- **Architectural layer:** `integration testing`

### 2. Main classes / interfaces / objects inside the file
- `PathCaseMatrixIntegrationTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `normalization uses canonical separators and resolves dot segments` consumes no explicit parameters.
  - `comparison follows OS matrix case rules` consumes no explicit parameters.
  - `case-only path differences follow validation behavior per OS` consumes no explicit parameters.
- **Outputs produced:**
  - `normalization uses canonical separators and resolves dot segments` returns Unit/implicit.
  - `comparison follows OS matrix case rules` returns Unit/implicit.
  - `case-only path differences follow validation behavior per OS` returns Unit/implicit.
- **Side effects:**
  - No strong side-effect signals detected from static analysis.


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.topic.PathPolicy`
  - `com.authord.mkdocs.core.topic.TopicTreeValidationIssueType`
  - `com.authord.mkdocs.core.topic.TopicTreeValidationService`
  - `com.authord.mkdocs.ports.topic.TopicNavNode`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.topic.PathPolicy`, `com.authord.mkdocs.core.topic.TopicTreeValidationIssueType`, `com.authord.mkdocs.core.topic.TopicTreeValidationService`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `PathCaseMatrixIntegrationTest` (class, public (default))
- **Key public/internal functions:**
  - `normalization uses canonical separators and resolves dot segments() : Unit/implicit` (public (default))
  - `comparison follows OS matrix case rules() : Unit/implicit` (public (default))
  - `case-only path differences follow validation behavior per OS() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** IntelliJ Platform plugin integration component.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `tests/integration/topic-tree/PathCaseMatrixIntegrationTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *PathCaseMatrixIntegrationTest`.

## SeededValidationDatasetIntegrationTest.kt

### Basic file information
- **Path to the `.kt` file:** `tests/integration/topic-tree/SeededValidationDatasetIntegrationTest.kt`
- **What does it do?** Provides integration-level verification for `SeededValidationDatasetIntegrationTest` scenarios across module boundaries.
- **Why it matters:** It validates real coordination paths and protects against regressions that unit tests miss.
- **Dependencies:**
  - Internal: com.authord.mkdocs.core.topic.PathPolicy, com.authord.mkdocs.core.topic.TopicTreeValidationIssueType, com.authord.mkdocs.core.topic.TopicTreeValidationService, com.authord.mkdocs.ports.topic.TopicNavNode
  - External: java.nio.file.Files, java.nio.file.Path
- **Responsibilities:**
  - Encode expected outcomes using deterministic fixtures and assertions.
  - Verify cross-module orchestration under realistic runtime/project conditions.
  - Document behavioral intent for maintainers through test naming and setup.
- **Key functionalities:**
  - Main declarations: `SeededValidationDatasetIntegrationTest`.
  - Key callable members include `seeded validation dataset declares required case families`, `validator detects each seeded validation family`.
  - Scenario-focused test cases include: `seeded validation dataset declares required case families`, `validator detects each seeded validation family`.

### 1. Package / module context
- **Package name:** `integration.topic.tree`
- **Module / feature area:** `integration-tests` / `topic-tree integration`
- **Architectural layer:** `integration testing`

### 2. Main classes / interfaces / objects inside the file
- `SeededValidationDatasetIntegrationTest` (class, public (default)): Test declaration that codifies expected behavior and regression scenarios.

### 3. Inputs and outputs
- **Inputs consumed:**
  - `seeded validation dataset declares required case families` consumes no explicit parameters.
  - `validator detects each seeded validation family` consumes no explicit parameters.
- **Outputs produced:**
  - `seeded validation dataset declares required case families` returns Unit/implicit.
  - `validator detects each seeded validation family` returns Unit/implicit.
- **Side effects:**
  - Filesystem reads/writes


### 4. How it interacts with other files
- **Who uses this file:**
  - No strong static usage signal found; may be used indirectly by framework wiring.
- **What this file calls:**
  - `com.authord.mkdocs.core.topic.PathPolicy`
  - `com.authord.mkdocs.core.topic.TopicTreeValidationIssueType`
  - `com.authord.mkdocs.core.topic.TopicTreeValidationService`
  - `com.authord.mkdocs.ports.topic.TopicNavNode`
- **Upstream / downstream relationships:**
  - Upstream callers include framework or indirect callers. Downstream collaborators include `com.authord.mkdocs.core.topic.PathPolicy`, `com.authord.mkdocs.core.topic.TopicTreeValidationIssueType`, `com.authord.mkdocs.core.topic.TopicTreeValidationService`.

### 5. Entry points / exposed APIs
- **Main exposed types:**
  - `SeededValidationDatasetIntegrationTest` (class, public (default))
- **Key public/internal functions:**
  - `seeded validation dataset declares required case families() : Unit/implicit` (public (default))
  - `validator detects each seeded validation family() : Unit/implicit` (public (default))

### 6. Business rules or important logic
- Contains validation/policy and normalization decisions relevant to correctness.
- Includes guard conditions (`require`/`check`/assertions) to fail fast on invalid state.

### 7. Error handling / failure behavior
- No explicit local error strategy detected; behavior likely follows caller/contract expectations.

### 8. Lifecycle / state behavior
- Lifecycle is test-framework managed; setup/teardown controls state boundaries.

### 9. Configuration / annotations / framework roles
- **Annotations/framework markers:** `@Test`
- **Framework role:** Kotlin module component without specialized annotation-driven framework role.

### 10. Data model details
- This file is not primarily a DTO/entity model container; it focuses on behavior/contracts.

### 11. Concurrency / async behavior
- No explicit coroutine/flow constructs detected; execution appears synchronous or callback-driven.

### 12. Security / privacy relevance
- No direct sensitive-data handling signals detected in this file.

### 13. Testing notes
- **Unit-testable:** This file is itself a test.
- **Existing test file path(s):**
  - `tests/integration/topic-tree/SeededValidationDatasetIntegrationTest.kt`
- **Mocked collaborators:**
  - Primarily uses hand-written fixtures/fakes and direct assertions.
- **Untested risky areas:**
  - Test linkage appears available through matching unit/integration files; verify branch-level coverage for critical paths.

### 14. Known limitations / technical debt
- No explicit TODO/FIXME markers detected in source; maintainability risks are primarily structural/compositional.

### 15. Example usage
Run via test task (example): `./gradlew test --tests *SeededValidationDatasetIntegrationTest`.
